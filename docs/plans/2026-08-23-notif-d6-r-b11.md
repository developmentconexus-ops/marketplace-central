# NOTIF-01 D6-R B11 Implementation Plan

> **For agentic workers:** execution scaffolding only. Product/frontend authority remains in the routed rebaseline documents.

**Goal:** Render only B11 — the operator-approved full personal Inbox — as executable low-fidelity HTML inheriting the locked B00/B00-R2 shell and Notification semantics.

**Architecture:** Keep the locked global sidebar and topbar bell unchanged in meaning. Place the Organization-scoped Inbox as a utility content route outside the sidebar, using a structured vertical list with admitted read/archive/kind/cursor controls, explicit awareness mutations, source re-authorization behavior and deterministic negative/recovery scenarios.

**Tech Stack:** standalone HTML + CSS + vanilla JavaScript + deterministic local fixtures; Node structural verifier; existing repository gate.

**Spec:** `docs/engineering/rebaseline/D6-R2-NOTIF-01-D6-R-FRONTEND-FEED-FORWARD.md`

## Global Constraints

- Product OAD remains 104 operations / 31 ordinary Permissions / H-A-S.
- B00/B01/B00-R2 remain operator-LOCKED and are not reopened.
- B11 is P8 evidence only; no production React/runtime implementation.
- Full Inbox is an Organization-scoped utility route outside the global sidebar.
- Representation is a structured vertical list, not a table/grid.
- Admitted controls only: active/archived lens, all/unread/read lens, closed NotificationKind filter, limit/cursor continuation.
- No text search, unread count, total_count, mark-all-read, bulk archive, severity/priority or saved views.
- Source navigation never implicitly mutates read/archive awareness and must simulate current source re-authorization.
- Awareness writes trace only `UpdateMyNotificationAwarenessState` and preserve stale-write semantics.
- Unavailable != known-empty.
- B12 routing Settings remains NOT RENDERED.
- Artifact remains `CANDIDATE` until explicit operator visual adjudication.

---

### Task 1: Add B11 structural verifier first

**Files:**
- Create: `scripts/verify-d6-r-b11-inbox-wireframe.mjs`
- Modify: `package.json`

Verifier must fail while `qualification/d6-r2-wireframes/b11-notifications-inbox.html` is absent and later assert:

```text
P8 CANDIDATE / no self-LOCK
R128/full Inbox identity
locked B00 IA labels retained; no global Notificações sidebar item
B00-R2 bell retained and traced to ListMyNotifications
structured-list representation; no table baseline
active/archived + all/unread/read + closed kind controls
cursor continuation without total_count
heterogeneous item examples including F02 and F14 typed outcomes
source-open separate from awareness mutation
read/unread + archive/restore trace UpdateMyNotificationAwarenessState
stale-write and source-denied deterministic scenarios
unavailable distinct from empty
Organization switch invalidates Inbox cursor/state
mobile action/content structure remains usable
B12 explicitly not rendered
```

Wire the verifier after the B00-R2 verifier in `gate` and `gate:full`. Observe RED after all earlier gates pass.

### Task 2: Render B11 executable low-fi HTML

**File:**
- Create: `qualification/d6-r2-wireframes/b11-notifications-inbox.html`

Implement deterministic scenarios:

```text
populated
known-empty
request-unavailable
stale-awareness-write
source-access-denied
organization-switch
```

Use a structured list with explicit item identity/presentation, lenses/filter, `Carregar mais` cursor simulation, separate `Abrir origem`, read/unread and archive/restore controls, and local-only deterministic state transitions. B12 remains absent.

Run the focused verifier and full repository gate; fix HTML rather than weakening the verifier.

### Task 3: Record rendered candidate only

**Files:**
- Modify: `docs/engineering/rebaseline/D6-R2-P8-BLOCK-LEDGER.md`
- Modify: `docs/roadmap.md`
- PR #61 metadata only

Record B11 as `RENDERED CANDIDATE / VISUAL ADJUDICATION REQUIRED`, keep B00-R2 `LOCKED`, B12 not rendered, and make operator visual adjudication of B11 the exact next action. Do not set B11 `LOCKED` without operator approval.