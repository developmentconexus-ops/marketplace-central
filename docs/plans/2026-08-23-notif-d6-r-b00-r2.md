# NOTIF-01 D6-R B00-R2 Implementation Plan

> **For agentic workers:** execute this plan task-by-task with fresh verification at each checkpoint. This plan is execution scaffolding only; Product/frontend authority remains in the routed rebaseline documents.

**Goal:** Render only the operator-approved B00-R2 topbar Notification utility-slot reopen as executable low-fidelity HTML, with a bounded recent-Inbox preview and no full Inbox/Settings realization.

**Architecture:** Preserve the already-LOCKED B00 sidebar, Organization workspace, page-local Installation context, responsive shell and route semantics. Add one Organization-scoped bell utility to the header, drive it with deterministic local fixture states for unread-present / unread-empty / unavailable, and render one bounded popover whose source navigation and awareness mutation are explicitly separate. Keep the artifact as P8 evidence only; no Product runtime/frontend implementation is opened.

**Tech Stack:** standalone HTML + CSS + vanilla JavaScript + deterministic local fixtures; Node structural verifier; existing repository gate.

**Spec:** `docs/engineering/rebaseline/D6-R2-NOTIF-01-D6-R-FRONTEND-FEED-FORWARD.md`

## Global Constraints

- Product OAD remains 104 operations / 31 ordinary Permissions / H-A-S.
- B00/B01 existing LOCKs are not reopened beyond the accepted B00-R2 header utility slot.
- P8 decides layout, hierarchy, density, state placement, responsive transformation and interaction structure only; no visual-design authority.
- Bell self-Inbox access must not require `notifications.manage`.
- Unread presence uses `ListMyNotifications(archive_state=active, read_state=unread, limit=1)`; unavailable is not empty; no numeric unread count.
- Opening source must not implicitly mark read; awareness mutation is explicit via `UpdateMyNotificationAwarenessState`.
- Full Inbox B11 and Settings B12 must not be rendered as baseline in this task.
- Organization switch closes incompatible preview state before presenting the new Organization context.
- Artifact remains `CANDIDATE` until explicit operator visual adjudication; assistant/tool must not set `LOCKED`.
- Product implementation remains BLOCKED UNTIL accepted D9.

---

### Task 1: Add the B00-R2 structural verifier first

**Files:**
- Create: `scripts/verify-d6-r-b00-r2-wireframe.mjs`
- Modify: `package.json`

**Interfaces:**
- Consumes: approved D6-R spec and expected artifact path `qualification/d6-r2-wireframes/b00-r2-notifications.html`.
- Produces: a deterministic structural proof included in `gate` and `gate:full`.

- [ ] **Step 1: Write the verifier before the HTML exists**

The verifier must fail when the artifact is absent and, once present, assert at least:

```text
artifact status contains CANDIDATE and does not claim LOCKED
existing B00 sidebar IA labels remain present
no sidebar nav-item named Notificações is introduced
bell exposes ListMyNotifications trace metadata
bell has unread-present / unread-empty / unavailable deterministic scenarios
no numeric unread count/badge contract is present
preview is bounded and has a Ver todas continuation placeholder
source-open control and explicit awareness-mutation control are distinct
awareness mutation traces UpdateMyNotificationAwarenessState
bell/preview does not require notifications.manage
Organization switch closes preview state
mobile structure retains menu + title + bell with local context below
B11 full Inbox and B12 routing Settings are explicitly not rendered
```

- [ ] **Step 2: Wire the verifier into both repository gates**

Append:

```text
&& node scripts/verify-d6-r-b00-r2-wireframe.mjs
```

to both `gate` and `gate:full` after the existing NOTIF-01 OAD verifier.

- [ ] **Step 3: Verify RED on the exact candidate HEAD**

Expected result: historical Product/OAD checks pass, then the new verifier fails specifically because `qualification/d6-r2-wireframes/b00-r2-notifications.html` does not yet exist.

- [ ] **Step 4: Commit the RED proof checkpoint**

Commit message:

```text
test(d6-r): add B00-R2 structural wireframe proof
```

---

### Task 2: Render only B00-R2 as executable low-fidelity HTML

**Files:**
- Create: `qualification/d6-r2-wireframes/b00-r2-notifications.html`
- Reference only: `qualification/d6-r2-wireframes/b00-app-shell.html`

**Interfaces:**
- Consumes: locked B00 shell dimensions/IA and D6-R spec.
- Produces: one executable P8 candidate for operator visual adjudication.

- [ ] **Step 1: Preserve the locked shell verbatim in meaning**

Keep:

```text
desktop sidebar ≈264px
header ≈64px
Organization as only global workspace
page-local Installation host
same global IA groups/items
tablet collapse / mobile drawer
```

Do not add `Notificações` to the sidebar.

- [ ] **Step 2: Add the bounded header utility slot**

Desktop hierarchy:

```text
page title | page-local context | bell
```

Mobile hierarchy:

```text
menu + page title + bell
local context below
```

The bell must have an accessible name, `aria-expanded`, keyboard reachability and a non-color-only unread/unavailable treatment.

- [ ] **Step 3: Add deterministic prototype scenarios**

Prototype-only scenario controls must include:

```text
unread-known-present
unread-known-empty
knowledge-unavailable
Organization switch
```

The unread-present state uses a dot/presence marker without a number. Unavailable must remain visibly distinct from known-empty.

- [ ] **Step 4: Add the bounded recent-Inbox preview**

The preview must contain a small deterministic set of heterogeneous recent items demonstrating:

```text
unread item
read item
one typed-result item
subject_display_label-style presentation
human NotificationKind label
time context
```

Include `Ver todas` as a continuation placeholder to B11 without rendering B11.

- [ ] **Step 5: Prove source navigation and awareness mutation are separate**

Each applicable unread item must expose separate controls:

```text
Abrir origem
Marcar como lida
```

`Abrir origem` must not mutate the local read fixture. `Marcar como lida` may update only local Notification awareness fixture state and must carry `data-operation="UpdateMyNotificationAwarenessState"`.

- [ ] **Step 6: Add Organization-switch invalidation behavior**

Changing Organization must close the preview and reset/refetch the deterministic Notification fixture for the newly selected Organization. It must not preserve the prior Organization preview as if compatible.

- [ ] **Step 7: Verify GREEN**

Run the structural verifier and the full repository gate. Expected:

```text
d6_r_b00_r2_wireframe=PASS
repository full gate=PASS
```

- [ ] **Step 8: Commit the rendered candidate**

Commit message:

```text
feat(d6-r): render B00-R2 notification utility candidate
```

---

### Task 3: Record only the rendered-candidate checkpoint

**Files:**
- Modify: `docs/engineering/rebaseline/D6-R2-P8-BLOCK-LEDGER.md`
- Modify: `docs/roadmap.md`
- Modify: PR #61 body metadata only

**Interfaces:**
- Consumes: green B00-R2 artifact and verifier result.
- Produces: current status that asks the operator to visually adjudicate B00-R2 without advancing B11/B12.

- [ ] **Step 1: Add B00-R2 to the P8 ledger as CANDIDATE**

Record:

```text
artifact path
spec parent
rendered: YES
operator visual adjudication: REQUIRED
LOCKED: NO
B11/B12: NOT RENDERED
```

Do not alter the existing B00/B01 LOCK records except to state that B00-R2 is a bounded utility-slot reopen candidate.

- [ ] **Step 2: Update roadmap exact next action**

Set the next action to operator visual adjudication of only B00-R2. D7-R/D8-R/B11/B12 remain blocked from becoming baseline.

- [ ] **Step 3: Run a fresh full gate on the final exact HEAD**

Require CI + pr-title + repository full gate success on that HEAD before reporting completion.

- [ ] **Step 4: Update PR checkpoint metadata**

Keep PR #61 Draft/unmerged. Report the exact HEAD and green checks and state that B00-R2 awaits operator visual `LOCK` or revision.

---

## Self-review

- Spec coverage: B00-R2 only; no B11/B12 baseline work included.
- Placeholder scan: no implementation placeholder remains; `Ver todas` is intentionally a P8 continuation placeholder because B11 is a later block.
- Authority consistency: no Product operation/Permission/runtime change; no assistant-set `LOCKED` status.
- TDD: structural verifier is committed and observed failing before HTML creation, then reused unchanged for GREEN.
