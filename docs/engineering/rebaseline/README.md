# Architecture Rebaseline — Compatibility Pointer

> **Compatibility pointer only. This file owns no program status, allowed work, next action or architecture meaning.**

Older links may still reach this path during the current checkpoint transition. Read:

1. [`AGENTS.md`](../../../AGENTS.md)
2. [`docs/README.md`](../../README.md)

`docs/README.md` is the sole current-program status and selective documentation index. Detailed D-stage artifacts remain in this directory and are opened only when that index routes a concrete task to them.

Delete this pointer after the consolidated checkpoint reaches `main` and repository-wide references no longer depend on the old path. Git history preserves the previous router.
