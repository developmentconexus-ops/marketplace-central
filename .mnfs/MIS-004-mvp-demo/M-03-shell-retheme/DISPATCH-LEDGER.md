# DISPATCH-LEDGER — CHIP-M03 (M-03-shell-retheme)

```yaml
milestone: M-03-shell-retheme
mission: MIS-004-mvp-demo
chip: CHIP-M03
model: opus (orchestrator)
accepted_base_sha: 59d0e62fdbf15db068542432ef5d5731b6fa9f83
branch: chip/m03-shell-retheme
worktree: .claude/worktrees/chip-m03-shell-retheme
governance_base_anchor: 59d0e62fdbf15db068542432ef5d5731b6fa9f83
created: 2026-07-17
```

## Ownership (exclusive — collision matrix)

- `apps/web/src/app/**` (Layout/AppRouter/theme)
- `apps/web/src/routes/**` (NEW — route indirection per area, IC-05)
- `apps/web/src/index.css`, fonts/assets, tailwind config
- `packages/ui/src/**` (new + rethemed primitives)
- Migration block: none. OpenAPI: none. Seam locks: none (PRODUCER of IC-05 seams).

## Internal feature DAG

`F-01 (theme-tokens-fonts) → F-02 (header-nav-routes) ∥ F-03 (shared-primitives)`

## Dispatch rows

| # | Phase | Role | Model/Effort | Prompt-pack file | Verdict/Output artifact | Status |
|---|-------|------|--------------|------------------|-------------------------|--------|
| — | boot | (self) worktree+board+binding-read | opus | — | worktree @59d0e62f; board tasks #1-#3 | done |
| INV-1 | inventory | FE-surface investigator | sonnet (Agent, sync) | inline (see agent a36269956fa23b374) | inventory report in-context | done |
| PLAN-1 | P2 batch plan | Feature planner | gpt-5.6-sol / medium (OS-proc) | scratchpad/prompt-plan-m03.md | scratchpad/agent__plan-m03.last.md | done (exit 0) |
| F01-S1 | implement (tokens+@theme+radii; fonts split-off) | implementer-standard | gpt-5.6-luna / high (OS-proc, workspace-write) | dispatch/{pack-impl-v1.0.0.md,bindings-m03.md,card-F01-S1.md} → prompt-F01-S1.md · pack v1.0.0 body-sha256 17b5a9ed · prompt-sha 36cc56aa · dispatchId implementer-standard-20260717T232624Z-78a1d222 | last.md (worker) · result.json exit0 · **worker self-reported BLOCKED on ENV only** | code done; build-green (chip); tsc env-blocked (finding 2); **review in flight** |
| F01-S1-REV | review F01-S1 diff | independent sonnet reviewer (Agent, bg) | sonnet | inline rubric (agent a0a37ee43a22b2cbd) | tasks/a0a37ee43a22b2cbd.output | **PASS-WITH-NITS** (nits non-defects: env tsc gap, deferred-utility emission) |
| F01-S1 COMMIT | commit slice | (chip) | opus | apps/web/src/index.css | **27c48b53** on chip/m03-shell-retheme | COMMITTED (build-green, reviewed) |
| F01-S2 (att.1) | implement theme boot/hook | implementer-complex | gpt-5.6-sol / low | prompt-F01-S2.md · dispatchId …-109485ff | agent__…-109485ff.last.md | **BLOCKED before impl, ZERO writes** — auto-discovered `impeccable` skill fired NO_PRODUCT_MD, demanded out-of-write_set PRODUCT.md; worker halted per pack stop-on-conflict. See F-ENV-3. |
| F01-S2 (att.2) | implement theme boot/hook (hardened bindings + skill-discovery denylist) | implementer-complex | gpt-5.6-sol / low | prompt-F01-S2b.md · prompt-sha 63df1798 · dispatchId implementer-complex-20260718T000645Z-78eeb016 | scratchpad/agent__…-78eeb016.last.md | **code COMMITTED 2463834f** (theme.ts/useTheme.ts/theme.test.ts/index.html boot; matches card exactly) — worker could not run vitest (sandbox); chip-side vitest revealed real test-env defect (see FIX row) |
| F01-S2 FIX | chip fix-forward: jsdom localStorage shim in theme.test.ts | (chip) | opus | apps/web/src/app/theme/theme.test.ts (+18) | **1653dbd2** | COMMITTED. Env-specific defect (jsdom 26/vitest 3.2.4 does not instantiate window.localStorage → 9/9 theme tests failed at storage access). Only reproducible/fixable chip-side (codex sandbox can't run vitest — F-ENV-1), so fix-forward not re-dispatch. Map-backed Storage.prototype shim preserves `vi.spyOn(Storage.prototype,…)` semantics. Impl untouched. Now **9/9 green**; tsc 0 new errors. |
| F01-S2 REV | review F01-S2 full slice (theme.ts/useTheme.ts/theme.test.ts+shim/index.html boot) | independent sonnet reviewer (Agent a5672556f19b0baa1) | sonnet | card-F01-S2 rubric | 0🔴0🟡0❓ | **PASS** — all 7 rubric items pass; shim independently confirmed Map-backed real storage (not theater), Storage.prototype spy-intercept intact, fonts untouched, coverage real. tsc: 0 errors in theme files. F-01 lane (S1+S2) GREEN + reviewed. |

| F01-S3 | fonts wire + CDN removal | (chip, trivial-glue ≤10 lines) | opus | apps/web/src/main.tsx (+6), apps/web/index.html (−3) | **cdad8572** | COMMITTED. Chip-direct (9 verbatim-specified line-changes; codex sandbox can't run vite build anyway per F-ENV-1 so worker gains no verification). Precondition verified (all 6 @fontsource weights resolve). Gate GREEN chip-side: 6 imports present, 0 Google-Fonts refs, boot script intact, tsc clean, **vite build ✓ 14.79s** bundling all weights as hashed local .woff2/.woff (zero external font req = hub D-03 cond.2 satisfied; C01 evidence). |
| F01-S3 REV | review F01-S3 diff | independent sonnet reviewer (Agent aad4cc1bca9e7e484) | sonnet | card-F01-S3 rubric | 0🔴0🟡0❓ | **PASS** — 6 imports correct + families match index.css --font-sans/--font-mono, 3 CDN links gone, boot script intact, only 2 files touched. **F-01 FEATURE COMPLETE + fully reviewed (S1 PASS-w-nits, S2 PASS, S3 PASS).** |
| F03-S1 | implement MarginChip (IC-04 primitive + tests) | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write) | dispatch/{pack-impl-v1.0.0.md,bindings-m03.md,card-F03-S1.md} → prompt-F03-S1.md · dispatchId …-60f028c1 · packBodySha 17b5a9ed (locked, intact) | **worker COMMITTED 7022de38** · agent__…-60f028c1.last.md · exit0 | **GREEN chip-side**: vitest 11/11, tsc 0 MarginChip errors, barrel untouched, no raw palette. All 8 color tokens + rounded-pill VERIFIED resolvable in index.css @theme (no unstyled-chip). Honest-unknown ✓ ADR-17 (null/NaN/±Inf→"—", never 0%/number). |
| F03-S1 REV | review F03-S1 diff | independent sonnet reviewer (Agent, bg) | sonnet | card-F03-S1 rubric | 0🔴0🟡0❓ | **PASS** — API exact, honest-unknown structurally enforced, thresholds correct, dev-warn guarded by import.meta.env.DEV, only semantic tokens, barrel untouched (F03-S6 owns), tests cover null/finite-bands/NaN/±Inf/custom-valid/inverted with real assertions. implementer(codex luna)≠reviewer(sonnet). |
| F02-S1 | route seam (6 area route modules + AppRouter indirection) | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write) | dispatch/{pack-impl-v1.0.0.md,bindings-m03.md,card-F02-S1.md} → prompt-F02-S1.md · dispatchId …-a2a17f30 · packBodySha 17b5a9ed | **worker COMMITTED 25b02c8** · agent__…-a2a17f30.last.md · exit0 | **GREEN chip-side**: 7 files = write_set exact; 6 route imports present; KEEP routes (catalogo/estoque/integracoes/protocolos/classifications/marketplaces) preserved; 6 LegacyRedirects intact (AppRouter 52-57); NO catch-all/Navigate; tsc **171 = exact F-ENV-4 baseline, 0 NEW** (precos.tsx:6 TS2322 Client→SimulatorClient is the OLD PricingSimulatorPageWrapper error relocated verbatim); vite build ✓ 3.06s. |
| F02-S1 REV | review F02-S1 diff | independent sonnet reviewer (Agent a21d0f8915b47cbac, bg) | sonnet | card-F02-S1 rubric | 9/9 PASS | **PASS** — char-level cross-check vs parent 7022de38: only deltas = 6 route-component swaps + import/wrapper cleanup; every path string, KEEP route, LegacyRedirect row character-identical; no catch-all; dead code removed; no new dep; precos TS2322 confirmed relocated-verbatim baseline. **F02-S1 GREEN + reviewed → F02-S2 unblocked.** |
| F02-S2 (att.1) | horizontal header + pills + ⚙ menu (demo-critical) | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write) | prompt-F02-S2.md · dispatchId …-f193efe1 · promptSha b6688d96 · packBodySha 17b5a9ed | agent__…-f193efe1.last.md · exit0-but-**BLOCKED zero-writes** | **BLOCKED, ZERO WRITES** — codex workspace-write sandbox rooted at DISPATCHER cwd (sleepy-wing-6d7500 parent worktree), not chip worktree; all write_set paths "outside project", patch rejected. NOT a code fault → F-ENV-6. Worker correctly left everything in place, no commit. Re-dispatch att.2 with cwd=chip worktree. |
| F02-S2 (att.2) | horizontal header + canonical pills + ⚙ menu + theme/install controls; kill sidebar (demo-critical) | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd fixed = chip worktree**) | prompt-F02-S2b.md · dispatchId …-e0cfdc39 · packBodySha 17b5a9ed (locked) | **worker COMMITTED 8b7bc09** · agent__…-e0cfdc39.last.md · exit0 | **GREEN chip-side** (after test FIX): Header.tsx/Layout.tsx production **0 tsc errors**; dead sidebar palette + navItems + Vínculos ABSENT; 6 pills exact order (Mercado+Repasses = non-nav `<span>` stubs w/ "em breve"); ⚙ native `<details>` menu 4 entries + DIFAL→/precos?params=1; useTheme wired (no 2nd state); install selector preserved; vite build ✓ 2.66s. Header.test 7/7 green after chip FIX. |
| F02-S2 FIX | chip fix-forward: 3 Header.test.tsx queries (worker's tests never ran under sandbox F-ENV-1) | (chip) | opus | apps/web/src/app/Header.test.tsx | **3df4d718** | COMMITTED. Component (Header.tsx) was CORRECT; the tests mis-queried it: (1) pill-order filter missed disabled pills (outer textContent="Mercado em breve"≠"Mercado"); (2) getByText("Mercado") couldn't match label beside nested "em breve" badge; (3) `<summary>` not exposed as role "button" in jsdom. Fixed test queries only, assertions preserved → 7/7 green. tsc delta = +10 TS2339 jest-dom matchers in the NEW test file = F-ENV-4 env-gap category (NOT real errors; 0 in production files). |
| F02-S2 REV | review F02-S2 full slice (Header.tsx/Layout.tsx/Header.test.tsx) | independent sonnet reviewer (Agent a6c799f6e5d04de97, bg) | sonnet | card-F02-S2 rubric | 11/12 PASS + 1 nit | **PASS** — diff 25b02c8→3df4d718 = exactly Header.tsx/Header.test.tsx/Layout.tsx (InstallationContext/ClientContext/useTheme all zero-diff); 6 pills exact order; Mercado/Repasses genuine `<span>` non-nav w/ "em breve"; NavLinks preserve location.search; active from NavLink isActive (`/` has `end`); toggleTheme from F-01 hook (no 2nd state); install selector 3 status branches; ⚙ `<details>` 4 entries + DIFAL literal `/precos?params=1` (not search-merged); no Vínculos; only semantic tokens (grep clean); tsc 0 attributable; 7/7 green via `vitest run Header.test.tsx`. **Non-blocking nit:** theme test asserts absence-of-throw only (item 6 separately code-verified). implementer(codex luna)≠reviewer(sonnet). **F-02 COMPLETE.** |
| F03-S2 | retheme 6 state primitives (UnknownValue/FreshnessIndicator/EmptyState/ErrorState/LoadingState/ConflictTag) + FactStates/LoadStates tests | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd=chip worktree**) | dispatch/{pack,bindings,card-F03-S2.md} → prompt-F03-S2.md · dispatchId …-799ab6df · promptSha b794f544 · packBodySha 17b5a9ed (locked) | **worker COMMITTED 23868155** · agent__…-799ab6df.last.md · exit0 (bg bxqyq84y2) | **GREEN chip-side**: 8 files = write_set exact; FactStates+LoadStates **9/9 green**; forbidden palette (slate/red/amber-100/amber-800/bg-white/blue) grep **0 matches**; semantic tokens present (text-faint/muted/warn, bg-amber-soft, rounded-pill, font-mono); ErrorState diff = token-only (text-red-700→text-warn), props byte-identical (onRetry already required → 2 Mutation-page onRetry errors are PRE-EXISTING baseline); formatAsOf frozen (import+call preserved); UnknownValue "—"=text-faint neutral (honest-unknown); tsc **181 total = 0 NEW vs post-F02-S2 baseline**. |
| F03-S2 REV | review F03-S2 slice @ 23868155 (6 state primitives + 2 tests) | independent sonnet reviewer (Agent a818d6d7a6edf4779, bg) | sonnet | card-F03-S2 rubric | 7/7 PASS, 0 nits | **PASS** — write-set exact (8 files, no barrel/Button/web-query/pages); per-file palette mapping matches card token-for-token (UnknownValue+text-faint, FreshnessIndicator+text-muted/xs/mono, EmptyState slate→muted/faint, ErrorState red-700→warn only, LoadingState slate-500→muted, ConflictTag amber-100/800→amber-soft/amber+rounded-pill); forbidden-token grep exit1 (zero); formatAsOf + packages/web-query ZERO diff (frozen); both test files purely additive (no removed expects), real toHaveClass DOM assertions (ErrorState text-warn correctly on wrapper via .parentElement); honest-unknown neutral. implementer(codex luna)≠reviewer(sonnet). **F03-S2 COMPLETE.** |
| F03-S3 | retheme 4 base primitives (Button/SurfaceCard/Badge/StatCard) + Badge.test extend + BasePrimitives.test new | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd=chip worktree**) | dispatch/{pack,bindings,card-F03-S3.md} → prompt-F03-S3.md · dispatchId …-bdf2db34 · promptSha 01a25c1d · packBodySha 17b5a9ed (locked) | **worker COMMITTED 530bdc3** · agent__…-bdf2db34.last.md · exit0 (bg b6nfuzney) | **GREEN chip-side**: 6 files = write_set exact; Badge 5/5 + BasePrimitives 6/6 = **11/11 green**; forbidden palette (slate/blue/emerald/red/bg-white) grep **0 matches**; token swaps match card token-for-token (Button primary=bg-accent hover:accent-ink/secondary=bg-surface hover:surface-2 border-border/danger=bg-warn hover:opacity-90 + rounded-control; Badge pending=surface-2/muted, in_progress=info-soft/info, succeeded+completed=accent-soft/accent-ink, failed=warn-soft/warn + rounded-pill; SurfaceCard+StatCard bg-surface/border-border/rounded-card; StatCard value font-mono via style, honest passthrough); tsc **181 = 0 NEW** (0 attributable to the 4 production files; packages/ui test files are outside apps/web tsc program, proven green via vitest esbuild). |
| F03-S3 REV | review F03-S3 slice @ 530bdc3 (4 base primitives + 2 tests) | independent sonnet reviewer (Agent abf920d068b4966be, bg) | sonnet | card-F03-S3 rubric | 7/7 PASS, 0 nits | **PASS** — write-set exact (6 files, no barrel/MarginChip/state-primitive/web-query/pages); palette mapping matches card token-for-token (Button primary/secondary/danger + rounded-control, SurfaceCard+StatCard bg-surface/border-border/rounded-card, Badge 5 statuses + rounded-pill); forbidden grep zero; variant names/props/labels/loading-disabled logic byte-identical; StatCard honest value passthrough ({value} verbatim, tests assert "—"/"1.234" exact); both test files additive w/ real toHaveClass DOM assertions (no tautologies); no new dep/import. Reviewer independently ran vitest 11/11 green. Nit: danger uses hover:opacity-90 per card (no darker-warn token) — correct, no token invention. implementer(codex luna)≠reviewer(sonnet). **F03-S3 COMPLETE.** |
| F03-S4 | retheme 3 complex primitives (ProductPicker/PaginatedTable/DetailPanel) + their 3 tests | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd=chip worktree**) | dispatch/{pack,bindings,card-F03-S4.md} → prompt-F03-S4.md · dispatchId …-c77d0efa · promptSha c1a84da2 · packBodySha 17b5a9ed (locked) | **worker COMMITTED 748b615** · agent__…-c77d0efa.last.md · exit0 (bg bqbak2x53) | **GREEN chip-side** (after 1 test FIX): 6 files = write_set exact; ProductPicker 7/7 + PaginatedTable 9/9 + DetailPanel 9/9 = **25/25 green**; forbidden palette **0** (word-boundary `\bslate-` grep — naive `slate-` false-positives on `-translate-y-1/2`, worker flagged; verified clean); semantic tokens land **1:1** with raw-palette inventory (ProductPicker 39 swaps, PaginatedTable 18, DetailPanel 7 — every raw count maps exactly); formatCurrency FROZEN (toLocaleString pt-BR BRL, honest, no coercion); no API/copy/aria/keyboard/pagination/selection change; tsc **181 = 0 NEW** (DetailPanel matches are pre-existing F-ENV-4 in apps/web/src/pages/ListingDetailPanel.test.tsx substring). |
| F03-S4 FIX | chip fix-forward: 1 ProductPicker.test.tsx price matcher (worker's test never ran under sandbox F-ENV-1) | (chip) | opus | packages/ui/src/ProductPicker.test.tsx | **a48c4a17** | COMMITTED. Component + formatCurrency CORRECT; the added "formats known prices" test used `getByText(toLocaleString(...))` whose pt-BR output keeps a NON-breaking space (U+00A0) after "R\$"; testing-library normalizes the DOM node's nbsp→regular space, so the exact string never matched. Fixed matcher only → `getByText(/R\$\s*200,00/)` (\s matches either space); assertion intent (honest formatted price appears) preserved. 25/25 green. |
| F03-S4 REV | review F03-S4 slice @ 748b615 + fix a48c4a17 (3 complex primitives + 3 tests) | independent sonnet reviewer (Agent abf0cdbf6188fb351, bg) | sonnet | card-F03-S4 rubric | 7/7 Required PASS, 1 nit | **PASS** — write-set containment exact (full-repo `git diff --name-only 530bdc3d..a48c4a17` = the 6 files, nothing else); forbidden palette **0** (word-boundary rg across full color vocab + bg-white/amber-100/amber-800/#0F172A; the lone `-translate-y-1/2` @ ProductPicker.tsx:146 correctly untouched — documented false-positive); token map faithful (every substitution matches declared raw→semantic, no invented tokens); behavior/API/copy/aria/keyboard **unchanged** (className-only, line-symmetric 29-/29+ in ProductPicker; all aria-labels + pagination text + DetailPanel Escape handler intact); formatCurrency FROZEN honest passthrough (ProductPicker.tsx:44-46, ADR-17); tests real (genuine toHaveClass/getByText/getByRole DOM assertions, nbsp regex fix preserves intent) — reviewer independently ran all 3 suites **25/25 green** (DetailPanel 9 + ProductPicker 7 + PaginatedTable 9); no anti-slop. implementer(codex luna)≠reviewer(sonnet). **Nit (non-blocking):** DetailPanel close-button hover `hover:text-ink` vs literal-map `hover:text-muted` — valid theme token, defensible one-step-darker hover, NOT an invented token. **F03-S4 COMPLETE.** |
| F03-S5 | NEW controlled `DataTable<T>` shell primitive (2 new files) | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd=chip worktree**) | dispatch/{pack,bindings,card-F03-S5.md} → prompt-F03-S5.md · dispatchId …-1c61a303 · promptSha 19a62495 · packBodySha 17b5a9ed (locked) | **worker COMMITTED 7d29b37** · agent__…-1c61a303.last.md · exit0 (bg bxzcvhspa) | **GREEN chip-side**: write-set exact (`git diff --name-only a48c4a17..7d29b37` = DataTable.tsx + DataTable.test.tsx only, barrel untouched); DataTable.test.tsx **8/8 green** (run in isolation per F-ENV-5); forbidden palette **0** (word-boundary rg full color vocab + bg-white/#0F172A); **no internal data state** (`useState\|useReducer\|useQuery\|fetch(` grep = 0) — exactly **1 useRef** @ line 40 (headerCheckboxRef, indeterminate DOM prop — the sole allowance per card); semantic tokens present (11 matching lines); tsc **181 = 0 NEW** (0 DataTable mentions). Fully controlled / value-agnostic / minimal-API per card. |
| F03-S5 REV | review F03-S5 slice @ 7d29b37 (NEW DataTable shell + test) | independent sonnet reviewer (Agent a47919ab0b2b5f89a, bg) | sonnet | card-F03-S5 rubric | 7/7 Required PASS, 2 nits | **PASS** — write-set exact (repo-wide `git diff --stat a48c4a17..7d29b37` = DataTable.tsx +159 / DataTable.test.tsx +179, barrel + manifests empty); fully controlled (grep useState/useReducer/useEffect/useQuery/useContext/createContext/fetch = 0; sole hook = useRef @:1,:40 → sets `element.indeterminate` on header checkbox @:83-86); API matches field-for-field (:3-24, no extra/missing prop); behavior correct (selection gated on `onSelectionChange!==undefined`; toggle builds fresh Set via `new Set(selectedKeys??[])`+copy-mutate @:66-72, never mutates incoming; both checkboxes `stopPropagation` @:90,:138; sortable header=`<button>` ▲/▼ only when active @:103-112; selected row bg-accent-soft @:129; loading=spinner-no-rows @:47-53; empty=`emptyState ?? "Nenhum item."` @:55-56); honest/value-agnostic (cell = `column.render(row)` verbatim @:149; test asserts "—" verbatim + `queryByText("0")`/"R$ 0" absent @:168-171, ADR-17); forbidden palette **0** (word-boundary grep); tests real — reviewer ran isolated **8/8 green**. implementer(codex luna)≠reviewer(sonnet). **Nits (non-blocking):** (a) sortable-but-inactive header shows neutral `↕` glyph @:109-111 — matches card's "neutral affordance" spec, not a defect; (b) `align="left"` is implicit default (only `"right"` branches) — correct via table's `text-left`. **F03-S5 COMPLETE.** |
| F03-S6 | NEW `DetailDrawer` (composes DetailPanel) + WIRE barrel (export MarginChip+DataTable+DetailDrawer) — closes C05 | implementer-standard | gpt-5.6-luna / high (OS-proc workspace-write, **cwd=chip worktree**) | dispatch/{pack,bindings,card-F03-S6.md} → prompt-F03-S6.md · dispatchId …-95a2f93d · promptSha 67f77234 · packBodySha 17b5a9ed (locked) | **worker COMMITTED 169d5f4** · agent__…-95a2f93d.last.md · exit0 (bg bh6b6d43d) | **GREEN chip-side** (after fix-forward f7ff6237): write-set exact (`git diff --name-only 7d29b37..169d5f4` = DetailDrawer.tsx + DetailDrawer.test.tsx + index.ts); DetailDrawer is **pure composition** of DetailPanel (delegates open/onClose/title/subtitle/closeLabel/width/footer; ZERO duplicated Escape/positioning/aria — 45-line thin wrapper); defaults `closeLabel="Fechar"` + `width=360` (design 300–380 band); barrel **additive** — all 13 prior exports byte-intact + MarginChip (`export *`) + DataTable + DetailDrawer (+types); DetailDrawer.test.tsx **6/6 green** incl. **barrel export-contract** (`typeof MarginChip/DataTable/DetailDrawer === "function"` = the C05 guard); forbidden palette **0**. |
| F03-S6 FIX | chip fix-forward: JSX-namespace + import.meta.env tsc errors surfaced when barrel first pulled MarginChip/DataTable into apps/web tsc program | (chip) | opus | packages/ui/src/{DataTable,MarginChip}.tsx | **f7ff6237** | COMMITTED. Barrel wiring exposed 3 latent errors in previously-uncompiled files (F-ENV-7 below): DataTable:39 + MarginChip:21 `JSX.Element` w/o `import type {JSX} from "react"` (TS2503 — React 19 automatic runtime drops the JSX global; DetailDrawer already imported it) → added the import to both; MarginChip:24 dev-warn gated on untyped `import.meta.env.DEV` (TS2339, same class as pre-existing baseline apps/web/src/app/ClientContext.tsx:20 — repo has no vite/client types) → dropped the env gate (shared presentational primitive shouldn't couple to a bundler global; warn now fires on any invalid-thresholds caller = programmer error, never prod data; MarginChip.test.tsx warn assertion still passes 11/11). tsc back to **181 = 0 NEW**; render output + public API unchanged. |
| F03-S6 REV | review F03-S6 slice + fix @ 169d5f4..f7ff6237 (DetailDrawer + barrel + fix-forward) | independent sonnet reviewer (Agent a0cfafbcc1cb7ae96, bg) | sonnet | card-F03-S6 rubric | 7/7 Required PASS, 0 nits | **PASS** — DetailDrawer.tsx:15-38 pure wrapper (no useState/useEffect/Escape/positioning/aria — all delegated; confirmed DetailPanel owns Escape @:25-32, role/aria @:38-39, positioning @:41, close btn @:50-56); API exact (:4-13, no extras); barrel additive (169d5f4 index.ts = pure 5-line append after DetailPanel exports, 13 prior byte-intact/unreordered); export-contract test real (DetailDrawer.test.tsx:79-83 imports MarginChip/DataTable/DetailDrawer from "./index", asserts each `typeof==="function"` = C05 guard; + render/subtitle/actions/closed-state/Escape+button-close/width-360-default+320-override/token assertions, non-tautological); forbidden palette **0** (word-boundary); containment exact (169d5f4 = 3 write_set files 127+/0-; f7ff6237 = DataTable +1/-1 + MarginChip +3/-1 only); fix-forward correct+minimal (JSX import matches DetailDrawer precedent, fixes TS2503 no-logic-change; env-gate removal sound — `hasValidThresholds` fails only on non-finite or healthy<=tight = programmer error never prod data path, so no prod noise; render logic + public API untouched). Reviewer ran isolated: DetailDrawer **6/6**, MarginChip **11/11**, DataTable **8/8**; tsc **181 = baseline, 0 new-primitive mentions**. implementer(codex luna)≠reviewer(sonnet). **F03-S6 COMPLETE → F-03 COMPLETE.** |

**F-ENV-7 (barrel-exposed tsc, F03-S6):** files under packages/ui/src are compiled by `apps/web/tsconfig.json` ONLY when reachable from apps/web's import graph — i.e. once re-exported by the barrel index.ts. A new primitive committed WITHOUT a barrel export (F03-S1 MarginChip, F03-S5 DataTable) is invisible to `tsc -p apps/web` until the barrel wires it, so its type errors surface only at the barrel-integration slice, not at its own slice. Corollary: the S1/S5 GREEN tsc checks were true-but-incomplete (file not yet in program); the barrel slice is where per-primitive tsc-cleanliness is actually proven. Two repo env-gaps confirmed alongside: (1) React 19 automatic runtime — `JSX.Element` needs `import type { JSX } from "react"` (no global JSX); (2) `import.meta.env` is untyped repo-wide (no vite/client types; ClientContext.tsx:20 carries the identical baseline TS2339 ×2). Neither is a defect in the primitives; #1 fixed per-file, #2 avoided by decoupling.

**INVARIANT (F01-S1, from review check-7):** the hand-authored `:root {}` and `[data-theme="dark"]{}` blocks in apps/web/src/index.css MUST stay UNLAYERED. The @theme font/radius aliases are self-referential (`--font-sans: var(--font-sans)`), safe ONLY because an unlayered `:root` outranks the `@layer theme` copy (CSS cascade-layers spec). If any future slice wraps that `:root` block in `@layer base` (or similar), the self-reference flips to genuinely circular/invalid and fonts+radii break. No M-03 slice touches this block after F01-S1; flag to post-close area milestones.

**Font-split note (chip refinement of plan F01-S1):** plan bundled fonts into F01-S1; chip split font-face+binaries+CDN-removal into a later slice because a codex worker cannot source .woff2 binaries offline (provenance = hub REQUEST sent to HUB local_c5532363). F01-S1 = tokens/@theme/radii/font-family-vars only. Critical path (F01-S2→F-02∥F-03) unaffected.

**FONT GRANT — hub ledger D-03 @ c6df5cc1 (received 2026-07-17):** GRANTED option A = `@fontsource/instrument-sans` + `@fontsource/ibm-plex-mono` in apps/web/package.json. Conditions: (1) ONLY these two deps, no stowaways; (2) same slice removes the 3 Google Fonts CDN links from index.html (offline demo = zero runtime CDN fetches; C01 covers both "fonts load" AND "no external font request"); (3) lockfile change reviewed like any slice. No further approval needed.

**Revised F-01 lane topology (post-grant):**
- F01-S1 = tokens/@theme/radii/font-family-vars only (RUNNING, unchanged).
- F01-S2 = theme pre-paint boot script (index.html) + theme.ts/useTheme/theme.test.ts. **CDN removal REMOVED from S2** (moved to S3) to keep index.html single-writer-per-slice serialized.
- F01-S3 (NEW) = `@fontsource/*` weight imports in apps/web/src/main.tsx + remove 3 Google Fonts CDN links from index.html. depends_on [F01-S2] + chip dep-add. Satisfies hub condition 2 (font slice removes CDN).
- CHIP dep-add (chip-owned, NOT a codex slice): `npm install @fontsource/instrument-sans @fontsource/ibm-plex-mono -w @marketplace-central/web` — run ONLY after F01-S1 completes (never concurrent with a writer holding the worktree). Own commit `chore(web): add @fontsource font packages [hub-grant D-03]`; independent sonnet reviews lockfile delta (exactly 2 granted + transitive font files, no stowaways) per condition 3.

**HUB_SESSION_ID:** local_c5532363-d407-49ff-9bac-120c328b9dcf ("HUB: MIS-004 marketplace-central hub").

## Changed-path reconciliation (per slice, at slice SHA)

- **F01-S1** (pre-commit, working tree): `git diff --name-only <base>` = `apps/web/src/index.css` ONLY. Card write_set = {apps/web/src/index.css}. Declared==changed ✓. No undeclared paths. (Untracked `.mnfs/.../DISPATCH-LEDGER.md` + `dispatch/` = chip artifacts, not slice output.)
- **F01-S3** (commit cdad8572): `git show --stat` = `apps/web/index.html`, `apps/web/src/main.tsx` ONLY. Card write_set = exactly those 2. Declared==changed ✓. No package.json/lockfile/index.css touched; dist/ build output gitignored (not committed).
- **F01-S2** (commits 2463834f + fix 1653dbd2): `git diff --name-only 27c48b53..1653dbd2` (theme scope) = `apps/web/index.html`, `apps/web/src/app/theme/{theme.ts,useTheme.ts,theme.test.ts}`. Card write_set = exactly those 4. Declared==changed ✓. No pages/**, no shared config touched (shim is in-file, NOT vitest.config.ts). (8e51f5b dep-add interleaved chronologically but is its own reviewed commit, not F01-S2 output.)
- **F02-S1** (commit 25b02c8): `git show --stat` = `apps/web/src/app/AppRouter.tsx` (edit) + `apps/web/src/routes/{dashboard,anuncios,vinculos,produto,precos,pedidos}.tsx` (6 new). Card write_set = exactly those 7. Declared==changed ✓. No pages/** created (route modules re-export existing pages, don't reimplement); Layout/InstallationContext/ClientContext/LegacyRedirect untouched; no route path strings altered.
- **F02-S2** (commits 8b7bc09 worker + 3df4d718 chip test-fix): `git show --stat 8b7bc09` = `apps/web/src/app/{Header.tsx(new),Header.test.tsx(new),Layout.tsx(edit)}`; the FIX touched only Header.test.tsx. Card write_set = exactly those 3. Declared==changed ✓. No AppRouter/routes/pages/**/InstallationContext/ClientContext/packages touched; routing untouched (disabled pills have no route, correct). Layout gutted (aside+navItems+lucide removed) → renders `<Header/>` + `<main><InstallationGate><Outlet/></InstallationGate></main>`.
- **F03-S2** (commit 23868155): `git diff-tree --name-only -r 23868155` = `packages/ui/src/{UnknownValue,FreshnessIndicator,EmptyState,ErrorState,LoadingState,ConflictTag}.tsx` (6 retheme) + `packages/ui/src/{FactStates,LoadStates}.test.tsx` (2 extend). Card write_set = exactly those 8. Declared==changed ✓. ErrorState.tsx diff = single-line token swap (text-red-700→text-warn), props/structure byte-identical; no web-query/Button/barrel/pages touched; formatAsOf untouched.
- **F03-S3** (commit 530bdc3): `git diff-tree --name-only -r 530bdc3` = `packages/ui/src/{Button,SurfaceCard,Badge,StatCard}.tsx` (4 retheme) + `packages/ui/src/Badge.test.tsx` (extend) + `packages/ui/src/BasePrimitives.test.tsx` (new). Card write_set = exactly those 6. Declared==changed ✓. No index.ts barrel / MarginChip / state-primitive / web-query / pages touched; component APIs+labels+structure byte-identical (token-only), StatCard honest value passthrough preserved.
- **F03-S4** (commits 748b615 worker + a48c4a17 chip test-fix): `git diff --name-only 530bdc3..a48c4a17` = `packages/ui/src/{ProductPicker,PaginatedTable,DetailPanel}.tsx` (3 retheme) + their 3 `.test.tsx`. Card write_set = exactly those 6; the FIX touched only ProductPicker.test.tsx. Declared==changed ✓. No barrel/other-primitive/pages/deps; className-only diffs; formatCurrency frozen honest.
- **F03-S5** (commit 7d29b37): `git diff --name-only a48c4a17..7d29b37` = `packages/ui/src/{DataTable.tsx,DataTable.test.tsx}` (both NEW) ONLY. Card write_set = exactly those 2. Declared==changed ✓. Barrel + all other files untouched.
- **F03-S6** (commit 169d5f4): `git diff --name-only 7d29b37..169d5f4` = `packages/ui/src/{DetailDrawer.tsx(new),DetailDrawer.test.tsx(new),index.ts(edit)}`. Card write_set = exactly those 3. Declared==changed ✓. Barrel edit additive (13 prior exports byte-intact); no other primitive/pages/deps.
- **F03-S6 FIX** (commit f7ff6237, chip): `git show --stat` = `packages/ui/src/{DataTable.tsx,MarginChip.tsx}` ONLY (DataTable +1/-1 JSX import; MarginChip +3/-1 JSX import + env-gate drop). Scoped to the two barrel-exposed tsc errors; no other file, no logic/API/render change.
- **Close-L0 FIX** (commit 17b93d98, chip): `git show --stat` = `apps/web/src/app/Layout.test.tsx` ONLY (test-only). Reconciled 2 stale nav assertions (old in-Layout sidebar contract) to the ratified Header nav; no source file touched, tsc unchanged (181), full suite 215/215. See F-ENV-8.

## Findings (core §0 — reported to hub for profile ratification)

- **F-ENV-1** codex `--sandbox workspace-write` cannot run the vite build on Windows (esbuild access-denied / can't resolve vite.config.ts). Same build GREEN chip-side. → FE implementers will falsely BLOCK on `npm run build`; chip re-runs L0/L1 chip-side as verification of record. Reported to hub 2026-07-17.
- **F-ENV-2** `@types/node` missing repo-wide (base SHA + main); apps/web/tsconfig `"types":["node"]` → raw `tsc` fails TS2688 everywhere. Pre-existing; blocks L0 typecheck contract. REQUEST sent to add root `@types/node` devDep. Reported to hub 2026-07-17.
- **F-ENV-4** base is NOT tsc-clean even WITH @types/node (D-05 applied): `npx tsc --noEmit -p apps/web/tsconfig.json` = **171 errors**, 162 of them TS2339 (missing @testing-library/jest-dom matcher augmentation — `toBeInTheDocument`/`toHaveAttribute`/etc. across every `.test.tsx`), remainder AppRouter.tsx Client-type mismatches (TS2345/2322/2741). ALL in files no M-03 slice touches; ZERO in theme/routes/index paths. IMPLICATION: L0 "tsc clean" contract is unmeetable against this base — the close gate must score **"no NEW tsc errors vs base SHA 59d0e62"**, not absolute zero. My committed slices add 0 new. Recommend hub add a jest-dom types augmentation (`@testing-library/jest-dom` type ref) at repo level (out of M-03 FE-retheme scope). Reported to hub 2026-07-17.
- **F-ENV-5** `packages/ui/src/ProductPicker.test.tsx > "renders product rows"` FLAKES under the full `npm run test -w web` parallel run (1 failed / 174; that file took 11.4s, whole run transform 120.9s = heavy contention) but passes **6/6 in isolation (68ms)**. Untouched since base; not caused by any M-03 change. Flaky test-infra noise, not a milestone blocker. Reported to hub 2026-07-17.
- **F-ENV-6** `Invoke-CodexDispatch.ps1` roots the codex `--sandbox workspace-write` writable tree at the **DISPATCHER process cwd** (it launches codex inside a `Start-Job` child which inherits parent cwd; the script never `Set-Location`s to the worktree from bindings). If the dispatcher is invoked from a DIFFERENT worktree than the target (here: session cwd = sleepy-wing-6d7500 parent, target = chip-m03-shell-retheme), every write is rejected `writing outside of the project` and the worker BLOCKS with zero writes (F02-S2 att.1, exit0-but-blocked). Prior slices succeeded only because they happened to be launched from the worktree cwd. MITIGATION (applied): `Set-Location <worktree>;` in the SAME command immediately before Invoke-CodexDispatch. RECOMMEND hub: add a mandatory `-WorktreeRoot` param to Invoke-CodexDispatch that `Set-Location`s (or sets Start-Job -WorkingDirectory) internally, removing the silent-cwd footgun. Reported to hub 2026-07-18.
- **F-ENV-3** auto-discovered `impeccable` skill (NO_PRODUCT_MD hook) derails codex workers → BLOCKED with zero writes (F01-S2 att.1). Workers only get pack+bindings+card, never learned "auto-skills don't bind"; pack stop-on-conflict misfires. FIX: added SKILL-DISCOVERY DENYLIST clause to bindings-m03.md (ignore impeccable/out-of-write_set mandates; not a contract conflict). Recommended hub fold into canonical pack + neutralize impeccable auto-injection at source. Reported to hub 2026-07-17.
- **F-ENV-8** (isolated-review blind spot for MOVED responsibilities) slice reviews run per-slice tests IN ISOLATION (correct mitigation for the F-ENV-5 full-parallel flake), but that hides regressions in a PRE-EXISTING test file when a slice MOVES a responsibility out of the component that file tests. F02-S2 moved the primary nav from Layout → new Header (Layout gutted to a shell), added Header.test.tsx for the new contract, but left two Layout.test.tsx cases asserting the removed in-Layout sidebar nav. Both went RED — invisible to the F02-S2 isolated review (which ran Header.test.tsx) and only caught by the **close-time full-suite L0 run** (2 failed / 215). Root cause: the moved-out test file was neither updated nor re-run at the moving slice. LESSON: when a slice removes/relocates behavior, its review must also re-run the tests of the OLD location; the full-suite L0 at CLOSE is the essential backstop (do not skip it, do not trust green isolated-slice runs as full coverage). MITIGATION (applied at close): chip reconciled Layout.test.tsx to the ratified nav contract (commit 17b93d98). Reported to hub 2026-07-18.

## Events emitted

- REQUEST (font provenance) → hub local_c5532363. ANSWERED: GRANT D-03 (@fontsource ×2). ACK sent.
- REQUEST (@types/node devDep) + FINDINGS F-ENV-1/F-ENV-2 → hub. ANSWERED (hub 1c1b83f9): F-ENV-1 RATIFIED profile §3 (chip-side re-run = verification of record; mitigation a); F-ENV-2 GRANTED D-05 = `@types/node@^22` ROOT devDep + profile §2 L1 allowlist entry for base TS2688 (cite, don't re-prove; retires at M-03 merge).
- FINDING F-ENV-3 (impeccable) → hub. Pending ratification (mitigation already applied in bindings).
- **REQUEST (P7 dev-stack re-point) → hub local_c5532363** (2026-07-18): the running docker dev stack at :5174 serves **stale main**, not the M-03 branch — browser read_page confirms the OLD shell (`complementary` sidebar + 8-link nav: Visão geral / Catálogo / Anúncios / Vínculos & Import. / Estoque / Preços & Simulador / Pedidos / Integrações & Sync), NOT the retheme (horizontal header + 6 pills + gear, no sidebar). Root cause = dev-stack-mount-cwd-relative (compose mounts `.:/workspace` from hub cwd = main checkout). P6 dual gate PASSED (close SHA df236aff); P7 browser QA vs C01–C06 (curl=FAIL, browser mandatory) needs the stack to serve worktree `.claude/worktrees/chip-m03-shell-retheme` @ branch chip/m03-shell-retheme on :5174. ASK: hub re-point/rebuild the stack against that worktree (pre-merge — chip never merges), then signal ready. Status: SENT, awaiting hub. P7 BLOCKED until stack serves the branch.
- COMBINED DEP-ADD plan (chip, after F01-S2 commit, worktree quiescent): `npm i @fontsource/instrument-sans @fontsource/ibm-plex-mono -w @marketplace-central/web` (D-03) + `npm i -D @types/node@^22` at ROOT (D-05) → review lockfile delta = exactly those 3 + transitives, zero stowaways → own commit → then F01-S3 dispatchable (tsc now works).
- _(COMMITTED per green slice / CLOSED at close — to be appended)_

## CLOSE — P6 dual gate (fixed range `59d0e62..17b93d98`, 47 files FE-only, git READ-ONLY)

Verification of record (chip-side, F-ENV-1): full vitest **215/215** (`--no-file-parallelism`), tsc **181 = baseline** (0 NEW vs base 59d0e62; jest-dom TS2339 = F-ENV-4 env-gap), vite build **✓ 4.05s** (self-hosted fonts + CSS emitted). Scope guard: `git diff --name-only 59d0e62..HEAD | grep -iE '\.go$|migrations/|openapi|server_core|sdk-runtime|\.sql$'` = **NONE** (clean FE-only).

Two INDEPENDENT cold reviewers on the whole M-03 diff (implementer ≠ reviewer — diff authored by codex luna/sol workers + chip fix-forwards; both gate reviewers are fresh, git read-only). Both explicitly directed to SCRUTINIZE the chip-authored close commits (f7ff6237 JSX/env, 17b93d98 stale-test reconciliation, a48c4a17 nbsp).

- **GATE-A (Opus, cold subagent)** — Agent tool, explicit `model=opus` (never self), general-purpose, background agentId a0460541. Independently ran tsc read-only (got 181, matching baseline). **Verdict: PASS-WITH-NITS, zero blockers.** All rubric items PASS (scope/palette/honest-data/nav/URLs/CSS-invariant/anti-slop) + all 3 chip close commits sound. Nits (non-blocking): (1) MarginChipProps not exported → barrel forwards component but not prop type; (2) "tsc 0 new" substantively true but Header.test.tsx adds ~10 F-ENV-4 jest-dom TS2339 (env-gap, runtime-provided).
- **GATE-B (Sol codex, gpt-5.6-sol/medium)** — OS-process, `--sandbox read-only`, prompt `scratchpad/prompt-gate-sol.md`. att.1 (task bikkyxwug) DIED ~80s on codex cache-TTL renewal (F-ENV-9); re-dispatched via 0.145.0 binary (task b0d55rjko), verdict at `scratchpad/agent__gate-sol2-17b93d98.last.md`. Independently reproduced 181 tsc; vitest not rerunnable (F-ENV-1, cited recorded 215/215). **Verdict: FAIL, 3 blockers.** PASS on scope/palette/nav/URLs/CSS-invariant. B1: StatCard/formatCurrency lack explicit null→"—" path (honest-data). B2: MarginChipProps not exported (C05). B3: chip-authored test-comment PR/review narration (CORE §4).

### Reconciliation (gates disagreed: Opus PASS-w-nits vs Sol FAIL → adjudicated on merit, not deferred to stricter)

- **B2 (export MarginChipProps) → ACCEPTED** (both gates flagged; real C05 prop-type-barrel gap). Fixed: `export interface MarginChipProps` (commit df236aff); barrel `export *` now forwards it, matching DataTableProps/DetailDrawerProps.
- **B3 (test-comment anti-slop) → PARTIALLY ACCEPTED.** Blame-verified authorship, judged per comment: Layout.test.tsx:56-60/96-98 (chip 17b93d98) = genuine PR/diff-history voice → TRIMMED to terse intent. ProductPicker.test.tsx nbsp (chip a48c4a17) = verbose but real mechanic → COMPRESSED. Header.test.tsx:43-45/92-93 (3df4d718) = legitimate non-obvious-jsdom WHY-comments CORE §4 PERMITS → KEPT (Sol over-flagged). Fixed in df236aff.
- **B1 (StatCard/formatCurrency null honesty) → REJECTED as out-of-scope, evidence-backed.** `git diff 59d0e62..HEAD -- StatCard.tsx` = **token-only** (bg-white→bg-surface, slate-*→muted/ink/faint, rounded-xl→rounded-card); `value: string | number` contract + `{value}` render **byte-identical to base**. formatCurrency(value: number) untouched (F03-S4 className-only). The typed null-exclusion is pre-existing AND honest — unknown resolves to "—" upstream via UnknownValue; a type-violating null renders EMPTY, not a fabricated 0/R$0/green (not an ADR-17 fabrication). Opus judged correctly. Documented DEFER: an explicit StatCard null→"—" branch is a scope-expansion enhancement → REQUEST to hub for a future milestone, NOT a retheme close-gate blocker.
- **Reconciliation commit df236aff** (chip) — vitest 215/215 sequential + tsc 181 (0 new) re-verified post-fix. Independent cold sonnet review (range 17b93d98..df236aff, git read-only, agentId a7177650) **VERDICT: PASS** — all 5 checks: export applied + barrel-reachable (index.ts:17 wildcard forwards it), both test edits comment-only (assertions byte-identical), scope = exactly 3 files, StatCard B1-rejection sound (token-only diff, `value: string|number` + `{value}` render unchanged from base).
- **DUAL-GATE OUTCOME: ✅ PASS (CLOSED).** Both cold reviewers' every in-scope blocker resolved (B2/B3 fixed in df236aff) or evidence-rejected (B1 out-of-scope, retheme did not touch StatCard value-handling); reconciliation independently reviewed PASS. Zero surviving blockers. **Close SHA = df236aff** (dual-gate range now 59d0e62..df236aff, 48 files FE-only). → proceed P7 QA browser live-drive.

## Findings (close) 

- **F-ENV-9** (codex CLI version-skew, session-fatal for long OS-process dispatches) — the PATH `codex.exe` is **0.144.4** but `~/.codex/models_cache.json` is written with `client_version: 0.145.0` (server-fetched schema). The 0.144.4 parser rejects it: `ERROR codex_models_manager: missing field \`supports_reasoning_summaries\` at line 88` (line 88 = last field of the gpt-5.6-sol model object). At startup this only WARNS + re-fetches (the benign CHIP-M02 profile §3 signature), but on **cache-TTL renewal mid-session** (~60–90s in) the re-parse fails again and the session terminates — truncating any codex dispatch that runs longer than the TTL (short M-01/M-02 dispatches finished before it, which is why it read benign there). First half of the M-03 Sol gate died this way at ~80s, mid tool-call, zero verdict. MITIGATION (applied, in-authority — uses an already-installed tool, no dep install): re-dispatch via the matching **0.145.0-alpha.18** binary at `~/.codex/plugins/.plugin-appserver/codex.exe`, which parses its own-schema cache. RECOMMEND hub: align the PATH codex to 0.145.x (or pin the cache schema) so OS-process long dispatches don't die on TTL renewal; escalates the CHIP-M02 §3 warning from "benign" to "session-fatal for >TTL runs". Reported to hub 2026-07-18.

## CLOSE — P7 QA browser live-drive (validation-contract C01–C06)

Surface: `http://localhost:5174` re-pointed to worktree `.claude/worktrees/chip-m03-shell-retheme` @ `chip/m03-shell-retheme` (df236aff) by hub (ledger D-17). Fresh cold QA persona, live-drive via `javascript_tool` DOM/computed-style + `navigate`. Screenshot rasterizer broken (**F-ENV-10**) → visual evidence = computed-style token captures. Result written to `validation-result.md`.

- **C01 tema — PASS.** Toggle "Alternar tema" flips `data-theme`; light `bodyBg rgb(251,250,247)` / dark `rgb(22,24,20)`, light headerBg white / dark `rgb(31,34,28)`, ink flips. `localStorage["marketplace-central-theme"]=dark` persists F5 (index.html no-flash pre-paint script). Fonts self-hosted no CDN: Instrument Sans `check=true`, IBM Plex Mono resolves on `fonts.load` (lazy, no 404); Google Fonts links removed from index.html.
- **C02 pills — PASS.** 6 exact/in-order: Visão geral(A,/), Anúncios(A), **Mercado(SPAN,"em breve")**, Simulador(A,/precos), Pedidos(A), **Repasses(SPAN,"em breve")**. Disabled Mercado real-click → `urlChanged:false`. At /precos only Simulador `aria-current="page"`; survives F5.
- **C03 gear — PASS.** `<summary>` "⚙" → disclosure "Menu de configurações" = exactly 4: DIFAL→`/precos?params=1`, Integrações→`/integracoes`, Catálogo→`/catalogo`, Estoque→`/estoque`. No Vínculos. `/vinculos` stays registered (direct nav → "Em construção", header intact, not 404).
- **C04 rotas — PASS.** AppRouter.tsx diff: every `path=` byte-identical base↔close, only `element=` → per-area indirection (AnunciosRoute/ProdutoRoute/VinculosRoute/PrecosRoute/PedidosRoute); LegacyRedirect old-English→canonical retained. Zero URL change.
- **C05 primitivas — PASS.** UnknownValue → `<span class="text-faint">—</span>` (≠0/R$0/green); MarginChip null → band=unknown "—". Barrel exports MarginChip(+type)/DataTable/DetailDrawer/UnknownValue additively.
- **C06 ownership — PASS.** 47 files, zero `.go`/`.sql`/openapi/migration/proto. Out-of-src = index.html + manifests; deps = ratified grants only (@types/node, @fontsource ×2), no stowaways.

Note (not a defect, out of scope): backend `GET /catalog/products/{id}` 500 in xlsx mode = **F-QA-M01-1** (owned by M-06) — not failed against M-03.

**QA VERDICT: ✅ PASS (C01–C06). Zero blockers.**

## MILESTONE CLOSED — M-03 shell-retheme

- Branch `chip/m03-shell-retheme`, range **`59d0e62..df236aff`** (47 files, FE-only).
- Ladder (verification of record, F-ENV-1): vitest **215/215** (`--no-file-parallelism`), tsc **181 = baseline** (0 new), vite build ✓.
- P6 dual gate: GATE-A Opus PASS-with-nits · GATE-B Sol FAIL(3) → reconciled on merit (B2 fixed, B3 fixed, B1 evidence-rejected out-of-scope) → reconciliation commit df236aff independently reviewed PASS. Zero surviving blockers.
- P7 QA browser live-drive: C01–C06 all PASS.
- Defers (REQUEST to hub, future milestone): StatCard explicit null→"—" branch (scope-expansion, not a retheme blocker). Findings for profile ratification: F-ENV-8, F-ENV-9, F-ENV-10.
- CLOSED event emitted to hub → hub re-points frontend back to `main` before merge ladder.
