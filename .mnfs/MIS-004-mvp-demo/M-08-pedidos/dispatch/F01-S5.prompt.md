impl-pack v1.0.0 · milestone M-08-pedidos · body-sha256 1dec366af1bfd4b4f49f060e39e12d39457d0f3b06ff54d7d2474d055cc2fa55

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Mocks prove contract shape, never integration.
- Before writing, answer: G1 — right for the WHOLE system (contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note. G3 — does this block a NAMED upcoming slice (F-02 S2 KPI row)?
- A new abstraction requires a SECOND named consumer now or in a declared brief. The summary DTO/interface has TWO consumers: the SDK return type + F-02 S2 KPI row (declared) — justified.
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy. Mirror the existing enriched-handler wiring (NewHandlerWithEnricher) + the existing summary service.
- No blanket recover/fallback on integrity reads — a summary that cannot be read returns an HONEST error status, never fabricated zero counts. unknown != zero.
- No comment narration, no dead code, no unanchored TODOs; match the module idiom.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on ran with captured output. The DB-backed GetOrderSummary path is could-not-run in this session (no stack, integration build tag) — prove the handler+service+DTO via a FAKE store; state the DB SQL path as could-not-run (QA live-drives it). Do NOT boot postgres.
- Validation failed? Reproduce in isolation, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Contract/architecture conflict: stop and report.
- Final report: status - changed paths vs write_set - commands with evidence types - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows, never WSL. Go workspace + npm monorepo.
- Go: before Go commands set GOCACHE ABSOLUTE: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY (build/vet/test split). Integration tests (`//go:build integration`) are NOT compiled by default — expected. Do NOT boot DB/dev-stack.
- SDK typecheck from packages/sdk-runtime: `npx tsc --noEmit`. Do NOT install deps. TS2688 @types/node = known false alarm.
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/bind a port/load .env. Commit per green slice on chip/m08-pedidos — ONE commit, message `feat(orders): S5 expose GET /orders/summary (counts) + additive contract/SDK`.
- PRE-AUTHORIZED GRANTS: ROOT-M08 (additive edit to the ORDERS region of composition/root.go — pass the existing ordersSummarySvc to the orders handler; additive, no reorder, no other module region). OpenAPI /orders* additive (exclusive write). SDK-ORDERS-M08 (index.ts orders region, additive INSERT-only: new OrderSummary interface + new getOrderSummary client method; never modify/delete/reorder existing lines; never touch listings/pricing/market regions).

## SLICE CARD — F01-S5 (expose GET /orders/summary counts)
CONTEXT / WHY: The orders module already has a summary service + store returning per-installation counts, but there is NO dedicated HTTP endpoint for it — it currently only feeds the dashboard. F-02 S2 (KPI row) needs an authoritative, pagination-honest count source for the pedidos page. This slice adds an additive read-only `GET /orders/summary` endpoint exposing the EXISTING OrderSummary, plus the additive OpenAPI path + SDK method. Runtime DTO is the source of truth (D-02); contract + SDK mirror it and land in the SAME commit (CORE §4).

FACTS (verified against branch HEAD; reuse, do not re-derive):
- `ports.OrderSummary struct { Today int64; SevenDays int64 }` (orders/ports/order_store.go:36-39). `OrderSummaryStore.GetOrderSummary(ctx, installationID string, referenceTime time.Time) (OrderSummary, error)` (:41-43).
- `application.SummaryService`: `NewSummaryService(store ports.OrderSummaryStore) SummaryService` (summary_service.go:21); `(s SummaryService) Summary(ctx, installationID string, referenceTime time.Time) (ports.OrderSummary, error)` (:25) — returns `ErrSummaryStoreNotConfigured` / `ErrSummaryInstallationRequired` on the honest-failure paths; otherwise delegates to the store. There is an existing fake-store unit test (summary_service_test.go) — mirror its `fakeOrderSummaryStore` idiom for the transport test; do NOT re-run the DB integration test.
- Transport: `orders/transport/http_handler.go` — Handler struct (~:35) currently holds importer/reader/enricher; constructor `NewHandlerWithEnricher(importer, reader, *application.EnrichService)` (S4a). `Register` wires the existing /orders routes on the mux. Follow the EXACT pattern the existing handlers use to read `installationID` from the request (query param or header — READ how handleReadList/handleGet obtain it and reuse the SAME source; do not invent a new param) and to write JSON + error status.
- root.go ORDERS region: `ordersSummarySvc := ordersapp.NewSummaryService(ordersRepo)` (root.go:500) already exists and is passed to the dashboard (:603). ROOT-M08: additively pass this SAME instance to the orders handler via a new/extended constructor. Do NOT construct a second summary service; reuse the one at :500.
- `referenceTime`: pass `time.Now()` at call time (the handler's clock) — mirror how other handlers get "now" (the codebase injects `time.Now` in several constructors; if the orders handler has no clock, using `time.Now()` inline is acceptable — report G2).

WHAT TO BUILD:
1. transport/http_handler.go (EDIT): add the summary handler method (e.g. `handleSummary`) that resolves installationID the SAME way the existing read handlers do, calls `summarySvc.Summary(ctx, installationID, time.Now())`, and marshals a response DTO `{ "today": <int>, "seven_days": <int> }`. On `Summary` error: map `ErrSummaryInstallationRequired` → 400, `ErrSummaryStoreNotConfigured` → 503 (or the status the existing handlers use for "not configured" — mirror them), any other error → 500 with the module's standard error envelope. NEVER emit `{today:0,seven_days:0}` as a success on error — honest status only. Register `GET /orders/summary` on the mux in `Register` (additive; keep existing routes intact and ordered so /orders/summary does not shadow /orders/{provider_order_id} — verify the mux pattern precedence).
2. Handler wiring: the Handler gains the `application.SummaryService` (value or pointer — match how EnrichService is held). Prefer a NEW additive constructor `NewHandlerWithSummary(importer, reader, enricher, summary)` OR extend the enricher constructor's signature ONLY if no other caller breaks (the sole prod caller is root.go + tests) — grep to confirm callers, report your choice (G2). Keep existing constructors compiling for any test that uses them, or update those call sites if they are within this module's tests (report).
3. root.go (ROOT-M08 additive): pass the existing `ordersSummarySvc` (root.go:500) into the orders handler constructor. Additive edit to the ORDERS region only; do not touch the dashboard wiring that also consumes it.
4. transport/http_handler_test.go (EDIT/NEW): drive `handleSummary` with a FAKE OrderSummaryStore (mirror application/summary_service_test.go's fake) wrapped in a real `NewSummaryService`, asserting: (a) success ⇒ 200 + `{"today":N,"seven_days":M}` exact JSON keys; (b) store error ⇒ 500 (or mapped status) and NO fabricated zero body; (c) missing installationID ⇒ 400. Prove the route is registered + reachable via the handler under test (httptest), NO database.
5. OpenAPI (contracts/api/marketplace-central.openapi.yaml, additive): add path `GET /orders/summary` (mirror the existing `/orders` path's parameters for installation + the standard responses) returning a new `OrderSummary` component schema `{ today: integer, seven_days: integer }` (both required inside the NEW schema — that's this schema's own required block, allowed). Do NOT add to OrderRead/OrderReadItem required. Place the schema near the other Order* schemas.
6. SDK (packages/sdk-runtime/src/index.ts, additive [SDK-ORDERS-M08]): add `interface OrderSummary { today: number; seven_days: number }` adjacent to the order interfaces, and a new client method `getOrderSummary(installationId: string): Promise<OrderSummary>` mirroring the existing `listOrders`/`getOrder` method idiom (same getJson helper, same path/param style). INSERT-only; do not modify/reorder existing lines.

CONSISTENCY: the JSON keys `today` / `seven_days` MUST be byte-identical across the runtime DTO tags, the OpenAPI schema, and the SDK interface. Produce a 2-field consistency table in your report.

DO NOT (this slice): add status_tab/exception bucket counts (the existing OrderSummary has only today/seven_days — extending it is a separate later slice; do NOT invent buckets the store doesn't compute), touch any web file, add margin/retorno/difal/decomposicao, build new cost/shipment adapters, or modify the dashboard wiring.

FAILING_TEST_FIRST: write the handleSummary httptest cases (red — handler/route not built), then implement to green.
COMMANDS (split, evidence-typed): from apps/server_core: `go build ./...` · `go vet ./...` · `go test ./internal/modules/orders/...` · `go test ./internal/composition/...` (GOCACHE absolute). From packages/sdk-runtime: `npx tsc --noEmit`. Report integration/DB tests as could-not-run.
EXPECTED_ARTIFACTS: changed files; captured go test + tsc output (green); the 2-field consistency table.
DONE_CRITERIA: build+vet+test green; `GET /orders/summary` returns the existing counts with honest error status (never fabricated zeros); OpenAPI parses + SDK typechecks; today/seven_days keys consistent across runtime/OpenAPI/SDK; contract+SDK+runtime in ONE commit; ROOT-M08 additive only; ZERO web changes; no invented buckets.
OPEN_QUESTIONS: if wiring the summary service forces changing a handler constructor with callers beyond root.go + this module's tests, or if the mux pattern would make /orders/summary collide with /orders/{provider_order_id}, STOP and report. Otherwise none.
