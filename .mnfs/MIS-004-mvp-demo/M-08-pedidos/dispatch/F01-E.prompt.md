impl-pack v1.0.0 · milestone M-08-pedidos · slice F01-E (derived `bucket` on the order read DTO)

YOU ARE A SLICE IMPLEMENTER (backend + contract). Hard rules:
- Touch ONLY the files in WRITE SET below. Anything else: stop and report as a REQUEST.
- This is a STRICTLY ADDITIVE contract change: add ONE new response field `bucket` to the order read shape in three places that must stay byte-consistent (runtime DTO, OpenAPI schema, hand-maintained SDK). Do NOT rename, remove, reorder, or retype any existing field. Contract spec (OpenAPI) + generated SDK land in the SAME commit as the handler change.
- ADR-17 (unknown ≠ zero): `bucket` is ALWAYS derivable (DeriveOrderBucket never returns empty), so it is a REQUIRED, non-omitempty field — never absent, never a fabricated blank. This is the ONE enrichment field that is always present; do not copy the pointer/omitempty idiom of the optional fields around it.
- No speculative abstraction, no comment narration beyond one short doc line, no test theater. Match surrounding Go/TS idiom exactly.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on `ran` with captured output.
- Validation failed? Reproduce, fix, re-run the FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status · changed paths vs WRITE SET · commands with evidence · confirmation the three shapes agree on the field name+enum · what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, etc.) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows only, never WSL. npm + Go workspace monorepo (do NOT install deps, do NOT run go mod tidy/download that mutates go.sum).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port/touch DB. Commit ONE commit on chip/m08-pedidos, message: `feat(orders): F01-E derive bucket on order read DTO (additive)`.
- Go build/test env (REQUIRED, Windows/pwsh): set GOCACHE to an ABSOLUTE path before any go command: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run build, vet, test as SEPARATE commands (do not chain with `&&` semantics that hide a failure). If the module needs its module cache warmed and a lane fails with HPG_MIGRATION_FAILED or a missing-dep signature, that is a known false-alarm class — warm the cache and re-run, do not "fix" it.

## WRITE SET (exclusive this slice — nothing else):
1. `apps/server_core/internal/modules/orders/transport/http_handler.go` — add the field to `enrichedOrderDTO` + set it in `mapEnrichedOrder`.
2. `apps/server_core/internal/modules/orders/transport/http_handler_test.go` (or the existing handler test file in that dir — locate it; if none, add a focused `_test.go` in the same package) — one assertion that the derived bucket is emitted.
3. `contracts/api/marketplace-central.openapi.yaml` — additive `bucket` property on the `OrderRead` schema.
4. `packages/sdk-runtime/src/index.ts` — additive `bucket` field on the `OrderRead` interface (orders region — SDK-ORDERS-M08 grant).

## FACTS (verified — do not re-derive, just consume):
- `domain.DeriveOrderBucket(providerStatus string, hasShipment bool) domain.OrderBucket` lives in `apps/server_core/internal/modules/orders/domain/order_bucket.go`. It is the SINGLE authority for bucket membership (same function that backs GET /orders/summary?by=status). Enum values (exact strings): `novo`, `faturar`, `enviar`, `enviado`, `cancelado`.
- `enrichedOrderDTO` is defined around http_handler.go:344; `mapEnrichedOrder` around :359. It already reads `e.Order` (the OrderReadModel, whose `.Status` is the RAW provider status) and checks `e.Shipment != nil`. Both list (`handleReadList`) and detail (`handleGet`) map through `mapEnrichedOrder`, so setting the field once covers list + detail.
- `SummaryService.BucketCounts` already derives KPI counts from the same DeriveOrderBucket over the same store — this field makes each order's bucket authoritative and consistent with those counts (that consistency is the whole point: the pedidos Lista tabs + Kanban columns filter on this field, F02-S3).

## WHAT TO BUILD (exactly this, nothing more):
1. **Runtime DTO** — add to `enrichedOrderDTO`:
   `Bucket domain.OrderBucket \`json:"bucket"\`` (required, no omitempty). Place it with the other order-level enrichment fields.
2. **mapEnrichedOrder** — set `dto.Bucket = domain.DeriveOrderBucket(e.Order.Status, e.Shipment != nil)`. Use `e.Shipment != nil` as the hasShipment proxy — identical to how SLA/Rastreio are gated on `e.Shipment != nil` a few lines below. Do NOT re-normalize status anywhere else; DeriveOrderBucket owns it.
3. **OpenAPI** — on `components.schemas.OrderRead.properties` (the read schema near line 4599; add AFTER the `rastreio` property, ~line 4614), add:
   ```yaml
   bucket:
     type: string
     enum:
       - novo
       - faturar
       - enviar
       - enviado
       - cancelado
   ```
   Do NOT touch the `OrderSummary.by_status` block or any other schema. STRICTLY ADDITIVE.
4. **SDK** — in `packages/sdk-runtime/src/index.ts`, define a bucket string-union type in the orders region and add the field to the `OrderRead` interface (after `rastreio?`), e.g.:
   ```ts
   export type OrderBucket = "novo" | "faturar" | "enviar" | "enviado" | "cancelado";
   ```
   and `bucket: OrderBucket;` (REQUIRED — server always emits it). The union members MUST match the OpenAPI enum and the Go constants byte-for-byte.
5. **Test** — one focused assertion in the handler package: build an `application.EnrichedOrder` (or drive `mapEnrichedOrder` directly) for at least two cases proving the wiring — e.g. a `paid` order with `Shipment == nil` → `"faturar"`, and a `paid` order with a shipment → `"enviar"` (or `delivered` → `"enviado"`). Assert the JSON field name is `bucket`. Mirror the existing handler test's construction idiom (reuse existing fixtures/builders in that test file rather than inventing new ones).

DO NOT (this slice): add any new endpoint, query param, or write path; change DeriveOrderBucket's rules; touch summary/by_status; touch pricing/connectors/product_links/market/listings modules; edit apps/web/**; add a client-side bucket derivation (that would defeat the single-source purpose); fabricate any value.

## COMMANDS (evidence-typed — run from the paths shown, capture output):
- Go, from `apps/server_core` (set GOCACHE absolute first, as above):
  - `go build ./internal/modules/orders/...`
  - `go vet ./internal/modules/orders/...`
  - `go test ./internal/modules/orders/transport/...`
- SDK, from `packages/sdk-runtime`: `npx tsc --noEmit` (report the exact result; if there is pre-existing unrelated baseline noise in OTHER packages, that is out of scope — only your file's correctness matters here).
- OpenAPI: it is not linted by a lane you may run; assert structural correctness by eye (valid YAML, additive, correct indentation under `OrderRead.properties`) and state that lint is could-not-run.

## DONE CRITERIA:
Go build+vet+test green for the orders module; SDK tsc green for your change; `bucket` present and REQUIRED in all three shapes with identical enum members; both list and detail responses carry it (via the shared mapEnrichedOrder — state this, one path covers both); ONE commit; only the four WRITE SET files touched.

## OPEN QUESTIONS:
If `mapEnrichedOrder` does NOT actually feed both list and detail (verify by reading handleGet + handleReadList), or the handler test file uses a fixture shape you cannot honestly extend, STOP and report before inventing. Otherwise none.
