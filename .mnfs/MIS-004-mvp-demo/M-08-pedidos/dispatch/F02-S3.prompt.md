impl-pack v1.0.0 · milestone M-08-pedidos · slice F02-S3 (Lista rework to bucket tabs + Kanban read-only + KPI→tab wiring)

YOU ARE A SLICE IMPLEMENTER (frontend). Hard rules:
- Touch ONLY files under apps/web/src/pages/pedidos/** (WRITE SET below). Anything else: stop and report as a REQUEST. Do NOT edit packages/ui/**, apps/web/src/app/**, index.css, routes/**, or the SDK — consume only.
- READ-ONLY RICH acceptance bar (HUB ruling D-57): every view shows REAL read data; EVERY mutation control renders DISABLED with an "em breve" affordance; NO working mutations, NO write SDK calls. Gated data (RETORNO/margem, DIFAL) renders honest "—" via UnknownValue, NEVER a fabricated number (ADR-17: unknown ≠ zero).
- Match the ratified design 1:1 (read it — DESIGN SOURCE below). No AI-slop: no decorative emojis, no gradient soup, no new fonts (app loads Instrument Sans + IBM Plex Mono). Restrained status marks only where design uses them.
- Before writing, answer G1 (idiom/tokens consistent?), G2 (non-trivial decision → 1-3 line note), G3 (blocks a NAMED next slice? F02-S4 drawer).
- Reuse shared primitives; never fork. Cite path:line for each reused primitive.
- No dead code, no comment narration, no speculative abstraction. Match surrounding TSX idiom.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on `ran` with captured output.
- Validation failed? Reproduce, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status · changed paths vs WRITE SET · commands with evidence · which fields are real vs gated "—" vs client-derived · what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, etc.) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows only. npm monorepo (do NOT install deps).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port/touch DB. Commit ONE commit on chip/m08-pedidos, message: `feat(pedidos): F02-S3 Lista bucket tabs + Kanban read-only + KPI tab wiring (read-only rich)`.
- EXCLUSIVE WRITE (my grant): apps/web/src/pages/pedidos/** only this slice.

## DESIGN SOURCE (read before building — the 1:1 QA target; read-only via git show, NOT checkout):
`git show main:.mnfs/MIS-004-mvp-demo/design/handoff/Pedidos.dc.html` and `git show main:.mnfs/MIS-004-mvp-demo/design/DESIGN-REFERENCE.md`.
Design facts already extracted for you (verify against the file, do not re-invent):
- **Lista tabs (7)** — from the design's `tabDefs`: `novo`→"Novos", `faturar`→"A faturar", `enviar`→"A enviar", `enviado`→"Enviados", `concluido`→"Concluídos", `cancelado`→"Cancelados", `devolucao`→"Devoluções". Each tab label renders as `label + ' ' + count`. The active tab uses accent-soft bg / accent-ink fg; `cancelado`/`devolucao` are placeholder tabs (design sets `tabPlaceholder=true` for them → shows an "em breve" panel, NOT a table).
- **KPI → Lista tab wiring** — design: NOVOS→tab `novo`, A FATURAR→tab `faturar`, A ENVIAR→tab `enviar`, ENVIADOS→tab `enviado` (each also switches view to `lista`). DIFAL card → view `fila` (no bucket).
- **Lista table columns** — PEDIDO · DATA · COMPRADOR · ITENS · VALOR · RETORNO · SLA · DIFAL · AÇÃO. Mass-select checkbox column + a bulk action bar when rows are selected ("Etiquetas", "Marcar enviados" — DISABLED here).
- **Kanban (4 read-only columns)** — from the design's `colDefs`: `novo`→"NOVOS", `faturar`→"A FATURAR", `enviar`→"A ENVIAR", `enviado`→"ENVIADOS". Cards grouped by bucket; NO drag affordance; card actions disabled.

## FACTS (verified — consume, do not re-derive):
- **NEW: `OrderRead.bucket`** (SDK, shipped F01-E commit b7623934) is a REQUIRED field, type `OrderBucket = "novo"|"faturar"|"enviar"|"enviado"|"cancelado"`, server-derived by the SAME authority that produces the KPI `by_status` counts. USE IT for Lista tab filtering and Kanban column grouping. Do NOT write a client-side bucket derivation — that would drift from the KPI counts (the whole reason the field exists).
- Current shell: `apps/web/src/pages/pedidos/PedidosPage.tsx` already has the KPI row (KpiCard), the view-switcher (fila/lista/kanban, default fila), the shared `ordersQuery` (listOrders) + `summaryQuery` (getOrderSummary by_status), Loading/Error/Empty gating, and a `KanbanPlaceholder` you will REPLACE with a real read-only Kanban. The Fila view (`FilaView.tsx`) stays as-is this slice.
- `apps/web/src/pages/pedidos/pedidosTabs.ts` — CURRENT `PedidosTab = "todos"|"atrasados"|"sem_vinculo"|"cancelados"` with `filterOrdersByTab`. You will REWORK this to the 7 design bucket tabs. This is the file that closes the KPI→tab wiring gap.
- `apps/web/src/pages/pedidos/PedidosTable.tsx` — CURRENT columns Pedido/Status/Comprador/Destino/Vínculo/SLA/Custo via `DataTable`. You will REWORK columns to the design set and add selection.
- `apps/web/src/pages/pedidos/pedidosFormatters.ts` — `formatMoney(number|null)`, `formatDateTime(string|null)`, both return `null` on absent (render UnknownValue on null). Reuse; add a new honest formatter only if needed.
- `DataTable` (packages/ui/src/DataTable.tsx:11) props: `columns: DataTableColumn<T>[]`, `rows`, `rowKey`, optional `onRowClick`, `selectedKeys?: ReadonlySet<string>`, `onSelectionChange?: (next: Set<string>) => void` (passing onSelectionChange ENABLES the checkbox column + header select-all), `sortKey`/`sortDir`/`onSortChange`, `loading`, `emptyState`, `stickyHeader`. `DataTableColumn<T>` = `{ key, header, render:(row)=>ReactNode, align?, sortable? }`.
- `UnknownValue`, `StatCard`, `EmptyState`, `ErrorState`, `LoadingState` from `@marketplace-central/ui` (already imported in the page). `useClient`/`useInstallation` from app/** — consume, do not edit.
- `OrderRead` fields you may render: provider_order_id, provider_code, status, bucket, buyer?{display,city?,uf?}, total?, currency?, provider_created_at?, sla?{due?,atrasado?}, destino_uf?, rastreio?{shipment_id,status}, items[]{...}, nf_state?, vinculo_status?, componentes_desconhecidos?. There is NO retorno/margem/difal field on OrderRead (slice C gate-held) → those columns render UnknownValue "—".

## WHAT TO BUILD (exactly this):
1. **pedidosTabs.ts rework** — `PedidosTab = "novo"|"faturar"|"enviar"|"enviado"|"concluido"|"cancelado"|"devolucao"`. `pedidosTabs` = 7 defs with design labels (Novos/A faturar/A enviar/Enviados/Concluídos/Cancelados/Devoluções). Mark the 4 bucket tabs as "live" and the 3 (`concluido`,`cancelado`,`devolucao`) as `placeholder: true` (no honest bucket derivation exists for them — DeriveOrderBucket folds delivered into `enviado`; cancelado exists as a bucket but demo data is REVIEW-capped so treat it, plus concluido/devolucao, as honest placeholder tabs). `filterOrdersByTab(items, tab)`: for a live bucket tab return `items.filter(i => i.bucket === tab)`; for a placeholder tab return `[]`. Add a helper `bucketTabCount(items, tab)` (or compute inline) so the tab label can show the live count; placeholder tabs show no count. G2-note this choice (tab counts derive from the loaded page's `bucket` field — consistent with the rows the tab renders; the KPI cards keep using server `by_status`; for the single-page demo dataset they agree).
2. **KPI → Lista tab wiring (close the gap)** — in PedidosPage, change the 4 bucket KpiCards so `onSelect` sets `view="lista"` AND `tab=<matching bucket>` (NOVOS→novo, A FATURAR→faturar, A ENVIAR→enviar, ENVIADOS→enviado). DIFAL card unchanged (→ fila). The initial `tab` state must be a valid new value (e.g. `"novo"`).
3. **Lista view rework** — the lista branch renders: the 7-tab bar (active-tab styling per design; label shows live count for the 4 bucket tabs), then:
   - if the active tab is a placeholder (`concluido`/`cancelado`/`devolucao`): render an "em breve" panel (honest placeholder, e.g. "Cancelados — em breve" per design's placeholderMsg tone), NOT a table.
   - else: render the reworked `PedidosTable` filtered to that bucket, with mass-select enabled, plus a bulk action bar shown when ≥1 row selected (buttons "Etiquetas" / "Marcar enviados" — DISABLED, title "em breve", no handler). Selection state lives in the lista view (a `Set<string>` keyed by provider_order_id); reset it on tab change.
4. **PedidosTable rework** — columns exactly: PEDIDO (provider_code||provider_order_id + faint provider_order_id) · DATA (formatDateTime(provider_created_at) ?? UnknownValue) · COMPRADOR (buyer?.display ?? UnknownValue) · ITENS (item count + optional first-item hint; honest, no fabrication) · VALOR (formatMoney(total) ?? UnknownValue) · RETORNO (UnknownValue "—" — gated, slice C) · SLA (reuse the existing renderSla: due + atrasado tag) · DIFAL (UnknownValue "—" — gated, slice C) · AÇÃO (a DISABLED button whose label depends on `bucket`: novo→"Faturar", faturar→"Faturar", enviar→"Etiqueta", enviado→"—"/none; all disabled, title "em breve", no handler). Enable selection via `onSelectionChange`+`selectedKeys` props (controlled from the lista view). Keep `rowKey = provider_order_id`. Wrap the table in `overflow-x:auto` with an inner min-width (design Lista is wide, ~980px) so it scrolls on narrow viewports, page body never scrolls horizontally.
5. **Kanban view (new `KanbanView.tsx`)** — replace `KanbanPlaceholder`. 4 columns keyed novo/faturar/enviar/enviado with design headers (NOVOS/A FATURAR/A ENVIAR/ENVIADOS) and a count per column; group the SAME `items` (prop, no second query) by `item.bucket`; each card shows order id + comprador + valor(or "—") + SLA state; NO drag handles/affordance; any card action rendered DISABLED "em breve". Columns in a horizontal `overflow-x:auto` flex row with a per-column min-width; cancelado-bucket items are simply not shown (no 5th column — design has 4).
6. **Tests (PedidosPage.test.tsx)** — extend the existing suite (mirror its vi.hoisted client mock; add `bucket` to the mocked OrderRead fixtures so filtering works). Assert: (a) clicking the A FATURAR KPI switches to Lista AND selects the "A faturar" tab; (b) the 4 live bucket tabs render with counts; (c) a placeholder tab (Cancelados) shows the "em breve" panel, not a table; (d) Kanban renders 4 columns with the design headers; (e) at least one mutation control (AÇÃO button or a bulk-bar button) is `disabled`. Keep existing passing assertions green (the /pedidos heading, DIFAL "—", view switching).

DO NOT (this slice): build the detail drawer or ?order= deep-link (F02-S4); wire any mutation/write or issue a write SDK call; add a client-side bucket derivation (consume OrderRead.bucket); fabricate RETORNO/DIFAL/margem numbers; add a second listOrders query (share the one in PedidosPage; pass items as props to Lista/Kanban/Fila); edit anything outside pages/pedidos/**.

FAILING_TEST_FIRST: extend PedidosPage.test.tsx (red for bucket-tab wiring + Kanban columns + placeholder tab), then implement to green.
COMMANDS (evidence-typed, from apps/web): `npx tsc --noEmit` · `npx vitest run src/pages/pedidos --config vitest.config.ts`. Report other web suites as not-run (scoped). NOTE: there is a KNOWN pre-existing repo-wide jest-dom matcher typing baseline in tsc unrelated to pedidos/** (verify via `git stash` that the same errors exist on untouched files) — that baseline is NOT your defect; only NEW errors in files you touched count. State this explicitly in evidence.
EXPECTED_ARTIFACTS: changed files vs WRITE SET; captured tsc + vitest output (green for pedidos scope); the real-vs-gated("—")-vs-client-derived field map; a note that pixel-1:1 vs Pedidos.dc.html is QA-verified at P7 (you assert structure/labels/counts via tests, not pixels).
DONE_CRITERIA: tsc (no NEW pedidos errors) + scoped vitest green; Lista uses the 7 design bucket tabs with live counts on the 4 buckets + placeholder panels on the 3; KPI cards 1-4 deep-link to their bucket tab; PedidosTable shows the design columns with RETORNO+DIFAL honest "—"; mass-select + disabled bulk bar; Kanban 4 read-only columns grouped by OrderRead.bucket with no drag affordance; every mutation control disabled "em breve"; no fabricated gated values; zero writes; only pages/pedidos/** touched; ONE commit.
OPEN_QUESTIONS: if `OrderRead.bucket` is somehow absent from the built SDK (it shipped in b7623934 — verify), or a needed reuse primitive isn't exported, STOP and report (do not invent). Otherwise none.
