impl-pack v1.0.0 · milestone M-08-pedidos · slice F02-S4 (detail drawer + ?order= deep-link)

YOU ARE A SLICE IMPLEMENTER (frontend). Hard rules:
- Touch ONLY files under apps/web/src/pages/pedidos/** and apps/web/src/routes/pedidos.tsx (WRITE SET below). Anything else: stop and report as a REQUEST. Do NOT edit packages/ui/**, apps/web/src/app/**, index.css, or the SDK — consume only.
- READ-ONLY RICH acceptance bar (HUB ruling D-57): the drawer shows REAL read data; EVERY mutation control renders DISABLED with an "em breve" affordance; NO working mutations, NO write SDK calls. Gated data (decomposição, DIFAL, retorno/margem) + data with no honest backing (buyer doc, NF number, tracking code) render "—" via UnknownValue, NEVER a fabricated value (ADR-17).
- Match the ratified design 1:1 (read it — DESIGN SOURCE). No AI-slop: no decorative emojis, no gradient soup, no new fonts. Restrained status marks only where design uses them.
- Before writing, answer G1 (idiom/tokens consistent?), G2 (non-trivial decision → 1-3 line note), G3 (blocks a NAMED next slice? this is the LAST F-02 impl slice before the feature review #15).
- Reuse shared primitives; never fork. Cite path:line for each reused primitive.
- No dead code, no comment narration, no speculative abstraction. Match surrounding TSX idiom.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on `ran` with captured output.
- Validation failed? Reproduce, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status · changed paths vs WRITE SET · commands with evidence · which drawer fields are real vs gated "—" vs client-derived · what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, etc.) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows only. npm monorepo (do NOT install deps).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port/touch DB. Commit ONE commit on chip/m08-pedidos, message: `feat(pedidos): F02-S4 detail drawer + ?order= deep-link (read-only rich)`.
- EXCLUSIVE WRITE (my grant): apps/web/src/pages/pedidos/** + apps/web/src/routes/pedidos.tsx this slice.

## DESIGN SOURCE (read before building — the 1:1 QA target; read-only via git show, NOT checkout):
`git show main:.mnfs/MIS-004-mvp-demo/design/handoff/Pedidos.dc.html` and `git show main:.mnfs/MIS-004-mvp-demo/design/DESIGN-REFERENCE.md`. Study the RIGHT-SIDE detail drawer: its section order and the disabled action buttons in its footer.
Design drawer sections (verify against the file): (1) header (order id + bucket/status + valor + data), (2) itens + vínculo, (3) decomposição + DIFAL block, (4) timeline (ML → interno), (5) NF, (6) rastreio, (7) comprador. Footer action buttons per the order's bucket (Faturar via ERP / Etiqueta / Marcar enviado / DIFAL agendar / Devolução…) — ALL disabled here.

## FACTS (verified — consume, do not re-derive):
- `client.getOrder(installationId, providerOrderId)` → `OrderRead` (packages/sdk-runtime/src/index.ts:1684). Detail routes through the SAME enrichedOrderDTO as the list, so getOrder returns the full enriched shape: bucket, buyer?{display,city?,uf?}, items[]{provider_item_id,seller_sku?,title?,quantity,unit_price?,sale_fee_amount?,link_quality,internal_product_id?,custo_unitario?}, sla?{due?,atrasado?}, destino_uf?, rastreio?{shipment_id,status}, nf_state?, vinculo_status?, componentes_desconhecidos?, total?, currency?, provider_created_at?, provider_closed_at?, provider_updated_at?, created_at?. There is NO decomposição/retorno/margem/DIFAL field, NO buyer document, NO NF number, NO carrier tracking code on OrderRead — those are gated/absent → "—".
- `DetailDrawer` (packages/ui/src/DetailDrawer.tsx:15) props: `{ open, onClose, title, subtitle?, children, actions?, closeLabel?, width? }` (default width 360). Pass `width={372}` per design. `actions` renders in the footer — put the disabled action buttons there. REUSE it, do not fork or build a new drawer/modal.
- `UnknownValue` (@marketplace-central/ui) — the honest "—". `formatMoney`/`formatDateTime` (pages/pedidos/pedidosFormatters.ts) return null on absent → render UnknownValue on null.
- `useSearchParams` from `react-router-dom` is the deep-link mechanism (query param, NOT a route path param — so routes/pedidos.tsx likely needs NO change; only touch it if you find a real need). `useClient()`/`useInstallation()` from app/** — consume, do not edit.
- `ordersQueryKeys` in @marketplace-central/web-query has ONLY `.list` (no detail key), and web-query is OUT of your write scope. Use a LOCAL stable query key for the detail query, exactly as PedidosPage already does for the summary query (a `["orders","detail",installationId,orderId] as const` with a one-line code comment explaining no barrel key exists). Do NOT add a key to web-query.
- Existing views: ListaView renders `PedidosTable` (DataTable supports `onRowClick`), plus FilaView and KanbanView render rows/cards. All share the single `ordersQuery` in PedidosPage.

## WHAT TO BUILD (exactly this):
1. **Deep-link state** — in PedidosPage, read `?order=` via `useSearchParams`. The selected order id = that param (or null). Opening a row/card sets the param; closing the drawer clears it. Use `setSearchParams` with `replace: true` so back-button isn't polluted (G2-note). The drawer is open iff the param is present.
2. **Row/card → open drawer** (READ interaction, not a mutation): wire `onRowClick` on the Lista `PedidosTable`, and an `onClick`/keyboard-accessible affordance on Fila rows and Kanban cards, to set `?order=<provider_order_id>`. These open the drawer only — no writes.
3. **Detail query** — a `useQuery` in PedidosPage (or a small `usePedidoDetail` hook in pages/pedidos/) keyed by the local detail key, `queryFn: () => client.getOrder(installationId, orderId)`, `enabled: !!orderId`, `staleTime: QUERY_STALE_TIME.orders`. Loading → drawer shows LoadingState; error → ErrorState with retry; success → sections. Do NOT block the main list on this query.
4. **PedidoDrawer.tsx (new)** — wraps `DetailDrawer` (width 372, title = order id, subtitle = bucket/status). Sections in design order, each a small honest sub-block:
   - **Itens + vínculo**: per item → title||seller_sku, `quantity × unit_price` (formatMoney; "—" if unit_price absent), link_quality as the vínculo tag, custo_unitario (formatMoney or "—"), internal_product_id if present. If `componentes_desconhecidos.length>0` show an honest "custo incompleto" tag (reuse the existing stateTag idiom from PedidosTable — copy the tiny helper into pages/pedidos if not exported; do NOT import from packages/ui internals).
   - **Decomposição + DIFAL**: ALL "—" via UnknownValue (retorno_liquido, margem_pct, decomposição breakdown, DIFAL {valor, rota UF, vencimento, pago}). Gated on slice C / IC-04 — render an honest "não disponível" block, never fabricated numbers. G2-note.
   - **Timeline**: CLIENT-DERIVED from present timestamps ONLY (ruling-6): e.g. Recebido = provider_created_at, Atualizado = provider_updated_at, Fechado = provider_closed_at, Enviado = rastreio present + rastreio.status. Render only events whose timestamp is really present; omit absent ones (do NOT invent dates). Do NOT label anything "NF emitida" from nf_state — see NF section. G2-note the derivation.
   - **NF**: nf_state is the vínculo 'linked' MARKER, not a real NF number (taxonomy finding) — render it honestly (e.g. "vínculo: <state>" or "—" when null); NO fake NF number, NO NF ERP deep-link (no real number in storage). Ruling-6.
   - **Rastreio**: rastreio.shipment_id + rastreio.status real where present; destino_uf real; carrier tracking CODE = "—" (no code in backend). "—" when rastreio absent.
   - **Comprador**: buyer.display + city/uf real where present; buyer document (CPF/CNPJ) = "—" (no doc in backend, ruling-6).
   - **Footer actions** (`actions` prop): the design's bucket-appropriate buttons (Faturar via ERP / Etiqueta / Marcar enviado / DIFAL agendar / Devolução…) — ALL `disabled`, title "disponível em breve", no handlers.
5. **Tests (PedidosPage.test.tsx or a new PedidoDrawer.test.tsx)** — mock `getOrder`. Assert: (a) navigating with `?order=<id>` (or clicking a row) opens the drawer and calls getOrder; (b) the drawer renders itens + comprador (real) and the decomposição/DIFAL block as "—"; (c) buyer-doc / NF-number / tracking-code render "—"; (d) at least one footer action button is `disabled`; (e) closing the drawer clears the `?order=` param. Mirror the existing vi.hoisted client mock idiom; wrap render in the router/QueryClient the existing tests use. Keep prior assertions green.

DO NOT (this slice): wire any mutation/write or issue a write SDK call; fabricate decomposição/DIFAL/retorno/margem/buyer-doc/NF-number/tracking-code; add a backend field or endpoint; edit packages/ui/**, app/**, index.css, or the SDK; add a second list query. This is READ-ONLY: the drawer only opens/reads/closes.

FAILING_TEST_FIRST: add the drawer/deep-link test (red), then implement to green.
COMMANDS (evidence-typed, from apps/web): `npx tsc --noEmit` · `npx vitest run src/pages/pedidos --config vitest.config.ts`. Report other web suites as not-run (scoped). NOTE the KNOWN pre-existing repo-wide jest-dom matcher-typing baseline in tsc unrelated to pedidos/** — verify via `git stash` that the same errors exist on untouched files; that baseline is NOT your defect, only NEW errors in files you touched count. State this in evidence.
EXPECTED_ARTIFACTS: changed files vs WRITE SET; captured tsc + vitest output (green for pedidos scope); the real-vs-gated("—")-vs-client-derived drawer field map; a note that pixel-1:1 vs Pedidos.dc.html is QA-verified at P7.
DONE_CRITERIA: tsc (no NEW pedidos errors) + scoped vitest green; a row/card click and a `?order=` URL both open a drawer of REAL detail data via getOrder; decomposição/DIFAL/retorno/margem + buyer-doc + NF-number + tracking-code all honest "—"; timeline client-derived from present timestamps only; every footer action disabled "em breve"; closing clears the param; no writes; only pages/pedidos/** (+ routes/pedidos.tsx if truly needed) touched; ONE commit.
OPEN_QUESTIONS: if getOrder is absent from the built SDK (it is at :1684 — verify) or a reuse primitive isn't exported, STOP and report. Otherwise none.
