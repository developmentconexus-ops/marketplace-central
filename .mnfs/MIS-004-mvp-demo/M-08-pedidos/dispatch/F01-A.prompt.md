impl-pack v1.0.0 · milestone M-08-pedidos · slice F01-A (status-bucket counts)

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Mocks prove contract shape, never integration.
- Before writing, answer: G1 — right for the WHOLE system (contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note. G3 — does this block a NAMED upcoming slice (F-02 Lista/Kanban counts, KPI row)?
- A new abstraction requires a SECOND named consumer now or in a declared brief. The bucket function + count DTO have TWO named consumers: the SDK return type AND F-02 Lista status-tab counts + Kanban column counts + KPI row (declared, ruling D-57) — justified.
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy. Mirror the EXISTING summary pipeline end-to-end (ports.OrderSummary → application.SummaryService → transport handleSummary → GET /orders/summary), the enriched-handler wiring, and the existing fakeOrderSummaryStore test idiom.
- No blanket recover/fallback on integrity reads — a bucket count that cannot be read returns an HONEST error status, never fabricated zero counts. unknown != zero (ADR-17).
- No comment narration, no dead code, no unanchored TODOs; match the module idiom.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on ran with captured output. The DB-backed bucket SELECT path is could-not-run this session (no stack, integration build tag) — prove handler+service+DTO+DeriveOrderBucket via FAKE store + pure unit tests; state the DB SQL path as could-not-run (QA live-drives it). Do NOT boot postgres.
- Validation failed? Reproduce in isolation, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Contract/architecture conflict: stop and report.
- Final report: status - changed paths vs write_set - commands with evidence types - the DeriveOrderBucket truth table you tested - the by_status key-consistency table - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows, never WSL. Go workspace + npm monorepo.
- Go: before Go commands set GOCACHE ABSOLUTE: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY (build/vet/test split). Integration tests (`//go:build integration`) are NOT compiled by default — expected. Do NOT boot DB/dev-stack.
- SDK typecheck from packages/sdk-runtime: `npx tsc --noEmit`. Do NOT install deps. TS2688 @types/node = known false alarm.
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/bind a port/load .env. Commit per green slice on chip/m08-pedidos — ONE commit, message `feat(orders): F01-A add GET /orders/summary?by=status bucket counts + additive contract/SDK`.
- PRE-AUTHORIZED GRANTS: OpenAPI /orders* additive (exclusive write). SDK-ORDERS-M08 (index.ts orders region, additive INSERT/extend of the OrderSummary interface + getOrderSummary option — never modify/delete/reorder existing lines or other regions). No root.go edit needed (the summary service is already wired via NewHandlerWithSummary at root.go ORDERS region — do NOT touch root.go this slice).

## SLICE CARD — F01-A (status-bucket counts on the summary endpoint)
CONTEXT / WHY: The pedidos page (F-02) needs SERVER-ACCURATE per-status-bucket counts for the Lista status tabs, the Kanban column headers, and the KPI row (HUB ruling D-57: client-side derivation REJECTED; must be server-computed over ALL orders, not one page). The existing `GET /orders/summary` returns only `{today, seven_days}`. This slice ADDITIVELY extends it: `GET /orders/summary?by=status` returns bucket counts `{novo, faturar, enviar, enviado}`. The no-param call keeps returning `{today, seven_days}` unchanged. Runtime DTO is source of truth (D-02); OpenAPI + SDK mirror it and land in the SAME commit (CORE §4).

FACTS (verified against branch HEAD; reuse, do not re-derive):
- `ports.OrderSummary struct { Today int64; SevenDays int64 }` (orders/ports/order_store.go:36-39). `OrderSummaryStore.GetOrderSummary(ctx, installationID, referenceTime) (OrderSummary, error)` (:41-43).
- `application.SummaryService.Summary(...)` (summary_service.go:25) returns `ErrSummaryStoreNotConfigured` / `ErrSummaryInstallationRequired` on honest-failure paths. There is an existing `fakeOrderSummaryStore` in summary_service_test.go — mirror it.
- Transport already has `handleSummary` + `NewHandlerWithSummary(importer, reader, enricher, summary)` and registers `GET /orders/summary` (F01-S5, http_handler.go). installationID is resolved via `requiredInstallation(r.URL.Query())`. On error the handler maps ErrSummaryInstallationRequired→400, ErrSummaryStoreNotConfigured→503, other→500; it NEVER emits fabricated zeros. REUSE all of this — you are ADDING a `by=status` branch, not rebuilding.
- Read-store SQL (adapters/postgres/order_repo.go): ListOrders SELECTs `o.provider_status` and `o.shipping_id` per order (:48-58); `GetOrderSummary` (:182-205) counts over `orders_marketplace_orders WHERE tenant_id=$1 AND installation_id=$2 AND provider_created_at IS NOT NULL`. The concrete read store is `OrderReadRepository`; the summary store is `OrderRepository` (both wrap `r.pool`, `r.tenantID`). Follow the SAME tenancy predicate (`tenant_id=$1 AND installation_id=$2`) and the SAME `provider_created_at IS NOT NULL` guard the existing summary uses.
- TAXONOMY (established, binding): `domain.OrderReadModel.Status` is the RAW provider status ("paid"/"shipped"/"delivered"/"cancelled"/pending-family) with NO bucket normalization. Orders storage has NO true ERP "NF emitted" signal (the `nf` column scanned in scanReadModel is the vínculo 'linked' marker, not an NF-emission state). Therefore the design's faturar("pago·falta NF") vs enviar("NF emitida") boundary CANNOT be proven from a real NF state. Honest proxy (G2, decided at chip level — implement exactly, do NOT invent an NF lookup): use `shipping_id` presence as the faturar/enviar discriminator.

BUCKET CONTRACT (implement EXACTLY — this is the single source of truth; unit-test it as `ran`):
Pure domain function, new file `domain/order_bucket.go`:
```
type OrderBucket string
const (
    BucketNovo     OrderBucket = "novo"
    BucketFaturar  OrderBucket = "faturar"
    BucketEnviar   OrderBucket = "enviar"
    BucketEnviado  OrderBucket = "enviado"
    BucketCancelado OrderBucket = "cancelado"
)
// DeriveOrderBucket maps the RAW provider status + shipment presence to a workflow bucket.
// It is the single authority for bucket membership across the API and the pedidos UI.
func DeriveOrderBucket(providerStatus string, hasShipment bool) OrderBucket
```
Rules (case-insensitive match on trimmed providerStatus):
- "cancelled" | "invalid" → BucketCancelado
- "delivered" | "shipped" → BucketEnviado
- "paid" | "confirmed" WITH hasShipment==true → BucketEnviar   (label/shipment exists, ready to dispatch)
- "paid" | "confirmed" WITH hasShipment==false → BucketFaturar (paid, awaiting invoice+label)
- anything else (pending / payment_required / payment_in_process / new / "" / unknown) → BucketNovo
G2 to record: shipping_id presence is a PROXY for the design's NF-emitted state because orders storage carries no ERP NF signal; counts approximate design semantics using available marketplace data, degrading honestly per ruling D-57(6). BucketCancelado is derived but is NOT one of the four KPI buckets returned in `by_status` (see DTO) — it is computed so cancelled orders are excluded from the four, not silently miscounted into `novo`.

WHAT TO BUILD:
1. domain/order_bucket.go (NEW) + domain/order_bucket_test.go (NEW): the function above + an exhaustive truth-table test covering every rule branch incl. case-insensitivity, empty status, unknown status, both hasShipment values for paid/confirmed. This test is `ran` evidence and is the bucket authority.
2. ports/order_store.go (EDIT, additive): add `type OrderBucketCounts struct { Novo, Faturar, Enviar, Enviado int64 }` and extend the summary store contract with `GetOrderBucketCounts(ctx context.Context, installationID string) (OrderBucketCounts, error)` on a NEW additive interface `OrderBucketStore` (do NOT widen OrderSummaryStore — keep it additive so existing fakes still satisfy their interface; the service will accept the bucket store separately or via an extended optional dependency — see step 4). Keep OrderSummary + OrderSummaryStore intact.
3. adapters/postgres/order_repo.go (EDIT, additive): implement `GetOrderBucketCounts` on the summary repo (`OrderRepository`). SELECT the minimal per-order fields needed by DeriveOrderBucket — `provider_status` and whether `shipping_id` is present — for ALL orders under `tenant_id=$1 AND installation_id=$2 AND provider_created_at IS NOT NULL` (NO pagination — server-accurate over the whole installation), then apply `domain.DeriveOrderBucket` per row IN GO and tally into OrderBucketCounts. Do NOT reimplement bucket rules in SQL (single-authority: the Go function). Honest error return on pool==nil / query error (never zeroed counts on error). This DB path is could-not-run this session.
4. application/summary_service.go (EDIT, additive): add `(s SummaryService) BucketCounts(ctx, installationID string) (ports.OrderBucketCounts, error)` mirroring `Summary`'s honest-failure guards (missing installationID → ErrSummaryInstallationRequired; store-not-configured → ErrSummaryStoreNotConfigured). Wire the bucket store into SummaryService additively: prefer adding an optional bucket-store field with a constructor variant `NewSummaryServiceWithBuckets(summaryStore, bucketStore)` OR accept both in the existing constructor ONLY if the sole callers (root.go + this module's tests + dashboard) are updated additively — grep callers, pick the lower-blast-radius option, record G2. If the bucket store is nil/unconfigured, BucketCounts returns ErrSummaryStoreNotConfigured (honest 503), never zeros.
5. application/summary_service_test.go (EDIT): add a fake bucket store (mirror fakeOrderSummaryStore) and assert BucketCounts success + missing-installation + not-configured paths.
6. transport/http_handler.go (EDIT, additive): in `handleSummary`, branch on `r.URL.Query().Get("by")`. When `by == "status"`: call `summarySvc.BucketCounts(ctx, installationID)` and marshal `{ "by_status": { "novo":N, "faturar":N, "enviar":N, "enviado":N } }`. When `by` is empty: existing `{today, seven_days}` path UNCHANGED. Any other `by` value → 400 (honest "unsupported summary dimension"), never a silent fallback. Same installationID resolution (`requiredInstallation`) and same error mapping (400/503/500, never fabricated zeros).
7. transport/http_handler_test.go (EDIT): add httptest cases for `?by=status`: (a) success ⇒ 200 + exact JSON `{"by_status":{"novo":..,"faturar":..,"enviar":..,"enviado":..}}`; (b) bucket-store error ⇒ 500 and NO fabricated zero body; (c) missing installationID ⇒ 400; (d) `?by=bogus` ⇒ 400. Drive via a FAKE bucket store through the real service. Assert the no-param call STILL returns `{today, seven_days}` (non-regression). No database.
8. OpenAPI (contracts/api/marketplace-central.openapi.yaml, additive): add the optional `by` query param (`enum: [status]`) to the existing `GET /orders/summary` operation, and additively extend the `OrderSummary` schema with an OPTIONAL `by_status` object `{ novo, faturar, enviar, enviado: integer }` (do NOT make it required; keep today/seven_days as they are). Document that `by_status` is present only when `by=status`.
9. SDK (packages/sdk-runtime/src/index.ts, additive [SDK-ORDERS-M08]): additively extend `interface OrderSummary` with optional `by_status?: { novo: number; faturar: number; enviar: number; enviado: number }`, and extend `getOrderSummary` to accept an optional second arg `options?: { by?: "status" }` forwarding `by=status` as a query param (mirror the existing query-param idiom of listOrders). INSERT/extend only; do not modify/reorder existing lines or other regions.

CONSISTENCY: the four bucket keys `novo` / `faturar` / `enviar` / `enviado` MUST be byte-identical across the Go handler DTO tags, the DeriveOrderBucket constants, the OpenAPI `by_status` schema, and the SDK interface. Produce a 4-key consistency table in your report.

DO NOT (this slice): touch root.go (already wired) unless step-4 grep proves the constructor signature must change (then additive ORDERS-region-only edit, report it); touch any web file; add margin/retorno/difal/decomposição; build new cost/shipment adapters; add mutation endpoints; invent an NF-state lookup; return BucketCancelado inside `by_status` (it stays out of the four KPI buckets); paginate the bucket count (must be whole-installation).

FAILING_TEST_FIRST: write the DeriveOrderBucket truth-table test AND the `?by=status` httptest cases (red — function/branch not built), then implement to green.
COMMANDS (split, evidence-typed): from apps/server_core: `go build ./...` · `go vet ./...` · `go test ./internal/modules/orders/...` · `go test ./internal/composition/...` (GOCACHE absolute). From packages/sdk-runtime: `npx tsc --noEmit`. Report integration/DB tests as could-not-run.
EXPECTED_ARTIFACTS: changed files vs write_set; captured go test + tsc output (green); the DeriveOrderBucket truth table; the 4-key consistency table.
DONE_CRITERIA: build+vet+test green; DeriveOrderBucket unit-tested across all branches (ran); `GET /orders/summary?by=status` returns bucket counts with honest error status (never fabricated zeros); `GET /orders/summary` (no param) unchanged; `?by=bogus` → 400; OpenAPI parses + SDK typechecks; four bucket keys consistent across runtime/OpenAPI/SDK; contract+SDK+runtime in ONE commit; additive-only (no forbidden region touched); DB SELECT path reported could-not-run.
OPEN_QUESTIONS: if wiring the bucket store forces a constructor change with callers beyond root.go + this module's tests + dashboard, STOP and report before editing root.go. Otherwise none.
