impl-pack v1.0.0 · milestone M-08-pedidos · slice F01-C1 (decomposição/DIFAL/retorno seam — honest-empty, hub D-62)

YOU ARE A SLICE IMPLEMENTER (backend + contract). Hard rules:
- Touch ONLY the files in WRITE SET below. Anything else: stop and report as a REQUEST. In particular you must NOT touch composition/root.go — see "C2 IS NOT YOURS".
- This is a CONTRACT SEAM slice, NOT a computation slice. You declare a `Decomposer` port + honest-empty enrichment, expose additive nullable fields on the order read shape (runtime DTO + OpenAPI + SDK, byte-consistent), and wire EnrichService to emit HONEST-UNKNOWN when no decomposer is present. You do NOT implement any decomposition/DIFAL/margin formula — that frozen "exactly ONE decomposition impl" (IC-04) is wired by the HUB post-merge (C2). Contract spec (OpenAPI) + SDK land in the SAME commit as the runtime.
- ADR-17 (unknown ≠ zero): every component/amount/margin you cannot honestly source is a nil pointer that renders "—"; NEVER a fabricated 0/false/date. `componentes_desconhecidos` NAMES every unknown component so the "—" is explained, not silent.
- No speculative abstraction beyond the one port + value types this seam needs; no comment narration beyond short doc lines; no test theater. Match surrounding Go/TS idiom exactly (mirror the existing CostReader/ShipmentReader nil-port honest-unknown pattern precisely).
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on `ran` with captured output.
- Validation failed? Reproduce, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status · changed paths vs WRITE SET · commands with evidence · confirmation the three shapes agree · confirmation root.go was NOT touched and NewEnrichService still constructs a nil-decomposer honest-empty path · what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, etc.) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD 52b570c). PowerShell/Windows only. Go workspace + npm monorepo (do NOT install deps; do NOT mutate go.sum).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port/touch DB. Commit ONE commit on chip/m08-pedidos, message: `feat(orders): F01-C1 decomposição/DIFAL/retorno seam — honest-empty (additive)`.
- Go env (REQUIRED, Windows/pwsh): `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')` (ABSOLUTE) before any go command; run build, vet, test as SEPARATE commands.

## HUB RULING CONTEXT (D-62 — binding):
IC-04 decomposition engine lives on chip/m07-simulador (denylisted merge). Hub ruled HYBRID: **C1 (this slice)** = declare the seam + emit honest-empty NOW (no IC-04 dependency, no formula). **C2 (HUB, post-merge)** = after M-07 closes+merges (IC-04 ports land on main) and M-08 merges, the hub wires a real decompose adapter at composition root importing pricing IC-04 ports — exactly the D-21 market-adapter pattern. So your job is the port + honest-empty; the hub's one-line wiring lights up real numbers for the demo. If M-07 slips, the honest-empty "—" is the automatic fallback. Either way C1 must be self-consistent and buildable with ZERO pricing import.

## C2 IS NOT YOURS (critical scope boundary):
- Do NOT touch `apps/server_core/internal/composition/root.go`. Keep `NewEnrichService(cost, shipment, logger)` working exactly as today; it must construct EnrichService with a NIL decomposer, which yields the honest-empty path. The HUB will, in C2, change that call site to pass a real decomposer. If you find yourself needing to edit root.go, STOP — you've overreached.
- Do NOT import anything from `modules/pricing/**` or reference IC-04 symbols. This slice compiles with zero pricing dependency.

## WRITE SET (exactly these — nothing else):
1. `apps/server_core/internal/modules/orders/domain/order_decomposition.go` (NEW) — value types + honest-unknown constructor.
2. `apps/server_core/internal/modules/orders/domain/order_decomposition_test.go` (NEW) — honest-unknown assertions.
3. `apps/server_core/internal/modules/orders/ports/decompose_reader.go` (NEW) — the `Decomposer` port.
4. `apps/server_core/internal/modules/orders/application/enrich_service.go` — add decomposer field + `NewEnrichServiceWithDecomposer` + emit profitability into `EnrichedOrder`.
5. `apps/server_core/internal/modules/orders/application/enrich_service_test.go` — nil-decomposer → honest-empty assertion.
6. `apps/server_core/internal/modules/orders/transport/http_handler.go` — DTO fields + map.
7. `apps/server_core/internal/modules/orders/transport/http_handler_test.go` — JSON-shape assertion.
8. `contracts/api/marketplace-central.openapi.yaml` — additive OrderRead fields + 2 new schemas.
9. `packages/sdk-runtime/src/index.ts` — additive OrderRead fields + interfaces (SDK-ORDERS-M08 grant).

## THE SEAM (build exactly this shape):
### domain/order_decomposition.go
- `type OrderDecomposition struct` with `*float64` pointer fields (nil = unknown, omitempty): `Comissao, TaxaFixa, Frete, Imposto, Difal, TarifaFull, Custo, MargemValor, MargemPct` + `ComponentesDesconhecidos []string`.
- `type OrderDifal struct { Amount *float64; UFRoute *string; DueDate *time.Time; Paid *bool }` — all nil = unknown.
- `type OrderProfitability struct { RetornoLiquido *float64; MargemPct *float64; Decomposition OrderDecomposition; Difal OrderDifal }` — the composite the port returns and EnrichedOrder carries.
- `func UnknownOrderProfitability() OrderProfitability` — the honest-empty value: every pointer nil, and `Decomposition.ComponentesDesconhecidos` = the names of ALL cost/fee components that are unknown, using these EXACT snake_case strings in this order: `comissao, taxa_fixa, frete, imposto, difal, tarifa_full, custo`. (margem_valor/margem_pct/retorno are derived-from-those, not listed as source components — G2-note this.) Doc-comment: this is the value emitted whenever no decomposer is wired (C1) so the UI renders honest "—", explained by the named components.
### ports/decompose_reader.go
- `type Decomposer interface { Decompose(ctx context.Context, order domain.OrderReadModel) (domain.OrderProfitability, bool) }` — the bool is `ok` (false ⇒ caller uses honest-empty). Mirror the doc-comment idiom of cost_reader.go/shipment_reader.go: orders-local consumer port; the real adapter (hub C2) resolves the pricing IC-04 engine — not this module's concern; a nil Decomposer is honest-unknown, never a panic.
### application/enrich_service.go
- Add field `decomposer ports.Decomposer` to `EnrichService`.
- Keep `NewEnrichService(cost, shipment, logger)` — delegate to a new `NewEnrichServiceWithDecomposer(cost, shipment, decomposer, logger)` passing `nil` decomposer (so root.go is untouched and stays honest-empty). Mirror how summary got `NewSummaryServiceWithBuckets` alongside `NewSummaryService`.
- Add field `Profitability domain.OrderProfitability` to `EnrichedOrder`.
- In `Enrich`, after `resolveShipment`, compute profitability: `prof := domain.UnknownOrderProfitability(); if s.decomposer != nil { if p, ok := s.decomposer.Decompose(ctx, order); ok { prof = p } }`. Set `Profitability: prof` on the appended `EnrichedOrder`. Do NOT populate Difal.UFRoute from destino in C1 — a route needs BOTH origin (seller UF, a tenant/engine fact NOT on the orders base) and destino; with only destino it would be a half-route. Emit UFRoute nil and let C2 (which has the engine's tenant origin) fill it. G2-note this decision. (destino_uf is already separately exposed on the order for the buyer/rastreio sections.)
### transport/http_handler.go
- Add `decomposicaoDTO` (pointer/omitempty float fields `comissao,taxa_fixa,frete,imposto,difal,tarifa_full,custo,margem_valor,margem_pct` + `componentes_desconhecidos []string` with `json:"componentes_desconhecidos"`) and `difalDTO` (`amount *float64 omitempty`, `uf_route *string omitempty`, `due_date *time.Time omitempty`, `paid *bool omitempty`).
- On `enrichedOrderDTO` add: `RetornoLiquido *float64 json:"retorno_liquido,omitempty"`, `MargemPct *float64 json:"margem_pct,omitempty"`, `Decomposicao decomposicaoDTO json:"decomposicao"` (always present — it carries componentes_desconhecidos), `Difal difalDTO json:"difal"` (always present).
- In `mapEnrichedOrder` map from `e.Profitability` (Decomposition→decomposicaoDTO field-by-field, Difal→difalDTO, RetornoLiquido/MargemPct straight). This flows to BOTH list + detail via the shared mapper (state this).
### OpenAPI (contracts/api/marketplace-central.openapi.yaml)
- On `components.schemas.OrderRead.properties` (additive, after `bucket`): `retorno_liquido` (number, nullable), `margem_pct` (number, nullable), `decomposicao` ($ref OrderDecomposicao), `difal` ($ref OrderDifal).
- New schema `OrderDecomposicao`: object; number(nullable) props comissao/taxa_fixa/frete/imposto/difal/tarifa_full/custo/margem_valor/margem_pct + `componentes_desconhecidos` array of string.
- New schema `OrderDifal`: object; `amount` number nullable, `uf_route` string nullable, `due_date` string (format date, nullable), `paid` boolean nullable.
- STRICTLY ADDITIVE — do NOT touch existing OrderRead props or other schemas.
### SDK (packages/sdk-runtime/src/index.ts, orders region)
- New interfaces `OrderDecomposicao { comissao: number|null; taxa_fixa: number|null; frete: number|null; imposto: number|null; difal: number|null; tarifa_full: number|null; custo: number|null; margem_valor: number|null; margem_pct: number|null; componentes_desconhecidos: string[]; }` and `OrderDifal { amount: number|null; uf_route: string|null; due_date: string|null; paid: boolean|null; }`.
- On `OrderRead` add: `retorno_liquido?: number | null; margem_pct?: number | null; decomposicao?: OrderDecomposicao; difal?: OrderDifal;`. (Optional `?` because older/list callers may not need them, but when present they follow the shapes above; server always emits decomposicao+difal objects.) Match member names byte-for-byte with the Go JSON tags + OpenAPI.

## TESTS:
- domain: assert `UnknownOrderProfitability()` has all pointers nil, RetornoLiquido/MargemPct nil, Difal all nil, and `ComponentesDesconhecidos == [comissao, taxa_fixa, frete, imposto, difal, tarifa_full, custo]` exactly.
- application: build an EnrichService via `NewEnrichService` (nil decomposer) and assert every returned `EnrichedOrder.Profitability` equals the honest-empty value (componentes named, all nil). Reuse the existing enrich_service_test fixtures/builders.
- transport: marshal a mapped order and assert the JSON carries `"decomposicao":{...,"componentes_desconhecidos":[...]}` with null component values and `"difal"` present with null amount/uf_route. Reuse the existing handler-test fixture (enrichedOrderModels()).

## COMMANDS (evidence-typed):
- from `apps/server_core` (GOCACHE absolute set first): `go build ./internal/modules/orders/...` · `go vet ./internal/modules/orders/...` · `go test ./internal/modules/orders/...`
- from `packages/sdk-runtime`: `npx tsc --noEmit`
- OpenAPI: no lint lane — assert valid additive YAML by eye, state lint could-not-run.

## DONE CRITERIA:
Go build+vet+test green for the orders module; SDK tsc green; decomposicao/difal/retorno_liquido/margem_pct present + byte-consistent across DTO/OpenAPI/SDK; nil-decomposer path emits honest-empty (all "—", componentes_desconhecidos names the 7 components); root.go NOT touched and NewEnrichService still yields the honest-empty path; both list+detail carry the fields via shared mapEnrichedOrder; ONE commit; only the 9 WRITE SET files touched.

## OPEN QUESTIONS:
If adding `Profitability` to `EnrichedOrder` breaks an existing enrich_service or transport test that constructs the struct positionally (unkeyed), fix those constructions to keyed fields WITHIN the WRITE SET files only. If a construction outside the WRITE SET breaks, STOP and report (it may signal a wider consumer). Otherwise none.
