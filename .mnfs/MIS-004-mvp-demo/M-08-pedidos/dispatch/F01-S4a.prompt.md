impl-pack v1.0.0 · milestone M-08-pedidos · body-sha256 1dec366af1bfd4b4f49f060e39e12d39457d0f3b06ff54d7d2474d055cc2fa55

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Mocks prove contract shape, never integration.
- Before writing, answer: G1 — right for the WHOLE system (contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note in your report. G3 — does this block a NAMED upcoming milestone/seam?
- A new abstraction requires a SECOND named consumer existing now or in a declared brief. None = do not build it.
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy.
- No blanket recover/try-catch or fallback on integrity-critical reads - unknown != zero/default; fail honest.
- No comment narration, no dead code, no unanchored TODOs; match the module's idiom.
- Evidence per command: type ran / assumed / could-not-run. Pass ONLY on ran with an artifact path or captured output. Never Pass on assumed or could-not-run.
- Validation failed? REPRODUCE the failure in isolation first, then fix, then re-run the FULL validation plan. Max ONE fixup this session; second failure = stop, report BLOCKED with the reproduction.
- Contract/architecture conflict: stop and report. You do not adjudicate.
- Final report: status - changed paths vs write_set (any undeclared path gets a one-line justification) - commands with evidence types - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base SHA 8b6c4b3093f9465cd3b91209b054af4fa702171a. PowerShell/Windows, never WSL. Go workspace.
- Env prepared. Before Go commands set GOCACHE ABSOLUTE: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY. CANNOT boot DB/dev-stack (hub-owned) — integration tests (`//go:build integration`) are not compiled by default; expected.
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT install deps. Commit per green slice on chip/m08-pedidos — one commit, message `feat(orders): S4a wire read-time enrichment into /orders transport (runtime, honest nulls)`.
- PRE-AUTHORIZED GRANT ROOT-M08: you MAY make an ADDITIVE edit to the ORDERS region of `apps/server_core/internal/composition/root.go` (construct the enricher + pass it to the orders handler). Additive only — do not modify unrelated wiring, do not reorder, do not touch other modules' regions.
- Non-negotiables: ADR-17 unknown != zero — when an enrichment fact is unavailable (nil reader, per-item/-order miss) the field is ABSENT/null, never 0/""/false/a fabricated date. C06/LGPD (BLOCKING) — the response must NOT expose raw buyer identity; the ONLY buyer data emitted is the masked buyer (empty display is fine — nickname is not stored) + destino_uf. The order list MUST NEVER fail because enrichment errored.

## SLICE CARD — F01-S4a (RUNTIME: wire EnrichService into the /orders read transport)
CONTEXT / WHY: The enrichment seam (EnrichService: masked buyer, vinculo_status, per-item cost + componentes_desconhecidos, shipment SLA/atraso/destino_uf/rastreio) is built and unit-tested (S1-S3) but NOT wired into the HTTP read path. Today `orders/transport/http_handler.go` marshals the raw `domain.OrderReadModel` directly (`handleReadList` :120 via `orderPageEnvelope`, `handleGet` :151-183). This slice maps the read models THROUGH EnrichService and emits an enriched response DTO. Per hub ruling D-02 (lock-exception), the RUNTIME shape is the source of truth and the OpenAPI/SDK contract is synced to it in the NEXT slice (S4b) — so THIS slice touches NO contract files.

SCOPE THIS SLICE = NON-GATED enrichment only: masked buyer, vinculo_status, componentes_desconhecidos, per-item unit cost, shipment sla{due,atrasado}, destino_uf, rastreio{shipment_id,status}. Do NOT add margin/retorno/difal/decomposicao (those are gated on M-07, a later slice). Emit them by simply NOT including those fields yet.

FACTS (verified against base; reuse, do not re-derive):
- EnrichService (`orders/application/enrich_service.go`): `NewEnrichService(cost CostReader, shipment ShipmentReader, logger *slog.Logger)` (per S3), `Enrich(ctx, installationID string, orders []domain.OrderReadModel) []EnrichedOrder` (no error). `EnrichedOrder{ Order domain.OrderReadModel; Buyer domain.MaskedBuyer; VinculoStatus domain.VinculoStatus; ItemCosts []ItemCost; ComponentesDesconhecidos []string; <shipment enrichment field, e.g. Shipment *ShipmentEnrichment> }` — READ the current struct to get exact field names for ItemCost (ItemIdentifier, UnitCost *float64) and the shipment enrichment shape (ShipmentID, Status, SLADue *time.Time, Delayed *bool, DestinationUF *string). `MaskedBuyer{Display string; City *string; UF *string}`.
- IMPORTANT: `OrderReadModel.BuyerNickname` is NEVER populated by the read repo (the projection SELECT/scan does not read it — confirmed) so `MaskedBuyer.Display` will be empty for current data. That is honest (nickname not stored); do NOT fabricate a display. `MaskedBuyer.UF` is filled from the shipment DestinationUF by EnrichService.
- ItemCosts is built in the SAME order as `Order.Items` (S2 impl) — you may zip them positionally to attach a per-item unit cost to each item DTO; VERIFY this by reading the S2 code before relying on it, and if the ordering is not guaranteed, match by the item identifier instead (report which you did, G2).
- Transport read paths: `handleReadList` (:120) builds `orderPageEnvelope{Items []domain.OrderReadModel, NextCursor *string}` (:115). `handleGet` (:151) marshals a single `domain.OrderReadModel`. The Handler struct (:35) holds importer/lister/readLister; constructors `NewHandler` (:41), `NewHandlerWithReader` (:45); `Register` (:49). The sole production caller is `root.go:501` `orderstransport.NewHandlerWithReader(ordersImportSvc, ordersReadSvc).Register(mux)`.
- root.go ORDERS region (:491-501): `ordersReadSvc := ordersapp.NewReadService(ordersReadRepo)` (:499), `ordersSummarySvc` (:500, unrelated to this slice), handler wired at :501. `slog` is already imported in root.go.

WHAT TO BUILD:
1. `EnrichService` NIL-GUARD (application/enrich_service.go EDIT): make `Enrich` safe when `cost == nil` (⇒ every item cost unknown + item into componentes_desconhecidos, exactly as a nil-Amount would) and when `shipment == nil` (⇒ no shipment lookup, shipment enrichment nil, Buyer.UF stays nil). No panic. This lets the enricher run in production before the real cost/shipment adapters are wired (a later slice). Add enrich_service_test.go cases: nil cost reader ⇒ all items unknown; nil shipment reader ⇒ shipment nil + reader-not-called (trivially). Keep all S1/S2/S3 behavior.
2. Enriched response DTO (transport/http_handler.go EDIT): define response structs (e.g. `enrichedOrderDTO`, `enrichedItemDTO`) that carry the existing OrderReadModel JSON fields PLUS the enrichment fields with these JSON names: order-level `vinculo_status` (string), `buyer` (object {display, city, uf} — omit nils), `componentes_desconhecidos` ([]string, omit when empty), `sla` (object {due (date-time), atrasado (bool)} — omit nils/whole object when no shipment), `destino_uf` (string, omit nil), `rastreio` (object {shipment_id, status} — omit when no shipment); item-level `custo_unitario` (number, omit nil). KEEP the existing top-level `buyer_nickname` field emitting its existing (nil) value — the additive SDK lock forbids removing it and it is already always null, so no raw PII leaks. Map `EnrichedOrder` → DTO. Unknown ⇒ omit the field (use `omitempty`/pointers), NEVER 0/false/"".
3. Handler wiring (transport/http_handler.go EDIT): the Handler gains an `*application.EnrichService` (or an interface it satisfies). Prefer a NEW additive constructor `NewHandlerWithEnricher(importer, reader, enricher)` (leave existing constructors intact so no other caller breaks), or extend `NewHandlerWithReader` if cleaner (report choice, G2). `handleReadList` and `handleGet` run the read model(s) through `enricher.Enrich(...)` and emit the enriched DTO envelope. If the enricher is nil (defensive), fall back to the current raw marshal so nothing regresses (report if you add this).
4. root.go wiring (ROOT-M08, additive): construct `ordersEnrichSvc := ordersapp.NewEnrichService(nil, nil, slog.Default())` (nil cost + shipment readers — real adapters are a later slice; nil ⇒ honest unknown) and pass it to the orders handler via the new constructor. Additive edit to the ORDERS region only.
5. Transport unit test (transport/http_handler_test.go EDIT or NEW): drive `handleReadList` + `handleGet` with a FAKE OrderReadLister returning a couple of read models (some items with InternalProductID, some without) and a real `NewEnrichService(nil, nil, slog.Default())`. Assert the JSON response: contains `vinculo_status`, a `buyer` object with empty display and NO raw nickname/CPF/email/phone, `componentes_desconhecidos` listing the items (cost unknown because cost reader is nil), NO `custo_unitario` (nil cost), NO `sla`/`rastreio`/`destino_uf` (nil shipment), and that the list still returns 200 with all orders. This proves the wiring + honest-null behavior WITHOUT a database.

WRITE_SET (exact — nothing else):
- `apps/server_core/internal/modules/orders/application/enrich_service.go` (EDIT — nil-guard)
- `apps/server_core/internal/modules/orders/application/enrich_service_test.go` (EDIT — nil-reader cases)
- `apps/server_core/internal/modules/orders/transport/http_handler.go` (EDIT — DTO + mapping + handler wiring)
- `apps/server_core/internal/modules/orders/transport/http_handler_test.go` (EDIT or NEW — enriched-response unit test)
- `apps/server_core/internal/composition/root.go` (EDIT — ROOT-M08 additive: construct enricher + wire handler)

DO NOT (this slice): touch OpenAPI (contracts/api/*.yaml), the SDK (packages/sdk-runtime), any web file, or add margin/retorno/difal/decomposicao. Do NOT build the real cost/shipment adapters (nil readers for now). Do NOT modify other modules' root.go regions. The OpenAPI+SDK sync is the NEXT slice (S4b).

FAILING_TEST_FIRST: write the transport enriched-response test + the enrich nil-guard tests first (red), then implement to green.
COMMANDS (split, evidence-typed): `go build ./...` · `go vet ./...` · `go test ./internal/modules/orders/...` · `go test ./internal/composition/...` (GOCACHE absolute, from apps/server_core). Report DB-integration tests as could-not-run if applicable.
EXPECTED_ARTIFACTS: captured `go test` output showing the transport + enrich tests green; the changed files.
DONE_CRITERIA: build+vet+test green; the /orders + /orders/{id} runtime response now carries masked buyer + vinculo_status + componentes_desconhecidos (and cost/sla/destino/rastreio when readers are wired), with honest omission of unavailable fields; NO raw buyer PII; list never fails on enrichment; ZERO changes to OpenAPI/sdk-runtime/web; root wiring is additive ROOT-M08 only.
OPEN_QUESTIONS: if wiring the enricher forces a change to a handler constructor that has callers beyond root.go + tests, or if the ItemCosts→item zip is not order-safe and no stable identifier exists, STOP and report. Otherwise none.
