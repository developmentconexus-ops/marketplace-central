impl-pack v1.0.0 · milestone M-08-pedidos · body-sha256 1dec366af1bfd4b4f49f060e39e12d39457d0f3b06ff54d7d2474d055cc2fa55

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Mocks prove contract shape, never integration.
- Before writing, answer: G1 — right for the WHOLE system (contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note in your report. G3 — does this block a NAMED upcoming milestone/seam?
- A new abstraction (interface, wrapper, config knob, generic param) requires a SECOND named consumer existing now or in a declared brief. None = do not build it.
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy.
- No blanket recover/try-catch or fallback on integrity-critical reads - unknown != zero/default; fail honest.
- No comment narration, no dead code, no unanchored TODOs; match the module's idiom.
- Evidence per command: type ran / assumed / could-not-run. Pass ONLY on ran with an artifact path or captured output. Never Pass on assumed or could-not-run.
- Validation failed? REPRODUCE the failure in isolation first, then fix, then re-run the FULL validation plan. Max ONE fixup this session; second failure = stop, report BLOCKED with the reproduction.
- Contract/architecture conflict: stop and report. You do not adjudicate.
- Final report: status - changed paths vs write_set (any undeclared path gets a one-line justification) - commands with evidence types - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base SHA 8b6c4b3093f9465cd3b91209b054af4fa702171a. PowerShell/Windows, never WSL. Go workspace.
- Env already prepared (mod cache warm). Before Go commands, set GOCACHE to an ABSOLUTE path: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY (never chain build;vet;test — cold build alone can exceed a short timeout). Use `GOMODCACHE=$(pwd)/.gomodcache` if a module is missing.
- Do NOT push, reset, revert, stash, clean. Do NOT read/print .env*. Do NOT install deps. Commit per green slice on the current branch (chip/m08-pedidos) — one commit for this slice, message `feat(orders): S2 enrichment cost — per-item ERP cost + componentes_desconhecidos`.
- Non-negotiables for this slice: ADR-17 unknown != zero — a cost that cannot be resolved (no product id, reader error, or nil amount) MUST stay nil AND surface the item in `componentes_desconhecidos`; NEVER substitute 0, "", or a fabricated value. The order list MUST NEVER fail because one item's cost is unknown — degrade that item to unknown and continue. No blanket recover; handle the specific `(CostAsOf, error)` return. PII: do not add any buyer field; do not log buyer data or full cost provenance.

## SLICE CARD — F01-S2 (Go-internal cost enrichment; NO contract files)
GOAL: Extend the read-time enrichment seam (`EnrichService`, built in S1) with per-item ERP unit cost sourced via the internal_read cost fact, and an honest `componentes_desconhecidos` list naming every item whose cost could not be resolved. PURELY internal Go — MUST NOT touch OpenAPI, the SDK (packages/sdk-runtime), the HTTP transport response JSON, or the composition root (root.go). No margin / retorno / difal here — those are a later GATED slice; S2 delivers cost + unknown-flagging ONLY.

FACTS (verified against base; reuse, do not re-derive):
- The cost fact is owned by the `internal_read` module. The ESTABLISHED cross-module consumption pattern is a module-local, SIMPLIFIED consumer port — see `apps/server_core/internal/modules/profitability/ports/internal_read.go:10-13`:
  `GetCostAsOf(ctx context.Context, productID int, effectiveAt time.Time) (internalreaddomain.CostAsOf, error)`
  and its translating adapter `profitability/adapters/internalread/fact_reader.go:25-35` (fills CompanyID=1, Basis=CostBasisCUSSEMICM). REUSE this exact port signature for orders — do NOT invent a different shape, do NOT import `internal_read/ports.CostAsOfInput` into the orders application layer (the adapter owns that translation).
- `internalreaddomain.CostAsOf` (`internal_read/domain/internal_cost.go:19-28`): `Amount *float64` (nil = cost unknown / not provable), `AmountScope` = `CostAmountScopePerUnit` ("per_unit"). Treat `Amount` as the per-UNIT cost. nil Amount ⇒ unknown.
- Item → product id: `domain.MarketplaceOrderItem.InternalProductID *int` (`orders/domain/order.go:74`). nil ⇒ item has no linked internal product ⇒ cost cannot be looked up ⇒ unknown (do NOT call the reader). This is the same field profitability keys on (`profitability/application/service.go:303` calls `GetCostAsOf(ctx, *item.InternalProductID, effectiveAt(order))`).
- `OrderReadModel.Items` are `[]domain.MarketplaceOrderItem` (same struct — has InternalProductID). Order dates available: `ProviderClosedAt`, `ProviderCreatedAt`, `CreatedAt` (all `*time.Time`).
- S1 seam (base for this slice): `application/enrich_service.go` — `EnrichedOrder{Order, Buyer, VinculoStatus}`, `EnrichService struct{}`, `NewEnrichService() EnrichService`, `Enrich(ctx, installationID string, orders) []EnrichedOrder` (no error). Buyer/vinculo logic there stays intact.

DECISIONS you make (report each as a 1-3 line G2 note):
- `effectiveAt` for the cost query: derive honestly from the order's own date — first non-nil of `ProviderClosedAt`, `ProviderCreatedAt`, `CreatedAt`. If ALL are nil, the cost is unknown (you cannot ask "cost as of when") — flag the item unknown, do NOT call the reader with a zero time.
- The identifier used in `componentes_desconhecidos`: pick ONE stable, non-PII item field (e.g. `SellerSKU`, else `ProviderItemID`) and document the fallback order. It must let the UI point at the specific unresolved item.
- The exact shape of the cost additions on `EnrichedOrder` (e.g. a per-item cost value plus the unknown list). Keep it a plain internal value; unit cost is `*float64`, nil when unknown.

WRITE_SET (exact — nothing else):
- `apps/server_core/internal/modules/orders/ports/cost_reader.go` (NEW) — the minimal orders-local `CostReader` consumer interface, signature identical to profitability's `InternalFactReader.GetCostAsOf` (cite it in a one-line comment as the reused pattern). No other methods.
- `apps/server_core/internal/modules/orders/application/enrich_service.go` (EDIT) — `EnrichService` gains a `CostReader` dependency (constructor updated: `NewEnrichService(cost CostReader) EnrichService`); `Enrich` resolves each item's per-unit cost and builds `componentes_desconhecidos`. `EnrichedOrder` gains the cost fields. Buyer + vinculo behavior unchanged. Enrich still returns no error (list never fails on a per-item unknown).
- `apps/server_core/internal/modules/orders/application/enrich_service_test.go` (EDIT) — add a FAKE `CostReader` and table tests: (a) known Amount ⇒ per-unit cost populated, item NOT in componentes_desconhecidos; (b) nil Amount ⇒ cost nil + item IN componentes_desconhecidos; (c) reader error ⇒ cost nil + item IN list + Enrich still returns the order (never panics/fails); (d) nil InternalProductID ⇒ reader NOT called (assert via fake call-count) + item unknown; (e) all-nil order dates ⇒ reader NOT called + item unknown; (f) buyer mask + vinculo_status from S1 STILL correct alongside cost. Never assert a fabricated 0.
- `apps/server_core/internal/modules/orders/domain/order_enrichment.go` (EDIT — ONLY if you choose to place a cost value-type or a pure helper in domain; if everything fits cleanly in application, do NOT touch it).

DO NOT (this slice): build the real internal_read adapter for orders, wire anything into root.go / composition, add margin/retorno/difal/decomposition, touch OpenAPI / packages/sdk-runtime / transport. The production adapter + root wiring is a later grant-gated slice; S2 proves the logic against a fake.

FAILING_TEST_FIRST: extend enrich_service_test.go with the cost cases (red — CostReader/fields don't exist yet), then implement to green.
COMMANDS (split, evidence-typed): `go build ./...` · `go vet ./...` · `go test ./internal/modules/orders/...` (all with GOCACHE absolute, run from apps/server_core).
EXPECTED_ARTIFACTS: captured `go test` output showing the new cost tests green; the changed files.
DONE_CRITERIA: build+vet+test green; ZERO changes to OpenAPI / packages/sdk-runtime / transport / root.go; cost is per-unit `*float64` nil-on-unknown (never 0); every unresolved item appears in componentes_desconhecidos; the order list never fails on a per-item cost miss; S1 buyer/vinculo behavior preserved.
OPEN_QUESTIONS: if resolving per-item cost cleanly requires importing `internal_read/ports` (CostAsOfInput) into the orders APPLICATION layer, STOP and report — that means the port shape is wrong (the adapter, not the app, owns that translation). Otherwise none.
