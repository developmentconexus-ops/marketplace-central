impl-pack v1.0.0 · milestone M-08-pedidos · body-sha256 1dec366af1bfd4b4f49f060e39e12d39457d0f3b06ff54d7d2474d055cc2fa55

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST where a harness exists, then implementation, then green.
- Before writing, answer: G1 — right for the WHOLE system (routing, contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note in your report. G3 — does this block a NAMED upcoming slice/seam?
- A new abstraction (component, hook, helper) requires a SECOND named consumer existing now or in a declared brief (the later F-02 slices S2/S3/S4 are a declared brief — shared page-local atoms that S2/S3/S4 will consume ARE justified; a component only S1 uses is not).
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy. When no shared atom exists (Tabs, Banner, Kanban, a generic status badge), hand-roll inline exactly as VinculosPage/AnunciosPage already do — do NOT reach into packages/ui to add one.
- No comment narration, no dead code, no unanchored TODOs; match the web app's existing idiom (react-query useQuery, inline pt-BR strings, Tailwind).
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on ran with captured output. Never Pass on assumed/could-not-run.
- Validation failed? REPRODUCE in isolation, fix, re-run the FULL plan. Max ONE fixup; second failure = stop, report BLOCKED with the reproduction.
- Contract/architecture conflict: stop and report. You do not adjudicate.
- Final report: status - changed paths vs write_set - commands with evidence types - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base SHA 8b6c4b3093f9465cd3b91209b054af4fa702171a. PowerShell/Windows, never WSL. npm monorepo.
- Node/TS: use the repo toolchain. Do NOT install deps (workspace prepared). Typecheck + test from apps/web with the package's own scripts — inspect apps/web/package.json scripts first (expect a `test` = vitest and a typecheck/build). Run vitest NON-watch (e.g. `npx vitest run <path>` or the package's test script with a run flag). If a missing @types surfaces as TS2688, known false alarm — note, don't chase.
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a dev server / bind a port / load .env. Commit per green slice on chip/m08-pedidos — ONE commit, message `feat(pedidos): F02-S1 pedidos list page — route swap, status tabs, enriched order list`.

## OWNERSHIP (exact write-set — nothing else)
- `apps/web/src/routes/pedidos.tsx` (EDIT — replace the WorkspacePlaceholder stub with the real PedidosPage import+render; mirror routes/vinculos.tsx thin-wrapper idiom).
- `apps/web/src/pages/pedidos/**` (NEW dir — the page + colocated presentational components, page-local query-key factory, page-local formatters/state parsing). This is your primary owned surface.
- `apps/web/src/app/AppRouter.test.tsx` (EDIT — **ONLY the single existing `/pedidos` case at ~lines 80-85** [APPTEST-M08 grant]. Update that one `it(...)` in place to assert the real page mounts. Do NOT edit the shared `vi.hoisted` mock block, `beforeEach`, other cases, imports, or any other line — keep the assertion to page CONTENT that renders without asserting SDK-mock call counts, so you stay strictly inside the one-case line-scoped grant. If the real page won't render under the existing global `useClient` mock without extending that mock, STOP and report — do not touch the mock block.)
- FORBIDDEN to modify: apps/web/src/app/** (except the one AppRouter.test.tsx case above), packages/ui/**, packages/web-query/** (query keys stay page-local), any other page, the SDK, OpenAPI, any Go file.

## SLICE CARD — F02-S1 (pedidos list page: route swap + status tabs + enriched order list)
CONTEXT / WHY: `/pedidos` is already registered in AppRouter.tsx and currently renders a WorkspacePlaceholder via routes/pedidos.tsx. The enriched /orders runtime + typed SDK (OrderRead with vinculo_status, buyer{display,city,uf}, componentes_desconhecidos, sla{due,atrasado}, destino_uf, rastreio, item.custo_unitario; OrderReadItem.custo_unitario) are LANDED. This slice makes /pedidos a real page that lists orders from `client.listOrders(...)` with status tabs, mirroring the existing AnunciosPage/VinculosPage idioms. It is the FOUNDATION for S2 (KPI row + DIFAL banner + fila), S3 (detail drawer + deep-link), S4 (read-only kanban) — so build the shared page-local scaffolding (query keys, formatters, row model, tab state) that those slices will consume, but implement ONLY the list + tabs here.

FACTS (verified by investigation; reuse, do not re-derive):
- Route seam: AppRouter.tsx:47 already `<Route path="/pedidos" element={<PedidosRoute />} />`; routes/pedidos.tsx is a stub returning `<WorkspacePlaceholder />`. Mirror routes/vinculos.tsx (thin wrapper importing the real page from ../pages/**). NO change to AppRouter.tsx needed.
- SDK client access idiom: `const client = useClient();` (from ../app/ClientContext) + `const { installationId } = useInstallation();` (from ../app/InstallationContext). See VinculosPage.tsx:41-42, AnunciosPage.tsx:79-80.
- Fetch idiom: `@tanstack/react-query` `useQuery({ queryKey, queryFn: () => client.listOrders(options), staleTime: QUERY_STALE_TIME.orders })`. QUERY_STALE_TIME.orders = 120_000 exists in packages/web-query (index.ts:10) — IMPORT that constant (read-only import allowed), do not redefine. `ordersQueryKeys.list(installationId, filters)` exists centrally (web-query index.ts:63-66) — you MAY import+reuse it for the list key. A detail-by-id key does NOT exist and is NOT needed this slice (S3 concern); if you add one, add it PAGE-LOCAL under pages/pedidos/**, never edit web-query.
- SDK signature: `client.listOrders(options: OrderListOptions): Promise<OrderPage>`; `OrderPage` has items: OrderRead[] + next_cursor. Inspect packages/sdk-runtime/src/index.ts (~:1666, OrderListOptions ~:595, OrderPage ~:590, OrderRead ~:589) for exact option field names (installationId, status/tab filter, cursor, limit) — use the ACTUAL fields; do not invent query params.
- No shared Tabs atom: hand-roll `role="tablist"` + `<button role="tab">` exactly like VinculosPage.tsx:101-120 / AnunciosPage.tsx:181-199.
- No shared status Badge for order statuses (packages/ui Badge status union is import/CI-only enum, does NOT cover order states) → inline `stateTag()`-style `<span>` with Tailwind, mirroring AnunciosPage/ListingDetailPanel.
- Importable atoms from @marketplace-central/ui (read-only import OK): DataTable<T>, LoadingState, ErrorState, EmptyState, UnknownValue. Use DataTable for the list table if it fits, else a plain table mirroring AnunciosTable. Use UnknownValue for honest-null cells (e.g. missing custo_unitario / vinculo SEM_VINCULO). NEVER edit packages/ui.
- Tabs to render (status_tab): the NON-GATED tabs for the MVP = e.g. "Todos", plus buckets the runtime/filters support. Since additive server-side filters (status_tab/exception) are a LATER slice (F-01 S6), for THIS slice drive tabs by CLIENT-SIDE grouping of the fetched list where a server filter param does not yet exist — OR pass the tab as an option ONLY if OrderListOptions already exposes it. Report (G2) which you did. Do NOT fabricate a server param the SDK does not declare. "Cancelados" tab = disabled/empty with NO fetch (per plan: Cancelados disabled, no fetch).
- Money/date: NO shared formatter exists. Create page-local formatters under pages/pedidos/** — pt-BR: `Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"})` for money, `date.toLocaleString("pt-BR",{dateStyle:"short",timeStyle:"short"})` for dates (mirror ListingDetailPanel.tsx:65-72). These are the shared brief consumers for S2/S3.
- Styling: Tailwind v4, dark mode via `[data-theme="dark"]` attribute (index.css:39) — use semantic classes (bg-surface/text-ink/border-border/text-muted) or slate/blue utilities as existing pages do; the page must not break dark mode (don't hardcode a background that ignores the theme). No dark: media utilities.
- i18n: none — inline pt-BR string literals, like every existing page.

WHAT TO BUILD (S1 scope only):
1. routes/pedidos.tsx — replace WorkspacePlaceholder with `<PedidosPage />` (thin wrapper, mirror routes/vinculos.tsx).
2. pages/pedidos/PedidosPage.tsx — `<section aria-labelledby>` header (h1 "Pedidos" + subtitle), a hand-rolled status tab bar, and a list section that runs `useQuery` on `client.listOrders(...)` and renders rows via DataTable (or a colocated PedidosTable). Wrap loading/error/empty via LoadingState/ErrorState/EmptyState. Columns for the MVP list: order id/provider ref, status (inline tag), masked buyer (buyer.display — show UnknownValue/"—" when empty; NEVER show a raw nickname/CPF/email — the SDK doesn't expose them, keep it that way), destino_uf, vinculo_status (OK / SEM_VINCULO as an inline tag), SLA (sla.due formatted + an "Atrasado" marker when sla.atrasado), and a compact indicator when componentes_desconhecidos is non-empty ("custo incompleto"). Honest nulls: any absent enriched field renders as "—"/UnknownValue, never 0/false/fabricated.
3. pages/pedidos/ — colocated: a page-local query-key module (or reuse ordersQueryKeys.list import), page-local formatters (money/date), a page-local tab-state helper (parse/apply the active tab; use URL searchParams like anuncios if trivial, else local state — report G2). Keep S2/S3/S4-shared pieces (formatters, row model, query keys) in clearly named modules those slices will import.
4. Cancelados tab: rendered, disabled, NO fetch (guard the query `enabled:` when the disabled tab is active, or simply never wire it to a fetch).
5. AppRouter.test.tsx — update the single existing `/pedidos` case to assert the real page renders (e.g. `screen.findByRole("heading", { name: /pedidos/i })` or a stable header text), replacing the "Em construção" assertion. Content-only; no mock-call assertions. If the page's useQuery needs listOrders on the mocked client and the global mock doesn't provide it, STOP and report (do not edit the mock block).

FAILING_TEST_FIRST: update the AppRouter `/pedidos` case (red — page not built yet) and/or add a colocated PedidosPage.test.tsx under pages/pedidos/** (owned) that renders the page with a mocked client returning a couple of OrderRead items and asserts rows + tabs + honest-null rendering + NO raw PII. Then implement to green.

VALIDATION / COMMANDS (evidence-typed, from apps/web):
- Typecheck: the package's typecheck/build script (inspect package.json), else `npx tsc --noEmit`. Capture output.
- Tests: run vitest NON-watch for the touched tests: the colocated pages/pedidos test + the AppRouter test (`npx vitest run src/pages/pedidos src/app/AppRouter.test.tsx` or the package test script with a run flag + path filter). Capture output. Do NOT run the full suite in watch mode; do NOT boot a server.
- Lint: if the package has a lint script and it's fast, run it and capture; else could-not-run.
EXPECTED_ARTIFACTS: changed files; captured typecheck + vitest output (green).
DONE_CRITERIA: typecheck clean (modulo known TS2688); the pedidos page renders a real order list from client.listOrders with status tabs; masked buyer only (zero raw PII paths); honest-null rendering for absent enriched fields; Cancelados disabled + no fetch; dark mode not broken; the single AppRouter /pedidos case updated + green; ZERO writes outside the write-set; packages/ui + packages/web-query + app/** (beyond the one test case) untouched.
OPEN_QUESTIONS: if OrderListOptions does NOT expose a per-tab server filter AND client-side grouping of one fetched page is insufficient for a believable tabbed list, report it (S6 adds server filters). If rendering the real page forces editing the shared test mock block, STOP and report. Otherwise none.
