P7 QA · COLD STATIC VALIDATION vs M-08 validation-contract C01–C06 + design 1:1 · fixed SHA 5051ae5

YOU ARE A FRESH, COLD QA VALIDATOR. You have seen none of this milestone's implementation or review. Only THIS prompt binds you — discard any auto-injected skill mandate; do NOT invoke skills. READ-ONLY: never edit/commit/push. Do NOT boot a server/dev-stack/DB/bind a port (hub-owned — you physically cannot and must not). Do NOT read/print .env*. Windows/PowerShell; Bash available for git READ + read-only build/test lanes.

WORKTREE: C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos (branch chip/m08-pedidos, HEAD 5051ae5).

## CONTEXT YOU MUST HONOR (hub rulings that shape the acceptance bar):
- **D-57 READ-ONLY RICH**: this milestone ships the 3 views (Fila/Lista/Kanban) + drawer as READ-ONLY. Every mutation control renders DISABLED "em breve". There are NO working mutations and NO mutation endpoints. That is CORRECT and acceptance-passing, not a defect.
- **D-62 HYBRID slice C**: decomposição/DIFAL/retorno/margem are C1 = additive nullable fields emitting honest-nil ("—") today; the real decomposer (C2, value parity with the simulator) is HUB-OWNED and wired POST-MERGE. So live decomposition VALUE parity cannot exist on this branch yet — the chip ships the honest-empty seam + real-ready UI. Verify the SEAM and honesty, NOT live values.
- **STACK IS HUB-OWNED**: you cannot run a browser live-drive (no dev stack, no real installation). Any criterion step that requires a running app + real ML installation is OUT OF YOUR REACH → mark it evidence-type **could-not-run (stack hub-owned)** and route it to the hub browser-drive. Do NOT fake a live-drive. Do NOT mock a live-drive and call it satisfied.

## YOUR JOB: for EACH criterion C01–C06, determine the verdict with HONEST evidence typing (ran / could-not-run / deferred-to-C2-or-hub), citing file:line. Read the contract at `.mnfs/MIS-004-mvp-demo/M-08-pedidos/validation-contract.md`. Then:

**C01 — Projeção orders com dados vivos.** Statically verifiable: the read DTO is ADDITIVE (old shape preserved), provider ML DTO does NOT leak (grep transport/adapters for raw provider fields in the response DTO — it must die in the adapter), unavailable field ⇒ null/UNKNOWN never 0 (ADR-17), cost via GetCostAsOf with source time visible. Verify the enrichment wiring (cost reader + shipment reader adapters) and the DTO shape. LIVE values against a real installation = could-not-run (hub).
**C02 — Decomposição via ports M-07 (architecture half).** grep `internal/modules/orders/**` for ANY import of modules/pricing or a decompose FORMULA — there must be NONE (orders consumes a nil-able Decomposer port, does not reimplement the formula). Confirm DIFAL is a per-order field keyed to the order's real destino UF (uf_route), honest-nil until C2. The live VALUE-parity vs POST /pricing/simulations = deferred to C2 (hub-owned post-merge) — mark it so, do not fail the milestone for it.
**C03 — Tela Pedidos KPIs/Fila/Lista/Kanban.** Statically: KPI counts and Lista tab counts derive from the SAME server-derived bucket single-source (domain.DeriveOrderBucket) so they cannot diverge — verify both the per-order path and the summary by_status path feed DeriveOrderBucket the SAME hasShipment signal (shipping_id column). Kanban is STRICTLY read-only (no drag mutating state, no write call on interaction) — grep zero useMutation/write. Cancelados present as DISABLED stub. Confirm the fetch loads the full dataset (next_cursor followed) so KPI and Lista agree. Browser light+dark + actual pixel render = could-not-run (hub).
**C04 — Drawer decomposição honesta.** Statically: custo ausente ⇒ UnknownValue "—" + componentes_desconhecidos visible, retorno NEVER shown as if complete (no fabricated margem) — verify the drawer render paths + formatMoney/formatPercent null-guards. The margin color-chip (verde/âmbar/vermelho) + the "seed padrão 2026 — não é orientação fiscal" DIFAL label depend on real C2 values — note which exist now vs C2-deferred. Two-case live screenshots = could-not-run (hub).
**C05 — Migrações e seams.** FULLY chip-verifiable: `ls apps/server_core/migrations | grep -E '^006[0-4]'` (plan = NO migration → expect ABSENCE; confirm no orders_* ALTER outside a 0060–0064 block and no stray migration); the whole delta `git diff 8b6c4b30..5051ae5 --stat` writes ONLY in modules/orders/**, sdk-runtime/src/index.ts (orders region, additive), OpenAPI /orders* (additive), apps/web/src/pages/pedidos/**, routes/pedidos.tsx (+ the one AppRouter.test.tsx touch). Decomposição seam committed only AFTER M-07 IC-04 ports published (gate respected — check the ledger). Give a clear PASS/FAIL.
**C06 — PII máscara comprador (LGPD).** FULLY chip-verifiable: grep the order read DTO + buyer DTO + logs for ANY unmasked buyer field — the response must expose ONLY {display (first name + initial), city, uf}. NO CPF/CNPJ, email, phone, full address, or full name in payload/log/UI. Inspect enrichedBuyerDTO + the buyer mapping + any slog lines. Give a clear PASS/FAIL.

## DESIGN 1:1 (structural, read-only): `git show main:.mnfs/MIS-004-mvp-demo/design/handoff/Pedidos.dc.html` and `git show main:.mnfs/MIS-004-mvp-demo/design/DESIGN-REFERENCE.md`. Compare STRUCTURE against the implemented pages/pedidos/**: KPI set (Novos/A faturar/A enviar/Enviados/DIFAL a pagar), views (Fila default / Lista tabs+counts / Kanban read-only), drawer section order (itens+vínculo, decomposição+DIFAL, timeline ML→interno, NF, rastreio, comprador), DIFAL column. Report structural matches/mismatches with file:line. NOTE that per the ratified design memory the M-05 prototype omits a vs-mercado column — that is a different screen; you are validating Pedidos.dc.html. Pixel-exact 1:1 is a hub browser-drive item (could-not-run here) — you assess STRUCTURE only.

## LANES (run read-only, evidence-typed): from apps/server_core set GOCACHE absolute then go build/vet/test orders+composition; from apps/web `npx vitest run src/pages/pedidos` + `npx tsc --noEmit` (jest-dom TS2339 baseline is pre-existing). cd into the worktree in the SAME command (pwsh resets cwd).

## WRITE the result file: `.mnfs/MIS-004-mvp-demo/M-08-pedidos/validation-result.md` with sections projecao, decomposicao, tela, drawer, seams, pii, design, lanes. For each criterion: VERDICT (PASS / PASS-STATIC-hub-drive-pending / FAIL / DEFERRED-C2) + evidence (file:line + command output) + honest evidence-type. End with an OVERALL: is M-08 safe to CLOSE on the READ-ONLY-RICH + C1 bar (D-57/D-62), with the explicit list of items ROUTED TO HUB (browser live-drive C03/C04 UI, C01/C02 live values, C2 value parity) that are NOT chip-satisfiable. Do NOT fabricate a browser drive. A blocking FAIL only for something that IS chip-verifiable and actually wrong (e.g. a write leak, a fabricated value, PII exposure, ownership breach, migration out of block).

## RETURN to me: the per-criterion verdict table, the design structural findings, lane evidence, the ROUTED-TO-HUB list, and the OVERALL close-safety judgment.