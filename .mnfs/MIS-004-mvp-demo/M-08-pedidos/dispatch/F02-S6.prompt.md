impl-pack v1.0.0 · milestone M-08-pedidos · slice F02-S6 (consume C1 decomposição/DIFAL/retorno fields — real-ready, null→"—")

YOU ARE A SLICE IMPLEMENTER (frontend). Hard rules:
- Touch ONLY files under apps/web/src/pages/pedidos/** (WRITE SET below). Anything else: stop and report as a REQUEST. Do NOT edit packages/ui/**, app/**, index.css, routes/**, or the SDK — consume only.
- READ-ONLY RICH bar (D-57) unchanged: no writes, all mutation controls stay disabled "em breve". You are wiring READ fields only.
- **THE POINT OF THIS SLICE (real-ready):** the order read shape now carries `decomposicao`, `difal`, `retorno_liquido`, `margem_pct` (shipped by F01-C1). Today every value is `null` (no decomposer wired) → must render honest "—". The HUB wires the real decomposer (C2) post-merge, after which the SAME components carry real numbers. So you must render each field FROM THE DATA (value → formatted, null/absent → UnknownValue), NEVER hardcode "—". When C2 lands, real numbers must light up with NO further UI change. Replace the current hardcoded "não disponível" blocks accordingly.
- ADR-17: null → "—" via UnknownValue; never fabricate 0/false/date. A monetary value renders via formatMoney; a percent renders as a percent; a null renders UnknownValue.
- No AI-slop; match surrounding TSX idiom; reuse the existing Section/stateTag/UnknownValue/formatMoney helpers already in these files. Cite path:line for reused primitives.
- Before writing, answer G1 (idiom/tokens), G2 (non-trivial decision note), G3 (this is the LAST F-02 impl slice before P6/P7 — blocks nothing after).
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on `ran` with captured output.
- Validation failed? Reproduce, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status · changed paths vs WRITE SET · commands with evidence · confirm each field renders FROM DATA (null→"—", value→formatted) and nothing is hardcoded "—" · what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, etc.) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD 9f9c9ff3). PowerShell/Windows only. npm monorepo (do NOT install deps).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port/touch DB. Commit ONE commit on chip/m08-pedidos, message: `feat(pedidos): F02-S6 consume decomposição/DIFAL/retorno read fields (real-ready, null→"—")`.
- EXCLUSIVE WRITE (my grant): apps/web/src/pages/pedidos/** only.

## DESIGN SOURCE (read the drawer's decomposição + DIFAL block and the Lista RETORNO/DIFAL columns):
`git show main:.mnfs/MIS-004-mvp-demo/design/handoff/Pedidos.dc.html` — the drawer shows a cost decomposition breakdown (comissão, taxa fixa, frete, imposto, DIFAL, tarifa full, custo → margem valor + margem %) and a DIFAL block (valor, rota UF, vencimento, pago). The Lista RETORNO column shows retorno líquido (+ margem %), the DIFAL column shows the DIFAL amount. Read-only via git show.

## FACTS (verified — shipped by F01-C1, consume as-is):
- `OrderRead` now has (SDK, packages/sdk-runtime/src/index.ts): `retorno_liquido?: number | null`, `margem_pct?: number | null`, `decomposicao: OrderDecomposicao`, `difal: OrderDifal` (the last two are REQUIRED — always present objects).
- `OrderDecomposicao = { comissao, taxa_fixa, frete, imposto, difal, tarifa_full, custo, margem_valor, margem_pct: number | null; componentes_desconhecidos: string[] }`. Every numeric member is currently `null` (honest-empty); `componentes_desconhecidos` currently lists the 7 source component names.
- `OrderDifal = { amount: number | null; uf_route: string | null; due_date: string | null; paid: boolean | null }`. All currently null (uf_route stays null until C2 supplies the seller origin).
- These fields ride on BOTH the list items (`listOrders`) and the detail (`getOrder`), so the Lista table AND the drawer both have them.
- Existing helpers already in the files: `formatMoney(number|null): string|null` and `formatDateTime(string|null): string|null` (pedidosFormatters.ts); `UnknownValue`, `Section`, `stateTag` (in PedidoDrawer.tsx / PedidosTable.tsx). Reuse them. If a percent formatter is needed, add a small honest `formatPercent(value: number|null): string|null` to pedidosFormatters.ts (null→null; else e.g. `${value.toFixed(1)}%` — decide the honest representation; margem_pct semantics are a ratio-or-percent question, so G2-note your assumption; if unsure whether the number is 0-1 or 0-100, render the raw number with a "%" ONLY if the design is unambiguous, else render via formatMoney-style passthrough and note it — do NOT guess a scale that could misrepresent).

## WHAT TO BUILD (exactly this — wiring reads, no new sections):
1. **PedidoDrawer.tsx — `DecomposicaoSection`**: change its signature to `DecomposicaoSection({ order }: { order: OrderRead })` and render the breakdown FROM `order.decomposicao`, `order.retorno_liquido`, `order.margem_pct`, `order.difal`:
   - A definition list of the decomposition components in design order: Comissão (`decomposicao.comissao`), Taxa fixa (`taxa_fixa`), Frete (`frete`), Imposto (`imposto`), DIFAL (`decomposicao.difal`), Tarifa Full (`tarifa_full`), Custo (`custo`) → each `formatMoney(v) ?? <UnknownValue/>`.
   - Then Margem valor (`margem_valor` → formatMoney) and Margem % (`decomposicao.margem_pct` → formatPercent) and Retorno líquido (`order.retorno_liquido` → formatMoney), each null→UnknownValue.
   - A DIFAL sub-block from `order.difal`: Valor (`amount`→formatMoney), Rota (`uf_route`→string or UnknownValue), Vencimento (`due_date`→formatDateTime), Pago (`paid`→ boolean: honest "sim"/"não" ONLY when non-null, else UnknownValue).
   - Keep the caller updated: wherever `<DecomposicaoSection />` is rendered, pass `order`. You may keep a SUBTLE honest note (e.g. small faint text) shown only when `decomposicao.componentes_desconhecidos.length > 0` explaining the "—" are pending the cost decomposition — but it must be DATA-DRIVEN (derived from componentes_desconhecidos), not a hardcoded "não disponível" paragraph. Remove the old hardcoded "ainda não estão disponíveis nesta instalação" copy so it can't lie once C2 lands.
2. **PedidosTable.tsx — RETORNO column**: `render: (item) => formatMoney(item.retorno_liquido) ?? <UnknownValue .../>`. Optionally append margem_pct as a faint sub-note when present (formatPercent). Remove the hardcoded `render: () => <UnknownValue/>`.
3. **PedidosTable.tsx — DIFAL column**: `render: (item) => formatMoney(item.difal?.amount) ?? <UnknownValue .../>`. Remove the hardcoded render. (uf_route stays null now; do not add a column for it.)
4. Update the gated-comment lines in both files: they currently say "no retorno/margem/difal field on OrderRead" — that is now FALSE. Replace with an accurate one-liner ("null until the decomposer is wired (hub C2); renders '—' honestly, real value when present").
5. **Tests (PedidosPage.test.tsx or PedidoDrawer.test.tsx)**: mock an order whose `decomposicao`/`difal`/`retorno_liquido`/`margem_pct` are all null and assert the drawer + RETORNO + DIFAL render "—" (UnknownValue). Add ONE test with a NON-null `retorno_liquido` and `difal.amount` (and a couple decomposicao components) asserting the formatted values render (proving the real-ready path — that C2's numbers will show without UI change). Mirror the existing vi.hoisted client mock idiom; ensure fixtures include the new required `decomposicao`/`difal` objects so types compile. Keep all existing assertions green.

DO NOT: fabricate any value; render 0/false for null; hardcode "—"; add mutations/writes; add new drawer sections beyond wiring the existing decomposição/DIFAL block; edit outside pages/pedidos/**; change the SDK/contract (C1 already shipped it).

FAILING_TEST_FIRST: add the null→"—" and non-null→formatted assertions (red against the current hardcoded "—"), then wire to green.
COMMANDS (evidence-typed, from apps/web): `npx tsc --noEmit` · `npx vitest run src/pages/pedidos --config vitest.config.ts`. Report other web suites not-run. The KNOWN repo-wide jest-dom matcher-typing tsc baseline is NOT your defect (verify via `git stash` same errors on untouched files); only NEW errors in files you touched count. State this in evidence.
DONE_CRITERIA: tsc (no NEW pedidos errors) + scoped vitest green; drawer decomposição/DIFAL block + Lista RETORNO/DIFAL columns render FROM `order.decomposicao/difal/retorno_liquido/margem_pct` (null→"—", non-null→formatted, proven by tests); no hardcoded "—"; no fabricated values; no writes; only pages/pedidos/** touched; ONE commit.
OPEN_QUESTIONS: if the SDK OrderRead does NOT have decomposicao/difal/retorno_liquido/margem_pct (it does after 9c3a681/9f9c9ff3 — verify) or margem_pct scale is genuinely ambiguous from the design, STOP and report the scale question rather than guessing. Otherwise none.
