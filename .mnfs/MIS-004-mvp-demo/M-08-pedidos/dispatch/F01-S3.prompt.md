impl-pack v1.0.0 · milestone M-08-pedidos · body-sha256 1dec366af1bfd4b4f49f060e39e12d39457d0f3b06ff54d7d2474d055cc2fa55

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Mocks prove contract shape, never integration.
- Before writing, answer: G1 — right for the WHOLE system (contracts, module map), not just this file? G2 — non-trivial decision -> 1-3 line alternatives-considered note in your report. G3 — does this block a NAMED upcoming milestone/seam?
- A new abstraction (interface, wrapper, config knob, generic param) requires a SECOND named consumer existing now or in a declared brief. None = do not build it.
- Duplicating an existing helper/pattern: cite it path:line and reuse; never copy.
- No blanket recover/try-catch or fallback on integrity-critical reads - unknown != zero/default; fail honest.
- No comment narration, no dead code, no unanchored TODOs; match the module's idiom.
- Evidence per command: type ran / assumed / could-not-run. Pass ONLY on ran with an artifact path or captured output. Never Pass on assumed or could-not-run.
- Validation failed? REPRODUCE the failure in isolation first, then fix, then re-run the FULL validation plan. Max ONE fixup this session; second failure = stop, report BLOCKED with the reproduction.
- Contract/architecture conflict: stop and report. You do not adjudicate.
- Final report: status - changed paths vs write_set (any undeclared path gets a one-line justification) - commands with evidence types - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base SHA 8b6c4b3093f9465cd3b91209b054af4fa702171a. PowerShell/Windows, never WSL. Go workspace.
- Env already prepared (mod cache warm). Before Go commands, set GOCACHE to an ABSOLUTE path: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY. Use `GOMODCACHE=$(pwd)/.gomodcache` if a module is missing.
- YOU CANNOT BOOT A DATABASE OR DEV STACK (hub-owned). DB-integration tests in adapters/postgres that require a live pool will SKIP or you must NOT run them — that is expected. See the evidence note in the slice card: the SQL column-add is compile+vet verified here and its DB behavior is `could-not-run` (QA live-drives it). Do NOT stand up postgres.
- Do NOT push, reset, revert, stash, clean. Do NOT read/print .env*. Do NOT install deps. Commit per green slice on the current branch (chip/m08-pedidos) — one commit, message `feat(orders): S3 enrichment shipment — SLA/atraso/destino_uf/rastreio + buyer UF`.
- Non-negotiables: ADR-17 unknown != zero — every shipment field (SLA, atraso, destino_uf, rastreio) stays nil/absent when the shipment cannot be read (no shipping_id, reader error, or the provider returns a nil field); NEVER fabricate a date, a false "on-time", a UF, or a tracking value. The order list MUST NEVER fail because one shipment lookup errored — degrade to nils, emit telemetry, continue. No blanket recover; handle the specific `(ShipmentInfo, error)` return. PII: destino_uf and the masked buyer's UF are the ONLY new location data allowed to surface; never add city/street/full address; do not log buyer identity.

## SLICE CARD — F01-S3 (Go-internal shipment enrichment + read-model shipping_id surface)
GOAL: Extend the enrichment seam with per-order shipment facts (SLA due, atraso/delayed, destino_uf, rastreio) sourced via a simplified orders-local ShipmentReader consumer port, AND surface `shipping_id` on the order read-model projection so the enricher has an id to look up. Also fill the masked buyer's UF (S1 deliberately left `MaskedBuyer.UF` nil "for a later shipment slice") from the shipment's destination UF. PURELY internal Go + the read repo's own SELECT/scan — MUST NOT touch OpenAPI, the SDK, the HTTP transport response JSON, or root.go/composition. No margin/retorno/difal/decomposition.

FACTS (verified against base; reuse, do not re-derive):
- Provider shipment fact type `connectorsdomain.ShipmentInfo` (`connectors/domain/shipping_read.go:5-13`): `ID string`, `Status string`, `SLADue *time.Time`, `Delayed *bool`, `Costs *ShipmentCosts`, `DestinationUF *string`, `FetchedAt time.Time`. THERE IS NO carrier tracking-URL/code field — honest `rastreio` = the shipment `ID` + `Status` ONLY; do NOT invent a tracking link.
- The connectors ShipmentReader port (`connectors/ports/shipping_read.go:9-12`) is keyed by `domain.ProviderAccountRef` (`connectors/domain/capability.go:94-99`: TenantID, InstallationID, ProviderCode, ProviderAccountID) + shipmentID. Resolving installationID → ProviderAccountRef is composition/account concern — it is NOT this slice's job. Therefore, mirror the S2 CostReader pattern: define a SIMPLIFIED orders-local consumer port keyed by installationID + shipmentID; the real adapter (deferred to the later root-wiring slice) will resolve the account ref and call the connectors ShipmentReader. Cite `orders/ports/cost_reader.go` (S2, this branch) as the pattern you are mirroring.
- Read-model projection: `postgres/order_repo.go` — `ListOrders` (query at :47) and `GetOrder` (query at :100) BOTH build `OrderReadModel` via `scanReadModel` (:123), which currently scans 9 columns and does NOT select `shipping_id`. The column EXISTS in table `orders_marketplace_orders` (round-trip queries select/scan it at :210, :273, :320, and write it at :346 via `order.ShippingID`). `scanReadModel` takes `interface{ Scan(...any) error }` — a FAKE scanner unit-tests the new mapping WITHOUT a database.
- `MarketplaceOrder.ShippingID` is a plain `string` (`domain/order.go:53`). `nf`/NULL handling pattern in scanReadModel uses `pgtype.Text` then copies `.String` when `.Valid` (:126,134-137) — mirror it for shipping_id.
- S1/S2 seam (base): `application/enrich_service.go` — `EnrichedOrder{Order, Buyer, VinculoStatus, ItemCosts, ComponentesDesconhecidos}`, `EnrichService` with `NewEnrichService(cost CostReader)`, `Enrich(ctx, installationID string, orders) []EnrichedOrder` (no error). `MaskedBuyer{Display, City *string, UF *string}` (`domain/order_enrichment.go`). Buyer/vinculo/cost behavior stays intact.
- Telemetry idiom = `slog` (no metrics library in this codebase). Emit a structured `slog` warn on a shipment fetch error; do NOT build a metrics/telemetry abstraction.

DECISIONS you make (report each as a 1-3 line G2 note):
- Shipment-fields shape on `EnrichedOrder`: e.g. a `*ShipmentEnrichment{ ShipmentID string; Status string; SLADue *time.Time; Delayed *bool; DestinationUF *string }` (nil when no shipment could be read). Rastreio is represented by ShipmentID+Status, nothing more.
- Whether `EnrichService` takes an injected `*slog.Logger` (preferred, testable) or uses `slog.Default()`. Constructor becomes `NewEnrichService(cost CostReader, shipment ShipmentReader, logger *slog.Logger)` (or your justified variant). No production caller exists yet (handler unwired), so the signature change is contained — grep to confirm.
- `shipping_id` field on `OrderReadModel`: plain `string` (empty ⇒ absent, mirrors MarketplaceOrder) vs `*string`. Empty/absent ⇒ enricher does NOT call the reader (not an error).

WRITE_SET (exact — nothing else):
- `apps/server_core/internal/modules/orders/ports/shipment_reader.go` (NEW) — simplified `ShipmentReader interface { GetShipment(ctx context.Context, installationID, shipmentID string) (connectorsdomain.ShipmentInfo, error) }`. One method. Cite cost_reader.go as the mirrored pattern.
- `apps/server_core/internal/modules/orders/domain/read_model.go` (EDIT) — add the `ShippingID` field (see decision). Additive; no reorder of existing JSON fields.
- `apps/server_core/internal/modules/orders/adapters/postgres/order_repo.go` (EDIT) — add `o.shipping_id` to BOTH the ListOrders (:47) and GetOrder (:100) SELECT column lists in the correct position, and extend `scanReadModel` to scan it (pgtype.Text → ShippingID, mirroring the `nf` handling). Nothing else in this file.
- `apps/server_core/internal/modules/orders/adapters/postgres/order_repo_scan_test.go` (NEW) — a fake-scanner unit test (NO database) that feeds a row including a shipping_id value into `scanReadModel` and asserts `model.ShippingID` is populated (and empty/NULL ⇒ empty/absent). This converts the scan-mapping to `ran`.
- `apps/server_core/internal/modules/orders/application/enrich_service.go` (EDIT) — `EnrichService` gains the ShipmentReader (+ logger) dependency; `Enrich` looks up each order's shipment by `order.ShippingID`, populates the shipment enrichment + fills `Buyer.UF` from `DestinationUF`; error ⇒ nil shipment fields + `slog` warn + order still returned; empty shipping_id ⇒ reader NOT called. Buyer mask/vinculo/cost unchanged. Still returns no error.
- `apps/server_core/internal/modules/orders/application/enrich_service_test.go` (EDIT) — add a FAKE ShipmentReader + table tests: (a) shipment present ⇒ SLA/Delayed/DestinationUF/ShipmentID+Status populated AND Buyer.UF set from DestinationUF; (b) reader error ⇒ shipment nil + order still present + reader-was-called + Buyer.UF stays nil; (c) empty shipping_id ⇒ reader NOT called (call-count fake) + shipment nil; (d) provider returns nil SLADue/Delayed/DestinationUF ⇒ those pass through as nil (never fabricated) + Buyer.UF stays nil; (e) S1 buyer-mask + vinculo + S2 cost STILL correct alongside shipment.

DO NOT (this slice): build the real connectors-backed adapter, resolve ProviderAccountRef, wire anything into root.go/composition, add margin/retorno/difal, touch OpenAPI/packages/sdk-runtime/transport. Production adapter + root wiring is a later grant-gated slice; S3 proves logic against fakes.

EVIDENCE NOTE (bind exactly): the `scanReadModel` mapping is verified `ran` via the fake-scanner test; the actual SQL string change (selecting `o.shipping_id`) against a live DB is `could-not-run` in this session (no stack) — state it plainly in your report as could-not-run and note it is QA-live-drive verified. Do NOT mark it Pass; do NOT fabricate a DB run. The column is known to exist (cited round-trip usage), so the risk is a column-position/typo error, which `go build`/`go vet` + the fake-scanner test largely cover.

FAILING_TEST_FIRST: write the enrich shipment cases + the fake-scanner test first (red), then implement to green.
COMMANDS (split, evidence-typed): `go build ./...` · `go vet ./...` · `go test ./internal/modules/orders/...` (GOCACHE absolute, from apps/server_core). If the postgres package's DB-integration tests try to connect and fail/skip, that is expected — capture that they SKIPPED (could-not-run), and confirm the non-DB tests (application, the new scan test) PASS.
EXPECTED_ARTIFACTS: captured `go test` output showing enrich shipment tests + scan test green; the changed files.
DONE_CRITERIA: build+vet+test green (DB-integration tests skipped/not-run is acceptable and must be reported as could-not-run); shipment fields nil-on-unknown (never fabricated); Buyer.UF filled only from a real DestinationUF; list never fails on a shipment error (telemetry emitted); shipping_id surfaced on the read model via both queries + scan; S1/S2 behavior preserved; ZERO changes to OpenAPI/sdk-runtime/transport/root.go.
OPEN_QUESTIONS: if surfacing shipping_id cleanly forces a change to a query OTHER than the two projection queries, or forces importing account-resolution logic into the application layer, STOP and report. Otherwise none.
