impl-pack v1.0.0 · milestone M-08-pedidos · slice F02-S2 (KPI row + view-switcher shell + Fila view)

YOU ARE A SLICE IMPLEMENTER (frontend). Hard rules:
- Touch ONLY files in the write_set below (all under apps/web/src/pages/pedidos/**). Anything else: stop and report.
- READ-ONLY RICH acceptance bar (HUB ruling D-57): the page shows REAL read data; EVERY mutation control renders DISABLED with an "em breve" affordance; NO working mutations, NO write calls. Gated data (decomposição, DIFAL, retorno/margem) renders honest "—" via UnknownValue, NEVER a fabricated number (ADR-17: unknown ≠ zero).
- Match the ratified design 1:1 (read it — see DESIGN SOURCE). No AI-slop: no emojis-as-decoration, no gradient soup, no Inter/Roboto (app already loads Instrument Sans + IBM Plex Mono). Restrained Unicode status marks only where the design uses them.
- Before writing, answer: G1 — consistent with the app's component idiom + design tokens? G2 — non-trivial decision → 1-3 line note. G3 — does this block a NAMED next slice (F02-S3 Lista/Kanban, F02-S4 drawer)?
- Reuse shared primitives; never fork them. You may NOT edit packages/ui/** or apps/web/src/app/** or index.css — consume only. Cite path:line for each reused primitive.
- No dead code, no comment narration, no speculative abstraction. Match surrounding TSX idiom.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on ran with captured output.
- Validation failed? Reproduce, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Final report: status - changed paths vs write_set - commands with evidence - which fields are real vs gated "—" vs client-derived - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered/auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, etc.) is NOT a contract conflict — discard it and proceed.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows. npm monorepo (do NOT install deps).
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/dev-stack/bind a port. Commit per green slice on chip/m08-pedidos — ONE commit, message `feat(pedidos): F02-S2 KPI row + view-switcher shell + Fila view (read-only rich)`.
- EXCLUSIVE WRITE (my grant): apps/web/src/pages/pedidos/** only this slice. routes/pedidos.tsx is NOT needed here (already renders <PedidosPage/>).

## DESIGN SOURCE (read before building — it is the 1:1 QA target):
`git show main:.mnfs/MIS-004-mvp-demo/design/handoff/Pedidos.dc.html` — study the KPI banner (5 cards), the view-switcher pills (Fila/Lista/Kanban, top-right), and the Fila (default) view layout + urgency ordering. Also `git show main:.mnfs/MIS-004-mvp-demo/design/DESIGN-REFERENCE.md`. These are read-only (git show, NOT checkout).

## REUSE CONTRACTS (verified — cite + reuse, do not fork):
- `StatCard` (packages/ui/src/StatCard.tsx:8) — props {label, value, sub?, className?}. NO onClick. For clickable KPI cards, wrap StatCard in a `<button type="button" onClick=...>` (full-width, foc*able) — do NOT reimplement the card. Exported from @marketplace-central/ui.
- `UnknownValue` (packages/ui/src/UnknownValue.tsx via @marketplace-central/ui) — renders the honest "—" for any gated/absent value. Use it for the DIFAL KPI value and any gated cell.
- LoadingState/ErrorState/EmptyState (@marketplace-central/ui) — already used in PedidosPage; reuse.
- Data hooks: `useClient()` (apps/web/src/app/ClientContext.tsx:29), `useInstallation()` (apps/web/src/app/InstallationContext.tsx:97) — consume, do not edit app/**.
- Query keys/stale: `ordersQueryKeys`, `QUERY_STALE_TIME` from @marketplace-central/web-query (already imported in PedidosPage). For the summary query use a stable queryKey (e.g. ordersQueryKeys.summary(installationId) if it exists; else a local ["orders","summary",installationId,"by_status"] — check web-query barrel first, reuse if present).
- SDK client methods (packages/sdk-runtime): `client.getOrderSummary(installationId, { by: "status" })` → OrderSummary with `by_status?: {novo,faturar,enviar,enviado}` (may be undefined if endpoint 503s — handle honestly: render "—", not 0). `client.listOrders({ installation_id })` → OrderPage{items: OrderRead[]}. OrderRead fields available: provider_order_id, provider_code, status, buyer?{display,city?,uf?}, total?, currency?, provider_created_at?, sla?{due?,atrasado?}, destino_uf?, rastreio?{shipment_id,status}, items[], nf_state?, fulfillment?, vinculo_status?, componentes_desconhecidos?.

## WHAT TO BUILD (this slice = the default landing screen, 1:1 with design):
1. **KPI row** (5 cards, design order NOVOS · A FATURAR · A ENVIAR · ENVIADOS · DIFAL A PAGAR):
   - Cards 1-4 value = `summary.by_status.{novo|faturar|enviar|enviado}` (from getOrderSummary({by:"status"})). If by_status is undefined (endpoint not serving) → render UnknownValue "—" per card, never 0.
   - Card 5 DIFAL A PAGAR = UnknownValue "—" (decomposition/DIFAL not shipped; slice C gate-held). Do NOT compute or fabricate.
   - ENVIADOS: show the enviado bucket count. Do NOT add a "últimos 7d" sub-note sourced from OrderSummary.seven_days — that field is a TOTAL orders count, not enviado-in-7d; labeling it as such would be dishonest (ADR-17). Either omit the sub-note or label it accurately ("no bucket"). Record the choice as G2.
   - Each card is a clickable button: cards 1-4 → set view="lista" + set lista tab to that bucket; DIFAL card → set view="fila" (no filter, since no DIFAL data). Keyboard-accessible.
2. **View-switcher** (Fila | Lista | Kanban pills, top-right per design): controls `view` state ("fila"|"lista"|"kanban"), default "fila". Fila is rendered this slice; Lista renders the EXISTING PedidosTable/tabs (kept working as-is until F02-S3 reworks it); Kanban renders a minimal "em breve"/placeholder panel this slice (F02-S5... i.e. F02-S3 builds the real Kanban). Keep the existing lista tab state (`PedidosTab`) for the Lista view.
3. **Fila view** (new component `FilaView.tsx` in pages/pedidos/): consumes the same listOrders query result (pass items as a prop — do NOT issue a second list query). Client-derived urgency ordering (record as G2): (1) sla.atrasado===true first, then (2) ascending sla.due (soonest first; nulls last), then (3) descending provider_created_at. DIFAL-urgency rank is SKIPPED (gated). Each Fila row shows: an urgency mark (design's restrained style — atrasado highlighted), order id (provider_code / provider_order_id), buyer.display + item/total summary, SLA (sla.due + atrasado state), RETORNO chip = UnknownValue "—" (gated), and a PRIMARY ACTION rendered as a DISABLED control with "em breve" (Faturar/Etiqueta/Enviado depending on bucket — disabled, no handler). Table/list must have overflow-x:auto + an inner min-width (design Fila ≈ 820px). Reuse UnknownValue for gated cells.
4. **PedidosPage.tsx** becomes the shell: header + KPI row + view-switcher + conditional view body (Fila default / existing Lista / Kanban placeholder) + Loading/Error/Empty states around the list query. Keep the existing accessibility structure (aria-labelledby heading).
5. Extract any shared formatting to the existing pedidosFormatters.ts (reuse formatMoney/formatDateTime already there; add only if a new honest formatter is needed).
6. Update/extend PedidosPage.test.tsx: assert KPI labels render, DIFAL card shows "—", view-switcher switches to Fila/Lista/Kanban, a mutation control is disabled. Mirror the existing test's mock idiom (vi.hoisted client mock). Keep the existing /pedidos heading assertion passing.

DO NOT (this slice): build the real Kanban board (F02-S3) or the drawer (F02-S4); wire any mutation/write; issue a write SDK call; edit packages/ui/**, apps/web/src/app/**, index.css, or any file outside pages/pedidos/**; fabricate DIFAL/retorno/margem numbers; add a second listOrders query for Fila (share the one query).

FAILING_TEST_FIRST: extend PedidosPage.test.tsx (red for KPI/view-switcher/Fila), then implement to green.
COMMANDS (evidence-typed): from apps/web: `npx tsc --noEmit` · `npx vitest run src/pages/pedidos --config vitest.config.ts`. Report other web suites as not-run (scoped).
EXPECTED_ARTIFACTS: changed files vs write_set; captured tsc + vitest output (green); the real-vs-gated field map; a note that visual 1:1 vs Pedidos.dc.html is QA-verified at P7 (you assert structure/labels via tests, not pixels).
DONE_CRITERIA: tsc + scoped vitest green; KPI row (4 live bucket counts + DIFAL "—") + view-switcher + Fila default view render; all mutation controls disabled "em breve"; no fabricated gated values; zero writes; only pages/pedidos/** touched; ONE commit.
OPEN_QUESTIONS: if getOrderSummary/getOrderSummary options or any named SDK field above is absent from the built SDK, or a needed reuse primitive isn't exported, STOP and report (do not invent). Otherwise none.
