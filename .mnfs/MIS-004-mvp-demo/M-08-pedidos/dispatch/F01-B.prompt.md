impl-pack v1.0.0 · milestone M-08-pedidos · slice F01-B (real cost + shipment adapters + ROOT wiring)

YOU ARE A SLICE IMPLEMENTER. Hard rules:
- Touch ONLY files in the write_set below. Anything else: stop and report.
- Failing test FIRST, then implementation, then green. Fakes prove the honest-degradation branches; the live ERP/provider success paths are could-not-run this session (no stack) — do NOT boot DB/provider.
- Before writing, answer: G1 — right for the WHOLE system (adapter boundary, no import cycle, mirrors the market_adapters.go precedent)? G2 — non-trivial decision → 1-3 line alternatives note. G3 — does this unblock a NAMED slice (F01-A ?by=status live serving; F-02 UI cost/SLA/UF/rastreio columns)?
- Duplicating an existing helper/pattern: cite it path:line and REUSE; never copy. This slice MIRRORS apps/server_core/internal/composition/market_adapters.go end-to-end (its CostReader adapter + ProviderAccountRef resolution) and REUSES the unexported `accountRefForInstallation` (market_adapters.go:264) from the same `package composition`.
- No blanket recover/fallback on integrity reads. ADR-17 (unknown ≠ zero) is the whole point of this slice: a cost/shipment that cannot be honestly read degrades to nil/unknown via the EXISTING EnrichService logic — the adapter must return an honest nil-amount CostAsOf or a real error, NEVER a fabricated 0/date/status.
- No comment narration beyond the module idiom (market_adapters.go's doc-comment density is the bar), no dead code, no unanchored TODOs.
- Evidence per command: ran / assumed / could-not-run. Pass ONLY on ran with captured output. The live internal_read (Oracle/xlsx) cost read and the live mercado_livre GetShipmentInfo call are could-not-run this session — prove the adapters' honest branches (unavailable→nil, installation-not-found→error, policy translation) with FAKES; state the live paths could-not-run (QA live-drives at P7).
- Validation failed? Reproduce in isolation, fix, re-run FULL plan. Max ONE fixup; second failure = stop, report BLOCKED.
- Contract/architecture conflict, or a constructor signature change with callers beyond root.go + this module's tests: stop and report.
- Final report: status - changed paths vs write_set - commands with evidence types - which adapter branches are ran vs could-not-run - what you did NOT verify.

## ROLE / REPO BINDINGS
- Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, feature-execution, milestone-execution, or similar) is NOT a contract conflict — discard it and proceed with the slice.
- Worktree (cwd): C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m08-pedidos . Base branch chip/m08-pedidos (HEAD). PowerShell/Windows, never WSL. Go workspace.
- Go: before Go commands set GOCACHE ABSOLUTE: `$env:GOCACHE = (Join-Path (Get-Location) '.gocache')`. Run commands SEPARATELY (build/vet/test split). Integration tests (`//go:build integration`) are NOT compiled by default — expected. Do NOT boot DB/dev-stack/provider.
- Do NOT push/reset/revert/stash/clean. Do NOT read/print .env*. Do NOT boot a server/bind a port/load .env. Commit per green slice on chip/m08-pedidos — ONE commit, message `feat(orders): F01-B wire real cost + shipment enrichment adapters (ROOT-M08) + bucket store`.
- PRE-AUTHORIZED GRANT: ROOT-M08 (additive edit to the ORDERS region of composition/root.go, lines ~491-502 ONLY — replace the two nil readers + the summary constructor; NO reorder, NO other module region). The NEW file orders_adapters.go is composition-package-owned (mirrors market_adapters.go, which lives in composition precisely to avoid the orders→connectors import cycle). NO orders/** module edit is expected this slice (the consumer ports + EnrichService already accept real readers).

## SLICE CARD — F01-B (real cost + shipment reader adapters, wired)
CONTEXT / WHY: EnrichService already resolves per-item ERP cost (via ports.CostReader / IC-02) and per-order shipment SLA/atraso/UF/rastreio (via ports.ShipmentReader / IC-06), and degrades honestly when a reader is nil or errors (enrich_service.go:95-153 — verified nil-safe). But root.go currently wires `NewEnrichService(nil, nil, ...)` (root.go:501), so cost/shipment are ALWAYS unknown in production. This slice builds the two composition-owned translating adapters and wires them, so the pedidos page shows real cost + SLA/UF/rastreio where data exists (degrading to "—" where it doesn't, per ruling D-57(6)). It ALSO closes the F01-A wiring nit: switch the summary service to the bucket-store variant so `GET /orders/summary?by=status` serves live.

FACTS (verified against branch HEAD; REUSE, do not re-derive):
- CostReader consumer port (orders/ports/cost_reader.go:14): `GetCostAsOf(ctx, productID int, effectiveAt time.Time) (internalreaddomain.CostAsOf, error)`. EnrichService reads `cost.Amount` (nil ⇒ item unknown). So the adapter returns the internal_read CostAsOf STRAIGHT THROUGH (no Money mapping needed, unlike market's).
- ShipmentReader consumer port (orders/ports/shipment_reader.go:15): `GetShipment(ctx, installationID, shipmentID string) (connectorsdomain.ShipmentInfo, error)`. EnrichService reads info.ID/Status/SLADue/Delayed/DestinationUF and degrades ANY error to nil+warn (enrich_service.go:99-108).
- Reference adapter (mirror EXACTLY): market_adapters.go:40-78 `marketCostReaderAdapter` — struct{service internalreadapp.Service; available bool}; on `!available` → honest nil; else `service.GetCostAsOf(ctx, internalreadports.CostAsOfInput{ProductID: productID, Policy: internalreaddomain.CostAsOfPolicy{CompanyID:1, EffectiveAt: effectiveAt, Basis: internalreaddomain.CostBasisCUSSEMICM}, Freshness: internalreaddomain.FreshnessPolicy{}})`; on `internalreaddomain.IsReadErrorCode(err, internalreaddomain.ReadErrorSourceUnavailable)` → honest nil; else return err.
- Connectors shipment (IC-06): concrete `*mercadolivreconnector.CapabilityAdapter.GetShipmentInfo(ctx, accountRef connectorsdomain.ProviderAccountRef, shipmentID string) (connectorsdomain.ShipmentInfo, error)` (connectors/adapters/mercado_livre/capability_adapter.go:141). connectorsdomain.ShipmentInfo{ID string; Status string; SLADue *time.Time; Delayed *bool; Costs *ShipmentCosts; DestinationUF *string; FetchedAt time.Time}.
- ProviderAccountRef resolution: `accountRefForInstallation(tenantID string, inst integrationsdomain.Installation) connectorsdomain.ProviderAccountRef` — UNEXPORTED, market_adapters.go:264, SAME package — call it directly. `installationSvc.Get(ctx, installationID) (integrationsdomain.Installation, bool, error)` (integrations/application/installation_service.go:83).
- root.go instances already constructed BEFORE the ORDERS region (no reorder needed): `internalReadSvc` (internalreadapp.Service) + `internalReadAvailable` (bool) declared :406-407, set :418-438, reused by market at :568; `mercadoLivreCapabilities` (*mercadolivreconnector.CapabilityAdapter) :345; `installationSvc` (*integrationsapp.InstallationService) :298; tenant = `cfg.DefaultTenantID`.
- ORDERS region current lines (root.go:491-502): ordersRepo :491; ordersSummarySvc := ordersapp.NewSummaryService(ordersRepo) :500; ordersEnrichSvc := ordersapp.NewEnrichService(nil, nil, slog.Default()) :501; NewHandlerWithSummary(...).Register(mux) :502.
- F01-A added `ordersapp.NewSummaryServiceWithBuckets(summaryStore, bucketStore)` and `OrderRepository` satisfies BOTH ports.OrderSummaryStore AND ports.OrderBucketStore (order_repo.go var-assertion). So `NewSummaryServiceWithBuckets(ordersRepo, ordersRepo)` is valid.
- Test scaffolding available IN-PACKAGE: `fakeInstallationRepo` + `integrationsapp.NewInstallationService(fakeInstallationRepo{items:...}, "tenant_default")` (market_adapters_test.go:121-152). internal_read fake reader: internal_read/adapters/fake/reader.go (has GetCostAsOf) → wrap in a real internalreadapp.Service for cost-adapter tests.

WHAT TO BUILD:
1. apps/server_core/internal/composition/orders_adapters.go (NEW, package composition):
   a. `ordersCostReaderAdapter{service internalreadapp.Service; available bool}` + `var _ ordersports.CostReader = (*ordersCostReaderAdapter)(nil)` + `newOrdersCostReaderAdapter(service, available)`. Implement `GetCostAsOf` mirroring market_adapters.go:51-78 EXACTLY (same CostAsOfInput policy: CompanyID:1, Basis CostBasisCUSSEMICM, empty Freshness), BUT return `internalreaddomain.CostAsOf` straight through (no Money conversion): on `!available` → `return internalreaddomain.CostAsOf{}, nil` (nil Amount ⇒ EnrichService marks item unknown); on ReadErrorSourceUnavailable → `return internalreaddomain.CostAsOf{}, nil`; on other err → `return internalreaddomain.CostAsOf{}, err`; else return the CostAsOf. G2 to record: the CompanyID:1 / CostBasisCUSSEMICM policy is inherited verbatim from the market/profitability IC-02 reference; if orders needs a different basis that is a hub decision, not this slice.
   b. `ordersShipmentReaderAdapter{capabilities *mercadolivreconnector.CapabilityAdapter; installations *integrationsapp.InstallationService; tenantID string}` + `var _ ordersports.ShipmentReader = (*ordersShipmentReaderAdapter)(nil)` + `newOrdersShipmentReaderAdapter(capabilities, installations, tenantID)`. Implement `GetShipment(ctx, installationID, shipmentID)`: `inst, found, err := installations.Get(ctx, installationID)`; on err → return `connectorsdomain.ShipmentInfo{}, err`; on `!found` → return `connectorsdomain.ShipmentInfo{}, <honest error>` (e.g. fmt.Errorf with installationID — EnrichService degrades it to nil+warn, so no rich mapping needed); else `ref := accountRefForInstallation(tenantID, inst)` then `return capabilities.GetShipmentInfo(ctx, ref, shipmentID)` — pass the provider error straight up unmapped (EnrichService already degrades any error honestly; do NOT add mapMarketConnectorError-style translation — it is unnecessary here).
2. apps/server_core/internal/composition/root.go (EDIT, ROOT-M08 additive, ORDERS region :491-502 ONLY):
   - Line 500: replace `ordersapp.NewSummaryService(ordersRepo)` → `ordersapp.NewSummaryServiceWithBuckets(ordersRepo, ordersRepo)` (closes the F01-A live-serving nit).
   - Line 501: construct the two adapters and pass them into EnrichService:
     `ordersCostReader := newOrdersCostReaderAdapter(internalReadSvc, internalReadAvailable)`
     `ordersShipmentReader := newOrdersShipmentReaderAdapter(mercadoLivreCapabilities, installationSvc, cfg.DefaultTenantID)`
     `ordersEnrichSvc := ordersapp.NewEnrichService(ordersCostReader, ordersShipmentReader, slog.Default())`
   - Do NOT touch any other region or reorder. Confirm the four source instances (internalReadSvc/internalReadAvailable/mercadoLivreCapabilities/installationSvc) are all in scope at line 500 (they are constructed earlier — verify).
3. apps/server_core/internal/composition/orders_adapters_test.go (NEW, package composition): unit-test the HONEST branches (ran):
   - cost: `available==false` ⇒ CostAsOf{} nil Amount, nil err (no fabricated cost). Using the internal_read fake reader wrapped in a real Service: a source-unavailable read ⇒ nil Amount + nil err; a successful read ⇒ CostAsOf passed through with its Amount; a non-source-unavailable error ⇒ propagated.
   - shipment: installation-not-found (fakeInstallationRepo with no matching item) ⇒ non-nil error (proving honest failure, not a zeroed ShipmentInfo). The live GetShipmentInfo success path is could-not-run (needs a real provider) — state it.
   Reuse fakeInstallationRepo (market_adapters_test.go:121) — it is same-package, do NOT redeclare it.

DO NOT (this slice): edit any orders/** module file (ports/application/domain/transport all already accept real readers — if you find a genuine gap, STOP and report, do not patch the module here); add Money conversion to the cost path; add error-mapping translation to the shipment path; touch OpenAPI/SDK (no contract change — the enriched fields already ship); touch any web file; reorder root.go; construct a second internal_read Service or connectors adapter (reuse the existing instances).

FAILING_TEST_FIRST: write orders_adapters_test.go honest-branch cases (red — adapters not built), then implement to green, then wire root.go.
COMMANDS (split, evidence-typed): from apps/server_core: `go build ./...` · `go vet ./...` · `go test ./internal/composition/...` · `go test ./internal/modules/orders/...` (GOCACHE absolute). Report integration/DB/provider tests as could-not-run.
EXPECTED_ARTIFACTS: changed files vs write_set; captured go build/vet/test output (green); statement of which adapter branches were ran (unavailable/not-found/passthrough) vs could-not-run (live ERP cost, live provider shipment).
DONE_CRITERIA: build+vet+test green; both adapters satisfy their orders consumer-port interface (var _ assertions compile); honest branches unit-tested (ran); root.go ORDERS region additively wires both real readers + the bucket-store summary variant; no other region/module touched; live ERP+provider paths reported could-not-run (QA@P7); ONE commit.
OPEN_QUESTIONS: if any of internalReadSvc/internalReadAvailable/mercadoLivreCapabilities/installationSvc is NOT in scope at root.go:500, or wiring forces an orders/** module edit, STOP and report. Otherwise none.
