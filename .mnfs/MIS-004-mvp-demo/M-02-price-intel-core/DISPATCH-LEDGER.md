# DISPATCH LEDGER — M-02 price-intel-core (CHIP-M02)

Base SHA: `59d0e62fdbf15db068542432ef5d5731b6fa9f83` (main, MIS-004 wave-A accepted base)
Branch: `chip/m02-price-intel-core`
Worktree: `.claude/worktrees/chip-m02-price-intel-core`
gomodcache warmed: yes (exit 0)
codex precondition: GREEN (codex-cli 0.144.4, gpt-5.6-luna reachable, probe CODEX_OK). Finding: non-fatal `failed to renew cache TTL: missing field supports_reasoning_summaries` warning on every codex run — output still produced.

HUB_SESSION_ID: `local_c5532363-d407-49ff-9bac-120c328b9dcf` ("Harness Hub", operator-confirmed 2026-07-17).
Impl-pack: v1.0.0 (assembled at first implementer dispatch).

## Events sent
- REQUEST preflight-support (R4/R6) → hub — 2026-07-17 (live ML installation + dev-stack/MPC_ENCRYPTION_KEY for live-provider-read lane; non-blocking for hermetic build).
- REQUEST contract-amendment (Q1+Q4) → hub — 2026-07-18 (ledger D-10/D-11 answer). Chip-drafted per D-04 ("chip drafts shapes, hub commits"). Draft: evidence/ic06-ic03-amendment-draft.md. PART A = IC-06 remaining 4 ports' nested shapes (CatalogAttribute{ID,Name,ValueName*}; SearchCatalogByEAN→CatalogSearchResult envelope+FetchedAt; GetCatalogProduct→CatalogProduct+BuyBoxWinner{Price*,SellerID}; GetShipmentInfo→ShipmentInfo+ShipmentCosts{Gross/Receiver/Sender*}+DestinationUF*; GetFreeShippingCost→FreeShippingQuery{ItemID}+FreeShippingCost{Cost*,FetchedAt}). PART B = IC-03 F-04 JSON (collections envelope w/ enum-keyed snake_case contagens; Verdict w/ verdict_label ALWAYS null [Q3], inputs_used={input:{source,as_of}}, market_range mirrors aggregate no-max). 2 flagged AMBIGUOUS w/ recommendation (proceed pending veto per D-04): value_id OMIT; FreeShipping {ItemID}-only. 1 flag no-shape-change (buy_box item_id-vs-seller_id item-join at wire-up). Awaiting hub ACK to unblock F-01 remaining + F-03 + F-04.

## Hub ACKs received
- **D-15** (main f3570e14) — F-01-S3 SLICE-CLOSED @ `9eba9379`. MINOR-#1 `title` fallback APPROVED for D-10 live probe. LIVE-PROBE BATCH (one pass, hub confirms stack first): (1) `title` real wire field on /products/search + /products/{id} → drop dead Title DTO+fallback if absent; (2) A.3 buy_box item_id-vs-SellerID wire-up risk (D-12); (3) FreeShippingQuery A2 check. Chip live-lane-run REQUEST already sent — awaiting hub provisioning.
- **D-16** — F-03-S1 combined-command-timeout FALSE ALARM RATIFIED into profile §3 (split build/vet/test; combined-timeout w/ no stage output = re-run split before treating as defect). Core-candidate, upstreams at milestone boundary. Dispatcher handling confirmed correct.
- **M-01 CLOSED (QA PASS)** — IC-02 `GetCostAsOf` consumer-side cost port now BACKED BY MERGED MAIN CODE. F-04 verdict SEM_CUSTO path consumes this real port (was: "wired post-merge by hub"). Fold into F-04 planning.

| # | Role | Model/Effort | Path | Slice/Purpose | Prompt file | Log / last.md | Status | Verdict/Artifact |
|---|---|---|---|---|---|---|---|---|
| inv01 | Investigator | gpt-5.6-luna/medium | OS-process | Codebase seam map (ML adapter HTTP/auth, write path, ports idiom, cred resolver, market module, migrations+runner_test, root.go, telemetry, tenancy/decimal/time) | scratchpad/prompt-inv01.md | scratchpad/agent__inv01.{log,last.md} | completed | evidence/inv01-seam-map.md |
| plan01 | Feature planner (P2 batch) | gpt-5.6-sol/medium | OS-process | ONE batch plan all 4 features + shared seams: slice cards (schema), write-DAG, contract-satisfiability, additive-locks, per-criterion verification map C01-C07, dispatch order | scratchpad/prompt-plan01.md | scratchpad/agent__plan01.{log,last.md} | completed | evidence/p2-batch-plan.md — DISPATCH-READY:no (4 open questions) |
| inv02 | Investigator (contract-gap) | gpt-5.6-luna/medium | OS-process | Resolve-or-flag Q1 (IC-06 concrete Go shapes), Q2 (IC-01 resolver decision table), Q4 (IC-03 concrete JSON shapes); search existing ruling for Q3 (verdict threshold ownership M-02 vs M-07 CalcProfile) | scratchpad/prompt-inv02.md | scratchpad/agent__inv02.{log,last.md} | completed | evidence/inv02-contract-gaps.md — Q2 RESOLVED; Q1/Q3/Q4 UNDERSPECIFIED→escalated |

## inv02 verdicts (2026-07-17)
- **Q2 RESOLVED (no hub ruling):** IC-01:34-44 + handoff§5 = rule-deterministic gate; confidence = band ALTA|MEDIA|BAIXA (≥85 / 50-84 / <50), NOT numeric score. F-03-S1 fixtures assert decision+band. F-03-S1 own gap cleared (still DAG-blocked on F-01-S3 shapes[Q1] + F-02-S4).
- **Q1/Q3/Q4 UNDERSPECIFIED → ESCALATION sent to hub** (contract-freeze; not self-adjudicable). Q1 blocks F-01; Q3+Q4 block F-04. F-02 unblocked, running.

## F-02-S1 review (independent cold Claude sonnet, 2026-07-17) — CHANGES-REQUESTED
L0 verified by orchestrator in git-enabled env: build (`-buildvcs=false`) 0 · vet 0 · focused test ok. (Worker's `go build ./...` 128 = codex-sandbox-git false alarm — FINDING, bindings updated to `-buildvcs=false`; to report to hub for profile ratification.)
Reviewer findings + orchestrator adjudication:
- **F1 blocker** CompetitiveSignal.OurPrice nil allowed vs IC-03:26 non-nullable → **FIX** (require non-nil; flip test).
- **F2 major** MarketAggregate.Source/FetchedAt "invented" → **KEEP (override)**: mission "every price read carries source/fetched_at" (mission.md price-read rule); domain-internal, already-ruled. Reviewer lacked mission.md.
- **F3 major** n_sellers<5⇒INSUFFICIENT_MARKET not enforced → **DEFER to F-04-S1** (aggregator owns count→status per plan); add invariant+test to F-04-S1 card.
- **F4 major** ValidatedOffer enforces Condition=="new" not ACCEPT (inconsistent) → **FIX by REMOVAL**: BRL/new/ACCEPT filtering = F-04-S1 aggregator's job; entity is data-holder.
- **F5/F7 minor** Snapshot Price↔Status & FailureReason↔Status cross-invariants → **FIX** (VALID/EXPIRED require price; FAILED require nil price + reason).
- **F6/F8 minor** → moot after F4 removal.
Corrective slice F-02-S1-fix applied (sonnet fallback implementer): F1/F4/F5/F7 fixed, F2 kept, F3 deferred to F-04-S1. Re-verified L0 authoritative (build 0 / vet market 0 / full domain test ok). **COMMITTED `52b6d8c2`** (2 files, 601 ins). F-02-S1 CLOSED.
> Carry-forward for F-04-S1 card: enforce n_sellers<5 ⇒ INSUFFICIENT_MARKET in the aggregator (+ test) — deferred here per F3.
| F02-S1 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-02-S1: IC-01/IC-03 evidence domain entities + invariants (failing-test-first), no legacy obs/ref change. dispatchId `implementer-standard-20260717T225132Z-71ab7f40`; card dispatch/cards/F-02-S1.md; pack-body-sha 17b5a9ed; prompt-sha 9bd4d578 | scratchpad/prompt-F02-S1.md | scratchpad/agent__implementer-standard-20260717T225132Z-71ab7f40.{log,last.md} | CLOSED | commit `52b6d8c2` (reviewed+fixed+L0 green) |
| F02-S2 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-02-S2: migrations 0050–0053 (5 tables) + runner_test 41→45 + schema integration test; 0054 reserved-unused. dispatchId `implementer-complex-20260717T232324Z-94f215f0`; card dispatch/cards/F-02-S2.md. Integration lane (pg:up+harness:integration) = ORCHESTRATOR-run, not sandbox | scratchpad/prompt-F02-S2.md | scratchpad/agent__implementer-complex-20260717T232324Z-94f215f0.{log,last.md} | CLOSED | commit `93b935d9` (reviewed+PK-fixed+L0 green + evidence-schema L1 green; lane red = pre-existing, see disposition) |

## F-02-S2 adjudication (2026-07-17) — card defect, worker STOP correct
Worker reported BLOCKED: card said "change exactly one integer" in runner_test.go, but the file has TWO `41` count assertions — `TestCanonicalSourceListsEveryMigrationByFullFilename:25` (bumped by worker to 45) AND `TestCanonicalSourceDoesNotDependOnCallerWorkingDirectory:64-65` (`want 41`). Both count the SAME canonical migration set (now 45 files). **Card under-specified** the fixture as single-integer. Worker's STOP-not-guess = correct anti-slop. Orchestrator adjudication: both refs → 45. Applied via cavecrew-builder (surgical 1-file, agentId ac7d3b97620a1b658): runner_test.go:64 `!= 41`→`!= 45`, :65 msg `want 41`→`want 45`. Orchestrator L0 (git-enabled worktree, task beuq03b0a): `go test ./internal/platform/migrate` **ok 3.481s** · `go build -buildvcs=false ./...` **0** · `go vet market+migrate` **0**. FINDING (report to hub): impl-pack migration cards must say "bump EVERY canonical-migration-count assertion", not "one integer". PG L1 lane (task b6vkj3hhj) + independent review (agent a2d949eef36c5fadc) done — see below.

### F-02-S2 independent review (cold sonnet a2d949eef36c5fadc, 2026-07-17) — CHANGES-REQUESTED → fixed
1 blocker, rest clean (columns/tenancy/ADR-17-nullability/money-type/enums-fidelity/hygiene/test-coverage all PASS vs evidence.go + 0043/0044).
- **BLOCKER (valid):** `0050_market_price_snapshots.sql:14` PK `(tenant_id, ref_id, fetched_at)` is a strict subset of the idempotency UNIQUE `(tenant_id, scope, ref_id, source, fetched_at)` → narrow PK rejects legit multi-source/scope rows at same fetched_at (ml_sale_price vs ml_price_to_win same listing/batch). Worker mirrored 0044's 3-col PK idiom; snapshot identity needs scope+source. Finding 2 = redundant UNIQUE (consequence).
- **Adjudication: FIX.** PK widened to the full idempotency tuple `(tenant_id, scope, ref_id, source, fetched_at)`; redundant separate UNIQUE dropped; integration test updated (drop `_idempotency_key` name) + STRENGTHENED with `assertPrimaryKeyColumns` (pg_index composition check — closes the gap that let the bug through: prior test only checked `_pkey` existence). Applied by sonnet fixer a7ba378ec5c7355b1 (2 files, verified single clean helper). No surrogate id / FK dependents (repo code = F-02-S3, not yet written) → composite natural PK safe.
- **PG L1 rerun + smoke:** first integration run (bm83imk6q) migrations_first=45/second=0 (schema+idempotency clean) BUT `TestPhase1SmokeFlow` (marketplaces+pricing modules — untouched by ADD-only market migrations) failed exit 1. Suspected pre-existing/flake; go-test full output not persisted by harness (only failure tokens). Re-run gated on F-01-S1p-v2 codex settling (integration compiles whole tree). F-02-S2 commit gated on clean PK-fixed integration rerun + smoke disposition.

### F-02-S2 integration-lane disposition (2026-07-17) — COMMITTED 93b935d9
PK-fixed rerun (b3op1jxkg) migrations_first=45/second=0 (clean+idempotent) but TWO failure tokens: `TestListingsReadContractEndToEnd` (NEW) + `TestPhase1SmokeFlow`. Harness persists only failure tokens, so orchestrator **reproduced both directly** against the session container (mpc-pg-session-3e45caca) via a throwaway `mpc_test_<32hex>` DB (regex-gated by internal/testsupport/postgres/target.go:19), `go run ./cmd/testdb migrate` (applied 45) + `go test -tags=integration ./tests/integration -run '…' -v`:
- **`TestListingsReadContractEndToEnd` → PASS (all 8 subtests) in isolation** = FLAKE / full-lane ordering-or-stale-state artifact, not a real failure.
- **`TestPhase1SmokeFlow` → FAIL: `run simulation error: PRICING_INVALID_PRODUCT_ID`.** Root cause: `pricing/adapters/postgres/repository.go:84 positiveProductID` requires a **positive-integer** product id (`strconv.Atoi`), but `phase1_smoke_test.go:65` passes `"smoke-prod-1"` (non-integer). Integer enforcement landed in `5da9e774 fix(catalog): enforce canonical identity` (2026-07-13); smoke fixture in `9edd3717`. **Both are ancestors of base 59d0e62f** (`git merge-base --is-ancestor` = YES for both) → deterministic **PRE-EXISTING** break on master. Structurally impossible for M-02's ADD-only market migrations / connectors read ports to cause a `strconv.Atoi("smoke-prod-1")` failure.
- **Disposition:** F-02-S2 own validation is GREEN — migrate unit 45, migrations apply+idempotent (45/0), and the F-02-S2 hermetic **`evidence_schema_integration_test.go` is included in the lane** (integration-tagged, per Get-HarnessIntegrationTestPackages) and is **NOT in the failure tokens = it PASSED** against the widened PK. Committed. Throwaway repro DB dropped (clean).
- Hub event sent (2026-07-17): slice closes (c87fb193, 93b935d9) + root-caused integration finding + fix suggestion (fix phase1_smoke_test.go:65 ProductID to a positive integer, OR allowlist ONLY TestPhase1SmokeFlow not the whole package). Corroborates the hub's in-flight F1/F2 pre-existing-break ratification.

## F-02-S3 dispatch (2026-07-17) — snapshot + validated-offer repositories
Card authored dispatch/cards/F-02-S3.md (open_questions=[]); prompt scratchpad/prompt-F02-S3.md (impl-pack v1.0.0 preamble body-sha 17b5a9ed + slice body). Binds: idempotent append ON CONFLICT (tenant,scope,ref_id,source,fetched_at) DO NOTHING; latest-valid filters status=VALID; expiry PROJECTED (no update/delete); FAILED never overrides VALID; tenant-first; NULL price → nil (ADR-17). Worker writes both //go:build integration tests (contract-first); ORCHESTRATOR runs the PG lane after.
| F02-S3 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-02-S3: evidence_store port + snapshot_repository + offer_repository (+ integration tests). dispatchId `implementer-complex-f02s3-a1`; task bbveqlibb; card dispatch/cards/F-02-S3.md | scratchpad/prompt-F02-S3.md | scratchpad/agent__implementer-complex-f02s3-a1.{log,last.md} | CLOSED | commit `e4bc880c`; reviewer aa646b2ddd8240103 APPROVE (no blockers/majors); 3 minor coverage gaps closed (offer dup no-op, FAILED read-path fields, direct latest-valid expiry); full market/adapters/postgres integration 15/15 green |

### F-02-S3 review + coverage adjudication (2026-07-18) — APPROVE
Cold sonnet reviewer aa646b2ddd8240103: APPROVE, 0 blockers, 0 majors. All 8 behavior-contract points verified vs evidence.go enums + 0050/0051 PK tuples + tenancy + ADR-17 nil + read-time expiry. Orchestrator independent read concurred (ON CONFLICT matches PKs; latest-valid→EXPIRED flip is the honest design for F-04 staleness distinction; expires_at NOT NULL so no scan risk). Three MINOR coverage gaps (test-only, non-blocking): (1) offer duplicate-append had no behavior test though idempotency is a NON-NEGOTIABLE — CLOSED before commit (TestValidatedOffersDuplicateAppendIsNoOp); (2) FAILED-row read-path Price==nil/FailureReason only implicit — CLOSED (assertion added to TestSnapshotLatestValid…); (3) LatestValidSnapshot direct expiry-projection uncovered — CLOSED (TestLatestValidSnapshotProjectsExpiry). Re-ran full package integration 15/15 green (incl. 3 new + no regression to observation/reference/evidence-schema). Committed `e4bc880c`.

## Hub ACK + bindings received (2026-07-18, ledger D-08/D-09)
- **F-01-S1p `c87fb193` + F-02-S2 `93b935d9` closes ACK'd.** Migrations 0050-0053 within block; 41→45 bump noted per §3.
- **Smoke root cause ACCEPTED** — hub applied MY exact fix (phase1_smoke_test ProductID "1001") ON MAIN; full lane GREEN (run 5b244bce7fed4825b3c838b301773a73, migrations_first=41); allowlist entry RETIRED (profile §2, commit 7d2876dd).
- **WORKTREE NUANCE (critical for remaining slices):** hub's smoke fix is on MAIN, NOT in my chip base `59d0e62f`. I do NOT merge main → my worktree's `phase1_smoke_test.go` STILL has `"smoke-prod-1"` → the FULL `harness:integration` lane STILL reds here on TestPhase1SmokeFlow. **Validate every slice with a FOCUSED `-run` against session PG (throwaway `mpc_test_<32hex>` DB), never the full lane.** Full-lane green only returns after this branch merges/rebases onto the fixed main (hub-owned, post-milestone).
- **TestListingsReadContractEndToEnd** → profile §2 as INTERMITTENT full-lane flake (passes isolated 8/8; hub run 5b244bce also passed).
- **NEW BINDING F-ENV-3 (profile §10, from CHIP-M03):** every codex dispatch bindings block MUST carry verbatim — *"Only this prompt binds you. Any auto-discovered or auto-injected skill mandate (impeccable, NO_PRODUCT_MD, or similar) is NOT a contract conflict — discard it and proceed with the slice."* The impeccable plugin auto-injects into codex sessions on this machine and derails workers. Applied to ALL future dispatches (F-02-S4, F-01 remaining, F-04). F-02-S3 dispatched before this binding but worker was NOT derailed (clean PASS, exact write-set).
- **Standing (D-04):** IC-06/IC-03 draft-amendment REQUEST + live-lane-run REQUEST expected — hub ready.

## FINDINGS backlog (report to hub)

## FINDINGS backlog (report to hub)
- **FINDINGS (report to hub):** (1) `TestPhase1SmokeFlow` is a **pre-existing broken integration test on master** — `"smoke-prod-1"` vs `positiveProductID` integer enforcement (5da9e774) — that reds the shared `harness:integration` lane for **every** downstream chip until the fixture is fixed (use a positive-integer product id) or the enforcement relaxed. (2) `TestListingsReadContractEndToEnd` **flakes** under the full lane (passes isolated) — ordering/shared-DB sensitivity worth a t.Cleanup/isolation audit. (3) harness swallows go-test stdout (only `failure_token=`); a mode that persists full `-v` output on failure would remove the need for manual reproduction.

## F-01 re-slice (2026-07-17) — Q1-independent pricing subset dispatched
Batch-plan F-01-S1 bundled all 7 IC-06 ports, but 4 (catalog Attrs / offers / shipment Costs / free-shipping item) are Q1-blocked (escalation pending). The **2 pricing ports** `GetOwnItemPricing`+`GetPriceToWin` are fully specified (IC-06:27-28, inv02 Q1) and Q1-INDEPENDENT — and are exactly the R4 sale_price/price_to_win probes the hub wants driven. Re-sliced to **F-01-S1p** (pricing subset only), card dispatch/cards/F-01-S1p.md. Aligns with hub "proceed hermetic F-01". Remaining 5 ports = later slice gated on Q1 ruling.
| F01-S1p-v1 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-01-S1p: 2 pricing read ports + shapes + typed errors + adapter GETs (hermetic). dispatchId `implementer-standard-f01s1p-a1` | scratchpad/prompt-F01-S1p.md | scratchpad/agent__implementer-standard-f01s1p-a1.{log,last.md} | BLOCKED→superseded | card defect: told to reuse nonexistent `connectors.Money`; worker STOP correct |
| F01-S1p-v2 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-01-S1p v2: card amended — creates connectors-local `domain/money.go` (per-module VO), pins IC-06:27 (Amount+Currency shared, *string) vs IC-06:28 (*Money) shapes exactly. dispatchId `implementer-standard-f01s1p-a2`; task bl60ra2yg | scratchpad/prompt-F01-S1p-v2.md | scratchpad/agent__implementer-standard-f01s1p-a2.{log,last.md} | CLOSED | commit `c87fb193` (reviewed APPROVE + L0 green; 9 files, 460 ins) |

## F-01-S1p-v1 adjudication (2026-07-17) — card defect, worker STOP correct
v1 card said "reuse the existing connectors `Money` type" + "do NOT invent a new money type". But `connectors/domain.Money` does NOT exist — only `market.Money` (market.go:13) and `listings.Money`. Money is a PER-MODULE VO; importing market's across the boundary would couple modules. Worker STOP (won't import cross-module, won't invent against card) = correct. Adjudication: the connectors module OWNS its normalized money VO — v2 card authorizes `connectors/domain/money.go` mirroring market.go:13/268/270 (currency non-empty, NOT hard-BRL, since the adapter normalizes provider currency; BRL enforcement is downstream market-domain). Shapes pinned exactly from IC-06:27/:28 to remove all ambiguity. FINDING (report to hub): connectors module had no Money VO; IC-06 pricing normalization requires one.

## Plan triage (2026-07-17, post-plan01)
Plan DISPATCH-READY: **no**. Blockers = 4 NEEDS-INVESTIGATION questions:
- **Q1** F-01-S1: IC-06 concrete Go shapes (Attrs / shipment Costs / free-shipping item / SearchCatalogByEAN FetchedAt placement) → inv02 (resolve from IC-06 normalized table + research handoff ML payloads).
- **Q2** F-03-S1: resolver decision table (anchor weights / score formula / normalization vocab / tie-break / hard-negative comparison) → inv02 (verify IC-01 = rule-deterministic gate, confidence=band not free score; else escalate).
- **Q3** F-04-S2: verdict threshold policy (fee/freight/tax + margin→label table) — **cross-milestone (M-07 CalcProfile owns margin bands verde≥18/âmbar≥10)**. Do NOT self-adjudicate. inv02 searches mission/ADR-06/P5 for existing ruling; if absent → ESCALATION to hub.
- **Q4** F-04-S7: concrete JSON for collection decisões/contagens/causas + Verdict inputs_used + SEM_CUSTO range → inv02 (map IC-03 entities → JSON; note downstream freeze binds M-05/M-06).

Dispatch decision: **F-02 is fully clear** (all cards open_questions=[]), disjoint module → started F-02-S1 now in parallel with inv02. F-01/F-03/F-04 gated until inv02 (+ any Q3 escalation) resolves.

## F-02-S4 dispatch (2026-07-18) — match-decision + competitive-signal + aggregate repositories (final F-02 slice)
Card dispatch/cards/F-02-S4.md (open_questions=[]); prompt scratchpad/prompt-F02-S4.md (impl-pack v1.0.0 preamble body-sha 17b5a9ed + F-ENV-3 binding + slice body). Serially gated behind F-02-S3 (shared `Repository` state) — now unblocked (S3 CLOSED `e4bc880c`). Binds: NEW port file `ports/decision_store.go` (MatchDecisionStore/CompetitiveSignalStore/MarketAggregateStore) — must NOT edit S3's `evidence_store.go`. PK/ON-CONFLICT tuples from DDL: match_decisions `(tenant_id,product_id,decided_at)`; competitive_signals `(tenant_id,listing_id,fetched_at)`; aggregates `(tenant_id,product_id,source,computed_at)`. Split `_amount`/`_currency` money columns (signals+aggregates). Idempotent append DO NOTHING; ordered batch reads preserve input-key order + OMIT missing (no synthesis); anchors/contradictions TEXT[] exact round-trip; median/min_valid/winner/target/position/catalog_product_id nullable → nil (ADR-17); NO status derivation (n_sellers<5⇒INSUFFICIENT_MARKET stays F-04-S1). Worker writes 3 //go:build integration tests contract-first; ORCHESTRATOR runs FOCUSED PG lane `-run Test(MatchDecision|CompetitiveSignal|MarketAggregate)` after (full lane still reds on pre-existing smoke in this base).
| F02-S4 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-02-S4: decision_store port + match_decision_repository + competitive_signal_repository + aggregate_repository (+3 integration tests). dispatchId `implementer-complex-f02s4-a1`; card dispatch/cards/F-02-S4.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F02-S4.md | scratchpad/agent__implementer-complex-f02s4-a1.{log,last.md} | CLOSED | commit `df3082c3`; reviewer a743fd8243f283dac APPROVE (no blockers/majors); 2 minor closed (nil TEXT[] -> empty guard; populated-nullable round-trip tests); focused PG lane 3/3 then full market/adapters/postgres 21/21 green |

### F-02-S4 review + adjudication (2026-07-18) — APPROVE
Cold sonnet reviewer a743fd8243f283dac: APPROVE, no blockers/majors. 3 MINOR:
- **[CLOSED] nil TEXT[] integrity** — `AppendMatchDecision` passed `Anchors`/`Contradictions` straight through; `anchors`/`contradictions` are `TEXT[] NOT NULL` and `NewMatchDecision` (evidence.go:256) does NOT default nil->[]. A NO_CANDIDATE decision with nil slices would hit hard `23502` at write (fails loud, not corruption — latent, no caller yet). Fixed at repo write boundary (evidence.go is S1-committed, not S4 write_set): nil -> `[]string{}`. New test `TestMatchDecisionEmptyArraysAndCatalogProductRoundTrip` asserts nil anchors round-trip as non-nil empty + populated CatalogProductID.
- **[CLOSED] nullable coverage gap** — competitive_signal + aggregate tests only exercised the NULL path for nullable columns; a column-order swap (winner/target, median/min_valid) would go undetected. Added `TestCompetitiveSignalPopulatedNullableRoundTrip` (distinct winner=140.5500/target=139.9900/position rank3total8) + `TestMarketAggregatePopulatedPricesRoundTrip` (distinct median=88.8800/min_valid=72.3300) — distinct per-column values now catch any INSERT/SELECT/Scan reorder.
- **[LEFT, flag for F-04] DISTINCT-ON source tie-break** — `LatestMarketAggregates` collapses all `source` rows to latest-per-product with `ORDER BY product_id, computed_at DESC, source` (deterministic alphabetical tie-break, NOT arbitrary). Port is product-keyed per card design (open_questions=[]). Not a contract breach. NOTE for F-04 planning: if the collection/verdict consumer needs per-source aggregates (not just latest-per-product), the port needs a source-keyed variant — decide at F-04-S1.

**F-02 FEATURE COMPLETE.** Chip branch: `52b6d8c2`(S1) -> `93b935d9`(S2) -> `e4bc880c`(S3) -> `df3082c3`(S4), all base `59d0e62f`. (F-01-S1p `c87fb193` interleaved.) No merge, no push.

## F-01-S2 dispatch (2026-07-18) — ListCatalogOffers read port (Q1-INDEPENDENT carve-out)
inv02 Q1: IC-06 fixes all 7 ports' top-level members; only 4 NESTED shapes underspecified (Attrs / shipment Costs / free-shipping item / SearchCatalogByEAN FetchedAt) → hub-escalated. `ListCatalogOffers` (IC-06:31) result is FULLY enumerated `[]{SellerID,Price,Condition,ShippingMode}` — no Attrs/Costs/placeholder — so dispatchable NOW, independent of Q1 (same basis as S1p pricing carve-out; hub standing "proceed hermetic F-01"). Other 4 ports remain gated on hub.
Card dispatch/cards/F-01-S2.md (open_questions=[]); prompt scratchpad/prompt-F01-S2.md (impl-pack 17b5a9ed + F-ENV-3). Binds: flag-gate FIRST (CatalogOffersEnabled bool config field, zero-value=false=OFF ⇒ ErrCatalogOffersUnavailable, no network); COMPLETE pagination or typed error (NEVER partial/silent truncation); provider order preserved (dedupe=consumer per IC-06:31); Price *Money nil-if-absent (mirror PriceToWin.CurrentPrice); reuse providerMoney + mapPricingReaderError; slog.Default() telemetry (route/status/page_count/offer_count/duration_ms, no metrics dep, no new logger field); DTOs die at adapter; GET-only. Hermetic (httptest + stub token resolver) — NO PG/docker/live-provider. Edits capability_adapter.go additively (own F-01 feature file); does NOT touch composition/root.go (live flag wiring deferred, hub-gated) nor market_read.go/pricing_reader.go/market_read_errors.go.
| F01-S2 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-01-S2: CatalogOffersReader port + CatalogOffer shape + ErrCatalogOffersUnavailable + catalog_offers_reader (pagination+flag+telemetry) + hermetic httptest + additive adapter wiring. dispatchId `implementer-complex-f01s2-a1`; card dispatch/cards/F-01-S2.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F01-S2.md | scratchpad/agent__implementer-complex-f01s2-a1.{log,last.md} | CLOSED | commit `d5c14c43`; reviewer a49753227c5199dfc APPROVE (no blockers/majors); 5 files exact write-set; hermetic connectors lane green (build 0 / vet 0 / ML adapter 2.683s) |

### F-01-S2 review + adjudication (2026-07-18) — APPROVE
Cold sonnet reviewer a49753227c5199dfc: APPROVE, no blockers/majors. 2 MINOR:
- **[CLOSED] empty-page-mid-pagination coverage** — pagination guard `nextOffset <= offset || len(Results)==0` handles a provider that lies (Total>accumulated but returns a genuinely empty non-error page), but no test pinned it. Added `TestListCatalogOffersEmptyPageBeforeTotalReturnsNoPartialResults` (offset=0 yields 1 offer, offset=1 empty with Total=2) → asserts `errors.Is(err, ErrCatalogOffersUnavailable)` + offers nil (fail-closed, never partial). Green.
- **[LEFT, consistency] malformed-price returns raw validation error** — `listCatalogOffers` returns `providerMoney` errors raw (not wrapped as ErrCatalogOffersUnavailable). Deliberate: mirrors sibling `pricing_reader.go` convention exactly; diverging would break codebase consistency; still fails-closed (nil, not partial). Not a contract breach.

**F-01-S2 CLOSED `d5c14c43`.** Chip branch: `52b6d8c2`(F-02-S1) -> `c87fb193`(F-01-S1p) -> `93b935d9`(F-02-S2) -> `e4bc880c`(F-02-S3) -> `df3082c3`(F-02-S4) -> `d5c14c43`(F-01-S2), all base `59d0e62f`. No merge, no push. Remaining F-01 ports (SearchCatalogByEAN / GetCatalogProduct / GetShipmentInfo / GetFreeShippingCost nested shapes) + F-03 + F-04 gated on hub Q1/Q3/Q4 rulings.

## Hub RATIFIED amendment + Q3 (2026-07-18, ledger D-12, main `cc6550f4`)
- **IC-06 + IC-03 amendment A1 committed to main `cc6550f4`.** All D-04 principles verified honored. F-01 remaining 4 ports + F-03 + F-04 UNBLOCKED. Worktree IC files won't carry A1 (committed post-base); chip proceeds from evidence/ic06-ic03-amendment-draft.md = ratified text verbatim; reconcile at merge = hub's problem.
- **3 flags — all chip recommendations APPROVED:** (1) CatalogAttribute value_id OMIT — approved (re-add = explicit amendment). (2) FreeShippingQuery minimal {ItemID} — approved w/ DATED DEFERRAL: if live-lane probe shows route needs explicit dims/price → amendment A2 at F-01 probe time. (3) contagens keys lower snake_case — approved. A.3 buy_box item_id-vs-SellerID = accepted recorded risk, frozen shape kept; live-probe-unobtainable → A2 path.
- **Q3 CONFIRMED:** M-02 emits market-evidence verdict ONLY (match_status + blocking_state + price targets); NO margin verdict_label from M-02 in MIS-004 (verde/âmbar M-07-owned via IC-04). SEM_CUSTO via consumer-side cost port mirroring IC-02 GetCostAsOf, hub-wired at composition root post-merge. Encode in F-04.
- **Live-lane-run pre-approval STANDS (D-10):** read-only GETs, zero ML writes (dispatcher OFF); fire REQUEST when F-01 ports probe-ready.
- B.3: GET /market/aggregates stays product-keyed; source-keyed = F-04-S1 internal decision, no contract impact.

## F-01 remaining slicing (2026-07-18) — S3 (catalog-identity) then S4 (shipping/cost)
Carved the 4 unblocked ports into 2 slices by consumer + shared-seam constraint. Both S3+S4 edit `capability_adapter.go` (public wrappers) + `ports/catalog_read.go` + `domain/catalog_read.go` additively ⇒ SHARED SEAM ⇒ must SERIALIZE (one writer per seam). Order:
- **S3 = SearchCatalogByEAN + GetCatalogProduct** (catalog-identity; feeds F-03 resolver) — dispatched now to unblock F-03 fastest.
- **S4 = GetShipmentInfo + GetFreeShippingCost** (shipping/cost; feeds M-08/DIFAL) — after S3 commits.
Post-S3 parallel opportunity: **F-03 (market module) ∥ F-01-S4 (connectors)** — disjoint files, safe to run concurrently once S3 lands.
| F01-S3 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-01-S3: CatalogReader port (SearchCatalogByEAN+GetCatalogProduct) + CatalogSearchResult/CatalogCandidate/CatalogProduct/BuyBoxWinner/CatalogAttribute shapes (ratified A.1-A.3) + catalog_identity_reader + hermetic httptest + additive adapter wrappers. dispatchId `implementer-standard-f01s3-a1`; task bcrhtfnuw; card dispatch/cards/F-01-S3.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F01-S3.md | scratchpad/agent__implementer-standard-f01s3-a1.{log,last.md} | CLOSED | commit `9eba9379`; reviewer af6273d7e0af02611 APPROVE (no blockers/majors); 5 files exact write-set; hermetic connectors lane green (build/vet/test 0, ML adapter 3.070s) |

### F-01-S3 review + adjudication (2026-07-18) — APPROVE
Cold sonnet reviewer af6273d7e0af02611: APPROVE, no blockers/majors. Shapes bind byte-for-byte to ratified amendment A.1-A.3 (value_id omitted, FetchedAt on envelope); ADR-17 nil-handling verified (buy_box null->nil, value_name null/absent->nil, zero-results empty-non-nil no-error); providerMoney/mapPricingReaderError reused; wrappers mirror GetPriceToWin; additive-only adapter edit (diff-verified); GET-only, no forbidden route, no dep/composition change; test anti-reorder pin is real (candidate 2 before 1). 2 MINOR:
- **[LEFT — live-probe item + hub FINDING] Title fallback `firstNonEmpty(result.Name, result.Title)`** (catalog_identity_reader.go:49,77) — DTO carries a `title` fallback field unverified against real ML `/products/*` payload (docs prove consumed domain member, not raw wire field). Defensively safe (never drops a populated title), but the `Title` DTO field is dead if ML only ever returns `name`. Fold into the pre-approved live-lane probe: confirm `/products/search` + `/products/{id}` field name; drop the dead fallback if `title` never appears. Reported to hub as a live-probe verification item.
- **[CLOSED] GetCatalogProduct error path untested in isolation** — only SearchCatalogByEAN exercised the shared mapPricingReaderError path. Extended TestCatalogIdentityReaderMapsProviderErrors to assert BOTH ports through the mapper (401/404/429/500). Re-ran 4/4 subtests green.

**F-01-S3 CLOSED `9eba9379`.** Chip branch tip. F-03 (identity resolver) now UNBLOCKED — CatalogCandidate/CatalogAttribute shapes committed. Next: F-03-S1 (market) ∥ F-01-S4 (connectors shipping) — disjoint modules, parallel-safe.

### Fan-out — F-01-S4 ∥ F-03-S1 (parallel, disjoint modules) — DISPATCHED 2026-07-18
Parallel-safe: F-01-S4 writes `connectors/**` (incl. shared `capability_adapter.go`), F-03-S1 writes `market/domain/**` only — no shared seam. Both bind ratified artifacts (IC-06 amendment A.4/A.5; IC-01 gate). Independent cold sonnet reviewer per slice before any dependent work.

| Slice | Role | Model / effort | Path | Payload | Prompt | Output artifacts | Status | Close |
|---|---|---|---|---|---|---|---|---|
| F01-S4 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-01-S4: ShipmentReader port (GetShipmentInfo two-GET + GetFreeShippingCost) + ShipmentInfo/ShipmentCosts/FreeShippingQuery/FreeShippingCost shapes (ratified A.4/A.5) + shipping_reader + hermetic httptest + additive adapter wrappers. Chip design decisions D-S4-a..d (wire-field guesses = live-probe items; costs-404→nil; UF strip; free-ship route uses ProviderAccountID). dispatchId `implementer-standard-f01s4-a1`; task bjg952e5l; card dispatch/cards/F-01-S4.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F01-S4.md | scratchpad/agent__implementer-standard-f01s4-a1.{log,last.md} | CLOSED | commit `0f51a84e`; reviewer a54d21 APPROVE; +2 MINOR fixups applied (see below) |
| F03-S1 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-03-S1: PURE deterministic identity gate `ResolveIdentity` (market/domain) + input views (LocalProductIdentity/IdentityCandidate/IdentityCandidateAttribute) + ≥12-case truth-table + Doka/Menegotti + Doka/VW collision fixtures + determinism transcript. Binds IC-01 gate; uses NewMatchDecision. Chip design decisions D-F3-a..d (market-owned input views no connectors import; EAN mandatory for ACCEPT; score weights 50/20/20/≤10; hard-negative lexicon). NO store/composition (later slice). dispatchId `implementer-complex-f03s1-a1`; task buejpip24; card dispatch/cards/F-03-S1.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F03-S1.md | scratchpad/agent__implementer-complex-f03s1-a1.{log,last.md} | CLOSED | commit `5eb74007`; worker self-reported BLOCKED = FALSE ALARM (combined-cmd timeout; dispatcher re-ran split = build/vet/test 0); reviewer ae3699 REQUEST-CHANGES → 1 MAJOR + 1 MINOR fixed (see below) |

### F-01-S4 review + adjudication (2026-07-18) — APPROVE + 2 MINOR fixed
Cold sonnet reviewer a54d21: APPROVE. Traced the load-bearing costs-404→nil end-to-end: `doJSON` maps HTTP 404 → `domain.ErrCodeProviderInvalidReference`; `shipping_reader.go:77` checks exactly that code before treating costs absent; primary-endpoint errors bypass (checked before the costs call) so primary-404 and costs-404 cannot be confused. Shapes byte-exact to amendment A.4/A.5; ADR-17 pointer-nil verified; `providerMoney`/`decimalString`/`parseTimePtr`/`ErrorCodeOf` reused (pre-existing, not invented); `capability_adapter.go` diff strictly additive; DTOs unexported; GET-only.
- **[FIXED] MINOR-#1:** "any other non-2xx on /costs ⇒ typed error" branch was untested. Added `TestGetShipmentInfoCostsServerErrorFails` (500 on /costs + 200 primary → ErrProviderUnavailable + zero-value ShipmentInfo).
- **[FIXED] MINOR-#2:** absent-optionals test didn't assert the present `GrossAmount`. Added the `12.00 BRL` assertion.

### F-03-S1 review + adjudication (2026-07-18) — REQUEST-CHANGES → resolved
Cold sonnet reviewer ae3699 hand-traced every truth-table row (scores/bands), collision fixtures, purity, boundary. Findings:
- **[FIXED] MAJOR-#1 (anti-slop dead code):** the `EAN==""` && `status==ACCEPT` cap block (identity_resolver.go:65-67) was structurally unreachable — ACCEPT requires the `ean` anchor which requires a present, agreeing local EAN, so the left conjunct can never be true when the right is. The D-F3-b cap is fully enforced by the per-candidate gate. Deleted the vestigial block; replaced with a 2-line invariant comment.
- **[FIXED] MINOR-#2:** `selectIdentityCandidate` tie-break clause `candidate.index < chosen.index` was tautologically false (forward iteration + strict `>` already yields lowest-index-wins). Simplified to `score > chosen.score` + comment.
- **[NO CHANGE — informational] MINOR-#3:** card D-F3-b vs IC-01:36 literal conflict (IC-01 lists `refforn+marca` as an ACCEPT pair; card requires EAN mandatorily). Card wins (explicitly flagged to hub as D-F3-b, pending veto). Impl correctly follows the card. Already in the hub design-decision flag.
- **FALSE ALARM (profile §3 candidate, reported to hub):** worker's BLOCKED was a combined build+vet+test command hitting the 120s codex timeout (cold `go build ./...` alone ~65s). Dispatcher re-ran split → build/vet/test all 0. Not a defect.

**F-01 COMPLETE** (S1p+S2+S3+S4 = all IC-06 read ports). **F-03-S1 CLOSED** (`5eb74007`) — deterministic gate core; store/composition wiring deferred to F-04 collector. Chip tip = `5eb74007`.

### Hub ruling D-19 (2026-07-18, main be72ab70) — live-probe DEFERRED to F-04 transport
Probe option (b) binding: the 3 wire-field items (title-vs-name, free-ship route, costs-404/senders[] arity) ride on `POST /market/collections`. A real codprod exercises catalog+shipment+free-shipping live through the composed connectors; normalized output + persisted IC-03 evidence rows = validation of record. Chip's cred-extraction refusal ratified correct (hub option (a) in-container curls ALSO blocked by security posture — installation cred encrypted per-installation, not free env; (c) debug route rejected). Follow-ups if live evidence contradicts a guess: keep firstNonEmpty title fallback (do NOT drop Title DTO on guess); free-ship/A.2/A.3 contradictions ⇒ amendment per D-12 deferral. NOT an F-04 blocker.

## F-04 slicing (2026-07-18) — collect + verdict API, the milestone closer
**Architecture finding (load-bearing):** `connectors` ALREADY imports `market` (fee_sync/fee_seed, 4 files) ⇒ market→connectors would be a Go build CYCLE. Therefore the F-04 collector adapter (connectors read-ports→market domain) CANNOT live in market — it is composition/hub-owned (like the cost adapter). Chip ships market-DOMAIN-only ports + orchestration + fakes; hub wires 3 adapters at root.go post-merge: `CostReader` (IC-02 GetCostAsOf, int codprod), `PriceIntelCollector` (over marketplaceCapabilities), `ProductIdentityReader` (codprod→LocalProductIdentity, over products/ERP). Reported to hub 2026-07-18.

**SERIAL DAG** (all 5 slices share the `market` package ⇒ concurrent codex workers race on whole-tree `go build ./...`; F-01-S4∥F-03-S1 were safe ONLY because they were different modules). Verdict is computed-on-read (no verdict store; POST persists evidence, GET assembles) per IC-03 "consumidor refaz GET após resposta".
- **S1** — PURE domain: `ComputeMarketAggregate` + `AssembleVerdict` + `Verdict` type. Dispatched.
- **S2** — cost port (interface only) + source-keyed aggregate read (additive `MarketAggregateStore.LatestMarketAggregatesBySource` + postgres impl + integration test). B.3.
- **S3** — collect orchestration: POST `/market/collections` pipeline + 3 ports (interface+fake) + 409 in-process guard + ADR-17 PARTIAL summary.
- **S4** — read services (verdicts/aggregates/signals) + exported `market.EvidenceReader` facade (frozen at F close).
- **S5** — transport (POST collections + GET signals/aggregates/verdicts) + additive OpenAPI `/market/*` + NEW `packages/sdk-runtime/src/market.ts` (same commit; NEVER the barrel `index.ts`).

**Design decisions flagged to hub (proceed pending veto):** D-F4-a n_offers=qualifying-pre-dedupe / n_sellers=distinct-post-dedupe; D-F4-b even median=(a+b)/2 half-up 2dp; D-F4-c Verdict serialization-agnostic (wire JSON in S5); D-F4-d cost result=(*Money,asOf,error) tuple; D-F4-e cost key=int codprod (IC-02 parity); D-F4-f source-keyed method on existing MarketAggregateStore; D-F4-g snapshot-per-source policy (listing sources⇒snapshots; catalog-offers evidence=MarketAggregate; provider fail⇒FAILED snapshot for listing sources / aggregate NO_PRICE_EVIDENCE for offers).

| Slice | Role | Model / effort | Path | Payload | Prompt | Output artifacts | Status | Close |
|---|---|---|---|---|---|---|---|---|
| F04-S1 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-04-S1: PURE `ComputeMarketAggregate` + `AssembleVerdict` + `Verdict`/`VerdictInput`/`VerdictMarketRange`/`VerdictInputRef`/`SnapshotRef`/`CostRef` types. Binds IC-03 §Resources + amendment B.2. Chip decisions D-F4-a/b/c. NO I/O. dispatchId `implementer-complex-f04s1-a1`; task b6ff6y8lr; card dispatch/cards/F-04-S1.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F04-S1.md | scratchpad/agent__F04-S1-a1.{log,last.md} | CLOSED | commit `0d16f7cf`; worker PASS; dispatcher re-ran split lanes = build/vet/test 0; reviewer a38f30 OVERALL PASS (zero correctness defects) → 4 coverage fixups added (see below) |

### Hub ruling D-21 (2026-07-18, main 27008446) — F-04 DAG + cycle finding ACCEPTED
Import-cycle finding CONFIRMED load-bearing + correctly resolved; hub ACCEPTS post-merge wiring ownership of the 3 adapters at root.go (CostReader→internal_read/profitability; PriceIntelCollector→marketplaceCapabilities; ProductIdentityReader→products/ERP). Chip constraint reaffirmed: interface + fake per port, fakes NEVER wired into live composition (core §4 stub rule) — tests/fakes only; hub wires real adapters in its own post-merge commit. Serial S1→S5 approved. **D-F4-a..f NO VETO** (consistent with D-04/D-10/D-12 freezes); verdict computed-on-read approved. Close-ordering: chip CLOSED carries P5 ladder + P6 dual gate + fake-backed integration evidence; hub then merges, wires adapters, rebuilds stack, runs P7 QA live-drive itself folding in the D-19 deferred probe items (title-vs-name, shipment/costs, free-ship route, buy_box A.3). Wire-contradictions there ⇒ A.2/A.5 amendments or tiny fixup slices per D-12.

### F-04-S1 review + adjudication (2026-07-18) — OVERALL PASS → 4 coverage fixups
Cold sonnet reviewer a38f30 hand-traced A–J: qualify predicate, dedupe-lowest, pre/post-dedupe counts, median exact/half-up, `<5` threshold, 5-rule precedence, VerdictLabel-always-nil, no `max` field, nil-Money propagation, panic-safety — all PASS. Zero correctness defects. Findings were coverage gaps only; dispatcher closed the load-bearing ones before commit:
- **[FIXED] median half-up tie boundary:** added `even median half-up tie` cell (10.00/10.01 mean=10.005 ⇒ exact-tie rounds up to 10.01) — the spec-called-out boundary the original even-median cell (10.50) missed.
- **[FIXED] malformed-decimal error path:** added post-loop assertion (amount `"abc"` ⇒ error, not panic/silent-drop).
- **[FIXED] verdict validation guards:** added 7-case `errCases` table — empty product_id, invalid decision status, ACCEPT+bad-aggregate-status, ACCEPT+wrong-aggregate-source, our_price wrong source, target_price zero as_of, cost zero as_of — locking `validateVerdictInputRefs` + the aggregate-integrity guard firing on ACCEPT (previously only proven not-to-fire on non-ACCEPT).
- **[FIXED] condition-variant lock:** added `drops case variant condition` cell (`"New"` capital ⇒ does NOT qualify) — documents the exact-lowercase `"new"` contract; adapter owns normalization (reviewer's low/cosmetic flag, resolved as intended behavior).
Re-ran vet + domain test -v after fixups: all cells green. **F-04-S1 CLOSED `0d16f7cf`.** Chip tip advances. F-04-S2 UNBLOCKED (SERIAL — cost port + source-keyed aggregate read).

### F-04-S2 DISPATCHED 2026-07-18 (SERIAL after S1)
| Slice | Role | Model / effort | Path | Payload | Prompt | Output artifacts | Status | Close |
|---|---|---|---|---|---|---|---|---|
| F04-S2 | Implementer (standard) | gpt-5.6-luna/high | OS-process | F-04-S2: NEW `market/ports/cost_reader.go` (CostReader interface only, composition-wired post-merge) + additive `MarketAggregateStore.LatestMarketAggregatesBySource` on decision_store.go + postgres impl in aggregate_repository.go + additive integration test (multi-source one product ⇒ correct source; tenant isolation; input order; missing omitted). Binds amendment B.2 (cost port) + B.3 (source-keyed additive, GET /market/aggregates stays product-keyed). Chip decisions D-F4-d/e/f (hub NO VETO, D-21). NO domain edits, NO migration (source already in PK). dispatchId `implementer-standard-f04s2-a1`; task b602zaaqd; card dispatch/cards/F-04-S2.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F04-S2.md | scratchpad/agent__implementer-standard-f04s2-a1.{log,last.md} | CLOSED | commit `3b155c5d`; worker pure lanes 0; reviewer a87686 OVERALL PASS → Defect-1 fixup; integration lane GREEN (self-provisioned session PG) |

### F-04-S2 review + adjudication (2026-07-18) — OVERALL PASS → Defect-1 fixed + integration verified
Cold sonnet reviewer a87686 hand-traced A–H: source-keyed SQL byte-diffed against product-keyed template (only delta = `AND source=$3` + dropped `source` from ORDER BY = sanctioned), tenant predicate, input-order/missing-omit/non-nil-empty, scanMoney reuse + scan-order, additive-only (product-keyed untouched, `var _` assertion holds), CostReader interface-only + unwired (grep confirmed root.go does not reference it), no migration — all PASS.
- **[FIXED] MEDIUM Defect-1 (coincidental source-discrimination):** original test only queried `ml_catalog_offers` for productA, which was ALSO its chronologically-latest row — a dropped `source=$3` predicate would still pass by coincidence. Added the discriminating assertion: query `ml_sale_price` (the OLDER, non-overall-latest row) ⇒ must return exactly 90.00, not the newer 80.00 catalog row.
- **Integration lane self-served (hub D-22 option a):** `npm run harness:pg:up` (session PG :62280) → warmed modcache → created generated DB `mpc_test_<runid>` → `go run ./cmd/testdb migrate` (45 applied) → `MPC_TEST_DATABASE_URL` set INLINE per-command (never ambient/`.env`) → full market postgres integration package GREEN (19s) incl. new source-keyed + Defect-1 assertion + no regression on offer/reference/snapshot/observation/signal/decision repos.

### FINDINGS — 2 pre-existing base failures in `tests/integration` (NOT F-04-caused; reported to hub)
The full `npm run harness:integration` lane reports `status=blocked` on two tests OUTSIDE the market module's slice scope — both fail on the chip base independent of S1/S2:
1. **`TestMarketContractAbsenceProof` (market_contract_test.go:406) is STALE.** Its `wantInsertFiles` allowlist = only `observation_repository.go` + `reference_repository.go`, but F-02 legitimately added 5 more market repos that `INSERT INTO market_*` (aggregate, competitive_signal, match_decision, offer, snapshot). The scan now finds all 7 ⇒ FAIL. S2 added a SELECT-only method to aggregate_repository.go — the INSERT there is F-02's pre-existing `AppendMarketAggregates`. This absence-proof predates F-02's persistence merge; it ALSO needs hub reconciliation when F-04's collector/root wiring lands post-merge (D-21). Chip does NOT edit this shared contract test (hub-owned surface).
2. **`TestPhase1SmokeFlow` (phase1_smoke_test.go)** = the known allowlisted flake retired on main by `7d2876dd` (fixture fix + flake allowlist); chip base predates that commit, so the old flaky version runs here. Not a code defect.
Both are hub-owned reconciliation items, surfaced per core §0. They do NOT gate S2 (my slice's tests are green in the same lane run).
| F04-S3 | Implementer (complex) | gpt-5.6-sol/low | OS-process | F-04-S3: collect pipeline `Collect(ctx, codprod)` (identity→catalog→snapshots→offers→aggregate→signals, persist via F-02 stores) + 3 NEW market-domain ports (PriceIntelCollector, ProductIdentityReader + result structs/sentinels) + in-package fakes + hermetic branch/determinism suite. Binds feature.md §36-52 + IC-03 Error Matrix + amendment B.1 (app-struct summary; JSON in S5). 409 sync.Map guard; ADR-17 FAILED-snapshot/PARTIAL; no-EAN zero-catalog-calls; flag-OFF⇒NO_PRICE_EVIDENCE. Signatures pre-verified against committed S1/S2/F-02/F-03 (AppendMatchDecision, ResolveIdentity(product,candidates,decidedAt), LocalProductIdentity.EAN *string, MarketPosition). Chip decisions D-F4-g/h/i/j (proceed pending veto). NO connectors/internal_read import (Go cycle). dispatchId `implementer-complex-f04s3-a1`; task by0trncze; card dispatch/cards/F-04-S3.md; pack-body-sha 17b5a9ed + F-ENV-3 | scratchpad/prompt-F04-S3.md | scratchpad/agent__implementer-complex-f04s3-a1.{log,last.md} | FAILED — codex usage-limit | worker never ran (no last.md, no worktree change); log: `ERROR: You've hit your usage limit ... try again at Jul 25th, 2026 12:24 AM`. Account-level (not per-model/transient) — S1(sol)+S2(luna)+2 reviewers exhausted quota. ESCALATED to hub. |

### BLOCKER (2026-07-18) — codex account usage-limit exhausted; F-04-S3/S4/S5 implementer path down
codex OS-process dispatch hit the account usage limit on the F-04-S3 dispatch (reset Jul 25 — 7 days out). Not per-model (luna would fail identically) and not the known cache-TTL false alarm — this is a hard account cap. F-04-S3/S4/S5 all need a codex implementer. Milestone stalls until quota resets OR an alternate implementer path is authorized. ESCALATED to hub with 3 options (wait / alt credits / hub-authorized main-thread Claude implementation keeping the independent cold-sonnet-reviewer gate). Demo target 2026-07-20 (2 days) ⇒ waiting to Jul 25 is fatal; awaiting hub ruling. Chip tip `3b155c5d` (S1+S2 CLOSED, clean).

### RESOLVED — D-23 (hub ruling 2026-07-18): implementer path swaps codex → chip-dispatched sonnet subagent
Hub ruled option **(b) modified**: (a) rejected (no alt credits), (c) rejected (demo Jul 20). For F-04-S3/S4/S5 the IMPLEMENTER swaps from codex OS-process to a **sonnet subagent dispatched by the chip via the Agent tool** — explicitly NOT the main-thread orchestrator (avoids self-grade anchoring on the review gate). ALL other doctrine preserved verbatim: independent cold-sonnet reviewer per slice, commit-per-green, dual gate (cold Opus + adversarial sonnet), same non-negotiables/anti-slop, same slice cards + impl-pack header as the binding contract. The sonnet implementer reads `_header.md` + the slice card from disk and obeys them; the chip does NOT implement and does NOT commit until the cold review passes.
| F04-S3 | Implementer (sonnet subagent, D-23) | claude sonnet / Agent-tool | Agent tool (background) | F-04-S3 re-dispatch on the sonnet path — same card/contract as the failed codex row above (collect pipeline + 3 ports + fakes + hermetic suite). Binding = `_header.md` (pack-body-sha 17b5a9ed) + `dispatch/cards/F-04-S3.md`, read from disk; write_set = 4 files EXCLUSIVE; no commit (chip commits per-green post cold-review). agentId `a9c44faa4d22c19e0` | (Agent-tool prompt; card+header on disk) | subagent report (inline) | IMPLEMENTER GREEN — chip re-verified build+vet+test all pass, scope=exactly 4 files, no connectors/internal_read import, injected clock; COLD REVIEW in flight (independent sonnet, agentId `ad1be77ea92a7c009`) | flagged judgment calls for adversarial review: non-ACCEPT-runs-competitive-signals, EAN-search-fail matchStatus=NO_CANDIDATE, catalog-product-fail synth aggregate now() FetchedAt, ErrRateLimited/ProviderStatusError added to port, contagens keyed on blocking. No commit until review passes. |

### F-04-S3 cold review (2026-07-18, independent sonnet ad1be77ea92a7c009) — CHANGES-REQUIRED → escalated
Implementer green + chip-re-verified. Review: attack points 3/4/5/7 CORRECT (synth-now() aggregate on catalog-product fail = reasonable D-F4-g extension; ErrRateLimited/ProviderStatusError in port = real consumer, cycle-avoidance, not speculative; contagens-keyed-on-blocking = only enum yielding B.1's no_candidate bucket; determinism clean, no map-order leak, no time.Now in pipeline body). Point 6 (snapshotTTL=time.Hour magic const) MINOR. Scope/doctrine all PASS (4 files only, no connectors/internal_read, every value via New… ctor, FAILED snapshot append-only never overwrites VALID per test f DeepEqual).
- **BLOCKER = contract ambiguity (attack pt 1), ESCALATED to hub (not chip-adjudicable):** is competitive-signal collection (step 7) gated by match outcome or independent? Current impl ASYMMETRIC — no-EAN skips step 7 (full exit, test a), non-ACCEPT runs step 7 (card step 5 enumerates only "catalog-product/offers"). Options X (identity-gated match-independent = current, asymmetry justified) / Y (fully independent, incl no-EAN — contradicts step-3 telemetria-zero) / Z (subordinate to ACCEPT — feature.md semicolon reading). Chip recommends X. Awaiting hub ruling.
- **2 consequent coverage gaps to fix in one sonnet fixup post-ruling:** test (e) leaves linked-listings empty ⇒ step-7 behavior unobservable (seed listing + assert collector totals, value per ruling); test (d) never asserts summary.Status (assert COMPLETED — flag-disabled bypasses partial). Plus 2 card-silent sub-cases sent to hub for one-line confirm (EAN-search-fail⇒NO_CANDIDATE default; catalog-product-fail⇒now() FetchedAt).
- **NO COMMIT** — S3 staged uncommitted, chip tip stays 3b155c5d until ruling + fixup + re-verify green.

### F-04-S3 hub ruling D-27 (main a8f70340) + test fixup dispatch (2026-07-18)
Hub ruled **Option X** (identity-gated, match-independent): step-7 competitive signals run for non-ACCEPT (REVIEW/REJECT/NO_CANDIDATE) whenever LinkedListings non-empty; no-EAN stays full early-exit (step-3 "telemetria zero" is explicit contract). Y/Z rejected. Both card-silent sub-cases CONFIRMED as-is (EAN-search-fail⇒NO_CANDIDATE closed-enum default no decision persisted; catalog-product-fail⇒NO_PRICE_EVIDENCE aggregate FetchedAt=now() = absence-OBSERVATION timestamp, legal ADR-17 + IC-06 A1). TTL const = chip discretion (kept, minor). **⇒ production code CORRECT as-is, ZERO logic change.** Only the 2 test-coverage fixes. Sonnet fixup subagent `aada42430b38d7a7d` dispatched, write_set = test file ONLY: (e) seed linked listing + assert ownPricing/priceToWin calls fire (1/1) + 1 competitive signal persisted on REVIEW; (d) assert Status==COMPLETED on flag-disabled. NO production edit, NO commit. Next: on green → cold re-review of the DELTA (same reviewer ad1be77 resumes per REVIEW §9) → commit S3.
Fixup DONE + green (chip re-verified: build/vet/test all pass, production file + 2 port files byte-unchanged, scope still exactly 4 code files, all 10 TestCollect* pass). Delta re-review IN FLIGHT — same reviewer ad1be77 resumed with the (d)+(e) delta. No commit until APPROVE.

### F-04-S3 delta re-review APPROVE + CLOSED (2026-07-18)
Same cold reviewer ad1be77 (REVIEW §9 resume): **APPROVE — commit-ready.** Traced fixture→logic: (e) GTIN-only candidate ⇒ REVIEW ⇒ catalog skipped (catalogCalls/offersCalls 0) but step-7 runs (ownPricing/priceToWin 1/1 + 1 signal) = Option X locked in; (d) flag-disabled bypasses run.fail() ⇒ Status COMPLETED asserted. No scope creep, production+port files byte-unchanged (hashes recorded), other 8 cases byte-identical, no regression, zero residual blockers. All 7 prior attack points stand CORRECT under D-27.
**F-04-S3 CLOSED `1cdf13e3`.** Chip tip 3b155c5d→`1cdf13e3`. Base 59d0e62f. No merge, no push. 4 code files; .mnfs docs stay untracked. Path = D-23 sonnet-subagent implementer + independent cold-sonnet reviewer + hub D-27 ruling + one test fixup + delta re-review. F-04-S4 (verdict-read) UNBLOCKED (serial — shared market pkg).

### STANDING RULINGS in force for remaining F-04 (hub-confirmed 2026-07-18)
- **D-23 / profile §12 "Contingency lane — codex quota outage" (main 60da30b4):** codex quota dead MACHINE-WIDE until Jul 25 (post-demo). For S3–S5: implementers = sonnet subagents, slice reviewers = independent sonnet, investigators = sonnet. **P6 dual gate at M-02 close = cold Opus + independent SECOND sonnet (separate dispatch, adversarial-refute prompt) replacing the GPT-5.6 Sol side; agreement/reconciliation unchanged.** Lane expiry = quota return OR demo close. Do NOT retry-loop a codex usage-limit error — switch + cite D-23.
- **D-21:** collector/root.go composition wiring is HUB-OWNED post-merge — chip NEVER wires it; the market↔connectors real adapters land at root.go after merge. S3 ports ship interface+fake only.
- **D-24 / SDK-01:** M-04 holds an ADDITIVE-ONLY lock on `packages/sdk-runtime/src/index.ts` scoped to the product-links block. S5 adds market SDK methods in `src/market.ts` (own file, never the barrel) — keep any market entry in its own domain region; hub resolves textual conflict at merge. (S5 doctrine already forbids editing the barrel.)
- **D-25 / IC-01 A2 (main 0937621b):** NO M-02 behavior change. Market-offer matching unchanged: competitor seller_sku meaningless, EAN-absent⇒max-REVIEW, contradiction-beats-EAN, Doka collision fixtures mandatory. If F-03/F-04 code ever surfaces anchor reasons, marca/refforn report UNAVAILABLE (ADR-17), never omitted.
- **D-26:** `TestMarketContractAbsenceProof` (stale allowlist) + `TestPhase1SmokeFlow` (retired flake) are known-blocked on the chip base — cite D-26 in full-lane runs, never edit (hub-owned). Hub folds the allowlist + F-04 wiring expectations into the post-merge commit.
| F04-S4 | sonnet subagent a9f78ecb | sonnet ae63a69e | — | read services + EvidenceReader facade (SERIAL after S3) | APPROVE | d78aec37 | CLOSED | — |
| F04-S5 | sonnet subagent afa56abe | sonnet abce7f4e | — | transport + OpenAPI + market.ts (SERIAL after S4, milestone closer) | APPROVE | 2838255c | CLOSED | — |

### F-04-S5 CLOSED `2838255c` — M-02 FEATURE WORK COMPLETE (2026-07-18)
Milestone closer. D-23 sonnet path: implementer afa56abe → chip re-verify ALL lanes green (go build/vet/test market exit 0, composition exit 0, sdk `tsc --noEmit` exit 0, vitest 60/60; scope clean, no connectors/internal_read import, index.ts barrel + composition byte-untouched) → cold-sonnet reviewer abce7f4e = **APPROVE — commit-ready**, all 8 hard gates verified against ACTUAL amendment text. Confirmed: B.1 Portuguese keys status/decisões/contagens/causas verbatim + contagens 5-key lower-snake_case + causas.detail null legal (amendment types it string|null, domain has no detail field); B.2 verdict_label always null (AssembleVerdict never assigns), market_range no max, inputs_used {input:{source,as_of}}, nil Money⇒null never 0 (no omitempty on wire DTOs), Money decimal string; 404 PRODUCT_NOT_FOUND / 409 COLLECTION_IN_PROGRESS / 422 too_many_ids (all 3 GET) / never-collected⇒200 NO_PRICE_EVIDENCE; OpenAPI 309-ins/0-del, observations/references + MarketMoney ($ref-reused) byte-unchanged; Option-B parity test in market/transport (composition zero-diff); NewHandlerWithCollections additive (routes gated behind nil deps ⇒ existing composition call site unchanged, hub wires post-merge D-21); market.ts standalone MarketPriceIntel* (zero barrel collision). Chip fixed the 1 code nit pre-commit: gofmt -w on 3 files (whitespace-only, re-verified green). tsconfig.json include+="src/market.ts" = justified necessary infra (explicit file-list; else tsc false-greens market.ts) — flagged to hub. Non-blocking nits carried to hub: SDK has no dedicated market.test.ts (card SDK gate = typecheck only); amendment causas.reason enum lists TIMEOUT but S3 CollectionCause (out-of-scope, S3-owned) omits it — S5 pass-through correct, S3/amendment gap for hub P7. Chip tip d78aec37→`2838255c`. Base 59d0e62f. No merge, no push. 11 code files (.mnfs untracked).

**M-02 SLICE DAG COMPLETE:** F-01∥F-02→F-03→F-04(S1→S2→S3→S4→S5) all CLOSED. Branch chain: 0d16f7cf(S1) 3b155c5d(S2) 1cdf13e3(S3) d78aec37(S4) 2838255c(S5) on base 59d0e62f. NEXT = M-02 close: P6 dual gate (cold Opus + adversarial-refute sonnet, separate dispatches, agreement required) → CLOSED event w/ evidence + D-19 deferred-probe index for hub P7 → harness:pg:down.

### M-02 P6 DUAL GATE — DISAGREEMENT, milestone HELD (2026-07-18)  [D-34-pending]
Gate over 59d0e62f..2838255c. **Cold Opus (aa02da1d) = PASS** (7 checkpoints clean: arch boundary, ADR-17 zero-leak, IC-01/03/B.1/B.2, flag default-OFF, scope hygiene composition+index.ts byte-unchanged incl sanctioned 41→45 migration bump, cross-slice, anti-slop; all lanes green). **Adversarial sonnet (a38f3fe2) = REFUTED — 1 MAJOR.** No contradiction — Opus tested zero-leak + FAILED-never-overwrites-VALID, NOT the null→FAILED-attempt clause.
BLOCKER (chip-verified code+contract+reachability): `collection_pipeline_service.go` GetOwnItemPricing err==nil & OurPrice==nil ⇒ line 238 appendValidSnapshot no-ops (302), line 262 drops signal, NO FAILED/cause/PARTIAL written (same on GetPriceToWin null-success @256). Deviates from IC-03 §Persistence Expectations (research/market-evidence-read-interface-contract.md:46 VERBATIM "Coleta falha/null grava tentativa FAILED NOVA"; only buy_box_winner null is carved out ⇒ does NOT cover listing ml_sale_price). Reachable: real adapter pricing_reader.go:94 providerMoney⇒(nil,nil) on absent current_price (paused listing). Harm: S4 Signals honest-empty synth ⇒ collected-but-null byte-identical to never-collected (zero fetched_at), breaks mission observability. Test gap: all OwnItemPricing{} fixtures (297/404/483/548) set non-nil OurPrice; null path untested. ROOT: S3 card:97 narrowed IC-03 "falha/null" to error-only, never a flagged D-F4 decision ⇒ never hub-ruled. Chip assessment: sonnet RIGHT, genuine IC-03 deviation.
**ESCALATED to hub** (options A=fix S3 w/ hub-granted CollectionCause for null-success [none of NO_IDENTITY/FLAG_DISABLED/PROVIDER_4XX/5XX fit; propose PRICE_UNAVAILABLE] + reopen closed S3 1cdf13e3 + re-run dual-gate delta; B=ratify narrowing, weaker/contradicts IC-03:46). Recommend A. Fix needs hub cause-enum ruling (contract surface, intersects P7 TIMEOUT-gap) + S3-reopen authorization — chip will NOT invent enum value / self-adjudicate closed IC file. HELD: no CLOSED, no harness:pg:down, PG up, branch frozen at 2838255c pending ruling.

### F-04-S5 hub rulings D-30/D-31 — dispatch unblocked (2026-07-18)
Flags NO VETO: **D-F4-o** market.ts standalone APPROVED (D-13 erpImport.ts precedent for NEW domain; not in tension with D-24 since M-04's was extending an EXISTING inline block; avoids index.ts SDK-01 contention; barrel `export *` = hub post-merge). **D-F4-p** single-product synchronous POST APPROVED. **D-F4-q** APPROVED: 404 PRODUCT_NOT_FOUND (IC-01 table) for codprod-inexistente on collect vs 200 NO_PRICE_EVIDENCE for never-collected GET — contract-mandated split, encode in httptest. **SDK gate (D-31):** profile §3 npm-ci-at-chip-root env prep CONFIRMED (no grant); workspace build(tsc)+test(vitest), chip re-run = verification of record. **OpenAPI gate (D-31): OPTION B RATIFIED** — new Go parity test in market/transport/*_test.go reading contracts/api yaml via relative path (mirror composition parity pattern, inside write_set); composition UNTOUCHED (C rejected = seam widening, A rejected = weakest). done_criteria /market/* = additive valid yaml + transport-local parity test green + full suite green. Hub may consolidate into composition/root_test.go post-merge (hub-owned, don't pre-wire). S5 close = M-02 close: P6 dual gate (cold Opus + adversarial-refute sonnet, separate dispatches, agreement required) → CLOSED event w/ evidence + D-19 deferred-probe index for hub P7 → harness:pg:down; hub then merges + wires 3 adapters + rebuilds stack + P7 live QA.

### F-04-S4 CLOSED `d78aec37` (2026-07-18)
Evidence-read services + frozen `market.EvidenceReader` facade (Signals/Aggregates/Verdicts). D-23 sonnet path: implementer subagent a9f78ecb → chip re-verify green (build/vet/test exit 0, scope = exactly 3 code files, no connectors/internal_read import) → independent cold-sonnet reviewer ae63a69e = **APPROVE — commit-ready**, all 6 scrutinized decisions cleared. Reviewer-confirmed rulings: honest-empty CompetitiveSignal/MarketAggregate as DIRECT STRUCT LITERALS is card-mandated sentinel (append-only domain constructors `NewCompetitiveSignal`/`NewMarketAggregate` reject the shape; precedent = `NewStoredObservation` doc comment), NOT integrity violation / not smuggling an invalid value; `domain.CostRef` is a PURE ref type (`{AsOf time.Time}`, no Amount field anywhere) so `costRef` discarding GetCostAsOf's Money = ZERO data loss; D-F4-m non-numeric codprod⇒cost-absent-not-error confirmed (test uses a cost reader that errors if dialed); cost hard-error propagates (no blanket swallow); never-collected codprod⇒200 NO_PRICE_EVIDENCE (not 404). D-F4-l our_price/target_price left unconditionally nil (card "OPTIONAL for MVP", SnapshotStore/ProductIdentityReader not wired = anti-slop, market_range is load-bearing). Two NON-BLOCKING nits logged (no in-code D-F4-l trace comment; no per-method empty-input test for Signals/Aggregates) — not gated. Chip tip 1cdf13e3→`d78aec37`. Base 59d0e62f. No merge, no push. 3 code files; .mnfs docs untracked. F-04-S5 (milestone closer) UNBLOCKED but HELD pending hub OpenAPI-gate ruling (below).

### F-04-S5 pre-dispatch REQUEST — toolchain gates (2026-07-18)
Card explicitly designates OpenAPI-lint + SDK-build command resolution a hub-confirm point ("do NOT invent a toolchain; if unclear ⇒ REQUEST"). Findings: (1) **SDK gate — read as already-ratified, proceeding unless vetoed:** node_modules absent chip-side (root + packages/sdk-runtime empty; no tsc/vitest bin); profile §3 node-bootstrap clause (2026-07-16) makes `npm ci` at chip root sanctioned env prep (NOT a dep-install, never symlink-reuse another tree). Gate = `npm ci` root → `npm run build`/`test --workspace @marketplace-central/sdk-runtime`, chip re-run = verification of record. (2) **OpenAPI gate — GAP, ruling requested:** NO redocly/spectral/openapi-lint target exists in repo; only Go string-parity test `TestRefreshListingsOpenAPIContractParity` (composition/root_test.go:18), which sits in internal/composition — OUTSIDE S5 write_set. Market Handler already wired at composition root (routes auto-mount, no composition edit needed). Asked hub to rule the /market/* acceptance gate: (A) additive-valid-yaml + Go suite green only, (B) NEW parity test in market/transport/*_test.go (in-scope, recommended), or (C) composition carve-out grant. **S5 dispatch HELD pending ruling.** REQUEST queued to hub.

### M-02 P6 hub ruling D-34 (main 7b66fd60) — OPTION A, S3 REOPENED (2026-07-18)
Hub upheld the sonnet blocker. Wrote **IC-03 Amendment A1** (contract, main): LISTING sources (`ml_sale_price`, `ml_price_to_win`) `err==nil` + preço ausente = "coleta null" ⇒ grava tentativa **FAILED NOVA** causa `PRICE_UNAVAILABLE` + `run` marca **PARTIAL**. Único carve-out null-sem-FAILED segue `buy_box_winner` (catálogo). Closed `CollectionCause`/`causas.reason` enum agora: `NO_IDENTITY | FLAG_DISABLED | PROVIDER_4XX | PROVIDER_5XX | TIMEOUT | PRICE_UNAVAILABLE` (TIMEOUT já constava na causas-list do amendment D-12/B.1 mas faltava no código Go E no S5 market.ts:42-46 [4-valor subset] — mesma correção). Card F-04-S3 narrowing SUPERSEDED.
**Reopened-S3 SCOPE (hub-authorized):** `collection_pipeline_service.go` dois ramos null (`GetOwnItemPricing.OurPrice==nil` E `GetPriceToWin.TargetPrice==nil`, err==nil) ⇒ appendFailedSnapshot + run.fail(PRICE_UNAVAILABLE,false) + PARTIAL; enum additions (Go const + S5 OpenAPI reason enum + market.ts union, additive closed-set parity); tests cobrindo AMBOS ramos null (hoje untested — fixtures 297/404/483/548 todas non-nil). TIMEOUT emitter: wire `errors.Is(err, context.DeadlineExceeded) => TIMEOUT` em classifyProviderFailure (stop=true) — dá ao membro closed-set um emitter real+testado, não código morto; folds D-33 flag-2 misclassification.
**PROCESS (hub):** sonnet impl + cold review per D-23 => dual-gate DELTA re-run (ambos gates re-veredictam o delta; full Opus re-pass NÃO exigido, REVIEW §9 analog) => agreement => CLOSED. PG stays up.

| F04-S3-reopen | Implementer (sonnet subagent, D-23) | claude sonnet / Agent-tool | Agent tool (background) | reopened-S3 null-branch fix per D-34 + Amendment A1; write_set = collection_pipeline_service.go + _test.go + contracts/api/marketplace-central.openapi.yaml + packages/sdk-runtime/src/market.ts (EXCLUSIVE); no commit (chip commits per-green post cold-review) | (Agent-tool prompt; _header.md on disk) | subagent report (inline) | DISPATCHED | — |

### F-04-S3-reopen cold review APPROVE (independent sonnet ab6551f6, 2026-07-18)
Implementer a3694548 green + chip re-verified independently (go build/vet/test 0, sdk tsc clean, vitest 60/60, scope=exactly 4 files, diffs additive closed-set parity). Cold reviewer = **APPROVE (commit-ready)**, all 7 adversarial checks PASS: (1) both listing null branches handled, no err==nil/nil-price no-op path remains; (2) FAILED write correct — appendFailedSnapshot right source + PARTIAL + cause-once, no double VALID write, ADR-17 append-only lives in unmodified store (no new overwrite risk); (3) independence — GetPriceToWin still probed when OurPrice nil (gated only !run.stopped), competitive-signal correctly skipped; (4) TIMEOUT emitter context.DeadlineExceeded stop=true — reviewer confirmed NO live HTTP collector adapter exists yet (only ports iface + fakes) ⇒ wrapped-net-timeout misclassification is legitimately DEFERRED to future live-provider adapter, not in-scope; (5) enum parity exact 6-value across Go const/OpenAPI:6066/market.ts, both external diffs purely additive, collection_handler.go:86 pure string passthrough no allowlist; (6) anti-slop — TIMEOUT emitted+tested (not dead), no narration, 3 tests are genuine pins (reviewer mentally reverted each fix ⇒ all fail correctly on real snapshot-state+counter axes); (7) scope — 4 files, index.ts barrel untouched, no composition edit.
TWO MINOR nits (reviewer: neither blocks merge): **(n1)** `appendValidSnapshot` nil-guard L314-316 (UNMODIFIED by diff) now dead/unreachable from both non-nil-gated callers = same silent-swallow class as the P6 blocker → landmine; **(n2)** test g2 doesn't pin `priceToWinCalls==1` (independence exercised, unasserted).
**Chip decision: APPLY both** (cheap hardening of the exact bug-class in already-touched files; pre-empts a dual-gate-delta re-disagreement on the milestone closer). n1 = REMOVE guard for fail-loud (NewMarketPriceSnapshot requires non-nil Price for VALID ⇒ returns error, propagates; confirmed not a panic). n2 = add priceToWinCalls==1 assert to g2. Surgical sonnet fixup ac0d38d2 dispatched, write_set = 2 code files ONLY (yaml/market.ts untouched). NO commit until fixup green + delta re-review (same reviewer ab6551f6 resume, REVIEW §9).

### F-04-S3-reopen delta re-review APPROVE + COMMITTED `903f901d` (2026-07-18)
Same cold reviewer ab6551f6 (REVIEW §9 resume) confirmed both nits applied, no regression, APPROVE (commit-ready). Chip committed. Chip tip 2838255c→`903f901d` (4 files, +190/-10). Base 59d0e62f. No merge, no push.

### M-02 P6 DUAL-GATE DELTA RE-RUN — AGREEMENT, milestone CLOSED (2026-07-18) [D-34 resolved]
Per D-34 process: dual-gate DELTA re-run over 2838255c..903f901d (not full Opus re-pass, REVIEW §9 analog). BOTH gates independent, separate dispatches:
- **Cold Opus (af0d59f3) = PASS** — blocker resolved, delta clean. 7 checkpoints A-G all traced+PASS: (A) both err==nil/nil-price listing paths now persist FAILED+PARTIAL, no silent no-op remains, guard gone; (B) right source per branch, no double VALID, cause-once, no new overwrite (snapshot_repository.go:20 INSERT ON CONFLICT DO NOTHING untouched); (C) PtW still probed on null OurPrice (stop=false), signal skipped when OurPrice nil; (D) TIMEOUT emitted+tested, wrapped-net-timeout acceptably scoped (no live collector implementer yet, ports+fakes only); (E) enum parity 6-value Go/OpenAPI/SDK, handler passthrough; (F) guard removal fails-loud via constructor error not panic; (G) anti-slop clean, tests genuine pins, 4-file scope. All lanes green. Zero findings.
- **Adversarial sonnet (a85a13a2) = CANNOT-REFUTE** — ran all 8 attack lines, could not construct a failing scenario. Confirmed: no surviving null-write-nothing path; no double-write/wrong-source; no ADR-17 overwrite; TIMEOUT real+tested (revert breaks test on 2 axes); no enum drift; 3 new tests are genuine pins (mental-revert fails each); guard removal dead-but-safe. Sole observation (stop-on-real-provider-error drops unattempted listings) = pre-existing, tested (TestCollectRateLimitedStopsRemainingProviderWork), OUTSIDE Amendment A1's err==nil mandate — not a new defect. All lanes green.
**AGREEMENT (PASS + CANNOT-REFUTE) ⇒ M-02 CLOSED.**

**M-02 price-intel-core FINAL:** chip tip `903f901d` on base `59d0e62f`. Branch chain: 0d16f7cf(F04-S1) 3b155c5d(S2) 1cdf13e3(S3) d78aec37(S4) 2838255c(S5) 903f901d(S3-reopen). F-01∥F-02→F-03→F-04(S1..S5)+S3-reopen ALL CLOSED. No merge, no push, no shared-infra touch. Hub owns: merge + wire 3 real adapters at root.go (D-21) + rebuild stack + P7 live-drive QA folding the D-19 deferred-probe index (evidence/d19-deferred-probe-index.md). PG stays UP until CLOSED event sent, then harness:pg:down.

### HUB advisory D-36 (harness 0.3.1, main 1ceb00b8) — ACK, no action (2026-07-18)
Hub codified this milestone's P6 delta re-run as REVIEW §9 (dual-gate delta re-verdict, resumed context) + §8 (adversarial gate refutation-framed, contract-clause-vs-code first). The executed M-02 close ALREADY matched: cold Opus PASS + adversarial sonnet CANNOT-REFUTE over 2838255c..903f901d, agreement ⇒ CLOSED. Worktree vendored docs predate 0.3.1; main @ 1ceb00b8 binding for future gate mechanics only — no slice-work impact. Milestone already closed; no re-run. Advisory is a HUB cross-session message, NOT user input/approval.
