## Slice card — F-02-S4 (match-decision + competitive-signal + aggregate repositories)

**Feature:** F-02 market persistence · **Slice:** S4 of 4 (final) · **Complexity:** complex (Sol-low, hermetic-integration)
**Upstream (green):**
- F-02-S1 domain committed `52b6d8c2` — `market/domain/evidence.go` (MatchDecision / CompetitiveSignal / MarketAggregate structs + enums). READ it; mirror exact field names + enum spellings. Do NOT redefine domain types.
- F-02-S2 schema committed `93b935d9` — migrations `0052_market_match_decisions.sql` + `0053_market_signals_aggregates.sql` (tables `market_match_decisions`, `market_competitive_signals`, `market_aggregates`). READ them; use the EXACT column names + the PK/idempotency tuples already in the DDL for your ON CONFLICT targets. Do NOT add/ALTER columns.
- F-02-S3 committed `e4bc880c` — `ports/evidence_store.go` (SnapshotStore/ValidatedOfferStore) + `snapshot_repository.go`/`offer_repository.go` on `Repository{pool,tenantID}`. These are COMMITTED and OWNED by S3 — do NOT edit them. Your new repos are additional methods on the SAME `Repository` type in NEW files, and your ports go in a NEW port file.

### goal
Implement PostgreSQL repositories for **match decisions**, **competitive signals**, and **market aggregates**, with idempotent append and **ordered batch reads** (results follow input-key order; missing keys are simply absent — never synthesized). Pure persistence: NO status/verdict derivation here (e.g. n_sellers<5⇒INSUFFICIENT_MARKET is the F-04-S1 aggregator's job, NOT this slice). Consumed later by F-03 (resolver decisions) and F-04 (collection/verdict).

### validation_kind
hermetic-integration (orchestrator runs the PG lane; see commands)

### commands
Worker runs (sandbox — capture real output, evidence type `ran`):
```
$env:GOCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\.gocache'
$env:GOMODCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\apps\server_core\.gomodcache'
cd apps\server_core
go build -buildvcs=false ./...
go vet ./internal/modules/market/...
go test ./internal/modules/market/... -count=1
```
**Do NOT run `npm run harness:integration` / `harness:pg:up` / docker** — the sandbox has no PostgreSQL. Write the three `//go:build integration` test files so they compile and fully express the contract; mark the integration run `could-not-run (orchestrator lane)`, NOT a failure. The ORCHESTRATOR runs the PG lane after your slice (FOCUSED `-run Test(MatchDecision|CompetitiveSignal|MarketAggregate)` — the full lane is red on an unrelated pre-existing test in this base).

### write_set (EXCLUSIVE)
- `apps/server_core/internal/modules/market/ports/decision_store.go` — NEW port file: MatchDecisionStore, CompetitiveSignalStore, MarketAggregateStore interfaces (minimal; only methods the repos implement and F-03/F-04 need). Do NOT touch `ports/evidence_store.go`.
- `apps/server_core/internal/modules/market/adapters/postgres/match_decision_repository.go`
- `apps/server_core/internal/modules/market/adapters/postgres/match_decision_repository_integration_test.go` (`//go:build integration`)
- `apps/server_core/internal/modules/market/adapters/postgres/competitive_signal_repository.go`
- `apps/server_core/internal/modules/market/adapters/postgres/competitive_signal_repository_integration_test.go` (`//go:build integration`)
- `apps/server_core/internal/modules/market/adapters/postgres/aggregate_repository.go`
- `apps/server_core/internal/modules/market/adapters/postgres/aggregate_repository_integration_test.go` (`//go:build integration`)

Nothing outside this set. If you need to touch `evidence.go`, a migration, `evidence_store.go`, or an S3 repo, STOP and report — that is a contract gap for the orchestrator, not a "fix while here".

### failing_test_first (contract expressed before the queries; PG run deferred to orchestrator)
Author the three integration tests FIRST:
1. `match_decision_repository_integration_test.go`: append a decision with ≥2 positive anchors and ≥1 contradiction; read it back and assert anchors + contradictions round-trip EXACTLY (same elements, same values — positive anchors retained, none dropped/reordered-away), match_status + confidence_band exact, catalog_product_id nil round-trips as unknown. Cross-tenant isolation (tenant A writes, tenant B reads nothing). Duplicate-append no-op (row count 1) against the 0052 PK.
2. `competitive_signal_repository_integration_test.go`: append signals; read back with our_price exact decimal string; winner_price/target_price/position_rank/position_total NULL round-trip as nil (ADR-17), never 0. Ordered batch read over multiple listing_ids preserves input order. Cross-tenant isolation. Duplicate-append no-op.
3. `aggregate_repository_integration_test.go`: append aggregates; assert median/min_valid nil round-trip as unknown; n_offers/n_sellers exact counts; source/fetched_at/computed_at preserved; status stored verbatim (NOT derived). Ordered batch read over multiple product_ids preserves input order and OMITS missing products (no synthesized zero rows). Cross-tenant isolation.

### behavior contract (bind exactly)
- **Idempotency**: append uses `INSERT ... ON CONFLICT (<exact PK tuple from 0052/0053>) DO NOTHING`; duplicate = no-op success. READ the DDL for each table's PK — do not guess the tuple.
- **Ordered batch reads**: a batch read over a list of keys returns rows in INPUT-KEY order; missing keys are ABSENT (not synthesized, not zero-filled). Reuse the existing input-order idiom — cite `observation_repository.go` (`TestObservationLatestPerListingPreservesInputOrder` + its query) and mirror it; do NOT invent a new ordering scheme.
- **Exact decimals**: all money/decimal values are strings — round-trip byte-exact; no float/cents/shopspring conversion. Reuse `moneyAmount`/`moneyCurrency`/`scanMoney` (defined in `observation_repository.go`); no duplicate converter.
- **ADR-17 nullability**: median, min_valid, winner_price, target_price, position_rank, position_total, catalog_product_id are nullable → nil pointer on read, NULL on write. NEVER 0/""/default.
- **anchors/contradictions**: store + read back EXACTLY as the S1 domain represents them (read `evidence.go` + the 0052 column type — text[]/jsonb — and mirror it). Positive anchors are integrity-critical: none may be dropped, coerced, or synthesized.
- **NO derivation**: persistence stores what it is given. Do NOT compute status/verdict/labels (n_sellers<5⇒INSUFFICIENT_MARKET etc. belong to F-04-S1). Missing rows are not synthesized.
- **Tenancy**: every query binds `tenant_id = $1` FIRST (mirror `Repository{pool,tenantID}` idiom). No query without the tenant predicate.
- **Time**: `timestamptz`; preserve fetched_at/computed_at/decided_at exactly. Tests use `time.Now().UTC().Truncate(time.Microsecond)`.
- Use `internal/testsupport/postgres.OpenPool(t, tenantID)` in the integration tests.

### done_criteria
- New port file `decision_store.go` with 3 minimal store interfaces; the 3 repositories implement them on `Repository`.
- Idempotent append (dup → row count 1); ordered batch reads preserve input order + omit missing; exact anchor/contradiction + decimal round-trip; nullable → nil; tenant-first; no derivation.
- `go build -buildvcs=false ./...` + `go vet ./internal/modules/market/...` clean; the three integration files compile.
- Integration lane marked could-not-run (orchestrator lane), tests ready.

### NON-NEGOTIABLES
- Read committed `evidence.go` + `0052`/`0053` BEFORE writing — mirror exact names/enums/columns/PK tuples. No new columns, no ALTER, no new dependency, no edit to S3's `evidence_store.go` or repos.
- ADR-17: unknown stays NULL/nil, never zero/default. Positive anchors + exact decimals are integrity-critical — no coercion, no synthesis of missing rows.
- One writer per seam: your write_set only. Anything else = REQUEST/ESCALATION to the orchestrator.
- No speculative abstraction, no comment narration, no blanket `recover`/fallback on integrity reads, no test theater (core §4 — REJECT on hit).
- If a needed shape is genuinely undefined by evidence.go / IC-03 / the committed DDL, STOP and report — do not invent.

### open_questions
[] — dispatch-ready.
