## Slice card — F-01-S1p (ML pricing read ports — Q1-independent subset)

**Feature:** F-01 ML adapter read ports · **Slice:** re-sliced S1-pricing · **Complexity:** standard (Luna-high)
**Re-slice rationale:** the batch plan's F-01-S1 bundled all 7 IC-06 ports, but 4 (catalog `Attrs`, catalog offers, shipment `Costs`, free-shipping `item`) are blocked on the pending Q1 hub contract-freeze. The **two pricing ports are fully specified** (inv02 Q1 → IC-06:27-28) and Q1-INDEPENDENT. This slice ships only those two, hermetically, now. The remaining 5 ports are a later slice gated on Q1. DO NOT declare/stub the gated ports here.

### goal
Publish the two IC-06 pricing read ports — `GetOwnItemPricing` and `GetPriceToWin` — as: normalized domain shapes + base typed errors + port interface + additive authenticated GET methods on the ML `CapabilityAdapter`. No provider DTO leaves the adapter.

### validation_kind
unit-contract (hermetic, mock-server). NOTE: the live sale_price/price_to_win probe (R4) against the real installation is a SEPARATE orchestrator/hub live-lane run (REQUEST) — NOT this slice. Do not call the network.

### commands
```
$env:GOCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\.gocache'
$env:GOMODCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\apps\server_core\.gomodcache'
cd apps\server_core
go test ./internal/modules/connectors/... -run 'Test(OwnItemPricing|PriceToWin|MarketReadShapes|MarketReadErrorMapping)' -count=1
go build -buildvcs=false ./...
go vet ./internal/modules/connectors/...
```
(`-buildvcs=false` REQUIRED in sandbox — git-128 false alarm, profile §3.)

### write_set (EXCLUSIVE)
- `apps/server_core/internal/modules/connectors/domain/market_read.go` — pricing shapes ONLY: `OwnItemPricing`, `PriceToWin`, `MarketPosition`. Each carries `FetchedAt time.Time`. Money as the connectors decimal-string convention (reuse existing Money idiom; nullable prices are pointers).
- `apps/server_core/internal/modules/connectors/domain/market_read_test.go`
- `apps/server_core/internal/modules/connectors/domain/market_read_errors.go` — base typed errors: `ErrUnauthorized`, `ErrNotFound`, `ErrRateLimited`, `ErrProviderUnavailable` (sentinel errors, `errors.Is`-comparable). DO NOT add `ErrCatalogOffersUnavailable` (belongs to the Q1-gated offers slice).
- `apps/server_core/internal/modules/connectors/ports/market_read.go` — interface declaring ONLY `GetOwnItemPricing(ctx, accountRef, itemID) (OwnItemPricing, error)` and `GetPriceToWin(ctx, accountRef, itemID) (PriceToWin, error)`. (Signatures per IC-06; use the existing `ProviderAccountRef` account-context type.)
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/pricing_reader.go` — impl of the two GETs.
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/pricing_reader_test.go`
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/capability_adapter.go` — ADDITIVE wiring of the two methods only (do not alter existing capabilities/fields).

### normalized shapes (IC-06:27-28 — fully specified; do NOT invent extra fields)
- `OwnItemPricing{ ItemID string; Amount *Money; Currency string; RegularAmount *Money; FetchedAt time.Time }` (sale_price). Absent amounts stay nil.
- `PriceToWin{ ItemID string; Status string; CurrentPrice *Money; TargetPrice *Money; Position *MarketPosition; FetchedAt time.Time }`.
- `MarketPosition{ Rank int; Total int }`.

### routes + reuse (seam map / bindings)
- GET `/items/{id}/sale_price` and GET `/items/{id}/price_to_win?version=v2`.
- Reuse `accessToken(ctx, accountRef)` + `doJSON(ctx, accountRef, token, http.MethodGet, path, nil, &out)` (capability_adapter.go:452-464, :522-542). NO new HTTP client, NO new token path.
- Raw ML response structs stay PRIVATE to package `mercado_livre`; map into the normalized public shapes.

### failing_test_first (mock httptest server)
- `TargetPrice`, `RegularAmount`, `Position` remain **nil** when absent in the payload (ADR-17); use the real payload shapes from the research handoff.
- `price_to_win` is NOT mapped to catalog minimum (research: price_to_win ≠ min). Assert a fixture where they differ.
- Error mapping: 401/403 → `ErrUnauthorized`; 404 → `ErrNotFound`; 429 → `ErrRateLimited` (exactly ONE request, no retry storm); 5xx/timeout → `ErrProviderUnavailable`. Use `errors.Is`.
- `FetchedAt` comes from the injected clock, is non-zero.

### done_criteria
- Two pricing capabilities retrievable/additive on the adapter; account context = existing `ProviderAccountRef`.
- No raw ML DTO crosses the adapter package boundary; no ML write path touched (`listing_writer.go`/`price_writer.go` untouched).
- Only GET, only the two approved routes. No forbidden `/sites/MLB/search`.
- build (`-buildvcs=false`) + vet + focused tests green.

### NON-NEGOTIABLES
- Read-only ML: zero PUT/POST. Provider DTOs die at the adapter. No new dep.
- ADR-17: nullable prices stay pointer-nil, never zero. price_to_win≠min.
- DO NOT declare, stub, or reference the Q1-gated ports (SearchCatalogByEAN / GetCatalogProduct / ListCatalogOffers / GetShipmentInfo / GetFreeShippingCost) — out of scope this slice; they need the hub Q1 freeze.
- Contract/shape genuinely undefined ⇒ STOP and report (do not guess).

### open_questions
[] — dispatch-ready (pricing subset is Q1-independent).
