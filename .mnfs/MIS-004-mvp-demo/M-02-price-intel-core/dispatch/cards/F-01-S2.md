## Slice card — F-01-S2 (ListCatalogOffers read port)

**Feature:** F-01 ML adapter read ports (IC-06) · **Slice:** S2 (catalog-offers subset — Q1-INDEPENDENT) · **Complexity:** complex (Sol-low, hermetic)
**Why now / scope:** IC-06 leaves only 4 nested shapes underspecified (Attrs, shipment Costs, free-shipping `item`, SearchCatalogByEAN FetchedAt) — all escalated to hub (inv02 Q1). `ListCatalogOffers` (IC-06:31) is the ONE remaining port whose result shape is FULLY enumerated — `paginado []{SellerID, Price, Condition, ShippingMode}`, no `Attrs`/`Costs`/`{...}` placeholder — so it is dispatchable now, independent of the Q1 ruling (same basis as the F-01-S1p pricing carve-out). The other 4 ports stay gated on hub.

**Upstream (green):**
- F-01-S1p committed `c87fb193` — established the connectors read-ports seam. READ these, mirror their idiom, do NOT redefine:
  - `connectors/ports/market_read.go` — `MarketReader` interface (2 pricing methods). Do NOT edit this file.
  - `connectors/domain/market_read.go` — `Money`-carrying shapes; `PriceToWin.CurrentPrice *Money` (line 16) is your template for a normalized-money member.
  - `connectors/domain/money.go` — connectors-local `Money{Amount string, Currency string}` VO + `ValidateMoney`. REUSE it; do NOT invent another money type, do NOT import market's.
  - `connectors/domain/market_read_errors.go` — typed errors (`ErrUnauthorized/ErrNotFound/ErrRateLimited/ErrProviderUnavailable`). Do NOT edit this file; add your new error in your new domain file.
  - `connectors/adapters/mercado_livre/pricing_reader.go` — private impl template: ML DTO structs, `a.doJSON(ctx, accountRef, token, GET, path, nil, &resp)`, `providerMoney(field, *json.Number, currency)` (line 94), `a.now()`, `mapPricingReaderError` (line 113). REUSE `providerMoney` + the error-mapping switch; do NOT copy-paste a second money converter.
  - `connectors/adapters/mercado_livre/capability_adapter.go` — `CapabilityAdapter{...}` + `CapabilityAdapterConfig{...}` + `NewCapabilityAdapter` (lines 27-70); public wrapper pattern `GetOwnItemPricing` (line 86: normalizeAccountRef → resolve token → private impl). This is the ONLY committed file you edit, additively (see write_set).
  - `connectors/adapters/mercado_livre/pricing_reader_test.go` — hermetic `httptest.Server` + stub `AccessTokenResolver` test template. Mirror it exactly.

### goal
Implement the `ListCatalogOffers` read port on the ML adapter: **flag-gated**, **complete pagination**, normalized `[]CatalogOffer` in provider order (ordering NOT guaranteed — consumer dedupes per IC-03), typed `ErrCatalogOffersUnavailable` on flag-OFF or route failure. Provider DTOs die at the adapter. Hermetic only (httptest + stub token resolver) — ZERO live Mercado Livre calls.

### validation_kind
hermetic (unit + httptest; NO PostgreSQL, NO docker, NO live provider)

### commands
```
$env:GOCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\.gocache'
$env:GOMODCACHE='C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m02-price-intel-core\apps\server_core\.gomodcache'
cd apps\server_core
go build -buildvcs=false ./...
go vet ./internal/modules/connectors/...
go test ./internal/modules/connectors/... -count=1
```
All hermetic — evidence type `ran` with captured output; Pass ONLY on real green.

### write_set (EXCLUSIVE)
- `apps/server_core/internal/modules/connectors/ports/catalog_read.go` — NEW: `CatalogOffersReader` interface (single method).
- `apps/server_core/internal/modules/connectors/domain/catalog_read.go` — NEW: `CatalogOffer` shape + `ErrCatalogOffersUnavailable` (`errors.New`).
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/catalog_offers_reader.go` — NEW: ML DTO structs + private `listCatalogOffers` impl with complete pagination + telemetry.
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/catalog_offers_reader_test.go` — NEW: hermetic httptest tests.
- `apps/server_core/internal/modules/connectors/adapters/mercado_livre/capability_adapter.go` — EDIT (ADDITIVE ONLY): add `CatalogOffersEnabled bool` to `CapabilityAdapterConfig` + `catalogOffersEnabled bool` to `CapabilityAdapter` + set it in `NewCapabilityAdapter` (zero value = false = flag OFF, the correct default); add the public `ListCatalogOffers` wrapper; add `var _ ports.CatalogOffersReader = (*CapabilityAdapter)(nil)`. Do NOT alter existing pricing wrappers, existing fields, or `ProviderCapabilitySet`.

Nothing else. Do NOT edit `market_read.go`, `market_read_errors.go`, `pricing_reader.go`, `composition/root.go`, or any migration. If you think you need to, STOP and report.

### interface + shape (bind exactly)
- `ports/catalog_read.go`:
  ```go
  type CatalogOffersReader interface {
      ListCatalogOffers(ctx context.Context, accountRef domain.ProviderAccountRef, catalogProductID string) ([]domain.CatalogOffer, error)
  }
  ```
- `domain/catalog_read.go`:
  ```go
  type CatalogOffer struct {
      SellerID     string  `json:"seller_id"`
      Price        *Money  `json:"price"`         // nil if provider omits (ADR-17); mirror PriceToWin.CurrentPrice
      Condition    string  `json:"condition"`
      ShippingMode string  `json:"shipping_mode"`
  }
  var ErrCatalogOffersUnavailable = errors.New("catalog offers unavailable")
  ```

### failing_test_first (hermetic, httptest — write these FIRST)
Mirror `pricing_reader_test.go` (httptest.Server returning ML JSON, stub `AccessTokenResolver`, `Now` injected). Cover:
1. **Complete pagination**: server returns 2+ pages (`/products/{id}/items?...offset=`), assert ALL offers across pages are returned, in provider order, normalized (Price → *Money exact decimal string; SellerID/Condition/ShippingMode trimmed). Assert the adapter does NOT dedupe/reorder/drop (that is the consumer's job).
2. **Flag OFF ⇒ typed error**: adapter built with `CatalogOffersEnabled:false` returns `ErrCatalogOffersUnavailable` WITHOUT hitting the server (assert zero requests). `errors.Is` must match.
3. **Route failure mid-pagination ⇒ typed error, NEVER partial**: server 500s (or a later page fails) → `ErrCatalogOffersUnavailable` (via `errors.Is`), and NO partial slice is returned.
4. **Nil price preserved**: an offer whose provider price is absent → `Price == nil`, never a zero Money.
5. **Auth/not-found/rate-limit mapping**: provider 401/404/429 map through `mapPricingReaderError`-style switch to `ErrUnauthorized`/`ErrNotFound`/`ErrRateLimited` (reuse the existing switch; do NOT hand-roll).

### behavior contract (bind exactly)
- **Flag gate FIRST**: `if !a.catalogOffersEnabled { return nil, domain.ErrCatalogOffersUnavailable }` before any token resolve or HTTP call.
- **Complete pagination MANDATORY**: loop until all pages consumed (ML `/products/{id}/items` paging: `paging{total,offset,limit}` + `results[]` — inspect the real shape via the httptest fixture you author; follow offset until `offset+len(results) >= total`). A page fetch error mid-loop ⇒ return `ErrCatalogOffersUnavailable` (wrapped), NEVER a truncated slice. No silent truncation.
- **Provider order preserved, NOT guaranteed**: return offers in the order pages/results arrive; do NOT sort, dedupe, or pick lowest — IC-06:31 assigns that to the IC-03 consumer.
- **Money**: `Price` via `providerMoney(...)` (reuse pricing_reader.go:94) → nil when absent, validated when present. Currency from the ML `currency_id`. No cents/float/shopspring.
- **DTOs die here**: ML JSON structs are unexported, live in catalog_offers_reader.go; only `domain.CatalogOffer` crosses the boundary. No raw ML JSON in logs.
- **Telemetry**: emit one structured `slog.Default().Info(...)` per `ListCatalogOffers` call with fields `route="/products/{id}/items"`, `status` (ok|unavailable|error), `page_count`, `offer_count`, `duration_ms`. slog only — NO metrics dependency, NO new logger field on the adapter.
- **Error mapping**: reuse the `mapPricingReaderError` switch (pricing_reader.go:113) for provider auth/404/429/transient; transient/unknown provider failure during offers ⇒ wrap as `ErrCatalogOffersUnavailable` so the consumer maps it to NO_PRICE_EVIDENCE.
- **Read-only**: GET only. ZERO PUT/POST to ML. Do NOT touch `listing_writer.go`/`price_writer.go`. FORBIDDEN route `/sites/MLB/search`.

### done_criteria
- New port + shape + typed error; adapter implements `CatalogOffersReader` (compile-time assertion present).
- Flag OFF (default) ⇒ typed error, no network. Complete pagination or typed error — never partial. Nil price preserved. Provider order untouched.
- `go build -buildvcs=false ./...` + `go vet ./internal/modules/connectors/...` + `go test ./internal/modules/connectors/... -count=1` all green (real output).

### NON-NEGOTIABLES
- Q1-independent ONLY: implement `ListCatalogOffers` and nothing about Attrs / shipment Costs / free-shipping item / SearchCatalogByEAN — those are hub-gated. If tempted, STOP.
- ADR-17: unknown price stays nil, never zero. No blanket recover/fallback on provider reads — a failed/partial fetch is a typed error, not an empty success.
- Reuse `Money`/`ValidateMoney`/`providerMoney`/`mapPricingReaderError`; cite path:line; no duplicate helper. No new dependency (dep change = STOP+report). No comment narration, no speculative abstraction, no test theater (core §4).
- Do NOT wire the flag to real env/config or edit composition — `CatalogOffersEnabled` defaults false; live wiring is a later hub-gated step. Never read `.env*`.
- Contract/architecture conflict, or a needed change outside write_set ⇒ STOP and report. You do not adjudicate.

### open_questions
[] — dispatch-ready.
