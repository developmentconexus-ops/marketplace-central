# D-19 Deferred-Probe Index — M-02 → hub P7 live-drive QA

Hub ruling **D-19** (main `be72ab70`) deferred all live wire-field verification to P7. These items cannot be settled chip-side: no live ML stack in the worktree, per-installation credentials are encrypted (not free env), in-container curls + debug routes both rejected by security posture. They are validated by driving a real codprod through **POST /market/collections** on the composed connectors post-merge; the normalized output + persisted IC-03 evidence rows are the validation of record. **None is an M-02 blocker.** Wire contradictions ⇒ A.2/A.5 amendments or tiny fixup slices per D-12.

| # | Probe | Code assumption (rides on it) | Location | Resolution if live contradicts |
|---|---|---|---|---|
| 1 | **title-vs-name** | `firstNonEmpty(result.Name, result.Title)` — `Title` DTO is a fallback wire field, unverified against real `/products/search` + `/products/{id}` payloads (docs only prove `name`). Defensively safe (never drops a populated title). | `connectors/adapters/mercado_livre/catalog_identity_reader.go:49,77` | If `title` never appears on the wire ⇒ drop the dead `Title` DTO field + fallback. **D-19: do NOT drop on a guess — keep the fallback until live evidence.** |
| 2 | **costs-404 → nil** | HTTP 404 on the shipment-costs sub-call ⇒ `ErrCodeProviderInvalidReference` ⇒ costs treated absent (nil), not an error. Primary-endpoint errors are checked BEFORE the costs call so a primary-404 cannot be confused with a costs-404. | `connectors/adapters/mercado_livre/shipping_reader.go:77` | If live 404 semantics differ ⇒ A.5 amendment. |
| 3 | **senders[] arity** | `ShipmentCosts` Gross/Receiver/Sender pointer shape (amendment A.4/A.5). Arity of the senders array on the real payload unverified. | `connectors/adapters/mercado_livre/shipping_reader.go` (ShipmentCosts mapping) | If real arity/shape differs ⇒ A.5 amendment. |
| 4 | **free-shipping route** | `FreeShippingQuery{ItemID}`-only; route uses `ProviderAccountID`. Minimal shape approved with dated deferral (D-12/A.2). | `connectors/adapters/mercado_livre/shipping_reader.go` (GetFreeShippingCost) | If route needs explicit dims/price ⇒ **A.2 amendment** at probe time. |
| 5 | **buy_box A.3** | `BuyBoxWinner{Price*, SellerID}` — `item_id`-vs-`seller_id` item-join at wire-up. Frozen shape kept; the item-join is live-probe-unobtainable chip-side (D-12 accepted recorded risk). | `connectors/adapters/mercado_livre/catalog_*` (BuyBoxWinner mapping) | Join contradiction ⇒ A.2/A.3 amendment path. |

**Note (M-02 close, ties to Amendment A1 / TIMEOUT emitter):** the `context.DeadlineExceeded → TIMEOUT` classifier is unit-tested against the fakes, but there is **no live HTTP `PriceIntelCollector` implementer yet** (only ports + fakes; the 3 real adapters are wired by the hub post-merge per D-21). So real-network-timeout → `context.DeadlineExceeded` chaining (vs a wrapped `net.Error`/`*url.Error`) is unverified end-to-end. Fold a timeout-classification check into P7 when the live collector is wired; if real timeouts arrive wrapped and bypass `errors.Is(context.DeadlineExceeded)`, extend `classifyProviderFailure` at that point (additive, no contract change).
