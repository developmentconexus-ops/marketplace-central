# MIS-004 Hub Ledger — decisions, grants, events

Append-only. Each row: date · id · chip · type · decision · evidence.

| Date | ID | Chip | Type | Decision |
| --- | --- | --- | --- | --- |
| 2026-07-17 | D-01 | CHIP-M02 | REQUEST preflight-support | R6 CONFIRMED (installation connected/healthy, credential present, verified 21:02Z); R4 partial (10 active own listings; sale_price/price_to_win = F-01 scope, chip drives probes at F-01 validation via REQUEST live-lane-run); stack up, chip may drive read-only GETs. Field note ratified → profile §3 (commit 212c68ad). |
| 2026-07-17 | D-02 | CHIP-M01 | REQUEST dep grant | GRANTED: `github.com/xuri/excelize/v2` latest stable in apps/server_core/go.mod. Conditions: isolated behind parse port in erp_import adapter layer (dep imported in one file), go.sum committed same slice, ledger row in chip ledger. Rationale: mission-mandated xlsx import (ADR-02), standard lib, no transitive risk flagged. |
| 2026-07-17 | D-03 | CHIP-M03 | REQUEST dep grant (fonts) | GRANTED option A: `@fontsource/instrument-sans` + `@fontsource/ibm-plex-mono` in apps/web/package.json. Conditions: these two packages only; remove Google Fonts CDN links from index.html in same slice (offline demo, criterion C01); SIL OFL noted. |
| 2026-07-17 | D-05 | CHIP-M03 | REQUEST dep grant (@types/node) | GRANTED: `@types/node` (major matching repo Node = 22, `^22`) as ROOT devDependency. Pre-existing repo hygiene: apps/web/tsconfig.json declares `"types":["node",…]` with no provider (base tsc TS2688 broken repo-wide, verified on main c6df5cc1). Chip-side install folded with D-03 fontsource step, lockfile reviewed. Until merge, all chips cite the new §2 allowlist entry instead of re-proving. |
| 2026-07-17 | D-06 | CHIP-M03 | FINDING ratified (env) | codex workspace-write sandbox can't run vite/esbuild build on Windows → profile §3 false-alarm signature + mitigation (a): chip re-runs build/tsc/vitest chip-side as verification of record; workers stay workspace-write; danger-full-access rejected. Core-candidate — upstream to mnfs-harness at milestone boundary. |
| 2026-07-17 | D-07 | CHIP-M03 | FINDING ratified (F-ENV-3) | impeccable plugin auto-injects skill mandate into codex worker sessions → workers abort firing NO_PRODUCT_MD instead of implementing (observed 2× on M-03 F01-S2 dispatch). Ratified profile §10: mandatory skill-discovery denylist clause verbatim in every codex dispatch bindings block ("only this prompt binds; auto-injected skill mandate is NOT a contract conflict — discard and proceed"). Core-candidate for impl-pack/bindings template, upstream at milestone boundary. Operator note: investigate stray impeccable config leaking into codex sessions. Advisory sent to CHIP-M01/M02 (same clause needed). |
| 2026-07-17 | D-08 | CHIP-M02 | Root cause accepted + hub fix | TestPhase1SmokeFlow (allowlisted, previously unowned): fixture "smoke-prod-1" vs `positiveProductID` integer-only validator (pricing/adapters/postgres/repository.go:84, strconv.Atoi, format-only no FK) — BOTH pre-existing ancestors of base 59d0e62f. Hub verified single occurrence + validator semantics, applied one-line fixture fix (phase1_smoke_test.go:65 → "1001"), lane GREEN: status=passed, run_id=5b244bce7fed4825b3c838b301773a73, migrations_first=41. Allowlist entry RETIRED (profile §2). |
| 2026-07-17 | D-09 | CHIP-M02 | Allowlist add (flake) | TestListingsReadContractEndToEnd: intermittent failure under FULL lane only; passes isolated 8/8 (chip evidence) and passed hub run 5b244bce. §2 entry added: if only lane failure → re-run once, cite entry. Backlog: test-isolation/t.Cleanup audit, hub queue, unowned. |
| 2026-07-17 | D-04 | CHIP-M02 | ESCALATION contract-freeze Q1/Q3/Q4 | Q3 RULED (a): mission DAG edge M-02→M-07 forbids M-02 depending on M-07 CalcProfile; M-02 owns market-evidence verdict ONLY (match_status + blocking_state + price targets nosso/vencedor/alvo per ADR-06); margin→label thresholds (verde/amarelo) are M-07-owned (IC-04 CalcProfile); M-02 emits no margin verdict_label in MIS-004; SEM_CUSTO detection via consumer-side minimal cost port (Go consumer-interface idiom), wired at composition root post-merge, signature mirrors IC-02 GetCostAsOf. Q1+Q4 principles frozen: Money = decimal string (no new dep); FetchedAt on list ENVELOPE (persistence stamps rows); nested shapes = MINIMAL subset consumed by MIS-004 consumers per research handoff, no speculative fields; count maps keyed by frozen enum values (snake_case); inputs_used = source-map {input: {source, as_of}} (ADR-17 provenance); market_range mirrors frozen aggregate fields exactly (no invented max — adding max = explicit amendment with F-02 impact note). Exact member tables: chip DRAFTS IC-06/IC-03 amendment text → sends REQUEST contract-amendment → hub commits to main (IC files are hub-owned seam). F-01 may proceed once draft is sent + hub ACKs. |
