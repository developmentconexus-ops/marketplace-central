# M-07-simulador — Dispatch Ledger

Chip: CHIP-M07 · branch `chip/m07-simulador` · base `8b6c4b3093f9465cd3b91209b054af4fa702171a`
Hub: local_7c41fdd8-dd6f-487c-a7ea-1b511e214fa9 · Contingency lane D-23 (codex DEAD → Claude-only)

Row written AT DISPATCH TIME (role · model · effort · prompt-pack path), completed with verdict/output artifact.

| # | Phase | Role | Model | Effort | Prompt-pack | Output/verdict artifact | Status |
|---|-------|------|-------|--------|-------------|------------------------|--------|
| D0 | boot | codebase investigator (base map) | sonnet | default | (inline) | agent a200b36ff4995ed09 report (in-transcript) | done |
| D1 | P2 | batch planner (cold Opus) | opus | default | `_evidence/packs/P2-planner.md` | `_evidence/M07-plan.md` (14 slices) | done · accepted-with-corrections (SECTION 0: C-A no 4th DIFAL table→columns on pricing_difal_rates, 0055–0058+0059 reserve, fixture 51→55; C-B npm not pnpm) |
| D2 | P3 | F01-S1 implementer (decimal helper) | sonnet | default | `_evidence/packs/IMPL-BINDINGS.md` + M07-plan F01-S1 | `pricing/domain/decimal.go`+`decimal_test.go` GREEN (5 tests), carrier commit `bb0a276f` (see F-boot-2) | done · orch-reviewed accepted (formal P4 batches w/ S2) |
| D3 | P3 | F01-S2 implementer (CalcProfile+DIFAL seed) | sonnet | default | `_evidence/packs/IMPL-BINDINGS.md` (rev: orch sole committer) + M07-plan F01-S2 | 5 files, 12 domain tests GREEN, commit `b16612d1` | done · orch-reviewed accepted; efetivo 2dp fix + override-2dp/7%-group tests finished by orchestrator (subagent hit session-limit wall mid-edit) |
| D4 | P3 | F01-S3 (ic04-ports: Decompose+DifalForUF) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S3 + IC-04 §Decomposition | 4 files (`domain/decompose.go`+golden, `ports/calc_ports.go`+contract), 12 golden vectors + 4 contract tests GREEN, whole pricing tree GREEN, vet+fmt clean, no 2nd decompose in modules/orders; commit `d288cd12b5422ca581791a5dd0c8c677bf0607d0` | done · **ic04-ports published** (COMMITTED to hub) · orch-reviewed accepted; independent P4 queued w/ S1/S2 for quota reset |
| D5 | P3 | F01-S4 (SolveTargetPrice bidirectional + UNREACHABLE_TARGET) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S4 + GT-2 | 2 files (`domain/solve.go`+golden), 5 tests GREEN (exact 15,00%; high-segment crossing 79 discontinuity; 60%@comissão16% UNREACHABLE ceiling 58.75; custo-nil blocks; per-segment monotonic); domain tree GREEN, vet+fmt clean; commit `139231db` | done · orch-reviewed accepted; independent P4 queued w/ S1/S2/S3 for quota reset |
| D6 | P3 | F01-S5 (migrations 0055–0058 + R-04 seed) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S5 (SECTION 0 C-A) | 5 files (3 tenant tables + seed + runner_test 51→55). Migrate runner GREEN@55. **Hermetic pg apply verified** (throwaway postgres): SELECT 27; efetivo SP 6.0/BA 13.5/MA 16.0/MG 6.0 = domain exact; tenant_id NOT NULL ×3 tables; override_* columns present; paired-override CHECK rejects lone override; 0059 reserved (no file); commit `63da61cf` | done · orch-reviewed accepted; P4 queued |
| D7 | P3 | F01-S6 (persistence: CalcRepository + cost port + scenarios) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S6 (depends S5+S2) | 7 files: `domain/scenario.go`, `ports/cost_read.go` (CostReader nil=absent ADR-17), `ports/calc_repository.go` (CalcRepository composes DifalReader; ErrDifalUFNotFound/ErrScenarioNotFound), `adapters/postgres/calc_repository.go` (numeric ::text; tenant per-call; profile default-on-first-read/upsert, difal read+override+list, scenarios CRUD), `adapters/costread/reader.go` (single float64→2dp decimal boundary over IC-02, nil-on-absent) +2 tests. costread unit tests GREEN (4); integration test `//go:build integration` typechecks (`go build -tags integration` OK), RUN deferred to P5 lane (host-port block F-tool-2). `go vet` + full pricing unit tree GREEN. **Repo SQL hermetically validated** (docker exec-psql): profile ::text exact 9.25/6.50 + tarifa NULL clear; DIFAL override 19.05@2dp round-trip, list ordered BA<SP, override clears; scenarios newest-first s2<s1, upsert-no-dup (count 2), delete. commit `6eb9e6f5` | done · orch-reviewed accepted; independent P4 queued w/ S1–S5 for quota reset |
| D8 | P3 | F01-S7 (HTTP transport + CalcService + IC-04 error matrix) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S7 (depends S3,S4,S6) | 6 files: `ports/product_check.go` (ProductChecker — 404 vs SEM_CUSTO split), `application/calc_service.go` (+test, orchestrates engine+repo+cost+check; profile/difal-override/scenarios/decompose/solve; Δ≤0,049pp no-persist), `transport/calc_handler.go` (+test; GET/PUT profile, GET difal+disclaimer, PUT difal/{uf}, GET/POST/DELETE scenarios, POST decompose, POST solve; WithCalc injector), `transport/http_handler.go` (ADDITIVE Register only — W1 untouched). **IC-04 matrix GREEN**: aliquota∉[0,35]⇒422 INVALID_RATE; preço≤0⇒422 INVALID_PRICE; UF⇒404 UF_NOT_FOUND; codprod⇒404 ITEM_NOT_FOUND; cenário⇒404 SCENARIO_NOT_FOUND; sem custo⇒200 SEM_CUSTO (custo null); inatingível⇒200 UNREACHABLE_TARGET; Δ≤0,049⇒200 no-persist. W1 route-preserved test GREEN; vet+integration typecheck clean. commit `2048cdb4` | done · orch-reviewed accepted; +F-plan-1; independent P4 queued |
| D9 | P3 | F01-S8 (OpenAPI /pricing/* + inline SDK [SDK-PRICING-M07] + composition wiring [ROOT-M07]) | **opus (orchestrator, self-impl)** | default | M07-plan F01-S8 (SECTION 0 C-C) | 5 files: `contracts/api/marketplace-central.openapi.yaml` (ADDITIVE 7 paths /pricing/profile·difal·difal/{uf}·scenarios·scenarios/{id}·decompose·solve + 12 schemas; /simulations preserved; exact IC-04 codes; unreachable=200+UNREACHABLE_TARGET), `packages/sdk-runtime/src/index.ts` (SDK-PRICING-M07 INSERT-only: 12 Pricing* types + 9 client methods getProfile/putProfile/listDifal/putDifalOverride/scenarios CRUD/decompose/solveTarget; existing PricingSimulation byte-identical; decimal strings; null components ADR-17), `packages/sdk-runtime/src/index.test.ts` (+IC-04 endpoint test — path/verb + SEM_CUSTO null + UNREACHABLE_TARGET), `apps/server_core/internal/composition/root.go` (ROOT-M07 additive: NewCalcRepository + costread shim over profitability FactReader + catalog-backed ProductChecker → NewCalcService via handler.WithCalc; gated on internalReadAvailable; W1 untouched), `pricing/adapters/catalog/product_checker.go` (NEW — 404 ITEM_NOT_FOUND vs 200 SEM_CUSTO split via ListProductsByIDs). **Verify ALL GREEN**: go build server_core ✓; go vet pricing+composition ✓; go test pricing/... ✓; go test composition/... ✓; market OpenAPI contract parity ✓; tsc sdk-runtime ✓; vitest 64/64 ✓; spec parses (9 pricing paths, 12 new schemas, 0 dangling $refs). OpenAPI+SDK same commit (invariant). commit `b41583b8` | done · **F-01 backend COMPLETE (S1–S8)** · SDK-PRICING-M07 surface published for F-02; orch-reviewed accepted; independent P4 (S1–S8) queued for quota reset |
| D10 | P3 | F02-S1 (/precos page shell + route rewire + AppRouter [APPTEST-M07]) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S1 | 5 files: `apps/web/src/pages/precos/{PricingPage.tsx,PricingPage.test.tsx,index.ts}` (shell: getPricingProfile + listCatalogProductFacts + pricingDecompose via useClient; selected-product/preço/modalidade state; regions decomposição·params-trigger·comparação·aplicar; deep-link ?params=1 read), `apps/web/src/routes/precos.tsx` (rewire legacy PricingSimulatorPage→PricingPage; IC-05 pkg survives), `apps/web/src/app/AppRouter.test.tsx` (APPTEST-M07: /precos case :93-97 only → "Preços & Simulador"; legacy redirect untouched). **web-vitest GREEN**: PricingPage 1 + AppRouter 18 = 19 passed. commit `d9d53d5` | done · orch-reviewed accepted; independent P4 queued |
| D11 | P3 | F02-S2 (per-component DecompositionPanel + MarginChip + unknown≠0/SEM_CUSTO) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S2 (C05) | 3 files: `apps/web/src/pages/precos/DecompositionPanel.tsx`(+test), `PricingPage.tsx` (F-plan-2 in-page wiring into region-decomposicao). Renders IC-04 breakdown (preço/−comissão/−taxa fixa/−frete/−imposto/−DIFAL/−tarifa Full/−custo ERP/retorno); unknown ⇒ UnknownValue "—" NEVER 0 (ADR-17); SEM_CUSTO blocking banner + no invented margin; MarginChip fed CalcProfile verde/amarelo thresholds (not legacy 20/10); reuses packages/ui (no fork). **web-vitest GREEN**: DecompositionPanel 5 + PricingPage 1 = 6 passed (combined S1+S2+AppRouter 24). commit `f2e200f` | done · orch-reviewed accepted; +F-plan-2; independent P4 queued |
| D12 | P3 | F02-S3 (Parâmetros + DIFAL drawers: deep-link, 27 UFs, disclaimer, recalc) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S3 (C02/C05) | 5 files: `apps/web/src/pages/precos/{ParamsDrawer.tsx(+test),DifalDrawer.tsx(+test)}` + `PricingPage.tsx`/`PricingPage.test.tsx` (F-plan-2 wiring: saveProfile+overrideDifal mutations invalidate profile/difal/decompose; ?params=1 deep-link opens drawer). ParamsDrawer: regime/alíquota(client-valid 0–35, blocks save)/limiares/tarifa Full/DIFAL toggle+27-UF destino select (BR_UFS). DifalDrawer: 27-row interna/inter./efetivo/override table, DIFAL_DISCLAIMER "seed padrão 2026 — não é orientação fiscal" on-surface, LoadingState/ErrorState canonical. **web-vitest GREEN**: ParamsDrawer 5 + DifalDrawer 5 + PricingPage 4 + AppRouter = 37 combined passed. commit `a987a09` | done · orch-reviewed accepted; independent P4 queued |
| D13 | P3 | F02-S4 (MarketComparison — IC-03 aggregates, honest unknown states) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S4 (C-comparação) | 4 files: `apps/web/src/pages/precos/{MarketComparison.tsx(+test)}` + `PricingPage.tsx`/`PricingPage.test.tsx` (F-plan-2 wiring region-comparacao; page test stubs child). Consumes IC-03 via standalone `createMarketPriceIntelClient` (useClient has NO market seam — confirmed; base URL resolved LOCALLY, decoupled from ClientContext so AppRouter.test's ClientContext mock doesn't break — see F-plan-3). Renders median/min_valid/n_offers/n_sellers/source/fetched_at; INSUFFICIENT_MARKET + NO_PRICE_EVIDENCE surface as-is with unknown marker, **never fabricates a price (ADR-17)**. **web-vitest GREEN**: MarketComparison 6 + precos + AppRouter 43 combined. commit `157a8e5` | done · orch-reviewed accepted; +F-plan-3; independent P4 queued |
| D14 | P3 | F02-S5 (Aplicar preço — price_update capped at previewed, no approve) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S5 (C-aplicar; contract L115/123/126) | 4 files: `apps/web/src/pages/precos/{ApplyPriceAction.tsx,ApplyPrice.test.tsx}` + `PricingPage.tsx`/`PricingPage.test.tsx` (F-plan-2 wiring region-aplicar; installation via listIntegrationInstallations, listing via listListingsByProduct). Stages `price_update` (intent `{new_price:{amount,currency:BRL}}`, selection `{mode:explicit,listing_ids:[…]}`) → createMutation → previewMutation → **STOPS at previewed; approve NEVER invoked; surface exposes NO approve/apply-to-ML control** (asserted). Result: state badge + /protocolos/:id link + "nada foi enviado ao Mercado Livre". No listing/price ⇒ disabled+honest hint. **web-vitest GREEN**: ApplyPrice 6 + precos + AppRouter 49 combined. commit `a9b4512` | done · orch-reviewed accepted; independent P4 queued |
| D15 | P3 | F02-S6 (feature-simulator retheme → shared MarginChip 18/10 + final web build) | **opus (orchestrator, self-impl)** | default | M07-plan F02-S6 | 1 file: `packages/feature-simulator/src/PricingSimulatorPage.tsx` — swap inline marginBg/marginColor (raw emerald/amber/red, 20/10) for shared `MarginChip` (18/10, IC-05 single band def) + margin text colors→design tokens (accent/amber/warn). IC-05 package stays alive (imported by AppRouter). **Final web build GREEN** (`npm run build --workspace @marketplace-central/web` ✓ built 1.85s) + **FULL web-vitest GREEN 260/260** (41 files). commit `3265353` | done · **F-02 frontend COMPLETE (S1–S6)** · orch-reviewed accepted; independent P4 (S1–S6) queued for quota reset |
| D17 | P6 | dual gate (cold Opus milestone-reviewer + adversarial-REFUTE sonnet) | opus + sonnet | default | milestone rubric + REFUTE prompt | **SPLIT → FAIL.** Adversarial sonnet: *could-not-refute* (9 invariants + fabricated-value hunt clean). Cold Opus milestone-reviewer: **VERDICT FAIL** — C05 "Tela Simulador completa" NOT MET, 3 blocking UI-completeness gaps vs feature.md Expected Output: **#1** no bidirectional solver (margem-alvo→preço; `pricingSolveTarget` never called in prod, only test mock — PricingPage.tsx:137-150 built only preço→margem); **#2** DIFAL toggle only mutates the PERSISTED global CalcProfile via putPricingProfile (ParamsDrawer.tsx:113-114 + PricingPage.tsx:90), violating "Toggle DIFAL é de SIMULAÇÃO (não muda config global)", and no "margem sem DIFAL" warning; **#3** no scenarios save/reload/delete UI (listPricingScenarios/create/delete only in mocks). All backend for the 3 exists+tested — pure FE gaps. Adjudication: cold-Opus FAIL authoritative on milestone completeness; the 6-slice F-02 plan under-covered the feature brief → remediate (not scope-creep: #1/#3 are the brief's own Expected Output). | done · **P6 FAIL** → remediation D18–D20 |
| D18 | P3(remediation) | F02-S7 (bidirectional solver UI) | **opus (orchestrator, self-impl)** | default | cold-Opus #1 + feature.md "Painel bidirecional (solver)" | 4 files: `apps/web/src/pages/precos/{SolverPanel.tsx,SolverPanel.test.tsx}` + `PricingPage.tsx`/`PricingPage.test.tsx` (region-solver wiring, F-plan-2). Margem-alvo input → `pricingSolveTarget({margem_alvo_pct,comissao_pct,modalidade,product_id})`; reached ⇒ suggested price; **reached=false ⇒ surfaces achievable `ceiling_pct` (UNREACHABLE_TARGET), NEVER fabricates a price (ADR-17)**; SEM_CUSTO blocks with named banner; error ⇒ ErrorState. RED (module-missing) → GREEN 6/6; page test stubs panel + asserts region-solver. commit `d1ef8ce` | done · closes cold-Opus #1 |
| D19 | P3(remediation) | F02-S8 (scenarios save/reload/delete UI) | **opus (orchestrator, self-impl)** | default | cold-Opus #3 + feature.md "Cenários: salvar/recarregar/excluir" | 4 files: `apps/web/src/pages/precos/{ScenariosPanel.tsx,ScenariosPanel.test.tsx}` + `PricingPage.tsx`/`PricingPage.test.tsx` (region-cenarios wiring). `listPricingScenarios`/`createPricingScenario`/`deletePricingScenario`; save snapshots the working sim payload {preco,modalidade,product_id}, id derived from name (re-save overwrites); reload re-applies payload to page state via callback; delete + list-invalidate; loading/empty/error states. RED → GREEN 6/6; page test stubs panel + asserts region-cenarios. commit `006aa0a` | done · closes cold-Opus #3 |
| D20 | P3(remediation) | F02-S9 (DIFAL-off warning — FE part of #2) | **opus (orchestrator, self-impl)** | default | cold-Opus #2 (FE-doable slice) | 2 files: `DecompositionPanel.tsx`(+test). When `profile.difal_enabled=false`, breakdown shows data-testid=`difal-off-warning` "Margem sem DIFAL — não use para decisão de venda interestadual."; hidden when DIFAL enabled. RED (1) → GREEN 7/7. commit `2ca0af6`. **NOTE:** the CONTRACT core of #2 (per-SIMULATION DIFAL toggle, not global) is NOT FE-fixable — `PricingCalcInput` (sdk-runtime/index.ts:1333) carries NO per-request DIFAL field; decompose/solve derive DIFAL solely from the persisted profile. A simulation-scoped toggle needs a backend + sdk-runtime contract extension, OUTSIDE the F-02 FE-only grant → see F-difal-1, escalated to hub. | done · closes FE part of #2; contract part escalated |
| D21 | P5(re-verify) | verification ladder (remediated tree, selective re-verify) | **opus (orchestrator, self-verify)** | default | profile §5 + PILOT | Clean tree @ `2ca0af6`. Diff since D16 = **FE-only** (apps/web/src/pages/precos/** + .mnfs) → Go lanes stay green (untouched since D9). **RAN @HEAD:** L0 `vite build apps/web` ✓ (built 2.67s); L1 full `vitest run apps/web` **186/186** (29 files) ✓ incl. new SolverPanel 6 + ScenariosPanel 6 + DecompositionPanel 7. tsc raw skipped per §2 allowlist. | done · **P5 re-verify GREEN** → re-gate P6 |
| D16 | P5 | verification ladder (clean state, selective re-verify PILOT) | **opus (orchestrator, self-verify)** | default | profile §5 ladder + PILOT | Clean tree @ `517ff7b`. Diff scope since F-01 close (`b41583b8`..HEAD) = **FE-only** (apps/web/{app,pages/precos,routes} + packages/feature-simulator/src + .mnfs) — **zero Go/backend/contracts touched** → PILOT: Go lanes verified at D9 stay green, not re-run wholesale. **RAN @HEAD:** L0 web build ✓ (built 2.36s); L1 full web-vitest **260/260** (41 files) ✓; hermetic Go confirm `go vet`+`go test ./modules/pricing/... ./composition/...` ALL ok ✓ (GOMODCACHE/GOCACHE worktree-local). L2 Go-`//go:build integration` RUN = **stack-gated** (needs live pg; chip never boots stack; host-port block F-tool-2) — backend integration hermetically validated via docker-exec psql at D6/D7, untouched since → **deferred to P7 live drive** on the running hub stack. tsc raw skipped per profile §2 allowlist (TS2688 node-types, CHIP-M03-owned; build+vitest are the FE verification of record). | done · **P5 GREEN** (FE ladder complete; Go-integration deferred to P7) |

## Findings log (for hub ratification, core §0)
- F-boot-1: chip-m07 worktree was NOT provisioned by launch flow (siblings m05/m08 had theirs). Session home `sleepy-wing-6d7500` is not a git worktree — resolves to the shared main checkout. Boot `git checkout -b` therefore transiently moved the shared main HEAD (reflog HEAD@{2}); hub recovered via D-45 (main@896c8659, clean). Self-resolved: created `.claude/worktrees/chip-m07-simulador` off 8b6c4b30 matching sibling convention. Recommend launch flow pre-provision chip worktrees. NON-BLOCKING — batch into first substantive event.
- F-quota-1 (SESSION-LIMIT WALL on sonnet subagents): during F01-S2's 2dp precision follow-up the sonnet implementer terminated on API error "You've hit your session limit · resets 6:30pm America/Sao_Paulo" mid-edit. Distinct from the codex-quota exhaustion (D-23). Impact: sonnet subagent dispatches unreliable until ~18:30 SP 2026-07-18. Mitigation this chip: orchestrator (Opus, alive) self-completes the highest-value/critical slices (S2 finish, S3 ic04-ports) directly under the sole-committer model; independent P4 review queued for when quota resets, still before P6 gate. Recommend hub confirm whether the session limit is account-wide (would also cap Opus soon) and pace dispatches accordingly. NON-BLOCKING for now.
- F-boot-2 (SHARED-INDEX COMMIT RACE): the chip worktree has ONE git index shared by the orchestrator process and every implementer subagent (subagents run git via Bash cwd inside the same worktree). During F01-S1: the S1 implementer `git add`ed its 2 write_set files; the orchestrator concurrently `git add`ed `M07-plan.md` and committed — the commit swept the implementer's already-staged files into the orchestrator's commit `bb0a276f` ("docs(m07): fold HUB v2 SDK-PRICING-M07…"). Net: decimal.go/decimal_test.go ARE correct + GREEN + in HEAD, but landed under a docs-labelled commit, not their own message/trailer. No history rewritten (prohibited + shared-worktree unsafe). **Mitigation adopted (this chip, effective S2):** ORCHESTRATOR IS SOLE COMMITTER — implementers write+test+report only, never touch the index; orchestrator serializes one clean commit per reviewed slice. Recommend hub ratify sole-committer for shared-index worktrees (or give each subagent its own worktree/index). NON-BLOCKING — batch into first substantive event (COMMITTED ic04-ports).
- F-scaffold-1 (PRE-EXISTING float64 MONEY IN pricing/ports — INVARIANT VIOLATION, relevant to M-08): the base scaffold at `8b6c4b30` already ships `pricing/ports/{fee_schedule.go,batch_ports.go}` typing money/percentages as `float64` (MarketplaceFees.CommissionPercent/FixedFeeAmount; BatchProduct.CostAmount/PriceAmount; FreightResult.Amount; etc.), contradicting the domain invariant "decimal money never float64". My IC-04 additions (`domain.Decompose`, `ports.CalcEngine`, `ports.DifalForUF`) are the correctly-typed decimal replacement and land ADDITIVELY (compile alongside the old scaffold). The float64 batch/fee ports are NOT rewired by S3 (outside slice scope; ripping them out now breaks dependent adapters/application). Recommend: the decimal engine is the single source of truth; M-08 must consume `ports.CalcEngine`/`ports.DifalForUF` (decimal strings), NOT the float64 batch scaffold; schedule the float64 scaffold retirement in a later M-07 slice (S6/S7 wiring) or an M-08-coordinated cleanup. NON-BLOCKING for S3 freeze.
- F-seed-tenant-1 (DESIGN, S5): 0057 seeds the 27 DIFAL rows for a SINGLE tenant (`tenant_default`, the pgdb config fallback) — a SQL migration can't enumerate dynamic tenant_ids. Fine for the single-tenant MVP demo, but FUTURE tenants won't get seed rows from this migration. Provisioning for new tenants is app-side (S6 repo seeds-on-first-read from domain.DifalSeed, same source). Recommend hub note this for post-demo multi-tenant work. NON-BLOCKING.
- F-tool-2 (OPERATIONAL, self-resolved, Windows): hermetic-pg via `docker run -p <host>:5432` fails on this host — `bind: An attempt was made to access a socket in a way forbidden` (Windows reserved/blocked port range, no admin). Workaround: publish NO host port and drive psql via `docker exec` in-container only (readiness = retry a real `SELECT 1`, not pg_isready per memory). Verified the S5 SQL this way. Recommend the profile's hermetic-pg lane use exec-only (or a container network) on Windows chips rather than host port publishing.
- F-plan-1 (PLAN REFINEMENT, S7, for hub ratification): the M07-plan F01-S7 write_set described the bidirectional decomposition/margem-alvo as an ADDITIVE extension of the existing W1 `POST /pricing/simulations` handler. Implemented instead as two DISTINCT routes `POST /pricing/decompose` + `POST /pricing/solve`, leaving `handleSimulations` (and its W1 float64 DTO + GET `{items:[...]}` shape) byte-identical. Rationale: (1) maximizes W1 regression safety (C03) — the W1 handler is untouched, not re-shaped; (2) 1:1 with the DISTINCT SDK methods the SAME plan lists for S8 (`decompose`/`solveTarget`), so OpenAPI+SDK map cleanly to one path each; (3) avoids overloading one POST with two response shapes. The "extended simulations" INTENT (expose decomposition + margem→preço direction over HTTP) is fully delivered. `http_handler.go` change is limited to ADDITIVE `Register` lines (guarded `if h.calc != nil`). Recommend hub ratify the route split; S8 OpenAPI will document `/pricing/decompose` + `/pricing/solve` accordingly. NON-BLOCKING.
- F-tool-1 (OPERATIONAL, orchestrator self-note): the Bash tool here is **Git Bash (POSIX sh)**, not PowerShell — PowerShell here-strings `@'…'@` passed to `git commit -m` are mis-parsed by bash (leading/trailing `@` leak into the message). First S3 commit `294f38f3` carried a stray `@` subject; amended pre-publish to `d288cd12` via `git commit -F <file>`. Rule for multi-line commit messages under the Bash tool: write the message to a file and use `git commit -F`, or a bash heredoc — never a PowerShell here-string. Self-resolved; recorded so later slices don't repeat it. NON-BLOCKING.
- F-env-1 (**BLOCKING** F-02 web lanes — worktree provisioning gap, hub-owned; extends F-boot-1): the chip worktree `.claude/worktrees/chip-m07-simulador` has **NO `node_modules`** (0 entries). Go lanes + `packages/sdk-runtime` vitest/tsc passed only via Node's UPWARD resolution to the main checkout's `node_modules` (`Documents/marketplace-central/node_modules`, 95 pkgs, complete). But `apps/web/vitest.config.ts` hardcodes `setupFiles: ["../../node_modules/@testing-library/jest-dom/dist/vitest.mjs"]` — a literal relative path to the **worktree-root** node_modules, which doesn't exist ⇒ every web-vitest suite fails to load (`Cannot find module …/jest-dom/dist/vitest.mjs`), and `tsc -p apps/web` floods with missing-ambient-type errors (`toBeInTheDocument`, `import.meta.env`) — all env artifacts, NOT code defects. Impact: F-02's entire validation surface (web-vitest S1–S6, web-build S6) cannot RUN; F01-S8 web SDK RUN also can't be exercised from web here. F-01 backend + sdk-runtime remain fully GREEN (unaffected). **Chips never install deps (doctrine) → REQUEST to hub** to provision the worktree's node_modules (repo-root `npm ci`, or a symlink `node_modules → ../../../node_modules` to the already-installed main checkout — same lockfile, zero fetch). Blocks all F-02 commits (commit-per-GREEN). REQUEST event sent to HUB v2. **BLOCKING F-02.**
- **F-env-1 — RESOLVED / RECLASSIFIED (self-inflicted, NOT a hub-owned gap): the fix was MY own profile §3 bootstrap, not a REQUEST.** Profile §3 "Fresh-workspace bootstrap" (ratified 2026-07-16, CHIP-SAT) already binds it verbatim: *"fresh chip worktrees have no `node_modules`; run `npm ci` at the worktree root before web `tsc`/`vitest` lanes. Lockfile-faithful install = env prep, NOT a dep change — no REQUEST needed."* And it EXPLICITLY forbids the symlink workaround F-env-1 floated: *"Never reuse another checkout's `node_modules`: npm workspace symlinks would resolve workspace packages … to the OTHER tree's sources, silently validating the wrong code."* Root cause of the false blocker: I ran the Go half of §3 bootstrap (gomodcache warm) but skipped the Node half, then misread the general "chips never install deps" rule as covering `npm ci` — the profile's ratified clause overrides it (npm ci = env prep, mirror of the gomodcache clause; harness-worker rule 6 makes running §3 bootstrap a REQUIRED first act). **Resolution:** ran `npm ci` at worktree root (157 pkgs, 23s); workspace symlinks under `node_modules/@marketplace-central/*` verified pointing to THIS worktree's packages (not another tree). F-02 web lanes now RUN and are GREEN (S1 19, S2 6). The HUB v2 REQUEST for node_modules provisioning is **withdrawn/moot** — no hub action needed. Lesson (self): on a fresh worktree, run BOTH halves of profile §3 (gomodcache + `npm ci`) before declaring any web-lane blocker. NON-BLOCKING — corrected.
- F-plan-2 (PLAN REFINEMENT, F-02, for hub ratification): the M07-plan write_sets for F02-S2..S5 create child components (DecompositionPanel, ParamsDrawer, DifalDrawer, MarketComparison, ApplyPriceAction) but do NOT list `apps/web/src/pages/precos/PricingPage.tsx` as an edited target — so as written the components would never render in the page and C05/C06 (the page must actually SHOW the decomposition / market / aplicar surfaces) could not be met. Resolution: each component slice performs a minimal ADDITIVE in-page wiring edit to PricingPage.tsx (the shell region S1 established as the integration seam), committed within that slice, keeping the /precos page working+demoable at every slice boundary (demo T-2 days) rather than a big-bang integration at S6. PricingPage.tsx is inside the F-02 granted paths (`apps/web/src/pages/precos/**`); no cross-owner collision; single-writer (orchestrator sole committer). First applied in D11 (S2 → region-decomposicao). Recommend hub ratify per-slice in-page wiring as the intended integration model for F02-S3..S5. NON-BLOCKING.
- F-plan-3 (ARCH NOTE, F-02 S4, for hub ratification): the app's `useClient()` (ClientContext) exposes ONLY the central `createMarketplaceCentralClient` — there is **no market-price-intel seam** on it, and NO existing FE consumer of `market.ts`/`createMarketPriceIntelClient` anywhere in `apps/web` or `feature-simulator` (grep-confirmed). Since `apps/web/src/app/**` is a **forbidden path** (feature.md F-02 Ownership), MarketComparison cannot add the market client to ClientContext. Resolution: MarketComparison instantiates its own `createMarketPriceIntelClient` (IC-03 is HTTP-only — no Go port), with the base-URL resolver **inlined locally** (a 4-line mirror of ClientContext's `resolveApiBaseUrl`) rather than imported from ClientContext. Reason the import was rejected: `AppRouter.test.tsx` `vi.mock("./ClientContext", …)` returns only `useClient`, so importing `resolveApiBaseUrl` from that module throws "No export defined on the mock" at render (and AppRouter.test is edit-frozen outside the APPTEST-M07 :93-97 grant). Local resolver = decoupled, no app/** dependency, testable via an injected `client` prop. Recommend hub note the missing market seam on ClientContext as post-demo cleanup (a proper `useMarketClient()` in app/** once that path reopens). NON-BLOCKING.
- F-difal-1 (**CONTRACT GAP**, F-02 S9, ESCALATED to hub): the P6 cold-Opus finding #2 asks the DIFAL on/off toggle to be a **per-SIMULATION** control ("Toggle DIFAL é de SIMULAÇÃO — não muda config global"), but the IC-04 contract has no per-simulation DIFAL lever: `PricingCalcInput` (packages/sdk-runtime/src/index.ts:1333) exposes only `preco?/margem_alvo_pct?/comissao_pct/modalidade/tarifa_full?/frete_produto?/custo?/product_id?` — **no `difal_enabled`/`difal_destino_uf` per request**. `pricingDecompose`/`pricingSolveTarget` derive DIFAL solely from the PERSISTED profile (get/putPricingProfile). So the ONLY DIFAL control the contract exposes is the global profile flag — exactly what ParamsDrawer drives. A true simulation-scoped toggle requires: (a) add optional `difal_enabled`/`difal_destino_uf` to `PricingCalcInput` + the OpenAPI request schema (SDK + contract, one commit), and (b) backend decompose/solve honoring the per-request override instead of the profile. Both are OUTSIDE the F-02 FE-only grant (backend + sdk-runtime are forbidden paths for this feature). **FE-doable part delivered** (D20: "margem sem DIFAL" warning). Escalated to HUB v2 (local_7c41fdd8) for a ruling: **(A)** accept global-only DIFAL + the D20 warning for the 2026-07-20 demo (toggle stays in Parâmetros, mutating global — honest + shipped), or **(B)** grant a small backend+SDK contract-extension slice (new owner or reopened grant) to make DIFAL per-simulation. Recommend (A) for the demo, (B) as post-demo cleanup. BLOCKING only if hub rules (B) mandatory before CLOSE. See D17/D20.
- F-listing-1 (DEMO WIRING, F-02 S5): "aplicar preço" needs an ML `listing_id` for the `price_update` selection, but the S1 /precos picker is built on `listCatalogProductFacts` (products, no listing linkage). Resolved in-page: PricingPage resolves the listing for the selected product via `listListingsByProduct({installation_id, product_id})` (SDK method already present) and the installation via app-wide `listIntegrationInstallations` — no picker rebuild, no new backend. If the product has no linked anúncio, ApplyPriceAction disables with an honest hint (truthful, no fabricated target). For the live P7 drive, select a product WITH a linked listing (demo 90001 ↔ MLB3758134295 per provisioned data). NON-BLOCKING.
