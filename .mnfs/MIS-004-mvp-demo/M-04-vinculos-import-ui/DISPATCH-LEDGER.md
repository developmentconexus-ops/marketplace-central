# M-04 vinculos-import-ui — Dispatch Ledger

Chip: CHIP-M04 · milestone branch `chip/m04-vinculos-import-ui` · base SHA `28b8447c82fc05f626d3e42404fa3267cc82e953`
Milestone worktree (all commits/dispatches/lanes/evidence): `C:\Users\leandro.theodoro\Documents\marketplace-central\.claude\worktrees\chip-m04-vinculos-import-ui` (hub-prepared; D-correction 2026-07-18). Orchestrator session cwd is scaffold `hungry-bell-ddf2a3` — every command cd's into the milestone worktree (PowerShell resets cwd between calls).
Codex binary: `C:\Users\leandro.theodoro\.codex\plugins\.plugin-appserver\codex.exe` (0.145.0-alpha.18, per profile §3 F-ENV-9)
Scratchpad (active session): `C:\Users\LEANDR~1.THE\AppData\Local\Temp\claude\C--Users-leandro-theodoro-Documents-marketplace-central--claude-worktrees-hungry-bell-ddf2a3\6631ff9c-7132-4829-b19e-f9d73eab1cbe\scratchpad`
(prior session 2ec05736 died mid-D01 with no output — re-dispatched here.)

| # | role | model/effort | path | prompt | output | status |
|---|---|---|---|---|---|---|
| D01 | investigator (baseline read: product_links, OpenAPI, SDK, FE seams) | gpt-5.6-luna / medium | OS-process (re-dispatch; prior companion attempt died with session) | `scratchpad/prompt-d01-investigator.md` | `evidence/d01-investigator-baseline.md` | COMPLETED exit 0 2026-07-18 |
| D02 | investigator (gap-closer: SDK gen-vs-handauthored + new-domain-client recipe, matcher/anchor data feasibility, FE tab/modal/toast pattern + web-query invalidation) | gpt-5.6-luna / medium | OS-process | `scratchpad/prompt-d02-investigator.md` | — | **FAILED exit 1 (bvhk0fs5c): codex USAGE LIMIT exhausted until Jul-25 — post-demo. F-ENV finding.** |
| D02b | investigator (same prompt as D02) — Claude fallback (codex quota down) | Claude sonnet subagent | Agent tool SYNC | `scratchpad/prompt-d02-investigator.md` | `evidence/d02-investigator-gaps.md` | pending BLOCKED ruling on Claude-fallback lane; read-only so proceeding |
| P2 | batch planner (plan-only; slice cards S1-S9, write-sets, contract-satisfiability IC-01-A2/IC-05, verification map C01-C05, DAG, migration ledger, S1 fixtures) — Claude-only lane D-23 | **cold Opus** subagent (Plan agent, read-only) | Agent tool SYNC | inline plan-only prompt (inputs: planning-readiness-M04.md + d01/d02 + ic01-amended-A2.md + milestone/feature/validation-contract) | `PLAN-M04.md` | COMPLETED 2026-07-18 · open_questions EMPTY = dispatch-ready. Plan agent read-only ⇒ orchestrator persisted verbatim. Key reconciliations: SDK-01 inline index.ts (productLinks.ts retired), routes `/product-links/link-resolutions/*` (live prefix), migrations 0065/0066/0067, undo single+batch, single-worktree ⇒ serialize implementers. |

## Claude-only contingency lane — RATIFIED by hub (D-23, 2026-07-18)

Hub ruling on F-ENV-M04-1. Profile §12 tail "Contingency lane — codex quota outage" (commits 60da30b4 + 4d1a7cfb on main; do NOT pull — lane text is operative binding). Expiry = codex return 2026-07-25 OR MIS-004 demo close, whichever first.

| Role | Contingency binding |
|---|---|
| P2 planner | cold Opus SUBAGENT (fresh context, plan-only; sonnet fallback if Opus limits bite). Plan outputs unchanged (write-sets, contract-satisfiability vs IC-01/IC-05, verification map). |
| Implementer | sonnet subagent (anti-slop contract + slice cards unchanged). |
| Slice reviewer | independent sonnet; implementer ≠ reviewer. |
| P6 dual gate | cold Opus + INDEPENDENT second sonnet, separate dispatch, ADVERSARIAL-REFUTE prompt (replaces GPT side; refutation framing MANDATORY — compensates lost cross-vendor diversity). Agreement/reconciliation rules unchanged. |
| Investigator | sonnet (haiku ok trivial greps). |

RESOLVED by hub 2026-07-18 (main commit 0937621b) — all 4 open questions closed, P2 UNBLOCKED:
- **Finding A → GRANTED (D-24, lock SDK-01):** temporary ADDITIVE-ONLY lock on `packages/sdk-runtime/src/index.ts`, scoped to product-links block. New batch-preview/batch/undo methods + enriched `ProductLinkCandidateItem` (confidence/band/reasons) inline adjacent to existing 7-method block (:1508-1545, types :850-949). ZERO edits/reformat of other domains. Lock releases at CLOSED; CLOSED payload MUST call out the index.ts diff. `productLinks.ts` new-file grant RETIRED. M-02 F-04 may also touch index.ts additively (market block) — disjoint regions, hub resolves textual merge conflict.
- **Finding B → RATIFIED (D-25, IC-01 Amendment A2):** written into `.mnfs/MIS-004-mvp-demo/research/identity-matching-interface-contract.md` @ 0937621b = verdade única (S1 fixtures read the FILE, not summaries). Anchors cross-side = seller_sku(exact→codprod, ACCEPT-grade ONLY in concordant pair) + ean(corroboration, unproved flag). seller_sku ALONE = MEDIA/REVIEW ("EAN ausente ⇒ max REVIEW" binding). Auto-ACCEPT/ALTA = seller_sku+ean concordant same codprod, no hard-neg. Title-only=BAIXA. SKU/EAN conflict or title hard-neg = cap BAIXA+AGAINST. marca/refforn = UNAVAILABLE in reasons[] (ADR-17). Thresholds 85/50 unchanged. F-01 authors ≥8 own fixtures incl Doka hard-negative. M-02 propagation hub-side (no M-02 change).
- Finding C: page-local tabs/modal/toast + page-local query keys — sound, no ruling needed.
- **ROOT-M04 → GRANTED (D-29, main 1a186ac5):** temporary ADDITIVE-ONLY lock on `composition/root.go` — (a) imports block additive lines, (b) product_links wiring region :391-467; swap :467 to `NewHandlerWithBatchPreview` (existing NewHandler untouched, orders precedent). Zero edits to other modules' wiring. Covers S2+S3+S4. Hub itself edits root.go ~:345 (market/D-21) post-M-02-merge — disjoint, hub-resolved at merge, do NOT rebase around it. Released at CLOSED; root.go diff called out in CLOSED payload.

## Slice execution log (F-01 → F-02, serial; Claude-only lane)

| slice | implementer | reviewer(s) | verdict | commit | notes |
|---|---|---|---|---|---|
| S1 candidate enrichment (COMPLEX) | sonnet subagent a031c1d1 | sonnet caveman-reviewer ×2 (1st died session-limit partial; 2nd full REJECT; re-review ACCEPT) | **ACCEPT** | `2d9e0b04` | REJECT round: medida/dimensão hard-neg missing (A2 needs all 4 kit/cor/medida/voltagem). Fixed + 10th fixture. 10 A2 fixtures + 7 prior green. runner_test 44→45. go.work.sum excluded (pre-existing bootstrap drift). |
| S2 batch-preview dry-run (STANDARD) | sonnet subagent a8e51134 | sonnet caveman-reviewer abdc69a0 | **ACCEPT** | `377ce810` | PreviewBatch + reusable evaluateApproval (read-only, zero-persist proven), additive NewHandlerWithBatchPreview. Nit (405 log action) folded into wiring pass. |
| S3 batch-apply + audit (COMPLEX) | sonnet subagent a6678ef6 | sonnet caveman-reviewer a7efc167 | **ACCEPT** (ADR-17 fix verified) | `377ce810` | Partial-failure non-transactional, reuses ResolutionService.ApproveCandidate (no rule dup), ZERO provider/envelope write. Migrations 0066 CREATE product_link_batches + 0067 audit ADD batch_id; runner_test 45→47. batch_id threaded additively. ADR-17 fix: installation_id from first VALID item, all-failed persists no row (2 new invariant tests). |
| WIRE root.go (ROOT-M04) | sonnet subagent aeca34ad | (build-verified) | **green** | `377ce810` | Constructed BatchService (reused productLinkCandidateRepo + productLinkResolutionSvc as CandidateApprover, Now=time.Now) + swapped :467 → NewHandlerWithBatchPreview. +7/-1, in-region, no import churn. `go build ./internal/composition/...` green. |
| S4 undo single+batch (COMPLEX) | sonnet subagent a07dd555 | sonnet caveman-reviewer ab0b1191 | **ACCEPT** | `bef3e585` | Append-only reversal derived from audit chain (no new migration). NOT_FOUND→404, SUPERSEDED/ALREADY_UNDONE→409 ordering deterministic (LatestAuditForIdentity). 3 tenant-scoped audit reads, ZERO provider write. Widened LinkWorkflowResolver additively → piggybacks already-wired productLinkResolutionSvc, **zero root.go edit** (orchestrator verified `go build ./internal/composition/...` EXIT:0). 5 undo tests; runner_test unchanged 47. |
| **S5 OpenAPI + SDK inline (SDK-01) — F-01→F-02 GATE** | sonnet subagent ac28002b | sonnet caveman-reviewer afb3bbac | **ACCEPT** (0 findings) | `28f6954a` | Additive mirror of final Go JSON shapes. OpenAPI +271 (enriched ProductLinkCandidate + 4 batch/undo paths + schemas, 422/404/409); SDK index.ts +66 (SDK-01 block: enriched ProductLinkCandidateItem + ProductLinkReason/band/status types + 4 methods, snake_case passthrough). Enums byte-exact vs Go. ALREADY_RESOLVED = per-item 200-body cause (not 409) — confirmed. `tsc --noEmit` clean; +337/-0. **SDK-01 diff region → call out in CLOSED payload.** GATE PASSED ⇒ F-01 done (S1-S5), F-02 unblocked. |

**F-01 COMPLETE** (S1-S5 committed+reviewed). Gate at S5 (`28f6954a`) unblocks F-02.

### F-02 (frontend) slice log

| slice | implementer | reviewer(s) | verdict | commit | notes |
|---|---|---|---|---|---|
| S6 /vinculos scaffold (STANDARD) | sonnet subagent ae57c4ae | sonnet caveman-reviewer abfa4a7e | **ACCEPT** (after rework) | `25ccd1c8` | Scaffold: Fila/Resolvidos tabs, 3 KPIs wired to listProductLinkCandidates/listProductLinkWorkflows, Loading/Error/Empty, S7-S9 seams only, page-local query keys. 🔴 (stale `/vinculos` assertion in hub-owned `AppRouter.test.tsx:66-71`) → **APPTEST-M04 GRANTED (D-32, main 0cbbba1a)**; orchestrator applied narrow swap (assert `tab "Fila"`, keep 1× install-fetch). 🟡 (Resolvidos count unfiltered) → implementer rework: shared `isResolved` predicate, +count-semantics test. 22/22 vitest green (AppRouter 18 + VinculosPage 4); new src 0-err tsc. **APPTEST-M04 + AppRouter.test.tsx diff → call out in CLOSED payload.** |
| S7 queue rows + drawer + individual resolve (STANDARD) | sonnet subagent a19f616f (stalled once on a sub-agent; resumed to implement directly) | sonnet caveman-reviewer a3b534d5 | **ACCEPT** (0 findings) | `82ed2176` | QueueTab/QueueRow/VinculoDrawer/useVinculosQueue. SDK-only (list/approve/reject/manual). **Invalidation plan-gap resolved:** onSettled invalidates PAGE-LOCAL `['product-links']` (linkage-ns `invalidateAfterMutation` alone would NOT refetch queue). IC-01 motivo-with-% chips, honest NO_CANDIDATE + absent-price UnknownValue (ADR-17), 409 ALREADY_RESOLVED banner+refetch, `?candidate=` deep-link drawer (width 360). 8/8 vitest, new src 0-err tsc, diff confined to pages/vinculos/. |
| S8 bulk select→preview→apply→feedback (COMPLEX) | sonnet subagent a10e7ef2 | sonnet caveman-reviewer ab0d6b4c | **ACCEPT** (0 findings) | `df062cd1` | BulkBar/BatchPreviewModal/BatchResultFeedback/useVinculosBatch. Pure dry-run (previewProductLinkBatch, NO write on modal open — proven by test). Apply itemizes applied[]+failed[]{candidate_id,cause}, "prosseguir só com válidos" applies valid subset, no optimistic mutation (refetch after 2xx). Reuses WEB_OPERATOR_ACTOR (real actor). Page-local `['product-links']` invalidate onSettled + cross-domain onSuccess. Selection cleared post-enqueue. 11/11 vitest (incl S6/S7 regress), new src 0-err tsc, diff pages/vinculos/ only. |
| S9 Importação (protocol read-only) | sonnet subagent a811c5e0 | sonnet caveman-reviewer a164105d | **ACCEPT** (0 findings) | `e8ce155b` | **ERPSDK-M04 GRANTED (D-35, main 85c825e7)** — 2 additive SDK methods listErpImports/getErpImport (reuse getJson + erpImport types, encodeURIComponent). Page-local useErpImports + ImportacaoSection (read-only history: protocolo, status COMPLETED/REJECTED, accepted/rejected/warning counts, expandable rejected_rows report; NO upload UI; ADR-17 rejections visible). 14/14 vitest, sdk-runtime tsc 0-err + new web src 0-err. SDK diff within grant (2 methods only). **ERPSDK-M04 diff → CLOSED payload alongside SDK-01.** |

**F-02 COMPLETE** (S6-S9 committed+reviewed). **ALL SLICES S1-S9 DONE.** Additive locks consumed: SDK-01 (product-links block), ROOT-M04 (root.go wiring), APPTEST-M04 (AppRouter.test.tsx /vinculos case), ERPSDK-M04 (2 erp SDK methods) — all to be called out in the CLOSED payload. Next: P5 ladder → P6 dual gate → P7 QA live-drive → P8 CLOSED.

## P5 — full-milestone verification ladder (2026-07-18) ✅
- L0 build EXIT 0 (product_links + composition + erp_import); L1 vet EXIT 0; L2 go test EXIT 0 (product_links + migrate, runner_test **47**); L3 sdk-runtime tsc EXIT 0; L3 web vitest **32/32** (vinculos 14 + AppRouter 18).
- **C05 diff-confinement verified**: full diff `28b8447c..HEAD` touches ONLY owned paths + granted regions (root.go=ROOT-M04, AppRouter.test.tsx=APPTEST-M04, index.ts=SDK-01+ERPSDK-M04, runner_test bump, migrations 0065/0066/0067, .mnfs/M-04). go.work.sum NOT committed. Zero forbidden-path touch.

## P6 — dual gate (D-23 Claude-only lane: cold Opus + adversarial-refute sonnet) — 2026-07-18
- **Round 1: BOTH gates AGREE → FAIL.** Independently found the SAME sole blocker: `confidence` double-scaled — QueueRow.tsx + VinculoDrawer.tsx did `Math.round(confidence * 100)` on an already-0-100 integer (OpenAPI + Go domain contract) → real ALTA candidate rendered **"9500%"**. Masked by fractional test fixtures (0.95) that the real API never emits (vacuous verification). Adversarial lane earned its keep (hub tip: hunt contract-clause deviations). Both gates explicitly CLEARED all other surfaces (A2 scoring 10 fixtures, enum byte-exactness, undo ordering, ADR-17 honesty, zero provider writes, ownership, invalidation).
- **Fix `b5bcbdb5`**: drop `* 100` (both components), correct fixtures to integer 0-100, add regression guard asserting confidence:95 → "95%" (not "9500%"/"0.95%"). 32/32 vitest, edited src 0-err tsc, 4 files only.
- **Round 2: BOTH gates AGREE → PASS.** Adversarial refuter re-verified `b5bcbdb5`: double-scale gone in both components, no new scaling/under-scaling, band-color untouched, fixtures now integer-contract-correct, regression guard genuine (95→"95%", not "9500%"), 32/32 vitest, fix confined to 4 files (zero backend/OpenAPI/SDK ripple) → **BLOCKER CLEARED**. Cold-Opus gate's only FAIL reason was this same bug (backend rated strong: A2 faithful, enums exact, undo deterministic, ADR-17 holds, zero provider writes, ownership clean). **P6 dual gate = PASS (agreement).**

## P7 — fresh QA live-drive — 2026-07-18
- Stack REQUEST sent to hub (:5174 compose re-point to chip worktree + migrations 0065-0067, D-17 pattern; hub pre-offered). Chip never boots servers. QA (fresh qa-validator) live-drives once stack up: C03 tela flow (bands+anchor chips motivo-with-%, NO_CANDIDATE honest, approve⇒leaves-queue, bulk preview→apply→Resolvidos no F5, deep-link+F5), C04 Importação (protocol+status+counts+rejection report), C01/C02 batch dry-run + apply/undo audit. Confidence chip shows "95%" not "9500%" (P6 fix live check).

### P7 STACK UP (hub, 2026-07-18) + live-probe findings
Hub brought stack up (backend :8080 healthy, frontend :5174=200, shared dev PG w/ M-01/M-02 data, migrations 0065-0067 applied, `MPC_PROVIDER_WRITES_ENABLED` unset ⇒ dispatcher OFF). No fixed seed id — discover via GET.
- **installation_id discovered** (GET /integrations/installations): `inst-mercado_livre-d373dc64-577f-4950-b11b-b90244f30cb2` (Mercado Livre, METALNOBREACABAMENTOS, connected/healthy).
- **ERP imports present** (GET /erp/imports): 2 imports — `#002-E` (COMPLETED, accepted 3 / rejected 1 / warning 2 → good C04 rejection-report fixture) + `#001-E` (accepted 55). Feeds C04.
- **⚠️ FINDING P7-A — stale enrichment on stored candidates.** GET /product-links/link-candidates returned all candidates with `confidence:0, confidence_band:"", match_status:"", reasons:[]`. Root cause: candidates stored `created_at` 2026-07-11 — PRE-DATE M-04 scoring. Enrichment is computed at GENERATION time (generation_service.go:356-428, IC-01-A2 tiers) + stored via repo upsert (link_candidate_repo.go), NOT read-time. Confirmed generation is snapshot+internal-matcher only, **ZERO ML fetch** (generation_service.go:75 ListListingSnapshots + matcher.FindProductsForLinking). Regeneration = internal write to my-owned `product_link_candidate` (same write-class as approve/undo QA must exercise). POST /product-links/link-candidates/generations{installation_id,limit:50} → generated_count 34, enrichment now POPULATED (band/status/reasons non-empty).
- **⚠️ FINDING P7-B — post-regen ALL 34 = NO_CANDIDATE (UNDER INVESTIGATION, may be BLOCKER).** Every regenerated candidate: `NO_CANDIDATE / BAIXA / reasons ×4 all UNAVAILABLE` — matcher (internal_read.FindProductsForLinking) returned ZERO internal products for every listing. But pre-regen those same listings had `internal_product_id` set (MLB3758134295→20317 "PLACA ADES.CALANDRADA DIRETORIA", state exact_ean, EAN 7898016503522). Distinguishing (A) DATA GAP — live-wired internal_read reader (fake/oracle/postgres) has no catalog in dev DB ⇒ hub REQUEST, not a defect — vs (B) MATCHER DEFECT — wired to populated source but query logic drops EAN/anchor ⇒ milestone FAIL. **A/B investigator dispatched (Explore, read-only, agentId held).** QA verdict HELD pending A/B.

### P7-B RESOLVED = (A) DATA GAP, not a defect (investigator, file:line evidence)
Live server runs `MC_ERP_SOURCE=xlsx` → matcher = xlsx/erp_import reader (`root.go:409-419`), which DOES match on EAN (`erp_import/adapters/internalread/reader.go:274`). Dev DB's ERP snapshot holds ONLY synthetic fixtures (products 2002-2004); `GET /catalog/products/20317`→not-found, `?q=PLACA`→empty. Connected ML listings don't overlap synthetic catalog on EAN/seller_sku ⇒ all-NO_CANDIDATE is the HONEST correct output (ADR-17). Pre-regen internal_product_id=20317 was persisted from a different fully-seeded env. **M-04 code SOUND** (matcher/scoring/enrichment correct). Auto-match happy-path + band spectrum need overlapping demo data = mission demo-prep (erp_import, not M-04 ownership).

### P7 HUB RULING = option (2) SCOPED-QA-NOW (hub, 2026-07-18)
Ratified scope: screen renders; approve/reject/batch-preview/batch/undo contract behavior; NO_CANDIDATE honesty live; C04 import history+rejection report; resolve/undo mechanics via manual-resolve→synthetic 2002; confidence chip "95%"-not-"9500%". C01 band spectrum = unit fixtures + P6 gate as evidence of record (live visual re-confirm folded into hub demo-prep QA once overlapping data loads = condition on DEMO not M-04). qa-validator PASS on that scope = M-04 QA PASS = closeable. Provision-first (overlapping ERP snapshot) = hub demo-prep task. Candidate-regen mutation ACK'd/sanctioned retroactively (my-owned surface, zero ML fetch).

### P7 EXECUTION — split live-drive, INTERRUPTED by stack-down
- qa-validator (fresh, API-sweep half) dispatched → **DIED on Anthropic session limit** (resets ~15:10 São Paulo) mid-sweep. Confirmed product 2002 exists; flagged `/catalog/products/{id}` single-GET = 500 for all ids (out of M-04 scope, noted).
- **PART A UI render (main-thread browser :5174/vinculos, read_page DOM) = PASS.** Evidence `evidence/p7-qa-live-drive.md`. Vínculos heading + KPI Resumo (3 tiles) + Fila/Resolvidos tabs + queue table render. NO_CANDIDATE honesty live-proven: "Sem candidato" pill + honest "nenhuma correspondência" + 4 UNAVAILABLE anchor chips + Confiança "sem confiança sem candidato" (honest UnknownValue, no fake 0%/NaN; P6 double-scale structurally absent — honest text). Abrir/Aprovar per-row present.
- **PART B API contract sweep = NOT RUN.** Backend :8080 + frontend :5174 went HTTP:000 (curl exit 7) before sweep fired. `docker ps` = NO marketplace-central containers; `metaldocs-*` stack (api :8081, pg :5433) now occupies docker = machine/docker contention reclaimed hub stack. Chip can't restart (hub seam). Sweep script ready (`scratchpad/p7-sweep.ps1`).
- **REQUEST sent to hub**: restore marketplace-central :8080/:5174 (note metaldocs contention). PART B fires immediately on healthz=ok. If account limits still bite, run PART B self + hub adjudicates verdict-independence, or wait for reset (hub's call).
- **QA VERDICT: HELD.** PART A PASS; PART B pending stack restore + independent review. No CLOSED until QA PASS.

### P7 STACK RESTORED (hub) + PART B + FINAL VERDICT = PASS (2026-07-18)
Hub root-caused stack-down = docker daemon/WSL restart (all non-metaldocs containers exit 255 same ts; no port theft, metaldocs binds :8081/:5433/:6379 disjoint). Containers REUSED (same bind mounts → still serve chip worktree; PG :5435 volume intact, migrations 0065-0067 present, no re-migrate). Verified :8080 healthz=200, :5174=200, candidates intact.
- **Fresh cold qa-validator (ab28a5c8, post-limit-reset) — PART B live sweep + PART A audit → VERDICT = PASS.** Evidence: `evidence/p7-qa-api-sweep.md` + `evidence/p7-qa-verdict.md` (+ PART A `p7-qa-live-drive.md` cross-checked). Zero-ML-writes preserved (dispatcher OFF).
- Per-item PASS: screen renders; NO_CANDIDATE honesty (34 items, confidence INTEGER, reasons{anchor,direction,detail}, zero fabricated ACCEPT; anchors match PART A 1:1); manual-resolve→2002 leaves-queue (clean resolve→undo→none on MLB3758134297); undo audit chain (reversal 200 append-only, SUPERSEDED 409, ALREADY_UNDONE 409, AUDIT_NOT_FOUND 404); batch-preview dry-run FAILED+cause snapshot-UNCHANGED (zero-persist); batch-apply applied=[]/failed[] honest cause/batch_id="" no-audit-all-failed (ADR-17); empty-batch 422 BATCH_APPROVALS_REQUIRED; approve-on-NO_CANDIDATE honest refusal 400 CANDIDATE_NOT_RESOLVABLE; C04 #001-E 55/0/0 + #002-E 3/1/2 + rejected_rows[row/code/detail]+warnings.
- **3 repo-truth reconciliations (prompt assumption < OpenAPI/source — NOT defects):** (a) manual-resolve→999999 = **200 not 404** — OpenAPI declares only 200; `ValidateInternalProductID` (domain/internal_product_id.go) checks positivity only (id<=0), catalog-existence NOT validated BY DESIGN (operator override; UI product-picker mitigates). Non-blocking observation. (b) approve-on-NO_CANDIDATE = **400 CANDIDATE_NOT_RESOLVABLE** not 422 (422 declared only empty-batch) — transport nuance, honest. (c) idempotent re-resolve = 200 no `cause` (per-item cause is batch-only semantics) — honest.
- **Residual live-state (benign, demo-prep for hub):** MLB3758134295 left `current_link=resolved→2002` (synthetic); transient 999999 write reversed; full unresolve-to-pending unreachable via API (superseded original audit + no unresolve endpoint by design). Hub resets pre-demo via candidate regen / DB (hub owns demo-prep + will load overlapping ERP anyway). MLB3758134297 = clean.
- **P7 = PASS.** ⇒ milestone closeable. Proceed P8 CLOSED.

### F-ENV-M04-3 / ERPSDK-M04 REQUEST (2026-07-18)
S9 card assumed ERP-import SDK client methods existed — they don't (only types in `erpImport.ts`). Backend `GET /erp/imports` (no params, tenant-scoped) + `GET /erp/imports/{id}` present. Requested narrow additive grant to add `listErpImports`/`getErpImport` to index.ts (erp block, reuse `getJson<ErpImportList|ErpImportDetail>`), outside product-links SDK-01 region. Awaiting hub ruling (grant to me OR other writer).

### F-ENV-M04-2 / APPTEST-M04 REQUEST (2026-07-18)
S6 placeholder-swap (granted `routes/vinculos.tsx`) breaks stale assertion in hub-owned `apps/web/src/app/AppRouter.test.tsx` (`it("mounts the em-construção stub at /vinculos")` :66-71). Redirect `it.each` row `["/product-links","/vinculos"]` :101 asserts pathname only — stays green. Requested narrow grant to retitle+swap that ONE case to assert the scaffold (`findByRole("tab",{name:"Fila"})`); keep single-install-fetch expectation. Awaiting hub ruling (grant to me OR hub edits).

## F-ENV-M04-1 (2026-07-18) — codex quota exhausted pre-demo
codex-cli usage limit hit after D01; resets Jul-25 (post-demo 2026-07-20). Blocks all codex roles (planner Sol-medium, implementers Luna-high/Sol-low, dual-gate GPT side). Mission-wide (all wave-B chips). BLOCKED sent to hub for ruling on Claude-only execution lane. Sanctioned already: sonnet fallback IMPLEMENTER (§1). Read-only investigators fall back to Claude sonnet without ruling.
