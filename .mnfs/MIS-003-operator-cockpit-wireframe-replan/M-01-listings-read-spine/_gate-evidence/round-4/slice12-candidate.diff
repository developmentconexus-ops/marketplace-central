diff --git a/apps/server_core/internal/modules/listings/application/read_service.go b/apps/server_core/internal/modules/listings/application/read_service.go
index 2efb2dd1..c1b2c514 100644
--- a/apps/server_core/internal/modules/listings/application/read_service.go
+++ b/apps/server_core/internal/modules/listings/application/read_service.go
@@ -154,7 +154,7 @@ func (s ReadService) List(ctx context.Context, query ports.ListingQuery) (ports.
 		if err != nil {
 			return ports.ListingRowPage{}, err
 		}
-		unavailable, err := s.enrich(ctx, page.Items, maxCeiling, policy, policyFound, ceilingErr)
+		unavailable, err := s.enrich(ctx, page.Items, maxCeiling, policy, policyFound, ceilingErr, "List")
 		if err != nil {
 			return ports.ListingRowPage{}, err
 		}
@@ -195,7 +195,7 @@ func (s ReadService) ByProduct(ctx context.Context, query ports.ListingGroupQuer
 	if err != nil {
 		return ports.ListingGroupRowPage{}, err
 	}
-	unavailable, err := s.enrichGroups(ctx, page.Groups, ceiling, policy, policyFound, ceilingErr)
+	unavailable, err := s.enrichGroups(ctx, page.Groups, ceiling, policy, policyFound, ceilingErr, "ByProduct")
 	if err != nil {
 		return ports.ListingGroupRowPage{}, err
 	}
@@ -224,7 +224,7 @@ func (s ReadService) scanGroups(ctx context.Context, q ports.ListingGroupQuery,
 		if err != nil {
 			return ports.ListingGroupRowPage{}, err
 		}
-		unavailable, err = s.enrichGroups(ctx, page.Groups, ceiling, policy, policyFound, unavailable)
+		unavailable, err = s.enrichGroups(ctx, page.Groups, ceiling, policy, policyFound, unavailable, "ByProduct")
 		if err != nil {
 			return ports.ListingGroupRowPage{}, err
 		}
@@ -265,12 +265,12 @@ func (s ReadService) scanGroups(ctx context.Context, q ports.ListingGroupQuery,
 	return result, nil
 }
 
-func (s ReadService) enrichGroups(ctx context.Context, groups []domain.ListingGroup, ceiling *float64, policy ports.PricingPolicy, policyFound bool, unavailable error) (error, error) {
+func (s ReadService) enrichGroups(ctx context.Context, groups []domain.ListingGroup, ceiling *float64, policy ports.PricingPolicy, policyFound bool, unavailable error, op string) (error, error) {
 	items := make([]domain.ListingReadModel, 0)
 	for _, group := range groups {
 		items = append(items, group.Listings...)
 	}
-	unavailable, err := s.enrich(ctx, items, ceiling, policy, policyFound, unavailable)
+	unavailable, err := s.enrich(ctx, items, ceiling, policy, policyFound, unavailable, op)
 	if err != nil {
 		return unavailable, err
 	}
@@ -327,7 +327,7 @@ func (s ReadService) Get(ctx context.Context, id domain.ListingID) (domain.Listi
 		return domain.ListingReadModel{}, nil, ceilingErr
 	}
 	items := []domain.ListingReadModel{model}
-	unavailable, err := s.enrich(ctx, items, maximumCeiling(ceilings), policy, policyFound, ceilingErr)
+	unavailable, err := s.enrich(ctx, items, maximumCeiling(ceilings), policy, policyFound, ceilingErr, "Get")
 	if err != nil {
 		return domain.ListingReadModel{}, nil, err
 	}
@@ -368,7 +368,7 @@ func (s ReadService) scan(ctx context.Context, q ports.ListingQuery, ceiling *fl
 		if err != nil {
 			return ports.ListingRowPage{}, err
 		}
-		unavailable, err = s.enrich(ctx, page.Items, ceiling, policy, policyFound, unavailable)
+		unavailable, err = s.enrich(ctx, page.Items, ceiling, policy, policyFound, unavailable, "List")
 		if err != nil {
 			return ports.ListingRowPage{}, err
 		}
@@ -407,8 +407,9 @@ func (s ReadService) scan(ctx context.Context, q ports.ListingQuery, ceiling *fl
 // sets unavailable and nulls the batch's facts; any other cost fetch error
 // propagates immediately instead of being swallowed into a false "unknown"
 // state. Callers own logging the outcome once they know whether the result
-// degrades or fails.
-func (s ReadService) enrich(ctx context.Context, items []domain.ListingReadModel, ceiling *float64, policy ports.PricingPolicy, policyFound bool, unavailable error) (error, error) {
+// degrades or fails. op identifies the calling read path for a propagated
+// error's log attribution.
+func (s ReadService) enrich(ctx context.Context, items []domain.ListingReadModel, ceiling *float64, policy ports.PricingPolicy, policyFound bool, unavailable error, op string) (error, error) {
 	ids := make([]int64, 0, len(items))
 	seen := map[int64]bool{}
 	if unavailable == nil {
@@ -426,7 +427,7 @@ func (s ReadService) enrich(ctx context.Context, items []domain.ListingReadModel
 		fetched, costErr := s.facts.GetCostFactsByIDs(ctx, ids)
 		if costErr != nil {
 			if !isSourceUnavailable(costErr) {
-				slog.Error("listings: cost fact read failed", "err", costErr)
+				slog.Error("listings: cost fact read failed", "err", costErr, "op", op)
 				return unavailable, costErr
 			}
 			unavailable = costErr
