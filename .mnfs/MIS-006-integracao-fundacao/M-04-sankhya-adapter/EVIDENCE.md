# M-04 SankhyaAdapter — Evidence Pack

Branch `claude/dreamy-lamarr-0bd9de`. Commits:
- `18b278dc` F-03 cache-key partition by active source incl sankhya + must-fail test
- `0af8d28c` F-01/F-02 SankhyaAdapter Sync + own mirror writer
- `b185cc3c` tests (sync mapping, mirror guards, integration keep-absent)
- `0738b400` sync-strategy doc (cherry-pick, ratified full-snapshot v1)
- `83b15d0e` dual-gate corrective #1 (bind-capture, C13 scope, NUTAB comment)
- `4914807f` refuter round-1 correctives: EAN trim + nullStr honest-NULL, SQL-placeholder bind assertion, C10/C12 reconciliation
- `63ba2e49` live-C04 sample-row proof dump
- `6cd1d0f6` GAP-2: Sync integration driver (`sync_integration_test.go`, integration&&cgo, env-gated)
- `f1fefb70` GAP-1: dual-gate artifacts + round-2 NO-REFUTATION + C10 ancestry correction
- `<pending>` close-out: live-drive results (C4/C5 PASS, U1/U2 PASS, U3 measured) + estoque-variance asserts + final test name/command

Base: mapping doc `docs/design/evidence/sankhya-mapping-M04.md` @`dbe089ac` (mission main, merged).

## Criteria ledger

| ID | Verdict | Evidence |
|----|---------|----------|
| M04-C1 | ran | Mapping doc @`dbe089ac` (db-consult, 6/6 items ratified, verified live METALPRD 2026-07-22). Sync entrypoint commit `0af8d28c` timestamp POSTERIOR to mapping. AC-07 clear. |
| M04-C2 | n/a | [TESTAR-SKW] not blocked (mapping returned). |
| M04-C3 | ran | `oracle/reader.go` read-side untouched — `git show 0af8d28c --stat` adds only `sync.go` (new file) + mirror writer; FindProductsForLinking/GetSellableStock/GetCostAsOf/GetTaxInputs signatures + bodies unchanged. SankhyaAdapter embeds `*Reader` verbatim. |
| M04-C4 | **PASS (ran) — hub live-drive, real METALPRD Oracle** | Driver: `sync_integration_test.go` `TestSankhyaSyncIntegration` (`//go:build integration && cgo`, gated on presence of `MPC_ORACLE_CONNECT_STRING` + `MPC_TEST_DATABASE_URL`, else `t.Skip`). Command (hub): `CGO_ENABLED=1 go test -tags "integration cgo" -run TestSankhyaSyncIntegration -v ./internal/modules/internal_read/adapters/oracle/`. It wires `NewSankhyaAdapter(realOracleDB, mirror.NewPgWriter(realPool), probeTenant)`, runs `Sync`, reads `products_mirror WHERE source='sankhya'` back, and proves honest-NULL + estoque variance; isolated probe tenant auto-cleaned; NOT stubbed (AC-04). **Hub run PASS 75.20s**: `MPC_C04_SANKHYA_MIRROR_ROWS=10526` (exact match to ATIVO='S' consult count), EAN_NULLS=3168, CUSTO_NULLS=413, FULLY_RESOLVED=5531, ZERO_CUSTO=2317 (real 0 balances, distinct from the 413 honest-NULL custos → AC-03 upheld), 15 real sample rows + variance asserts (distinct estoque_total>1, stock_locations rows>0 with per-CODLOCAL variance), `MPC_C04_LIVE_SANKHYA_SYNC_OK=true`. Chip itself couldn't compile cgo (godror cgo symbols undefined in chip env — AC-04 could-not-run boundary); hub backend container has instantclient. |
| M04-C5 | **PASS (ran) — hub live-drive, real migrated PG** | `mirror/writer_integration_test.go` (//go:build integration) proves 3-round keep-absent, present→absent flag + stale_since + last-known preserved, reappear-clears, tenant isolation, empty-snapshot guard. **Hub run PASS 0.33s** vs real migrated PG (64 migrations): `go test -tags integration -run TestApplySnapshotKeepAbsentRoundTrips ./internal/modules/internal_read/adapters/mirror/`. Guard logic also unit-proven in `writer_test.go`. |
| M04-C6 | ran | Honest-NULL: `sync.go` sets custo/preco/estoque/ean via `*T` pointers left nil when unresolved; `TestSankhyaSyncMapsSnapshotHonestNull` asserts product 300 (absent everywhere) → custo/preco/estoque NULL, product 200 non-EAN REFERENCIA → ean NULL, real 0 stock ≠ NULL. Grep: zero hardcoded 0/default on those columns (mirror migration NUMERIC nullable no-default; sync writes pointers). **Refuter fix (@corrective):** `nullStr` now `TrimSpace`→nil-if-empty, so Oracle CHAR-padded / blank text (descr/ref/marca/grupo/ncm) is honest-NULL not `""` (test: 300 whitespace-only descr → NULL); EAN is trimmed before AND stored after `IsValidGTIN` so a space-padded valid EAN resolves instead of dropping to NULL and no padding is persisted (test: 100 EAN `"  7894900011517 "` → `"7894900011517"`). EAN shape rule = `catalogdomain.IsValidGTIN`, deliberately identical to read-side `FindProductsForLinking` (reader.go:71) for live-read↔mirror consistency; the ratified 12–14-digit range is the observed METALPRD data, GTIN-8 accepted for read-side parity but absent in data. |
| M04-C7 | ran | `go build ./...` green; `Kind()` returns constant `sourcekind.LiveReadThrough`; `TestSankhyaAdapterKindIsLiveReadThrough`. |
| M04-C8 | ran | `Sync(ctx)` returns `ports.SyncResult{Processed:N, Errors:0}`; `TestSankhyaSyncMapsSnapshotHonestNull` asserts Processed==3. |
| M04-C9 | ran | Read-side signatures unchanged (same as C3); full-module `go build ./...` green = existing pricing/linking consumers compile. |
| M04-C10 | **reconcile-with-hub (M-03 C13 precedent)** | `git show 18b278dc --stat`: `cache.go` (canonicalKey→activeSourceKey incl sankhya) + `cache_test.go` in ONE commit; source is 3rd key value — the cache half is atomic. BUT the literal "same commit que adiciona sankhya ao domínio" is **unsatisfiable by M-04**: the sankhya domain value `SourceSankhya ActiveSource="sankhya"` (tenant_config/active_source.go:20) landed in **M-02 `4f1bff70`**. Verified topology (`git merge-base --is-ancestor`): 4f1bff70 is NOT in the declared base `138aac3d` (it landed after base), but IS an ancestor of **every** M-04 authorship commit — it is part of the onda-1 M-02 merge this branch was cut on top of. So the domain-add precedes all M-04 work and no M-04 commit can co-land with it. (Round-2 refuter correctly flagged that an earlier draft of this row mis-stated 4f1bff70 as pre-base; corrected here — conclusion unchanged.) The anti-pattern C10/C12 guard against (a cache key naming a source the domain doesn't recognise → an *exploitable* cross-source-cache window) does not exist: the domain already recognised sankhya before M-04, and no code serves a sankhya read until the HUB-OWNED F1 wiring activates it post-merge. Requesting hub ratification (as M-03 C13 @`7ba59c67`), not a bald pass. |
| M04-C11 | ran | `TestCatalogCachePartitionsSankhyaVsXlsx` (cache_test.go): (tenant,xlsx) populated, (tenant,sankhya) misses; proven load-bearing pre-commit by removing activeSourceKey → test failed ("got 1"), restored → green. |
| M04-C12 | **reconcile-with-hub** | Same reconciliation as C10: cache-key + its test are a single commit `18b278dc` (never split); the only "split" is from the M-02 domain-add, which is upstream of M-04's base and therefore outside M-04's authorship. Hub ratification requested. |
| M04-C13 | ran | Tenant scoping. **Grep scope = the whole sync flow (oracle read + mirror write), NOT sync.go alone** — a literal grep of `sync.go` finds zero `tenant_id` BY DESIGN and that is not a fail: the Sankhya Oracle tables carry no tenant column (one Oracle connection = one tenant's ERP, `sync.go` header), so the tenant concept exists only at the mirror write. Every `products_mirror`/`_stock_locations` statement in `writer.go` is tenant-keyed: `upsertSQL`/`keepAbsentSQL`/`deleteLocationsSQL`/`insertLocationSQL` all `tenant_id=$1`. Cross-tenant isolation proven by `writer_integration_test.go` (neighbour tenant row untouched by sweep). Both gate + refuter concurred this is architecturally sound, not AC-01. |
| M04-C14 | ran | No `.env`/credential read or print in any commit; no Oracle DSN in transcript/evidence. |

## User-drive (D-120) — hub live-drive RESULTS

| ID | Verdict | Evidence (hub-run, real Oracle + real PG + browser) |
|----|---------|------|
| M04-U1 | **PASS (API layer)** + FE gap routed to M-06 | DB flip active_source→sankhya ⇒ `GET /catalog/products` = HTTP 503 `{"error":"source_unavailable"}` — fail-closed honest, never other-source data, never empty list (exactly the M-04 contract). SCREEN finding (NOT M-04): /catalogo shows infinite "Loading catalog..." on the 503 (no error state) — pre-existing FE error-state gap, hub routed to M-06 (dono das telas de fonte). Not an M-04 defect. |
| M04-U2 | **PASS + collision worry CLEARED** | Flip back→xlsx: dataset byte-consistent (sha256 first-page identical pre/post = `da38bdb10f9c4880`), screen recovered same as_of 17:23:27. Hub verified the U2 warning: `products_mirror` count on the dev DB = **0** while /catalogo xlsx serves full data ⇒ the xlsx read path definitively does NOT read products_mirror (serves the import snapshot tables). No cross-adapter collision. |
| M04-U3 | **could-not-run-pre-F1 (named, honest)** — hub ruling "constraint-documented, verify-after-F1" | /precos with xlsx active shows R$ custo for all 34 linked products, header "custo ERP"; with Oracle configured and `listingCostReader` wired directly in root.go, these ARE Oracle-origin numbers while xlsx is the active source — the pre-existing F1 defect confirmed ON SCREEN (measurement, not pass/fail). M-04 adds no root.go read-path and does not introduce nor fix this; hub ruled U3's PASS criterion deferred to the post-merge F1 QA re-drive, named explicitly in the acceptance record + recorded as immediate post-merge hub work (F1 = 4 consumer re-routings + refuter-#8 per-tenant Oracle-handle binding). |

## Scope guards honored
- No root.go edits (F1 pre-activation routing = HUB-OWNED, sequenced post M-03+M-04 merge).
- Own mirror writer INSIDE internal_read (hub R-2 (i)), disjoint from M-03 erp_import; no import from erp_import.
- No ImportSource enum extension; no sankhya_linkage_*.go touched; no promo/preco_final (M-06).
- No stub for real-Oracle proof (AC-04); no push (AC-06).

## Hub rulings (escalated reconciliations — RESOLVED)
- **C10/C12 — RATIFIED as reconciliation** (M-03 C13 precedent @`7ba59c67`): literal same-commit unsatisfiable (SourceSankhya upstream in M-02 `4f1bff70`); spirit holds (18b278dc atomic cache+tests, no exploitable pre-activation window). Hub files the ratification row in the mission ledger at merge. Disposition: reconciled-pass.
- **U3 — RULED "constraint-documented, verify-after-F1"**: pre-F1 the 4 root.go direct-Oracle read-paths surface Oracle numbers regardless of active_source = the F1 defect itself, pre-existing on main, not introduced/fixable by M-04. U3 closes as could-not-run-pre-F1 (named) + hub-owned post-merge F1 QA re-drive.
- **Refuter #8 — CONFIRMED**: tenant↔Oracle-connection binding enforced in the F1 composition (per-tenant Oracle handle → same-tenant NewSankhyaAdapter, one composition site; no shared/global handle). Recorded as an F1 requirement.

## Dual-gate

**P6-DUAL-GATE: AGREEMENT** (cold gate closeable + refuter round-2 NO-REFUTATION on corrected code). All scope-boundary + reconciliation items ruled by hub above — none are chip-code defects.

**Cold gate** — reviewer `harness:gate-reviewer` (Agent, physically read-only: no Edit/Write/Bash). Verdict: **AGREEMENT, M-04 closeable**. Its file:line evidence was returned inline in the Agent transcript (no separate artifact file); summary: all statically-verifiable criteria PASS; it flagged that build/test + git single-commit + AC-06 no-push needed tool execution it couldn't run — supplied by chip: nocgo `go build ./...` green, oracle+mirror+cache unit tests PASS, `git show` single-commit confirmed, `git status -sb` shows origin has no branch (no push).

**Adversarial refuter** — REAL codex `gpt-5.6-sol` effort medium via `/codex:rescue` (hub onda-2 requirement; codex confirmed CODEX-OK live this session, so the Jul-25 quota-wall memory was stale). **Dispatch succeeded both rounds — no quota/transport error, so NO Claude fallback was used** (an earlier Claude refuter this session was discarded as non-sanctioned per hub ruling; it had missed the real defects #4/#6/#7, which is exactly why real Sol is mandated).
- Dispatch (both rounds): `codex exec --model gpt-5.6-sol --effort medium --cd <worktree> "<refuter prompt>"`.
- **Round 1** (tip 83b15d0e): **REFUTED** — 5 blocking (#1-#5), 3 material (#6-#8), 1 minor (#9). Triage table below.
- **Round 2** (tip 6cd1d0f6, corrected): **NO-REFUTATION**. Per-finding: #1 RESOLVED (real C4 driver), #2 OUT-OF-SCOPE-OK (milestone.md:276-285 excludes root.go), #3 OUT-OF-SCOPE-OK (milestone.md:53-63 assigns F1 read-path fixes to hub; M-04 modifies none), #4 RESOLVED (sync.go:142-146 trim-once/validate/store; reader.go:69-73 parity), #5 RECONCILE-OK (active_source.go:20 in 4f1bff70, upstream M-02 ownership → literal same-commit impossible), #6 RESOLVED (sync.go:302-310 trim→nil), #7 RESOLVED (sync_test.go:75-90 guards arg-order AND SQL placeholder positions; fixtures cover padded-EAN + whitespace→NULL), #8 OUT-OF-SCOPE-OK (tenant is composition-supplied invariant; connection carries no tenant identity), #9 RECONCILE-OK (ratified full-snapshot v1, atomicity = documented future hardening). Verdict line: **NO-REFUTATION**.

Round-1 triage (finding → disposition; all now confirmed by round-2):

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | blocking | C4/C5/U1-U3 unexecuted | **By design** — chip can't boot server (profile); these are the hub live-drive REQUEST (task #8). C4 entrypoint now provided as `TestSankhyaSyncIntegration`; C5 as the integration lane; U1-U3 hub browser drive. Not a code defect. |
| 2 | blocking | SankhyaAdapter is dead code — no production construction / Sync call; scheduler no-op | **Scope boundary** — production wiring = HUB-OWNED F1 step, sequenced post M-03+M-04 merge (no root.go edits in M-04). Adapter is proven against real Oracle via `TestSankhyaSyncIntegration` direct construction, so C4 is producible WITHOUT the scheduler. Runbook to hub. |
| 3 | blocking | root.go:436/561-567/607 read Oracle regardless of active_source → /precos can mix; violates U3 | **Scope boundary** — the KNOWN F1 pre-activation constraint (HUB-OWNED). M-04 adds no such read-path. U3 PASS gated on hub F1 wiring (see U3 row). Flagged to hub. |
| 4 | blocking | EAN filter unfaithful: IsValidGTIN accepts GTIN-8; validates trimmed but could store padded | **FIXED (partial)** — now trims before validate AND stores the trimmed value (padded valid EAN recovered, no padding persisted; test 100 `"  7894900011517 "`→`"7894900011517"`). GTIN-8 accept **kept deliberately** = read-side `FindProductsForLinking` parity (reader.go:71) for live-read↔mirror consistency; ratified 12–14 is observed data, no GTIN-8 present. Documented, hub can rule stricter if desired. |
| 5 | blocking | C10/C12 same-commit failed — sankhya domain landed in M-02 `4f1bff70` | **Reconcile-with-hub** — literal criterion unsatisfiable (domain-add is upstream of M-04 base); the exploitable-window it guards against doesn't exist pre-activation. M-03 C13 precedent. See C10/C12 rows. |
| 6 | material | nullStr writes `""` for empty/whitespace Oracle text vs honest-NULL | **FIXED** — TrimSpace→nil-if-empty (test 300 whitespace-only descr → NULL). |
| 7 | material | bind assertion checks Go arg order but fake discards binds → SQL placeholder swap (:1/:2) slips through | **FIXED** — added `queryFor` + SQL-text assertion `CODEMP=:1 AND DTATUAL<=:2` / `DTVIGOR<=:1`. Proven load-bearing via must-fail (swap SQL placeholders, Go args unchanged → test FAILS "cost SQL placeholder binding wrong"; restore → PASS). |
| 8 | material | "one Oracle connection = one tenant" is a comment, constructor accepts any handle+tenant → cross-tenant stamping | **Acknowledged, enforced at composition** — the tenant↔connection binding is a wiring-time invariant (HUB-OWNED composition, same as #2). Chip cannot fully enforce without the composition object; documented as a wiring constraint. Hub to bind in the F1 wiring. |
| 9 | minor | Q1-Q4 are independent reads, no consistent SCN → possible mixed snapshot | **Accepted / ratified** — full-snapshot v1 over 4 reads; the ratified sync strategy accepts eventual multi-query consistency (immune to in-place edits by design; TGFEST full). Candidate v2 hardening (read-only tx / SCN), noted not blocking. |

Codex also positively confirmed: Q2/Q3 window shapes + tiebreaks match ratified set-based semantics; Q4 matches available-stock aggregation; mirror writes transactionally tenant-keyed; empty snapshot fails closed; absent rows preserve last-known; M-04 commits did NOT edit root.go / ImportSource / linkage / promo-final; reader.go byte-unchanged.
