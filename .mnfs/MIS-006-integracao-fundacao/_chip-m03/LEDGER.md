# M-03 CHIP LEDGER

- **D0** boot: worker skill + doctrine read (CORE via skill + profile + milestone.md + validation-contract M03-C1..C17/U1..U4/AC-01..08). Worktree chip-m03 @78f02ac9 verified materialized (routing/reader.go present, branch claude/m03-xlsx-adapter, clean). ACK → hub.
- **D0** inventory complete (reader.go, import_service.go, adapter.go port, routing/reader.go, tenant_config, sourcekind, 0075/0076 migrations, domain/import.go, generation_service.go, sync_state_repo.go, root.go:278-487, ImportRepository iface). Findings → PLAN.md.
- **D0** two blocking items diagnosed: BLOCKING-1 (root.go additive-wiring lease for F-01 hook deps) + BLOCKING-2 (F-03/C13 import-cycle reconciliation). gomodcache warm done (exit 0). REQUEST → hub, awaiting ruling before P2 dispatch.
- **D0** codex readiness probe PASS: gpt-5.6-luna/low read-only, stdin closed (`< /dev/null`) → `PONG`, exit 0. Non-fatal 0.144.4 cache warning (`missing field supports_reasoning_summaries`) present as expected (profile §3). NOTE for real long dispatches: consider 0.145.0-alpha.18 binary (profile §3 F-ENV-9: 0.144.4 PATH binary can die mid-dispatch on cache-TTL renewal).
- **D0** HUB RULING received: BLOCKING-1 additive-lease GRANTED (opt b, setter/lazy-adapter, NO reorder of oracle block); BLOCKING-2 reconciliation RATIFIED + contract amended main@7ba59c67 (C13 = doc routing-pin + remove silent xlsx default reader.go:47 fail-closed). installationID = iterate InstallationService.List per-installation (P2 detail).
- **D0** BLOCKING-2 caller-audit DISCHARGED (ran, EVIDENCE.md): catalog+market handlers pin outside routing (`if present`) BUT both flow through internalReadSvc=routing which re-pins → silent default dead in wired path → removal SAFE. Side FINDING → hub: erp_source query-param handling dead post-M-02 (M-06 backlog). FINDING queued to hub.
- **D0** P2 dispatch: Sol-medium planner (gpt-5.6-sol/medium, read-only, OS-process bg id bgbb028rd) → agent__p2-m03.{log,last.md,done}. Prompt: prompts/p2-planner-sol-medium.md. Resolves installationID iface design, keep-absent SQL + tx boundary (C10), mirror-read semantics + catalogPage keyset (C11b), F-04 Sync=hook-path, F-03 removal mechanism. Awaiting.
- **D0** P2 plan LANDED (exit 0) + ACCEPTED. 7 slices S1..S7, write-DAG, contract-satisfiability map, seam-closure checklist. Design ratified: shared `mergeSnapshotTx` (F-01 hook + F-04 Sync = ONE impl), staging-table+CopyFrom keep-absent SQL (upsert touched + set-diff UPDATE absent, no DELETE), structural ifaces `LinkCandidateGenerator`+`SyncEnqueuer` in erp_import/application + late setter `SetPostImportHooks` (granted lease), fail-closed `activeSourceFromContext`, text-keyset catalog pagination (`ORDER BY codigo_produto ASC`, cursor=FormatInt round-trip filter), XlsxAdapter via `SyncLatestCompletedSnapshot`. CHIP corrections: (a) single worktree ⇒ ONE codex writer at a time (parallel OS-process writers = FS race) → SERIALIZE DAG S1→S2→S4→S5→S3→S6→S7, failing-test-first + commit per green slice; (b) VERIFIED no import cycle (product_links/sync/integrations do NOT import erp_import → S3 adapters in erp_import/adapters/{productlinks,sync} safe); (c) seams confirmed file:line — GenerateLinkCandidates{InstallationID,Limit≤0→20}, InstallationService.List(ctx)→([]Installation,err), AppendPendingCodigo atomic; merge hook site = import_repository.go:67 (after AcceptedRows loop, COMPLETED-only, pre-commit, same tx).
- **D0** S1 dispatch (DAG root, COMPLEX→Sol-low, OS-process bg id **bd1156gku**): atomic mirror materialization + shared `mergeSnapshotTx` + `SyncLatestCompletedSnapshot` + `ImportRepository` iface extension. Prompt: prompts→scratch/prompt-s1-m03.md. Artifacts scratch/agent__s1-m03.{log,last.md,done}. Owns repository.go+domain/import.go+erp_import/adapters/postgres/*. Proves C2-C7,C10.
- **D0** S1 REVIEWED + COMMITTED @**9fee2264** (7 files, +359). CHIP adversarial review: merge SQL correct — source-scoped set-diff keep-absent (NO `DELETE FROM products_mirror`, no product_links touch), honest-NULL casts (custo/preco_venda NULL-when-empty, estoque_total both-known-else-NULL), resurrection clears flag+stale_since, staging table ON COMMIT DROP, single `mergeSnapshotTx` shared by persist+Sync. C10 tx-boundary proof valid (preco_venda exists only in mirror → invalid text passes protocol insert, fails ::numeric cast, whole tx rolls back). Investigated dup-codprod ON-CONFLICT-cardinality risk → NOT triggerable: erp_import_products PK(tenant,protocol,codprod) makes Sync-path rows unique + persist-path pre-merge insert PK-violates first; xlsx parser never sets Local (nil) → locations insert no-op, M-04-forward-compat. internalread test diffs = pure iface-stub additions (return 0,nil), no read logic (S5 owns that). Build+unit GREEN in worktree (GOCACHE abs + GOFLAGS=-buildvcs=false per sandbox), integration lane compiles (vet -tags=integration exit 0), behavior lane could-not-run (no PG, hub P7).
- **D0** S2 dispatch (application hooks, STANDARD→Luna-high, OS-process bg id **bko8eu9p1**): post-persist hook in import_service.go — structural ifaces `LinkCandidateGenerator`+`SyncEnqueuer` (NO product_links/sync/integrations import), `WithLinkCandidateGenerator`/`WithSyncEnqueuer` opts + `SetPostImportHooks` late setter (granted lease). Hook fires COMPLETED-only after PersistSnapshotAtomically success (line 120), before GetImport; enqueue accepted codprods → generate per returned installationID; fail-loud propagate; NEVER Collect. write_set=import_service.go+import_service_test.go. Proves app-half C8,C9.
- **D0** S2 REVIEWED + COMMITTED @**c4d23b74** (2 files, +175/-7). Hook guarded `status==Completed && enqueuer!=nil && generator!=nil` (rejected/unconfigured skip), collect accepted Codprod order-preserving, enqueue fail-loud propagate → generate per installationID fail-fast, NEVER Collect, return GetImport only after hook. Two structural ifaces, `go list -deps` boundary EMPTY (no product_links/sync/integrations import). Build+test GREEN worktree.
- **D0** S4 dispatch (mirror query repo, COMPLEX→Sol-low, OS-process bg id **badli9iso**): tenant/source/not-absent-scoped mirror reads for F-02. CHIP design pins: (1) numeric keyset `ORDER BY codigo_produto::bigint ASC` + int64 cursor (C11b "chave estável equivalente" — preserves reader's existing int64 Cursor.InternalProductID + numeric page order, zero consumer drift; avoids plan's lexical text-keyset behavior-change), digit-guard `^[0-9]{1,18}$` (overflow-safe, mirrors ParseInt-and-skip); (2) MirrorProduct carries pre-computed nullable numerics (estoque_total NULL⟺missing-stock, custo NULL⟺missing-cost = honest-equivalent to today's flags); (3) 4 methods MirrorRows/MirrorProductByCode/MirrorCatalogPage(SQL-keyset)/MirrorEANCollisionCounts(global, catalogPage-only consumer). Owns test-fake stubs this round. Proves repo-half C11,C11b.
- **D0** S4 REVIEWED + COMMITTED @**89023c0d** (7 files, +334). All 4 mirror-read methods carry tenant+source+absent=false predicates; NULL→nil via sql.NullString (honest); MirrorCatalogPage numeric keyset verbatim as pinned (`ORDER BY codigo_produto::bigint ASC`, `^[0-9]{1,18}$` guard, `LIMIT CASE WHEN $5>0 THEN $5+1 END`→NULL=no-limit); EAN counts global GROUP BY HAVING>=2. domain/mirror.go MirrorProduct pre-computed nullable numerics. Fake stubs trivial-return only (reader_test fakeRepo+sourceKeyedRepo, source_contract contractRepo, import_service fakeImportRepository). Build+test+vet-integration GREEN. NOTE for S5: MirrorEANCollisionCounts returns RAW ean→count (incl invalid GTINs) — SAFE because identityQuality checks IsValidGTIN BEFORE consulting collisions[ean], so invalid dups never reached; do NOT filter.
- **D0** S5 dispatch (mirror-backed reader + F-03 fail-closed, COMPLEX→Sol-low, OS-process bg id **b0o38old8**): rewrite reader.go 4 named methods (FindProductsForLinking/GetSellableStock/GetCostAsOf/catalogPage) + GetTaxInputs to mirror reads; remove silent SourceXLSX default at reader.go:47 → activeSourceFromContext fail-closed (returns err when unpinned, C13/C14); preserve BOTH compile-asserts (:401 Reader, :402 CatalogPageReader) + ALL signatures (C12 consumer zero-diff). Pins: cost gate on MirrorProduct.UpdatedAt vs Policy.EffectiveAt; page.AsOf=max(UpdatedAt) of returned rows; MirrorProductByCode found=false→ERPProductNotFoundError{id}; update WithActiveSource/ActiveSourceFromContext/ParseActiveSource doc-comments to fail-closed wording. write_set=reader.go + reader_test.go + reader_integration_test.go + source_contract_test.go. Proves C11,C11b,C12,C13,C14.
- **D0** S5 REVIEWED + COMMITTED @**9a650cf9** (4 files, +333/-372). CHIP adversarial review: fail-closed correct — `activeSourceFromContext→(src,err)`, unpinned→`ErrUnknownActiveSource`, `sourceFor` wraps to `ReadErrorSourceUnavailable` (errors.Is preserved) with NO repo read on the miss path (C13/C14). All 5 reads mirror-backed (MirrorRows+global GTIN-validated collisions / MirrorProductByCode / MirrorCatalogPage keyset limit+1 has-more + NextCursor=last-id + AsOf=max(UpdatedAt) + MirrorEANCollisionCounts); honest-NULL flags preserved (EstoqueTotal/Custo nil→quality flag never 0); cost as-of gates on UpdatedAt.After(EffectiveAt). Correct asymmetry: FindProductsForLinking SKIPS non-int codes (MirrorRows=all), catalogPage ERRORS on non-int (repo already digit-filtered → invariant). `snapshot()` removed, no LatestCompletedSnapshot in reader.go, System honest-per-source. Both compile-asserts survive (:393-394); consumer zero-diff EMPTY (C12); parser.go/root.go/S4-files untouched; build+test GREEN worktree. NON-BLOCKING cosmetic noted (not defect): ErrUnknownActiveSource doc + GetCurrentPrice/GetSalesHistory msgs still name "xlsx" — behavior correct regardless of source. RED→green: TestUnpinnedReadsFailClosed, TestMirrorBackedReads (panics on LatestCompletedSnapshot), TestCostAsOfRejectsNewerMirrorRow, TestMirrorProductNotFound, TestMirrorUnknownValuesRemainUnknown, TestCatalogFactQuality, pagination 1,2,10.
- **D0** S3 dispatch (real post-import hook adapters + root.go additive-lease, STANDARD→Luna-high, OS-process bg id **bbedgyhoa**): NEW `erp_import/adapters/sync/enqueuer.go` (MarketEnqueuer satisfies SyncEnqueuer — structural installationLister+marketCursorAppender ports; List×codes → AppendPendingCodigo(EntityMarket) fail-loud → distinct installationIDs) + NEW `erp_import/adapters/productlinks/generator.go` (LinkCandidateGenerator satisfies iface — wraps GenerationService.GenerateLinkCandidates{Limit:0→20 internal-default}) + root.go additive-lease (2 adapter imports + syncpg + ONE erpImportSvc.SetPostImportHooks(NewMarketEnqueuer(installationSvc, syncpg.NewSyncStateRepository(pool,tenant)), NewLinkCandidateGenerator(productLinkGenerationSvc)) after ~:485, NO reorder). Verified pre-dispatch: product_links/integrations/sync do NOT import erp_import → cycle-safe; seams file:line-confirmed. NEVER Collect. write_set=2 adapters+tests+root.go. Proves C8,C9 (make S2 hook load-bearing).
- **D0** S3 REVIEWED + COMMITTED @**5fe2abc1** (5 files, +258). CHIP adversarial review: MarketEnqueuer fail-loud (list/append err wrapped+propagated, no swallow), empty-codes short-circuit before List (zero appends), distinct-installation dedup via seen-set stable order, appends syncdomain.EntityMarket via atomic AppendPendingCodigo. LinkCandidateGenerator forwards Limit:0 (svc-default 20), discards result, wraps engine err. Both compile-asserts present. root.go diff = PURE ADDITIVE: 3 imports (erpsync/erpproductlinks/syncpg) + ONE SetPostImportHooks block after productlinkstransport.Register (:486, all deps pre-exist erpImportSvc:284/installationSvc:306/productLinkGenerationSvc:469), ZERO existing-line reorder/change. nil-pool safe (NewSyncStateRepository stores pool, deref only when hook fires on real COMPLETED import). Cycle-free confirmed (go build ./... green). parser.go/import_service.go/reader.go/mirror untouched, no migration, no Collect/market-writes. RED=missing-constructor compile-fail; green=adapter unit tests + erp_import+composition suites + integration vet.
- **D0** S6 dispatch (F-04 concrete XlsxAdapter, STANDARD→Luna-high, OS-process bg id **bbq3c5puf**): NEW `erp_import/adapters/xlsx/adapter.go` (pkg xlsx, parser.go ZERO edit) — XlsxAdapter satisfies readports.ProductSourceAdapter via embedded readports.Reader (inherits read side verbatim) + Sync(ctx) delegating to S1 SyncLatestCompletedSnapshot(SourceXLSX) [SAME merge path as F-01 hook, no 2nd impl] mapped to SyncResult{Processed, Errors:0 all-or-nothing tx} + Kind()=sourcekind.UploadSnapshot; compile-assert `var _ readports.ProductSourceAdapter`. NO root.go wiring (BLOCKING-1 lease spent S3; F-04 = port satisfaction only per PLAN S5). structural snapshotSyncer port; cycle-safe (readports/sourcekind/erpdomain don't import xlsx pkg). write_set=adapter.go+test. Proves C15,C16,C17 (idempotency: adapter-level delegation+stable mapping; end-to-end keep-absent idempotency = S1 PG integration + hub P7).
- **D0** S6 REVIEWED + COMMITTED @**c727a164** (2 files, +169). CHIP review: XlsxAdapter embeds readports.Reader (read side verbatim, zero re-impl), Sync→SyncLatestCompletedSnapshot(SourceXLSX) shared merge path (no 2nd impl), Errors=0 justified (all-or-nothing tx, no honest partial count), err wrapped "xlsx sync:" + zero SyncResult, Kind()=UploadSnapshot, compile-assert `var _ readports.ProductSourceAdapter`. parser.go ZERO edit, only 2 new xlsx files, cycle-safe. Build+erp_import suite green.
- **D0** S7 STATIC/BOUNDARY GATE run by CHIP directly (mechanical, gate-authority): **PASS**. (1) parser.go zero-diff vs base @78f02ac9 EMPTY; (2) no new migration; (3) diff scope = ONLY erp_import/* + granted root.go 7-line additive-lease, ZERO consumer-package (pricing/product_links/catalog transport) edits; (4) forbidden-token sweep on ADDED lines: NO Collect(/market_aggregates/competitor_offers; production NEVER `DELETE FROM products_mirror` (keep-absent honored — only child-table `DELETE FROM products_mirror_stock_locations` sanctioned delete-touched-then-upsert @mirror_repository.go:132; all bare mirror deletes are _test.go teardown); (5) ALL compile-asserts survive — reader Reader+CatalogPageReader (:393-394), XlsxAdapter ProductSourceAdapter, MarketEnqueuer SyncEnqueuer, LinkCandidateGenerator; (6) go build ./... + full erp_import unit suite + composition (4.086s) GREEN. Integration lane (//go:build integration, S1 mirror_repository_integration_test + reader_integration_test) needs Postgres → deferred hub P7.
- **D0** P6 DUAL-GATE round-1 = **DISAGREEMENT**. Cold Claude (agent, read-only) → GATE: PASS, 0 defects, all code-side PASS w/ file:line, deferred honest. GPT-5.6 Sol-medium (bg byb0l9sf4, read-only) → **GATE: FAIL**, 3 defects. CHIP ADJUDICATION (no rubber-stamp, evaluated each on merits): **Sol correct on all 3**.
  - **D1 C11b BLOCKING (real correctness bug)**: `mirror_query_repository.go:88-92` numeric keyset `ORDER BY codigo_produto::bigint` non-unique — `"1"` and `"01"` both cast bigint 1; dedup is exact-STRING (`seenCodprod[row.Codprod]` validation.go:30) + `codigo_produto text` stored raw (TrimSpace only, no canonicalization) → both distinct rows land in mirror PK(tenant,codigo_produto); tie straddling page boundary (`> cursor`) SKIPS a row. VERIFIED reachable. Root cause = MY S4 design pin (numeric keyset to reuse int64 cursor, overrode plan's lexical text-keyset) collides w/ int64 cursor for non-canonical codes. FIX (in-scope, no seam change): domain identity IS int64 CODPROD (`catalog/domain/canonical_product.go:12`) → read ONE row per int64 identity via `DISTINCT ON (codigo_produto::bigint) ... ORDER BY codigo_produto::bigint ASC, updated_at DESC` → keyset unique-per-identity, skip-free, int64 cursor + consumers unchanged (C12 preserved).
  - **D2 C17 test-theater**: `adapter_test.go:38-51,96-118` idempotency implemented+asserted INSIDE the fake `fakeSnapshotSyncer`; real merge (SyncLatestCompletedSnapshot) called ONCE in integration test → 2×-call row-count invariant UNPROVEN. FIX: rename adapter test to delegation-only (honest), add real 2×-call assertion to mirror integration test (Postgres → runs hub P7). Recategorize C17: adapter=delegation ran, end-to-end 2×=could-not-run→P7.
  - **D3 C4 non-probative evidence**: `mirror_repository_integration_test.go:29-60` stales product X but checks link for UNRELATED product 42 → would miss targeted deletion of X's link. Production code correct (merge never touches product_links) but test proves nothing. FIX: create link FOR X, stale X, assert X's link survives.
  - Adjudication protocol: fix all 3 → re-run BOTH gates on the corrective (REAL 2nd refuter pass per CHIP-IMPORT-FIX lesson, not self-verified) → AGREEMENT required before CLOSED. Dispatching corrective S8.
- **D0** S8 CORRECTIVE REVIEWED + COMMITTED @**fff4128** (3 files, +94/-44). CHIP adversarial review of all 3 fixes — probative, no new theater:
  - **D1 fix** `mirror_query_repository.go`: `MirrorCatalogPage` → `SELECT DISTINCT ON (codigo_produto::bigint) ... ORDER BY codigo_produto::bigint ASC, updated_at DESC` — one row per canonical int64 identity, newest-wins, skip-free keyset. Signature/tenant+source+absent predicates/`^[0-9]{1,18}$` guard/ILIKE filter/`LIMIT CASE $5+1` has-more ALL preserved; int64 cursor untouched (`catalog_page.go` diff EMPTY). Tie integration case `TestMirrorCatalogPagesCollapseNumericIdentityWithoutSkipping`: inserts `"1"`(desc A, older)/`"01"`(desc B, newer)/`"2"`; page limit=1 → identity 1 collapses to NEWER row (desc "B", proves DISTINCT ON newest-wins + no dup identity), cursor→identity 2 reached, distinct set exactly {1,2} no dup/skip. Exercises real SQL (//go:build integration → hub P7).
  - **D2 fix** `adapter_test.go`: renamed→`TestXlsxAdapterSyncDelegatesToSnapshotSyncer`, fake merge-state/idempotency fields removed, asserts delegation+mapping ONLY (source=SourceXLSX, tenant fwd, Processed=2, Errors=0). Real 2×-call C17 invariant moved to `mirror_repository_integration_test.go` `TestSyncLatestCompletedSnapshotUsesSharedMerge`: `SyncLatestCompletedSnapshot(SourceXLSX)` called TWICE on same completed protocol → asserts `count(*) WHERE source='xlsx'` identical after both + row fields (descricao/custo/estoque/protocol_id) unchanged (PK-upsert no-dup). Runs real merge → hub P7.
  - **D3 fix** `mirror_repository_integration_test.go`: stale product now CODPROD `42`, its OWN `product_links.internal_product_id=42` row created before absence, then post-absent asserts SAME link unchanged (installation/provider_code/provider_item_id/state/internal_product_id). Probative — catches deletion of the stale product's own link.
  - KEEP-ABSOLUTE verified EMPTY at fff4128: parser.go / root.go / reader.go / catalog_page.go cursor. Chip lanes GREEN: build ./... exit 0, erp_import unit suite ok, `go vet -tags integration erp_import/...` exit 0. Integration behavior (tie SQL, 2×-call, C4 own-link) = could-not-run (no MPC_TEST_DATABASE_URL) → hub P7.
- **D0** P6 DUAL-GATE round-2 (corrective HEAD fff4128, REAL 2nd refuter pass per CHIP-IMPORT-FIX lesson — NOT self-verified): dispatching cold Claude gate-reviewer (agent, read-only) + GPT-5.6 Sol-medium (codex OS-process). AGREEMENT required before CLOSED.
- **D0** P6 DUAL-GATE round-2 (corrective HEAD **fff4128**) = **AGREEMENT / GATE: PASS**. Cold Claude (agent, read-only) → GATE: PASS (D1/D2/D3 FIXED+probative — tie case fails vs pre-fix SQL, C17 2×-call hits real merge, C4 same-identity; no S8 regression; AC clean). GPT-5.6 Sol-medium (bg b5tsqh0wi, read-only — the round-1 FAIL reviewer, now flips to PASS) → GATE: PASS (3 fixes verified, full rubric re-run, S8=exactly 3 declared files, parser.go/root.go/reader.go/catalog_page.go empty vs c727a164, defects: none). Both agree DB/browser criteria (C2-C10, C11b/C17 runtime, U1-U4) = honest could-not-run → hub P7. Dual-gate value re-proven: round-1 caught 3 real defects cold-Claude missed; round-2 both independent PASS on the corrective. EVIDENCE.md P6 block finalized **P6-DUAL-GATE: AGREEMENT**. NON-BLOCKING note carried → hub/QA: mission EXEMPLO-IO golden fixture (Sankhya 90008/xlsx 74606) not asserted by name in M-03 tests (not an M-03-VC criterion). **M-03 chip-side gate GREEN → emitting CLOSED.**
