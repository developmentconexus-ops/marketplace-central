S12-CATALOGO-FE — evidência
Base: 9c139f45. Worker: Claude sonnet (fallback sancionado core §1, ordem do operador).
Brief medido: .mnfs/MIS-006-integracao-fundacao/_pos-chip-vendavel/S12-BRIEF.md

===============================================================================
1. DRIFT MEDIDO ENTRE O SLICE CARD (BATCH-PLAN.md:801-830) E O REPO
===============================================================================
O card foi escrito antes de S9 e S11 existirem. Sete divergências medidas antes
do despacho; o brief corrigido manda onde os dois discordam.

D1  Contrato já pronto. sdk-runtime/src/index.ts:245-250 (CatalogPageOptions) e
    :258-265 (CatalogSearchPageOptions) JÁ declaram include_all?: boolean, e o
    handler já o interpreta (catalog/transport/http_handler.go:254-271, chamado
    nos dois assentos :131 lista e :234 busca). S12 é FE puro — sem OpenAPI, sem
    SDK, sem Go. O card não dizia isso.

D2  packages/web-query/src/index.ts e index.test.ts estavam no write_set do card
    e NÃO precisavam mudar: catalogQueryKeys.facts(params) (:30) e search(q,
    params) (:35-38) já aceitam params arbitrários. Removidos do write set —
    seam compartilhado não se toca sem necessidade medida.

D3  apps/web/src/app/AppRouter.tsx:23-29 JÁ monta o componente real com client +
    erpSource. Só o TESTE faltava. Removido do write set.

D4  O hook de contagem já existe desde S11: useCatalogAssortmentCountsQuery
    (web-query/src/activeSource.ts:89-99). O card é mudo sobre ele — um worker
    ingênuo escreveria um segundo hook.

D5  Estado sem fonte resolvida: o card fixa o texto do chip só para o caso
    resolvido. Com counts desabilitado (enabled: Boolean(erpSource)), renderizar
    "Vendáveis 0 de 0" seria fato desconhecido virando zero — proibido pelo
    profile §7 / ADR-17. Ligado: sem dado, sem chip.

D6  Badge Sem estoque: o card não diz o gatilho. Ligado a quantity != null &&
    quantity <= 0; quantity == null continua "— (motivo)" e nunca vira badge.

D7  ARMADILHA DE LANE: apps/web/vitest.config.ts inclui feature-products por
    NOME DE ARQUIVO EXATO ("../../packages/feature-products/src/CatalogPage.test.tsx"),
    não por glob. Um segundo arquivo de teste nesse pacote roda em lane nenhuma.
    Proibido no brief; respeitado.

D8  Comando do card (npx --no-install vitest) trocado pela forma verificada
    (npm run test --workspace). npx --no-install já produziu verde vacuoso neste
    repo (CHIP-M02).

===============================================================================
2. O ACHADO DE REVISÃO QUE VALEU A RODADA
===============================================================================
O critério do card "Ver todos ... never calls setSellableAssortment" era
IMPOSSÍVEL de reprovar como escrito: CatalogQueriesClient (catalogQueries.ts:9-16)
não declarava setSellableAssortment, então nenhum client montado no teste teria o
método e a asserção passaria mesmo com o botão gravando a regra do tenant.
Asserção estruturalmente incapaz de falhar = teatro (core §4).

Ligado no brief: o client do teste carrega um spy setSellableAssortment, e o
must-fail tem que mostrar a asserção VERMELHA.

===============================================================================
3. MUST-FAILS REPRODUZIDOS NO ASSENTO DO ORQUESTRADOR
===============================================================================
Não certifico execução por relato do próprio executor. Dois reproduzidos aqui,
por injeção e restauração byte a byte (diff-stat 295/30 antes e depois).

MF-1 — "Ver todos" grava a regra do tenant
  Injetado em CatalogPage.tsx:125:
    onClick={() => { void client.setSellableAssortment({} as never); setIncludeAll(...) }}
  RED:
    FAIL ../../packages/feature-products/src/CatalogPage.test.tsx > CatalogPage >
      opens with Vendáveis 2 de 4 and ver todos never mutates tenant config
    209|     expect(setSellableAssortment).not.toHaveBeenCalled();
       |                                       ^
    Test Files  1 failed (1)
         Tests  1 failed | 12 skipped (13)
  => a asserção TEM um mundo em que reprova. Não é teatro.
  Restaurado; verde de volta.

MF-2' — o teste ALHEIO editado ainda pega o próprio defeito
  O worker editou um teste pré-existente ("keeps one source's rows out of the
  other source's cache entry", CatalogPage.test.tsx:145-175): o hook de contagem
  novo dispara um fetch por montagem quando erpSource existe, e aquele teste
  enfileirava exatamente 2 mockResolvedValueOnce — a leitura de counts roubava um
  slot da leitura de facts. O mock passou a ramificar por URL e a asserção de
  contagem passou a filtrar /counts.
  Regra: teste alheio tocado se restaura OU se prova por observável. Provado.
  Injetado: erp_source fora da chave de facts (catalogQueries.ts:41).
  RED:
    FAIL ... > CatalogPage > keeps one source's rows out of the other source's cache entry
    TestingLibraryElementError: Unable to find an element with the text: Product 2.
    Test Files  1 failed (1)
         Tests  1 failed | 12 skipped (13)
  => a edição preservou o poder discriminante do teste. Restaurado; verde.

MF-3 (badge disparando em quantity == null) e MF-4 (refresh() sem include_all na
chave) foram executados no assento do worker, RED nomeando 1 teste cada, 34
passed. Não reproduzidos aqui — MF-1 e MF-2' cobrem as duas classes que este chip
já viu falhar (asserção incapaz de reprovar; teste alheio enfraquecido).

===============================================================================
4. LANE FINAL — ASSENTO DO ORQUESTRADOR, PÓS-RESTAURAÇÃO
===============================================================================
$ npm run test --workspace @marketplace-central/web -- --run \
    ../../packages/feature-products/src/CatalogPage.test.tsx src/app/AppRouter.test.tsx

  ✓ ../../packages/feature-products/src/CatalogPage.test.tsx (13 tests) 1214ms
  ✓ src/app/AppRouter.test.tsx (22 tests) 445ms

  Test Files  2 passed (2)
       Tests  35 passed (35)

passed=35  failed=0  skipped=0.
CatalogPage 7→13 testes (os 6 anteriores seguem vivos). AppRouter 22.

tsc (apps/web): 12 erros, TODOS pré-existentes e em arquivos alheios
(anunciosQueries, anunciosQueryState.test, AnunciosTable.test, ListingsRefreshControl.test,
MutationPreviewModal, MutationResultSummary, ProdutoPage*.test). Grep por
catalogQueries|CatalogPage|AppRouter na saída: 0 ocorrências. FE vermelho é
condição herdada conhecida (CHIP-ANCHORS-2), não regressão deste slice.

===============================================================================
5. COPY PROVISÓRIA (herda A-23) — PARA O QA/OPERADOR JULGAR
===============================================================================
- "Vendáveis {n} de {m}" — fixado pelo card, byte-exato.
- "Sem estoque" — bate byte a byte com a promessa já feita ao operador em
  IntegracoesPage.tsx:394.
- "Ver todos" — fixado pelo card.
- "Ver sortimento filtrado" — NOVO, não estava no card. É o rótulo do caminho de
  volta, que o card não nomeou. Provisório.
- O chip é hoje um <span text-xs text-faint>, não uma pílula. O card fixou o
  TEXTO, não o tratamento visual. Sujeito ao gate de design.
