S11-INTEGRACOES-FE — orchestrator seat
Worktree: .claude/worktrees/chip-vendavel, branch worktree-chip-vendavel.
Worker: GPT-5.6 Luna high (standard implement), two rounds.
Prompt: scratchpad/prompt-s11-r2.md · log: scratchpad/agent__s11r2.log
Last message: scratchpad/agent__s11r2.last.md

Owned files:
  apps/web/src/pages/integracoes/IntegracoesPage.tsx
  apps/web/src/pages/integracoes/IntegracoesPage.test.tsx
  packages/web-query/src/activeSource.ts
  packages/web-query/src/index.ts


== ROUND 1: REPROVED HERE, THREE REASONS ==

R2-1 — the counts fake was a CONSTANT. A card that never refetches after a toggle
and a card that refetches correctly both return that constant, so the test could
not tell them apart. That is the CHIP-IMPORT-CHAIN shape: an observable that
passes in both worlds is not evidence. The missing invalidateQueries was
INVISIBLE to an 18/18 green.

R2-2 — the two binding tests did not name which toggle bound to which field, so
swapping two booleans in the handler passed.

R2-3 — the no-active-source state was unhandled. With A-22 now answering that
state with 400 unknown_erp_source, an unhandled read error renders the card as
though the server had chosen an empty rule — three unchecked boxes that no
operator authored, which is the exact integrity rule this chip exists to enforce.


== ROUND 2 MUST-FAILS (worker verbatim; both re-run at this seat) ==

R2-1, invalidation removed:

    FAIL ... recalculates the result line after a toggle instead of showing the
    pre-change count
    TestingLibraryElementError: Unable to find an element with the text:
      Resultado: 3 de 4 produtos.
    Tests  1 failed | 17 passed (18)

R2-2, the two booleans swapped:

    AssertionError: Somente produtos de revenda must bind to only_revenda:
      expected false to be true // Object.is equality

The first RED is the load-bearing one: it is the defect round 1's constant fake
could not see. It only exists because the fake is now state-derived
(IntegracoesPage.test.tsx:142-144):

    getCatalogAssortmentCounts.mockImplementation(() =>
      Promise.resolve({ sellable_count: storedAssortment.only_revenda ? 2 : 3, total_count: 4 }),
    );

and the assertion checks BOTH directions — that "Resultado: 3 de 4 produtos"
appears AND that "2 de 4" is gone — so a card that renders both counts, or
neither, fails too.


== READ AT THIS SEAT, NOT TAKEN ON REPORT ==

1. The no-source state is discriminated EXACTLY, not by a status class
   (IntegracoesPage.tsx:302-306):

       function isSourceUnsetError(error: unknown): boolean {
         if (!error || typeof error !== "object") return false;
         const { status, error: code } = error as { status?: number; error?: unknown };
         return status === 400 && code === "unknown_erp_source";
       }

   A different 400 on the same surface (invalid_body) therefore still reads as a
   read failure, not as "no source chosen". ActiveSourceCard (:313-317) keeps the
   three states disjoint: readFailed = isError && !isSourceUnsetError(...),
   sourceUnset = !activeSource && !isFetching && !readFailed.

2. C-5 — compare the WIRE, not the struct. The discriminator above was checked
   against the SDK's real producer rather than against the neighbouring mock:

       packages/sdk-runtime/src/index.ts:1871  getCatalogAssortmentCounts -> getJson(...)
       packages/sdk-runtime/src/index.ts:1909  getSellableAssortment      -> getJson(...)
       packages/sdk-runtime/src/index.ts:1719  async function getJson<T>(path)
       packages/sdk-runtime/src/index.ts:1723    throw { status: response.status,
                                                   error: (data as ErrorResponse).error }
                                                   satisfies MarketplaceCentralClientError

   Both assortment reads go through getJson, and getJson throws the FLAT
   {status, error} object — not an Error subclass, not a nested body. The page's
   destructure and the test's rejection value both match the producer.

3. A-23 honoured on both counts, read in the diff:
   - invalidation is the TARGETED catalog prefix,
     `queryClient.invalidateQueries({ queryKey: queryKeyNamespaces.catalog })`
     in useSetSellableAssortmentMutation — not active-source's blanket
     invalidateQueries(), which A-23 explicitly said is not a pattern to copy.
     queryKeyNamespaces.catalog is ["catalog"] and catalogQueryKeys.counts
     returns ["catalog","counts",{...}], so the prefix genuinely matches.
   - erp_source is IN the counts key:
     counts: (erpSource) => ["catalog","counts",{ erp_source: erpSource ?? null }]
     so a source flip cannot reuse the previous source's count under a new label.
     The query is also `enabled: Boolean(erpSource)` — with no source resolved it
     does not fire at all, which is what makes the R2-3 tests' "counts never
     called" assertion meaningful.

4. Copy is the operator's pt-BR with no dev marker:
   `Nenhuma fonte definida ainda — escolha a fonte que o app vai ler.`
   (SOURCE_UNSET_COPY, IntegracoesPage.tsx:295). Provisional per A-23.

5. Named assertion messages are present rather than bare booleans (:160-162 and
   throughout), so a failure says which toggle and which field.


== LANES, COUNTED AT THIS SEAT ==

@marketplace-central/sdk-runtime — includes the S10-COND contract test:

     Ô£ô src/dashboard.test.ts (2 tests) 6ms
     Ô£ô src/erpImport.test.ts (4 tests) 10ms
     Ô£ô src/listings-signals.test.ts (5 tests) 4ms
     Ô£ô src/activeSource.test.ts (5 tests) 14ms
     Ô£ô src/index.test.ts (62 tests) 46ms

     Test Files  5 passed (5)
          Tests  78 passed (78)
    exit=0

@marketplace-central/web, src/pages/integracoes:

     Ô£ô src/pages/integracoes/IntegracoesPage.test.tsx (18 tests) 664ms

     Test Files  1 passed (1)
          Tests  18 passed (18)
    exit=0

Zero skipped in both — vitest reports skips explicitly and "78 passed (78)" /
"18 passed (18)" leave no remainder.

@marketplace-central/web-query has NO `test` script:

    npm error Missing script: "test"
    npm error workspace @marketplace-central/web-query@0.1.0

That is NOT a missing lane, and the first version of this file said it was. The
package has three test files of its own (index.test.ts, invalidation.test.ts,
failureCopy.test.ts) and apps/web's vitest config runs them:

    apps/web/vitest.config.ts  include: [
      "src/**/*.test.ts", "src/**/*.test.tsx",
      "../../packages/feature-products/src/CatalogPage.test.tsx",
      "../../packages/web-query/src/**/*.test.{ts,tsx}",
      "../../packages/ui/src/**/*.test.{ts,tsx}",
    ]

So web-query is covered by the @marketplace-central/web lane; only the
per-package script is absent. The filtered run above (`-- --run
src/pages/integracoes`) deliberately excluded them, which is why they show no
count here. They are counted in the S13 full-lane run.

Worth flagging for S12: feature-products is included by EXACT FILENAME
(CatalogPage.test.tsx), not by a glob. A new test file added anywhere in
feature-products would not run in any lane. S12's write set touches only
CatalogPage.test.tsx, so the slice is unaffected — but a future slice adding a
second test file there would get a silent zero.

TypeScript: 12 known pre-existing errors, none in the four owned files.


== CONTAMINATION CLEARED BEFORE ANY GREEN ABOVE WAS TRUSTED ==

The round-1 worker reported that the standard vitest lane was blocked and that
"Nenhum `.js` gerado permaneceu". Both refuted by measurement at this seat: the
standard lane works, and 186 untracked .js/.js.map files with same-basename
.ts/.tsx siblings were present in the tree, shadowing sources under extensionless
resolution. Root cause was my own `tsc -b` during the S11 review.

Deleted per-file under hub grant A-25 R-2(a): dry run reported
DELETED_COUNT=186 APPLY=False with the ONLY skip being my own untracked
apps/server_core/internal/composition/catalog_routes_test.go (not a .js), then
--apply. Tracked files untouched; no `git add -A`. Porcelain afterwards was 11
modified + 1 untracked, all accounted for.

The S11 lane was then RE-RUN post-purge and reproduced 18 passed / 0 skipped, so
the green is not read off shadow JS. `.gitignore` and `tsconfig` remain the hub's
seam per R-2(b) and are not proposed here.
