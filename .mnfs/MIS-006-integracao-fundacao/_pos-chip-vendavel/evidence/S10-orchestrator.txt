S10-CONFIG-HTTP — orchestrator review (2 rounds) and the evidence that closed it
Base: 595c15e3. Worker: gpt-5.6-luna high, OS process (agent__s10.log / agent__s10r2.log).
Checklists written BEFORE each diff was read: p4-checklist-s10.md, p4-checklist-s10-round2.md.

1. WHAT THE SLICE SHIPS
   GET/PUT /config/sellable-assortment on the tenant's three toggles, backed by
   tenant_config.Repository, plus the tenant_config entry in contracts/governance/modules.json.
   The published contract (marketplace-central.openapi.yaml:3380-3426, schemas :8295-8330) was NOT
   touched — it landed in S8 @73190f23 and this slice matches it.

2. ROUND 1 — ACCEPTED, WITH THREE FINDINGS AGAINST THE TESTS
   The handler passed all six round-1 hard kills on the first read:
     - *bool per field, so an absent boolean is a 400 and never a silent false
       (http_handler.go:112-116,124-128)
     - the 200 body is the RE-READ (store.Get after SetSellableAssortment), not the request echoed
       (http_handler.go:136-141)
     - ErrUnknownActiveSource -> 500 internal_error, no invented all-false default
     - flat {"error": code}, only invalid_body / internal_error, no 404 branch
     - both routes registered as httpx.InteractiveRouteClass (http_handler.go:45-46)
     - write set exactly the three granted files
   The TESTS did not hold up. Three findings were sent back as round 2:

   F-1 (the one that mattered) — NO test in the slice could catch a transposition of two of the
       three fields. Every fixture set only_revenda and only_ecommerce_eligible to the SAME value
       ({true,false,true} in both the PUT and the GET test), and two booleans carrying the same
       value are indistinguishable under a swap. Those two toggles mean different things to the
       operator, and their JSON names are exactly what S11/S12 bind to.
   F-2 — the main test built its expectation by calling the code under test:
       `want := newSellableAssortmentResponse(store.postSetCfg.SellableAssortment)`. A mapper that
       transposed two fields transposes both sides of the comparison identically.
   F-3 — the trailing-garbage branch (`decoder.Decode(&struct{}{}) != io.EOF`) had no test, so
       deleting it would have been invisible.

3. ROUND 2 — THE FIX, AND THE MEASUREMENT THAT PROVES IT FIXES SOMETHING
   New: TestHandlerSellableAssortmentFieldMapping — three cases, each setting exactly ONE toggle
   true, each run through BOTH directions (PUT: assert the typed value that reached the store;
   GET: assert the wire body). With three booleans no single fixture can make all three pairwise
   distinct, so the names are pinned one at a time. The response is compared as a
   map[string]bool, NOT unmarshalled into sellableAssortmentResponse — a struct with the same tags
   on both sides of the round trip cancels a tag swap out. F-2 fixed with a literal expectation.
   F-3 covered by TestHandlerPutSellableAssortmentTrailingGarbageReturnsInvalidBody.

   I re-ran the must-fails myself rather than accept the worker's report, as TWO independent
   mutations (copy-based backup/restore — never `git checkout --`, the file is uncommitted):

   M-A — swap the json tags only_revenda <-> only_ecommerce_eligible on the REQUEST struct
     (putSellableAssortmentRequest). transport lane, DB env set: PASS=20 FAIL=3 SKIP=0
       --- FAIL: TestHandlerSellableAssortmentFieldMapping/only_revenda/PUT
           http_handler_test.go:290: SetSellableAssortment called with {OnlyRevenda:false
           OnlyEmEstoque:false OnlyEcommerceEligible:true}, want {OnlyRevenda:true
           OnlyEmEstoque:false OnlyEcommerceEligible:false}
       --- FAIL: TestHandlerSellableAssortmentFieldMapping/only_ecommerce_eligible/PUT
           http_handler_test.go:290: SetSellableAssortment called with {OnlyRevenda:true
           OnlyEmEstoque:false OnlyEcommerceEligible:false}, want {OnlyRevenda:false
           OnlyEmEstoque:false OnlyEcommerceEligible:true}
     Only the PUT direction moves, which is right: M-A mutates the request tags only. The
     only_em_estoque case correctly stays green — it is not one of the swapped fields.

   M-B — the same swap on the RESPONSE struct (sellableAssortmentResponse).
     transport lane, DB env set: PASS=18 FAIL=5 SKIP=0 — the four revenda/ecommerce cases in both
     directions, naming the map both ways, e.g.
       http_handler_test.go:315: response fields = map[only_ecommerce_eligible:true
       only_em_estoque:false only_revenda:false], want map[only_ecommerce_eligible:false
       only_em_estoque:false only_revenda:true]

   THE PART THAT PROVES F-1 WAS REAL AND NOT A STYLE NOTE:
     TestHandlerSellableAssortmentPutThenGetReturnsStoredValues — the round-1 test — PASSES under
     BOTH M-A and M-B. Its {true,false,true} fixture survives a revenda<->ecommerce swap in either
     direction because both fields carry true. Round 1 would have shipped a live tag transposition
     under a green lane.
     TestHandlerGetSellableAssortment also passes under M-B, for the F-2 reason: it unmarshals into
     the production response struct, so the swapped tags cancel. Left as-is because the mapping
     table covers that ground with the map assertion; recorded here so nobody later reads its green
     as a wire-name proof. It is not one.

   Both mutations restored; `git status --porcelain` back to the same three modified paths.

4. LANE ACCOUNTING, GREEN, WITH THE DB ENV SET (profile §11 — per `--- ` line, never a package ok)
   ./internal/modules/tenant_config/...   RUN=39  PASS=39  FAIL=0  SKIP=0
   go vet ./internal/modules/tenant_config/... clean.
   A-15 in practice: the worker's own run reported SKIP=3 because MPC_TEST_DATABASE_URL was unset
   in its sandbox — TestRepository_Get_NoRow_ReturnsErrUnknownActiveSource,
   TestRepository_Set_Get_RoundTrip, TestRepository_SetSellableAssortment_RoundTripPersistsPerTenant
   never executed. Named, then re-run here with the env: all three PASS. SKIP=0 is the number that
   carries the proof.

5. GOVERNANCE — THE ENTRY MEASURED, NOT ASSUMED
   The worker reported status=failed without attribution. I isolated the delta by removing ONLY the
   tenant_config entry, running the lane, restoring it, and running again:
     without entry: 43 violations     with entry: 43 violations
     + ADDS    GOV_MODULE_LAYER  tenant_config-erp_import-adapters
     - REMOVES GOV_MODULE_COVERAGE tenant_config
     (GOV_MODULE_COVERAGE sourcekind is present on BOTH sides — pre-existing, not this slice)
   The lane was already red at base 554788d5. The entry reprove nothing that was not already
   reproving; it trades "module not declared" for one named cross-module edge.
   The edge is ONE symbol: tenant_config/active_source.go:74
     var ErrUnknownActiveSource = internalread.ErrUnknownActiveSource
   tenant_config reaches into erp_import's adapters layer only to republish an error sentinel.
   OPEN — REQUEST with the hub. modules.json carries temporary_exceptions with 4 precedents, all
   rule_id "module-target-layer" + reason + removal_owner. rule_id and reason I can write from the
   measurement; removal_owner is an owner identifier and is not mine to invent. No entry written.

6. WHAT I CHANGED MYSELF (glue, within the ceiling)
   The package doc block. Round 2 widened its subject to "the tenant configuration" while its body
   still described active-source only — including "fail-closed 400 when unset", which is FALSE for
   the assortment routes (they answer 500). A total claim that is false over part of its scope is
   R-24's case, and the fix is to make the claim match, not to soften it. The block now names both
   surfaces, states the missing-boolean-400 rule and the failure it prevents, and names the 500
   asymmetry as an open question with the hub instead of letting a reader assume symmetry.

7. WHAT THIS SLICE DOES NOT CLAIM
   - The missing-row status is PARKED at 500 internal_error pending the hub ruling. The tests
     assert 500 because that is what the published contract offers on this surface; they are not a
     ratification of 500 as the honest answer.
   - The flat vs nested error duality is untouched — transitional standard with a disposition
     (operator ruling, profile @1889d0dd) and a dedicated chip queued after this chip merges.
   - No route-class registration test: newMux uses http.NewServeMux, which does not implement
     routeClassRegistrar, so that branch is unexercised here. Pre-existing shape shared with the
     active-source routes, not introduced by this slice, and named rather than left silent.
   - No server was booted and no live HTTP call was made. The proof is the handler lane plus the
     two tag mutations.

8. PROCESS FINDING FOR THE LEDGER
   I dispatched round 2 without snapshotting the round-1 bytes of the accepted files, so afterwards
   I could not prove "only the test file changed" — the diff base is HEAD 595c15e3, which predates
   both rounds. I re-reviewed the handler and modules.json diffs in full against the round-1
   checklist instead, which is weaker than a byte comparison and is named as such. Snapshot the
   accepted files BEFORE dispatching a follow-up round.
