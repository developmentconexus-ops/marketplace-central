S7-MIRROR-CATALOG — P4 adversarial review at the orchestrator's seat
Worker: gpt-5.6-sol / low (complex), OS-process. Worker commit: e91b163e.
Reviewed at: worktree-chip-vendavel, tree at e91b163e.

1. WRITE-SET
   10 files: 8 code/test + 2 evidence. All authorized. No migration, no dep, no OpenAPI/SDK,
   no FE, no dev stack, no .env. `BATCH-PLAN.md` and `DISPATCH-LEDGER.md` were modified by me
   while the worker ran; it correctly left them out of its commit and said so.

2. LANES, COUNTED PER LINE AT MY SEAT, ENV LOADED (A-15)
   `. testdb-env.ps1` sourced; `env set: True` printed without exposing the value.

   run 1 — postgres (integration): RUN 32 · PASS 31 · SKIP 0 · FAIL 1
     the one FAIL is `TestGetImportChainCountsCurrentQueueAcrossInstallations`,
     `chain_query_repository_integration_test.go:84`, queue_read_at 114µs past the upper bound.
     That is the documented S13-CLOCK flake (GRANT G-1), not this slice.
   run 2 — postgres (integration): RUN 32 · PASS 32 · SKIP 0 · FAIL 0
   run 2 — erp_import/... (unit):   RUN 142 · PASS 142 · SKIP 0 · FAIL 0
   `go vet ./...` exit 0. `go build ./...` exit 0.

   **SKIP 0 on the integration lane is the load-bearing number.** This is the lane S5B faked by
   omission, so it is the one that had to be counted rather than read.

   The worker reported `go build` EXIT=1 with `error obtaining VCS status: exit status 128`.
   It does not reproduce at my seat, second slice in a row (S6 reported the same). Recorded as a
   worker-environment quirk. Both workers were right not to reach for `-buildvcs=false`.

   The clock flake fired 3 times across my runs today and passed on re-run each time. Recorded,
   not dropped; S13-CLOCK owns the fix, and it is a clock-domain fix, not a tolerance widening.

3. MUST-FAIL — FOUR MUTATIONS, FOUR KILLS, EACH NAMING A VALUE
   The worker shipped ONE red (`golden default descriptions = [a b c d], want [a d]`) and said
   plainly that the later assertions were written during implementation. Honest, and not enough:
   criterion 7 demanded a must-fail in BOTH directions. I injected the mutations myself.

   M1 — collisions computed BEFORE the cut (`validEANCounts(eligibleRows)` → `index.collisions`,
        the pre-S7 behaviour):
     FAIL TestFindProductsForLinkingComputesEANCollisionAfterAssortmentCut
     reader_test.go:231: strict survivor quality = [complete ean_collision],
       want non-ambiguous auto-approval candidate
     → the D-121 consequence, reproduced: cutting the twin left the survivor ambiguous, so the
       survivor stops auto-approving. That is the inversion the criterion exists to forbid.

   M2 — eligibility ignores the pinned policy and always applies the strict default:
     FAIL same test
     reader_test.go:241: relaxed twin 201 quality = [complete ambiguous_product],
       want EAN collision and ambiguity
     → the other direction. M1 and M2 kill the same test through different halves, which is what
       "must-fail in both directions" has to mean to be worth anything.

   M3 — stock clause neutralised inside the mirror predicate:
     FAIL TestMirrorCatalogSellableAssortmentGoldenDefaults
       mirror_repository_integration_test.go:438: golden default descriptions = [a c d], want [a d]
     FAIL TestMirrorCatalogSearchAndPaginationApplyAssortmentBeforeLimit
       mirror_search_integration_test.go:82: first filtered fetch = [301 303 304],
         want [301 303 305] (limit+1 after SQL cut)

   M4 — the page stops cutting in SQL while the counts still do:
     FAIL TestMirrorCatalogSellableAssortmentGoldenDefaults
       :438: golden default descriptions = [a b c d], want [a d]
     FAIL TestMirrorCatalogSearchAndPaginationApplyAssortmentBeforeLimit
       :82: first filtered fetch = [301 302 303], want [301 303 305] (limit+1 after SQL cut)
     → `[301 302 303]` is exactly "LIMIT applied before the cut": the excluded 302 consumed a
       slot. The fixture is 5 matches for a limit of 2, so the page/limit interaction is proved
       by a fixture larger than one page rather than by a sampled Limit=N.

   Every message prints the VALUE. The A-15 corollary is honoured: `renderString` is used for
   the `*string` assertions (`known-zero stock = %s`, `null-usoprod survivor = %s`).

4. WHAT I VERIFIED BY MEASUREMENT
   - The mirror predicate is the A-14 mirror form, NULL-tolerant on all three clauses, and it
     does NOT fold case. `TestMirrorCatalogAssortmentCountsAreTenantSourceScopedUncachedAndCaseExact`
     seeds a deliberately dirty `usoprod='r'` row and asserts sellable 1 of 2 — the dirty row is
     cut, which is the S5B-canonicalises-on-write contract holding. Criterion 8 discharged by a
     measurement, not by a claim.
   - `estoque_total` really is the sellable quantity: `mirror_repository.go:111` says "sellable
     stock = physical MINUS reserved", and the reader parses the same column
     (`reader.go:266,304,548`, `stock_batch_reader.go:82`). The SQL predicate and the screen read
     one column, so the page and the fact cannot disagree.
   - Criterion 9, producer side: `missing_stock` IS reachable. The measured path is an upload
     carrying physical stock 11 with no reserved value, which collapses `estoque_total` to NULL
     (0078 documents that exact collapse observed live on 164 products); `GetSellableStock`
     answers Quantity=nil plus `missing_stock`. That is a genuine unknown, so the flag is live on
     the producer side. With S6's consumer side, condition 1 of decision (b) is closed.
   - The count query does not carry the page's `DISTINCT ON (m.codigo_produto::bigint)`. I
     checked whether that can drift: `TestMirrorCatalogPagesCollapseNumericIdentityWithoutSkipping`
     exists and covers the collapse, and the pkey is `(tenant_id, source, codigo_produto)` on
     TEXT, so `'007'` and `'7'` are two rows that the page collapses and the count would not.
     Sankhya CODPROD is numeric and emits no leading zeros; an xlsx could. Latent, narrow, and
     NOT introduced blind — the worker wrote the collapse test. Recorded as a residual, not a
     block: if a leading-zero code ever lands, the counter over-reports by the number of numeric
     twins. Whoever changes either query owes the other the same identity rule.
   - No test was deleted or loosened. `MirrorEANCollisionCounts`'s existing test kept its name and
     its expectation and gained the new policy argument as `MirrorAssortmentPolicy{}` — an
     unchanged assertion, not a weakened one.
   - `var _ readports.CatalogAssortmentReader = (*Reader)(nil)` was added at reader.go, so the
     mirror reader is the SECOND implementer of the port. The condition I wrote at the port
     declaration during S6 still stands unmet for the four decorator seats — S9's work, unchanged.

5. BLOCKING FINDING — THE TENANT'S THREE TOGGLES DO NOT REACH THE CATALOG AT ALL
   Escalated to the hub; NOT decided at this seat.

   `catalogPage` takes the policy from `defaultSellableAssortment()` — hardcoded all-true —
   instead of from `SellableAssortmentFromContext(ctx)`, which is the mechanism S5 built and
   which `FindProductsForLinking` uses eight lines earlier in the same file. Same for
   `GetCatalogAssortmentCounts`. The only knob a caller has is `CatalogPageOptions{IncludeAll}`,
   which is all-or-nothing.

   Measured sweep of every non-test reference to the stored policy:
     tenant_config/active_source.go:26,66   the type and the config field
     tenant_config/repository.go:47,89      load and store
     routing/matcher.go:45-48               the ONE place that pins it onto a read
   Nothing else. The catalog path — mirror (this slice) AND Oracle (S6, already accepted by me,
   `catalogAssortmentPredicate(includeAll bool)`) — never reads the tenant's row.

   Consequence against the contract, not against taste:
     VC-3 requires `/catalogo` to show a zero-stock sellable product with a badge WHEN
     `only_em_estoque` is disabled. With the rule hardcoded on, flipping that toggle changes
     link generation and changes nothing on the screen. VC-3 cannot pass.
     VC-2 requires the card's "Resultado: N de M" and the catalog chip to match SQL running THE
     SAME rule. The counter is pinned to all-true, so it stops matching the moment a toggle is
     off. VC-2 cannot pass.

   Why it is not S7's to fix silently: the gap spans an already-merged slice (S6's Oracle
   predicate), this slice (the mirror reader), and the wiring slice (S9), and no card in the plan
   assigns "read tenant_config and pin the policy onto the catalog read path" to anybody. That is
   a plan gap, and the standing order is that real ambiguity goes to the hub rather than becoming
   a silent local decision.

   S6 shipped the same shape and I accepted it. I did not measure the producer side of the policy
   during S6's P4 — I checked that `IncludeAll` reached the query and stopped there. The lesson is
   the chip's own: I verified that the option ARRIVED at the SQL, and never asked where the value
   the option replaces was supposed to COME FROM.

6. VERDICT
   S7's own eight criteria are met and proved. The slice is NOT closed: §5 blocks it pending the
   hub's ruling on who owns pinning the tenant policy onto the catalog read path.
