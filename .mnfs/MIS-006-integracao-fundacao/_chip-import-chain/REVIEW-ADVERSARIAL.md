# ADVERSARIAL REVIEW — CHIP-IMPORT-CHAIN
Reviewer: cold Claude gate-reviewer (read-only). Base 5441fe18 -> HEAD 67e4a3d.

Filed verbatim as returned by the reviewer. Dispatched 2026-07-28 by the chip session; the reviewer
did not write any of the code under review (implementer = codex gpt-5.6-luna, dispatches #1/#2 in
`DISPATCH-LEDGER.md`). Chip responses to each finding are in `EVIDENCE.md`, never edited into this file.

## VERDICT
**REPROVA**

The *code* is the cleanest part of this chip: the chain is really consumed from `getErpImportChain`, the ADR-17 guard is a genuine runtime guard (not a type-level fiction), the error path cannot render a chain shell, `VinculosPage.tsx` was touched in exactly the two allowed lines, and the moved test kept every original assertion plus one. I found no fabricated zero and no counter derived from `listErpImports`. The REPROVA is for the **close-out half of the contract**: there is **no `EVIDENCE.md` in `.mnfs/MIS-006-integracao-fundacao/_chip-import-chain/`** (every other chip in this mission has one), and I7, I8 and I9 are *declaration-shaped criteria* — I7 says verbatim "FAIL se você declarou 'já existe' sem ter verificado", and nothing at all is declared. Per the anti-rationalization rule, an absent declaration is NOT-EVIDENCED, not an honest degrade. Second blocker: `apps/web/vitest.chip.config.ts` **still exists on disk** (its own header at line 4 says "CHIP-LOCAL, DELETED BEFORE COMMIT", chip.md:113-114 orders it deleted before the commit) — a merge hazard I cannot clear by reading. Third: the L2 live drive, which is the whole reason the operator's chain-endpoint waiver was granted (validation-contract.md:86-92), has not been run.

## Attack results

**1. INTEGRITY — no fabricated-zero path found. NO defect.**
`renderCounter` at `apps/web/src/pages/importacoes/ImportChainPanel.tsx:15-17` is `typeof value === "number" && Number.isFinite(value) ? value : <UnknownValue …/>`. Enumerated adversarially: absent → `undefined` → `typeof "undefined"` → `—`; `null` → `typeof "object"` → `—`; `NaN` → `isFinite` false → `—`; `Infinity`/`-Infinity` → `isFinite` false → `—`; `"0"` and `"42"` (strings) → `typeof "string"` → `—`; `0` → renders `0` (correct: the payload carried a real zero); `-1` → renders `-1` (a negative decomposition count would be visible rather than laundered — acceptable). `UnknownValue` really renders an em dash: `packages/ui/src/UnknownValue.tsx:6`.
ERROR path cannot render counters: `ImportChainPanel.tsx:32` short-circuits on `chainQuery.isError || !chainQuery.data` and returns only the `erp-import-chain-error` div (`:33-35`); the chain block at `:37` is the `else`. There is no state in which the shell and an error coexist.
Two residual honesty asymmetries, ressalva not blocker: (a) `protocol` at `:39` is rendered raw — a `200` with a missing `protocol` yields a blank span, not `—`, while its three sibling counters are guarded; (b) `queue_read_at` is guarded correctly (`:62`, `formatDateTime` returns `null` for absent/unparseable — `packages/web-query/src/index.ts:114-121`).
`ImportacaoDetailPage.tsx:20-26` no-`importId` branch: renders `<p role="alert">Identificador da importação não informado.</p>`, no panel, no counters. Trap not present. Note the branch is effectively unreachable under the route `/importacoes/:importId` (`app/AppRouter.tsx:69`), and it duplicates `enabled: Boolean(importId)` in `useErpImportChain.ts:21` — redundant defense, see attack 7.

**2. REAL CONSUMPTION — YES, chain-only. No client-side derivation.**
The only producer of the three counters is `chainQuery.data.importados|vinculados|enfileirados` (`ImportChainPanel.tsx:45,51,57`), fed by `useErpImportChain` → `client.getErpImportChain(importId)` (`useErpImportChain.ts:20`) → `GET /erp/imports/{id}/chain` (`packages/sdk-runtime/src/index.ts:1901`). A repo-wide grep for `getErpImportChain|useErpImportChain` returns only the hook, the panel and the two tests — no other call site. No `reduce`, `filter().length` or sum feeds them anywhere. `ImportacaoSection.tsx:158-166` renders `accepted_count`/`rejected_count`/`warning_count` — those are server fields of `ErpImportSummary` (`packages/sdk-runtime/src/erpImport.ts:38-40`), they sit on a different card, and they are not presented as the chain. The gambiarra D-D forbids is absent.

**3. TEST HONESTY — mostly real; one weak spot.**
Test 1 (`ImportChainPanel.test.tsx:29-44`) *alone* would pass against hardcoded 137/42/9 — `toHaveBeenCalledWith("imp_real")` proves the fetch fired, not the binding. But tests 2 and 3 use different values (138/8, 139/41 at `:59-60,77-78`), so hardcoding is refuted by the suite as a whole. Acceptable.
Tests 2/3 assert NOT-zero **explicitly**: `:58` and `:76` `not.toHaveTextContent("0")` alongside the `—` assertion. This is exactly what I5 demands and it is not theater.
Tests 4/5 assert absence of the chain: `:87` and `:101` `expect(screen.queryByTestId("erp-import-chain")).toBeNull()`. Test 4 also pins the 404 copy (`:86`), test 5 pins the generic copy and asserts the 404 copy is *not* shown (`:99-100`).
The 5000ms timeout at `:98` is **legitimate, not a defect blanket**: the hook overrides the QueryClient's `retry: false` with its own predicate (`useErpImportChain.ts:23-28`) — a 5xx returns `failureCount < 1`, i.e. one retry at react-query's default ~1000ms backoff; a 4xx returns `false` immediately, which is why test 4 needs no timeout. The comment at `:95-97` states this accurately. The predicate itself is the right behavior (a 4xx is a settled answer).
Moved `ImportacaoSection.test.tsx`: I **could** compare against base — the main checkout holds it at `C:/Users/leandro.theodoro/Documents/marketplace-central/apps/web/src/pages/vinculos/ImportacaoSection.test.tsx`. All 5 `it(` blocks are byte-identical in body; `expect(` count 14 → 15; the only delta is the `MemoryRouter` wrapper (`:44-49`) and the added `expect(screen.getAllByRole("link", { name: "Ver cadeia" })[0]).toHaveAttribute("href", "/importacoes/imp_1")` at `:89`. **Zero coverage lost.** Same check on `IntegracoesPage.test.tsx`: 31 `expect(` and 11 `it(` in both base and HEAD — the repair is the `MemoryRouter` wrap at `:32-36` only.

**4. OWNERSHIP / COLLISION — clean.**
`pages/vinculos/VinculosPage.tsx` HEAD vs base (base read from the main checkout): base `:8` `import { ImportacaoSection } from "./ImportacaoSection";` and base `:159` `<ImportacaoSection />` are gone; every other line is identical with a uniform −1 offset (base `:9-12` = HEAD `:8-11`; base `:157` `)}`= HEAD `:156`; the blank line before `</section>` existed at base `:158` and survives at HEAD `:157`). **Exactly two removals, no third hunk.** Nothing else under `pages/vinculos/` was touched (`ImportacaoSection.tsx`/`.test.tsx` are gone from that directory — `Glob` on `apps/web/src/pages/vinculos/*` returns 17 files, neither of them present; `QueueRow.tsx`, `VinculoDrawer.tsx`, `QueueTab.tsx`, `ResolvidosTab.tsx`, `useVinculosResolved.ts` all still there and not in the reconciliation list). `pages/vinculos/useErpImports.ts` was correctly left in place and is imported cross-directory at `pages/importacoes/ImportacaoSection.tsx:6`. Zero Go / migration / `contracts/**` / `packages/sdk-runtime/**` files in the reconciliation list; `ErpImportChain` (`packages/sdk-runtime/src/erpImport.ts:48-54`), the client method (`index.ts:1901`) and the OpenAPI schema (`contracts/api/marketplace-central.openapi.yaml:8078-8096`) are all pre-existing ANCHORS-2 work, consumed unmodified. NOT-VERIFIABLE-BY-READING: that no *other* file changed — I cannot run `git diff --name-only`, so I am trusting the supplied reconciliation for absence.

**5. ROUTE GATE JUSTIFICATION — the justification is TRUE.**
`AppRouter.tsx:63-66` claims these screens "must render with no marketplace account connected… read the ERP mirror, which exists before any marketplace does", and `/importacoes` + `/importacoes/:importId` sit at `:68-69`, outside `<Route element={<InstallationGatedRoutes />}>` at `:78-86`. Verified against every read those two screens make: `ImportacoesPage` → `ImportacaoSection` → `useErpImportsList()`/`useErpImportDetail()` (`pages/vinculos/useErpImports.ts:14-33`) call `client.listErpImports()` and `client.getErpImport(id)` — **no `installationId` parameter, no `useInstallation()`**; `ImportacaoDetailPage` → `ImportChainPanel` → `useErpImportChain` calls `client.getErpImportChain(importId)` (`useErpImportChain.ts:20`) — same, no installation. No component in the `pages/importacoes/` tree imports `InstallationContext`. The gate exclusion is correct and the one-line justification is honest.

**6. REACHABILITY — reachable end to end.**
`Header.tsx:178-183` gear menu → `<Link to="/importacoes">Importações</Link>` (same tier as Integrações/Catálogo/Estoque, so consistent with convention, not a dead corner) → `AppRouter.tsx:68` → `ImportacoesRoute` (`routes/importacoes.tsx:4-6`) → `ImportacoesPage.tsx:12` `<ImportacaoSection />` → per-row `<Link to={`/importacoes/${item.import_id}`}>Ver cadeia</Link>` (`ImportacaoSection.tsx:147-152`) → `AppRouter.tsx:69` → `ImportacaoDetailPage.tsx:21` `<ImportChainPanel importId={importId} />`. Back-link `/importacoes` at `ImportacaoDetailPage.tsx:14`. No dead link, no path typo. Second entry point preserved: `pages/integracoes/IntegracoesPage.tsx:6,449` still renders the same section (base did too, from `../vinculos/`; only the import path changed — both occurrences are at the identical line numbers 6 and 449 in base and HEAD), so `/vinculos` losing the section is not an operator dead end.

**7. SLOP — minor only, no blanket try/catch, no hand-rolled state components.**
Clean: `LoadingState`/`ErrorState`/`EmptyState`/`UnknownValue` all come from `@marketplace-central/ui` (`ImportChainPanel.tsx:1`, `ImportacaoSection.tsx:2`); date formatting is `formatDateTime` from `@marketplace-central/web-query`, never hand-rolled; no hex colors — `text-ink`, `text-faint`, `bg-surface`, `rounded-card` are the semantic tokens, and `text-[22px]`/`gap-[14px]` (`ImportacoesPage.tsx:5,7`, `ImportacaoDetailPage.tsx:8,10`) match the established page-header convention (`IntegracoesPage.tsx:439,441`, `MercadoPage.tsx:265,268`, `PedidosPage.tsx:211,213`), so they are consistency, not invention. No TODO, no stub, no try/catch around the integrity read. Three ressalvas: (a) `routes/importacoes.tsx` is a pass-through wrapper — but it matches `routes/integracoes.tsx:3-5` exactly, so it is convention; (b) the double guard noted in attack 1 (`enabled: Boolean(importId)` + the `importId ?` branch) is redundant belt-and-braces for a route param that cannot be empty; (c) `pages/importacoes/` reaching into `pages/vinculos/useErpImports` (`ImportacaoSection.tsx:6`) leaves the promoted screen coupled to the directory it was promoted out of — defensible (that file is not this chip's to move) but it is an unfinished promotion.

## Criteria I1..I9

| # | Verdict | Proof |
|---|---|---|
| I1 route exists + gate decision justified | **PASS (code) / NOT-VERIFIABLE-BY-READING (behavior)** | `AppRouter.tsx:68-69` registers both paths outside the gate at `:78-86`; justification comment `:63-66`, verified true in attack 5. The contract also demands "mostre a tela montando sem instalação" — no test asserts router placement (tests render pages directly, `ImportacoesPage.test.tsx:33-42`), so the behavioral half needs the live drive. |
| I2 section moved without losing function | **PASS (code+tests) / declaration NOT-EVIDENCED** | Component+test present at `pages/importacoes/ImportacaoSection.tsx` / `.test.tsx`; absent from `pages/vinculos/` (Glob). All 5 tests and all 14 base assertions preserved, +1 at `.test.tsx:89`. What `/vinculos` shows now is *behaviorally* answered (section retained at `IntegracoesPage.tsx:449`, new home `/importacoes`), but the contract requires that be **declared** — no EVIDENCE file exists to declare it. |
| I3 VinculosPage two lines only | **PASS** | Line-by-line comparison vs the base copy in the main checkout: only the `:8` import and the `:159` render removed; all remaining lines identical at −1 offset. No CHIP-VINC-NEUTRO collision. |
| I4 consumes `getErpImportChain`, not the old numbers | **PASS** | `useErpImportChain.ts:20` → `index.ts:1901` → `/erp/imports/{id}/chain`; counters bound only at `ImportChainPanel.tsx:45,51,57`; no derivation from `listErpImports` anywhere (attack 2). |
| I5 ADR-17: absent → `—`, never zero | **PASS** | Guard `ImportChainPanel.tsx:15-17`; tests `.test.tsx:46-61` (absent field) and `:63-79` (null), both asserting `—` **and** `not.toHaveTextContent("0")` at `:58,76`. REPORT-level note, out of this chip's scope per validation-contract.md:119: the server always emits an integer (`erp_import/transport/http_handler.go:133-138`, OpenAPI `required: [protocol, importados, vinculados, enfileirados, queue_read_at]` at `:8080`), so a real-world `0` reaching this screen means the *backend* decided zero — the FE guard cannot distinguish that from "unknown". |
| I6 chain-read error is honest | **PASS** | `ImportChainPanel.tsx:32-35` renders `erp-import-chain-error` and nothing else; 404 vs generic split at `:9-13,21-23` keyed on the shape the SDK actually throws (`index.ts:1715` throws `{status, error}`), so the 404 branch is real in production, not test-only. Tests `:81-102` assert the chain testid is null in both cases. |
| I7 F-02 verified, not rebuilt | **FAIL (NOT-EVIDENCED)** | The code side is right — `IntegracoesPage.tsx:295-346` consumes `useActiveSourceQuery`/`useSetActiveSourceMutation` against `/config/active-source` (`index.ts:1896-1898`) and nothing was rebuilt. But the criterion is a *declaration with a verification result and a commit id*, and there is **no `EVIDENCE.md` in `_chip-import-chain/`** (Glob returns chip.md, validation-contract.md, DISPATCH-LEDGER.md, dispatches/ only). The contract's own words: "FAIL se você declarou 'já existe' sem ter verificado." Nothing is declared and no toggle-persists verification is recorded anywhere I can read. |
| I8 F-03 resolved either way | **NOT-EVIDENCED** | By reading, "satisfied" is demonstrable: the screen needs `import_id` for the row link (`ImportacaoSection.tsx:148`) and `ErpImportSummary.import_id` already carries it (`erpImport.ts:32`); no contract/SDK change was made or needed. But the contract requires the chip to *say which field and where* — no artifact does. |
| I9 no collateral damage | **NOT-VERIFIABLE-BY-READING / NOT-EVIDENCED** | `tsc` and `vitest` cannot be run by me, and no cited before/after counts exist: the two worker `.last.md` files say "Vitest: could-not-run (sandbox)" (`…410e32e1.last.md:13`, `…c0849f51.last.md:9`) and the 15 tsc errors are asserted but never listed. `DISPATCH-LEDGER.md:36-41` says the chip re-ran vitest and repaired 3 files, but gives no pass/fail counts. Separately, a live hazard: `apps/web/vitest.chip.config.ts` **exists on disk** and rewrites every `@marketplace-central/*` import (`:16`) despite its own line 4 and chip.md:113-114 requiring deletion before commit; it is not in the reconciliation list, so it is presumably untracked, but I cannot confirm that. |

## What I could NOT verify
- Any `git` fact: the three commits `4b76a287` / `1bffdcfd` / `67e4a3d`, the true `git diff --name-only` against 5441fe18, and therefore the completeness of the supplied reconciliation (absence of Go/migration/contract changes rests on that list, not on my own diff). I compared against the **main checkout working tree** (`C:/Users/leandro.theodoro/Documents/marketplace-central/…`), which the session snapshot puts at `bcab8269` — two hub `.mnfs`-only commits above the 5441fe18 floor, so a code-level ancestor for the files I compared, but not literally the base sha.
- Whether `apps/web/vitest.chip.config.ts` is tracked at HEAD. The session-start snapshot showed it as `??` (untracked); I have no way to re-check. If it is tracked, I9 is a hard FAIL, not a hazard note.
- `tsc -p apps/web --noEmit` result and the 15-error baseline; `vitest` counts before/after. Execution required.
- The whole L2 (validation-contract.md:94-102): `/importacoes` on real data, chain numbers cross-checked against a direct DB query, non-existent id → honest error, active-source toggle persisting and invalidating globally, light/dark. This is the cash-in of the operator's chain-endpoint waiver (`:86-92`) and the exact place a composition-root/decorator wiring defect of the catalog-503 class would surface — no test in this diff can catch it, because every test mocks `useClient` directly (`ImportChainPanel.test.tsx:9-13`, `ImportacaoDetailPage.test.tsx:9-13`).
- Whether the `—` path can ever fire against the real server: the backend and OpenAPI both declare the counters required and always-present, so I5's guard is untested against production reality by construction.
