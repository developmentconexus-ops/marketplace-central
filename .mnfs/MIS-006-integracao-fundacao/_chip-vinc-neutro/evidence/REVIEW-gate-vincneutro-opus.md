# DUAL GATE — OPUS SIDE — verdict, verbatim

Reviewer: `harness:gate-reviewer` subagent, model **opus**, physically read-only seat
(Read/Grep/Glob only — no Bash, no Edit, no Write). Prompt: `PROMPT-gate-vincneutro-opus.md`,
frozen before dispatch. Independence rules in that prompt forbade the reviewer from reading this
chip's `EVIDENCE.md` and both GPT-side verdicts, so this is an independent read of the code
against the contract, not a proofread of the chip's case.

**Provenance, declared:** the reviewer's seat has no Write tool, so this artifact is the returned
final message pasted verbatim by the chip session. Nothing added, nothing removed, nothing
reordered. Everything below the rule is the reviewer's text.

---

```
VERDICT: REFUTED
```

V1: PASS — `directionClasses.INCOMPARABLE = "bg-info-soft text-info"` (apps/web/src/pages/vinculos/QueueRow.tsx:48) and `directionGlyphs.INCOMPARABLE = "?"` (QueueRow.tsx:187) are real design tokens (apps/web/src/index.css:27-28,74-75, pre-existing — also used at packages/ui/src/Badge.tsx:5), distinct from FOR/accent, AGAINST/warn and UNAVAILABLE/surface-2; the drawer's third map no longer exists, it imports the one map (apps/web/src/pages/vinculos/VinculoDrawer.tsx:6,121).
V2: PASS — `AnchorChips` maps→sorts→slices and never filters (QueueRow.tsx:296-302), so `shown.length === min(reasons.length, 2)` for every direction mix; I hand-traced all-INCOMPARABLE n=1/2/5, INCOMPARABLE-last mixes, one-of-each-direction and the empty array, and no input yields "+N with zero chips"; asserted on the DOM via `getAllByTestId("motivo-chip")` (apps/web/src/pages/vinculos/QueueTab.test.tsx:196-219), not on `shown`.
V3: PASS — the reverted three-direction enumeration is shown failing with signature cited: empty `<ul>` plus a lone `+2` button labelled "Mostrar todos os 2 motivos" (.mnfs/MIS-006-integracao-fundacao/_chip-vinc-neutro/evidence/V3-must-fail.txt:108-119,155), restored green at evidence/L1-vitest-after.txt:16.
V4: PASS — `side` is read from the field (QueueRow.tsx:129-148) and rides inline in the compact chip (QueueRow.tsx:205), the tooltip, the expanded list and the drawer (VinculoDrawer.tsx:121), DOM-asserted at QueueTab.test.tsx:206-207,245; a side-less INCOMPARABLE stays silent, and that path is real — generation_service.go:711 returns INCOMPARABLE with an empty side on the `!readable` branch.
V5: PASS — evidence/L0-tsc-baseline.txt:17-20 lists exactly the 3 `/vinculos` errors, evidence/L0-tsc-after.txt has 12 errors and none under `vinculos`, and the 12 match the baseline's non-vinculos 12 one-for-one; the baseline errors also prove `ProductLinkReasonDirection` already carried `INCOMPARABLE` before this chip, so the SDK was not touched here.
V6: PASS (implemented) — `decidingAnchors` (QueueRow.tsx:85-110) mirrors `decisionRuleForCandidate` (resolution_service.go:812-835) on all 25 `match_status`×`match_input` pairs, and the `ACCEPT → CODPROD+EAN` hardcode is sound because `LinkCandidateMatchStatusAccept` has exactly one assignment (generation_service.go:505, inside `buildConcordantCandidate`) reachable only from generation_service.go:293-303 where seller_sku and ean each resolve one and the SAME codprod; the `title FOR` negative case renders no anchor and the positional `td[5]` I re-derived is the Identificado por cell (select·anúncio·canal·produto·SKU HUB·identificado por) — QueueTab.test.tsx:498-526.
V7: PASS — `isAutoResolved` reads the latest `next_state === "resolved"` audit entry's `actor.actor_type` (apps/web/src/pages/vinculos/useVinculosResolved.ts:18-23,49-51); the only writer of `"system"` is `AutoApproveCandidate` (resolution_service.go:280) and approve/manual-resolve/reject all refuse a system actor (`errSystemActorNotPermitted`, resolution_service.go:860-865, called at :295,:325), so neither direction of the lie is reachable; three DOM cases including the pre-M-05 no-entry row at ResolvidosTab.test.tsx:80-136.
V8: NOT-PROVEN — the criterion is a claim about `EVIDENCE.md`, which this review is forbidden to open, so I cannot confirm the registration; I can only corroborate its substance independently (`rule_matched` has zero hits under `contracts/` and `packages/sdk-runtime/`; the CHECK at apps/server_core/migrations/0082_product_link_decisions.sql:54 is `actor <> 'system' OR rule_matched = 'concordant_codprod_ean'`; the predicate actually used is documented at useVinculosResolved.ts:29-48).
V9: PASS — `refforn` is KEPT with its D-A rationale (QueueRow.tsx:165-176) and `anchorShortLabel` still returns an unknown anchor verbatim (QueueRow.tsx:190-192), exercised by the unmapped anchors "SKU idêntico"/"Título parcial" at QueueTab.test.tsx:108-109.
V10: PASS — structural labels are neutral (QueueTab.tsx:137-141, ResolvidosTab.tsx:104) while the provider survives as a VALUE (QueueRow.tsx:358-362, VinculoDrawer.tsx:182), and no raw slug renders — `container.textContent` is asserted free of `"mercado_livre"` at QueueTab.test.tsx:337 and golden.test.tsx:110.
V11: NOT-PROVEN — vitest went 62 files/511 green (evidence/L1-vitest-baseline.txt:82-83) → 63/520 green (evidence/L1-vitest-after.txt:83-84) with no green→red and the golden still 3 green, and I independently confirmed the golden fixture flip is a correction (state `exact_ean` can only be CONFIRM or REJECT: generation_service.go:314→386-393→538-539, while collisions/conflicts carry state `conflict` at :335,:369), but I have no Bash/git in this seat so `git diff --name-only … HEAD` could not be run and the "zero paths outside `pages/vinculos`, zero `VinculosPage.tsx`/`ImportacaoSection.tsx`" half is unverified (what I could establish: `apps/web/vitest.chip.config.ts` no longer exists, `index.css` info tokens pre-date the chip, and nothing outside `pages/vinculos` imports the new exports).

```
FINDINGS (most severe first)
- .mnfs/MIS-006-integracao-fundacao/DECISOES-D122-anchors-telas.md:136 vs apps/web/src/pages/vinculos/QueueTab.tsx:138 — the chip closed a conflict with a frozen decision by itself instead of REPORT+REQUEST; D-122:136 says the new column "substitui 'SKU ML'/'GTIN'" (plural), and any rendered queue keeps NINE columns with `Canal` in the former `SKU ML` slot rather than eight with `Identificado por` absorbing both. On the merits I judge the deviation defensible and I verified its premise independently of the chip's reasoning — `ProductLinkCandidateItem` (packages/sdk-runtime/src/index.ts:1070-1089) carries no seller-SKU field, and the hub's own live drive recorded that the cell rendered `provider_code` (.mnfs/MIS-006-integracao-fundacao/_chip-anchors/hub-live-drive-u1-u3.md:81), so deleting it would erase the row-level provider datum that V10's second half positively requires — but "the frozen premise is factually wrong" is a ruling only the hub/operator may make, and it has not been made.
- apps/web/src/pages/vinculos/VinculosDesign.golden.test.tsx:53-57,117 — the fixture the chip corrected is still not a producible IO example: it now asserts `match_status: "CONFIRM"` together with `confidence: 92` / band `ALTA`, but the generator emits 60/`MEDIA` for an exact_ean CONFIRM (generation_service.go:538-539) and 70/`MEDIA` for exact_sku (:529); input = this EXEMPLO-IO row → the golden locks a band chip the backend cannot emit for that status, so the chip's stated correction of the fixture is half done.
- apps/web/src/pages/vinculos/QueueRow.tsx:61-63 — `providerDisplayName` falls through verbatim, so input `provider_code = "shopee"` (or the golden's own `"xyz-9"`, golden.test.tsx:139) → the raw wire slug renders in the Canal cell, which is the same class of defect V10 names ("nenhum slug cru na tela"); today only `mercado_livre` is mapped, so the exposure is latent rather than live.
- apps/web/src/pages/vinculos/QueueRow.tsx:109 — `statusDecidingAnchors[candidate.match_status](candidate.match_input)` invokes the lookup result, so input = a wire `match_status` outside the SDK union (e.g. a sixth status shipped before the SDK is regenerated) → `undefined is not a function` during render, which unmounts the whole queue table instead of degrading one cell; the sibling lookups (`bandLabels`, `directionClasses`) degrade to `undefined` without crashing.
```
