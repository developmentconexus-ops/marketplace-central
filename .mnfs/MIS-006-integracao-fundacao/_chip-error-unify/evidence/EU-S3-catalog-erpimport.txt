S3 — catalog + erp_import migrated to platform/apierror
Measured by the CHIP (re-run, not copied from the worker's report).
Checkout: .claude/worktrees/chip-error-unify  ·  apps/server_core
GOCACHE/GOMODCACHE bound absolute.

[ran] go build ./...            BUILD_EXIT=0
[ran] go vet ./...              VET_EXIT=0
[ran] go test ./internal/modules/catalog/... ./internal/modules/erp_import/... ./tests/unit/...
      TEST_EXIT=0   ok=14  FAIL=0  no test files=4

[ran] go test -count=1 ./internal/modules/catalog/transport/ \
        -run TestCatalogCountsInvalidErpSourceEmitsErrorEnvelope -v
      === RUN   TestCatalogCountsInvalidErpSourceEmitsErrorEnvelope
      --- PASS: TestCatalogCountsInvalidErpSourceEmitsErrorEnvelope (0.00s)
      PASS
      ok  .../internal/modules/catalog/transport  1.636s
      -count=1 forces execution: the first run reported "(cached)", which is not
      evidence that anything ran. Non-vacuous: "=== RUN" present, test NAMED.
      This is the day-1 golden test going RED -> GREEN. It was not edited by this
      slice (git diff shows error_envelope_golden_test.go untouched).

CENSUS — error-shaped raw writes in the two migrated modules
[ran] grep -rn 'httpx.WriteJSON' internal/modules/catalog internal/modules/erp_import \
        | grep -iE 'error|400|401|403|404|409|422|500|503|504'
      bytes=0 lines=0   <- empty result MEASURED, not assumed

POPULATION vs EXTRACTION (both printed, per profile §11)
  httpx.WriteJSON call sites remaining in the 2 modules ....... 12  (all success-path)
  apierror.Write call sites in the 2 modules .................. 29
  error-shaped httpx.WriteJSON remaining ......................  0
  The 12 and the 29 are disjoint populations: the 12 are 2xx bodies, which this chip
  does not touch. The extraction that matters is the third number.

Surviving write*Error functions in the 2 modules (INTENTIONAL — they map an error to a
status/code, which is real logic; their bodies now all go through apierror.Write, proven
by the zero above):
  catalog/transport/http_handler.go:416  writeCatalogPageError
  catalog/transport/http_handler.go:486  writeCatalogReadError
  erp_import/transport/http_handler.go:163 writeImportError
The producers that DIED: catalog writeError (9 callers repointed), erp_import writeError
(12 callers repointed), and the uncatalogued INLINE 503 inside requirePageReader — the
site no `write*Error` grep could ever have seen.

CHIP-SIDE CORRECTION (not in the worker's diff)
  http_handler_test.go:360 compared `trimJSON(actual) != tc.wantBody` — trimJSON only on
  ONE side. trimJSON round-trips through a Go map, so it alphabetises keys; the expected
  literals therefore had to be hand-alphabetised. Direction is safe (a natural-order
  literal yields a false RED, never a false green), so REPORT class, not BLOCKING — but
  it is a trap in a file this slice owns, so it was symmetrised to
  `trimJSON(actual) != trimJSON(tc.wantBody)` and re-run green.

KNOWN GAP CARRIED FORWARD (declared, not silently left)
  VC-2 wants ONE whole-body envelope pin per transport module. catalog has it (the golden
  test). erp_import does NOT: its tests assert `.error.code` and `.error.details[...]`
  field-by-field, which is the module's pre-existing idiom and does not prove the ABSENCE
  of extra top-level keys. Folded into the next slice's card as an explicit deliverable.
