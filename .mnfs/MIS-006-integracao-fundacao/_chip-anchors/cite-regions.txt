# Literal non-citations: strings that LOOK like `<something>:<digits>` and are not coordinates.
#
# Format: NOTACITE <literal> <why>
#
# Note what is deliberately absent: no line ranges and no historical regions. A line range decays the
# moment a corrective inserts a paragraph above it, and then silently excuses the wrong lines — which
# is the R-14 defect one level down, inside R-14's own fix. Historical citations declare themselves
# inline by carrying `@<sha>`; quoted output and quoted contract text declare themselves with a fence
# tag. Every declaration in this scheme travels WITH the content it describes.
#
# Each entry below is a literal the tool must see as data. If one stops appearing in the pack, the
# entry becomes inert rather than wrong — an unused NOTACITE excuses nothing.

NOTACITE mm:500/1  canonical dimension key in the A1 example — a rational value, not a file line
NOTACITE mm:400/1  canonical dimension key in the A1 example — a rational value, not a file line
NOTACITE GeneratedCount:0  a Go struct field and its value in quoted probe output
